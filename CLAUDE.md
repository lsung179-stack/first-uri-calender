# 우리 캘린더 (Our Calendar) — 프로젝트 인수인계 노트

> 이 파일은 새 세션이 시작될 때 자동으로 읽힙니다. 매번 설명하지 않아도 됩니다.
> 새 대화를 열면 먼저 이 파일 + 관련 코드/DB를 확인한 뒤 작업하세요.

## ⚠️ 유지보수 규칙 (Claude에게 — 반드시 지킬 것)
**이 저장소의 파일(특히 `index.html`)이나 DB(RPC/마이그레이션)를 변경할 때마다, 같은 커밋에서 이 `CLAUDE.md`도 갱신한다.**
- 무엇을 바꿨는지 아래 “이미 완료된 수정” 목록에 한 줄로 추가(또는 기존 항목 수정).
- 프리미엄/광고/결제 로직, 함수명, 계정, DB 스키마, 워크플로우가 바뀌면 해당 섹션도 함께 고친다.
- 날짜/버전 기준이 바뀌면 “이미 완료된 수정”의 날짜 표기도 갱신한다.
- 즉 코드 변경과 이 문서를 **항상 함께 커밋**한다 (문서가 뒤처지지 않게).

## 프로젝트 개요
- 한국어 공유 캘린더 앱. **단일 `index.html` SPA**를 Capacitor 8(WebView)로 감싼 구조. 모든 JS/CSS/HTML이 이 한 파일 안에 있음.
- 백엔드: **Supabase** (Auth, Postgres, RPC). 구독 결제: **RevenueCat** (@revenuecat/purchases-capacitor). 광고: **AdMob** (@capacitor-community/admob) — 하단 배너 1개.
- 빌드: **Codemagic**이 매 빌드마다 네이티브 프로젝트 재생성 → 실기기(iOS + Android) 설치.

## 배포 워크플로우 (중요)
- 개발자는 보통 **`index.html`을 GitHub에 직접 업로드**(“Add files via upload”)하는 방식으로 작업. 그래서 **로컬 최신본이 저장소(main)보다 앞서 있을 수 있음** — 새 세션은 반드시 현재 배포/로컬 버전이 무엇인지 확인할 것.
- 클라이언트 코드 변경은 **Codemagic 재빌드** 후에야 기기에 반영됨.
- **DB(RPC/마이그레이션) 변경은 재빌드와 무관하게 즉시 라이브.**
- ⚠️ 광고(AdMob)는 **네이티브 앱에서만** 동작. 웹/PWA에서는 절대 안 뜸(정상). 광고 확인은 반드시 네이티브 앱에서. 앱 설정 화면 하단 푸터에 `네이티브`/`웹` 및 `광고:<_admobDebug>` 문자열이 표시됨.

## Supabase
- 프로젝트 id: **`bgqzkkaslqchbovzrkao`**
- 핵심 테이블: `public.subscriptions` (user_id, status, expires_at, plan_type, is_manual)
- `public.members` (room_id, user_id, color, notifications_enabled, role, is_virtual, virtual_*, **`sort_order` int**=방목록 사용자별 순서, **`seal` text**=개인별 방 프로필 'color:sym', 마이그레이션 `add_members_seal`). RLS `members_update_self`(user_id=auth.uid())로 본인 행 수정 가능.
- `public.rooms` (id, name, code, owner_id, ~~`mascot` text~~). ⚠️ **방 프로필은 2026-07에 방 공유(rooms.mascot)→개인별(members.seal)로 전환** — `rooms.mascot` 컬럼과 RPC `set_room_mascot`은 **미사용 잔존**(마이그레이션 `add_room_mascot`). 클라이언트는 members.seal만 읽고 씀.
- ~~`public.anniversaries`~~ — **방별 공유 D-day/기념일 기능은 제거됨**(사용자 요청, "느낌 없음"). 클라이언트 코드(스트립/시트/JS/CSS) 전부 삭제. 단 DB 테이블 `anniversaries`(마이그레이션 `create_anniversaries`)는 **그대로 남아있음**(빈 테이블, 미사용). 나중에 재도입하거나 정리할 때 참고.
- 관리자 RPC: `admin_users_detailed()` — 회원별 활동지표(SECURITY DEFINER, 관리자만).
- 관리자 RPC: `admin_growth_stats()` — WAU/MAU·활성화율·7일 리텐션·구독전환·공유방 비율 + **초대 지표**(invite_joins_7d/30d, invite_conv_num/den=30일 신규 중 가입 7일 내 타인 방 입장, cancel_pending=해지예약)(대시보드 성장 섹션).
- 관리자 RPC: `admin_ads_cohorts()` — 최근 8주 주간 가입 코호트: 가입→7일 생존(일정·last_sign_in 기준, 7일 경과분만 분모)→초대 합류→체험(TRIAL)→유료(NORMAL)→해지예약. 체험/유료 판별=subscriptions.period_type, SANDBOX 제외(마이그레이션 `add_invite_metrics_and_ads_cohorts`).
- Edge Functions: `push-cron`(mode: daily/reminder/empty-rooms/**birthday**, **v13**), `send-push`(**v8**, body `kind` 선택 파라미터), `revenuecat-webhook`. pg_cron: daily 08:00·empty-rooms 08:05·**birthday 09:00(KST=0 0 UTC)**·reminder 5분마다·오래된 notifications 정리. 생일=`profiles.birthday`('MM-DD') 매칭, 같은 방 멤버+본인에게 축하 푸시. **알림 카테고리 설정**: `profiles.notif_prefs` jsonb(마이그레이션 `add_profiles_notif_prefs`) — send-push는 kind로, push-cron은 모드별 키(daily/nudge/reminder/birthday)로 false인 수신자 제외(코드 33 참고).
- 핵심 RPC: `my_premium_status()` — SECURITY DEFINER. `status='active' AND expires_at>now()` 인 구독이 있을 때만 `is_premium=true` 반환.
  - ⚠️ 과거 버그: 집계함수(max/array_agg/bool_or)를 GROUP BY 없이 써서 구독 0건이어도 항상 1행+상수 `is_premium:true`를 반환 → **모든 유저가 프리미엄으로 오판**. `HAVING count(*)>0` 추가로 수정 완료(라이브).

## 계정
- 관리자: `lsung179@naver.com` — `ANN_ADMIN_USER_ID = '3bc13a8b-618d-4d47-abea-1eec4d682ec0'`. 실구독 보유. 앱에선 `_adminPlanOverride`(무료/유료 플랜 체험 토글)로 프리미엄 표시를 제어(실구독과 무관).
- 무료 테스트: `lsung179@gmail.com` — `64c6502a-6365-40cb-ad05-49ec8cb6439e`. 구독 없음 → 무료여야 정상(광고O, 유료기능X).

## 프리미엄 / 광고 로직 지도 (index.html 함수명)
- 상태: `_isPremium`(= `_serverPremium`), `_serverPremium`/`_serverPremiumInfo`(서버 기준), `_rcPremiumActive`(기기 RevenueCat 기준 — 판정엔 미사용).
- `_recomputePremium()` → `_isPremium = _serverPremium` (**서버가 유일한 진실**). 기기 엔타이틀먼트로 프리미엄 부여 금지.
- `loadPremiumStatus()` → RPC `my_premium_status` 호출 → `_serverPremium` 설정.
- `isEffectivePremium()` → 관리자는 `_adminPlanOverride`, 일반 유저는 `_isPremium`.
- 결제: `onPremiumSubscribe()` → `_applyRcEntitlement()`(구매 직후 낙관 반영 후 서버 재동기화). **구매 복원 `onRestorePurchases()`는 `_applyRcEntitlement` 안 씀** — `loadPremiumStatus()` 후 `_serverPremium`일 때만 프리미엄(기기 Apple ID 구독 신뢰 금지 → 계정 간 프리미엄 누수 방지).
- `syncPremiumFromRevenueCat()` — **자동 호출 금지**(기기 구독을 직접 읽어 누수 위험). 정의만 두고 호출하지 않음.
- 광고: `showAdBannerIfNeeded()`, `hideAdBanner()`, `_anySubSheetOpen()`(열린 시트 있으면 광고 숨김), `_elVisible(el)`(실제 보이는 요소만 카운트 — 잔여 `.on` 오판 방지), `_admobDebug`(설정 화면 푸터에 노출).

## 이미 완료된 수정 (2026-07 기준)
1. ✅ **RPC `my_premium_status()` 수정** — `HAVING count(*)>0` 추가. DB 라이브. (무료 유저가 프리미엄으로 뜨던 근본 원인)
2. ✅ **광고 게이팅** — `_anySubSheetOpen()`이 `_elVisible()`로 실제 보이는 시트만 카운트(닫힌 시트 잔여 `.on`이 광고를 영구히 숨기던 버그).
3. ✅ **구매 복원 누수** — `onRestorePurchases()` 서버 단일 기준. 기기 엔타이틀먼트로 프리미엄 안 켜짐.
4. ✅ **멤버 없을 때 캘린더가 위로 올라가던 현상** — 노치 여백(`env(safe-area-inset-top)`)을 조건부로 표시되던 `.cal-member-tabs`에서 항상 존재하는 부모 `.screen.cal`로 이동.
5. ✅ 관리자 버튼 `전체 이용자 프리미엄`(globalTrialBtn)·`프리미엄 개별 관리`(premMgmtBtn) UI 삭제(충돌 우려). **(2026-07 추가 정리)** 남아있던 죽은 코드 완전 제거 — index.html의 `premMgmtOverlay/Card` 모달 마크업 + `loadGlobalTrialState/toggleGlobalTrial/openPremiumManage/premSearchUsers/premSetOverride` 등 함수 일체 삭제. 이 override 방식(`premium_overrides` 테이블·`app_config.global_premium_trial`)은 **애초에 무효**였음 — `my_premium_status()`가 `subscriptions`만 읽어서 override를 반영하지 않음. DB의 RPC(`admin_set_premium_override`/`set_global_premium_trial`)는 그대로 두되 미사용. ⚠️ **진짜 수동부여는 admin.html의 '수동 구독 부여'(`admin_grant_subscription` RPC → `subscriptions`에 `is_manual=true` 행 insert)이며 정상 작동함.** 이건 유지. 급하면 SQL로 직접 부여 가능: `insert into subscriptions(user_id,plan_type,status,started_at,expires_at,is_manual,manual_reason,granted_by,granted_at,source) values(<uid>,'manual','active',now(),now()+interval '1 year',true,'사유',<admin_uid>,now(),'manual');`
6. ✅ **7일 무료체험 노출(전환)** — 전 플랜 첫 결제 무료체험이 UI에 안 보이던 문제. `showUpgradeModal`(잠금 안내: "무료로 체험하기"), `openPremiumSheet`/CTA 버튼("7일 무료로 시작하기"), `_updateTrialCta()`(선택 플랜 가격 기준 "첫 7일 무료·이후 자동결제" 캡션 `#premTrialNote`), `renderTrialBanner`("첫 7일 무료")에 반영.
7. ✅ **혼자 방 초대 넛지(바이럴)** — 실멤버 1명인 방 캘린더 상단(`#calInviteNudge`)에 초대 배너 표시. `renderInviteNudge()`가 `_myMembers` 실멤버<2일 때 노출, `shareKakao()` 호출. `renderMemberTabs()` 시작부에서 호출. 배너에 ✕ 닫기 버튼(`dismissInviteNudge`) — 닫으면 그 방 id를 `localStorage['uricalv2.inviteNudgeDismissed']`에 저장해 다시 안 띄움(방별 기억). ✕ 클릭 시 `vConfirm`('다시 보지 않기'/'취소') 확인 후 저장(실수 방지).
8. ✅ **프리미엄 시트 탭바 숨김 버그** — 일정추가(탭바 숨김)→프리미엄 시트 진입 후 닫으면 하단 탭바가 안 돌아오던 문제. `openPremiumSheet`에 `hideTabbar()`, `closePremiumSheet`에 `showTabbar()` 추가(다른 풀스크린 시트와 대칭). 겸사겸사 프리미엄 안내 모달(`showUpgradeModal`의 `vConfirm`) 타이틀의 ✨ 제거.
9. ✅ **관리자 페이지 자동 로그인(세션 전달)** — `openAdminFullPage()`가 admin.html로 세션 토큰(`#at=&rt=`)을 안 실어 보내, 외부 브라우저/새 탭에서 세션이 없어 비번 폼으로 빠지던 버그. 관리자 계정이 **카카오/애플 소셜 로그인(비번 없음, `has_password:false`)**이라 비번 폼으로는 진입 불가였음. `getSession()` 토큰을 URL 해시로 실어 보내 admin이 `setSession`으로 자동 로그인하도록 수정.
10. ✅ **관리자 회원관리 상세화** — DB에 `admin_users_detailed()`(SECURITY DEFINER, 관리자 가드) RPC 추가: 유저별 최근활동(last_sign_in/이벤트/가입 GREATEST)·일정수·방수·구독·푸시토큰 유무. admin.html 회원관리를 **카드 대시보드**로 개편: 상단 KPI 타일(전체·7일/2주 활성·휴면·구독), 필터칩(+구독자), 회원 카드(활동색 아바타 링·이름·최근활동·📅일정·🏠방·가입경로·💎구독/휴면/🔔push 배지), 활동많은순·최근일정순 정렬, KPI에 7일 신규 추가. 카드=좌측 활동색 바+상태라벨(활발/보통/뜸함/휴면)+3칸 그리드(일정/방/최근접속)+'🗓 마지막 일정' 줄(last_event_at). '휴면(30일+ 미활동)'=앱삭제/이탈 추정(정확한 삭제 신호 없음). ※ '최근 활동'은 last_activity(=GREATEST(last_sign_in,last_event,가입)) 기준 — last_sign_in만 쓰면 로그인 유지 유저가 오래전 접속으로 오표시됨. 🔔=fcm_tokens OR push_subscriptions(알림 허용). 필터에 '최근 가입순' 있음.
11. ✅ **관리자 성장 대시보드** — `admin_growth_stats()` RPC + 대시보드에 '성장·리텐션·전환' 섹션(WAU/MAU, 활성화율, 7일 리텐션, 구독 전환율, 공유방 비율). Edge Function `push-cron`/`send-push`로 리텐션 푸시는 이미 가동 중.
12. ✅ **생일 축하 푸시** — `push-cron`에 `handleBirthday()`+`mode='birthday'` 추가(재배포 v12). 오늘 생일(`profiles.birthday`='MM-DD')인 유저와 같은 방 멤버에게 '🎂 OO님의 생일이에요', 본인에게 '🎂 생일 축하해요'. pg_cron `push-birthday`(매일 09:00 KST) 추가. Edge Function은 git 아닌 Supabase에 있음(수정 시 원본 백업 후 재배포).
13. ✅ **페이월 문구 전환 최적화(#2)** — 잠금 안내(`showUpgradeModal`) 문구를 '~는 프리미엄 전용/구독하면'(제한·결제 프레임)에서 '혜택+하고 싶은 것'(사진·테마·컬러/캐릭터팩·글꼴·할일) 프레임으로 교체. 모달이 이미 '무료로 체험하기' 버튼+7일 무료 문구를 붙이므로 전환 유도 강화.
14. ✅ **컬러 팩 카드 전체 클릭** — `renderStoreColors()`에서 기존엔 `.pack-btn` 버튼에만 onclick이 있어 버튼을 정확히 눌러야 안내모달이 떴음. 카드 전체(`.theme-card`)에 `onclick`(`_cardClick`: 잠금=`onUnlockColorClick`, 보유=`onLockColorClick`)+`cursor:pointer` 부여, 버튼에서는 onclick 제거(카드로 버블링 → 단일 발화, 중복 없음). 상자 아무 곳이나 눌러도 프리미엄 안내모달이 뜸.
15. ✅ **🔒 프리미엄 버튼 여백** — 자물쇠 이모지와 '프리미엄' 텍스트가 붙어 보이던 문제. 이모지를 `<span class="pack-btn-lock">🔒</span>`로 감싸고 `.pack-btn-lock{margin-inline-end:9px}`(카드 잠금태그는 5px) CSS 추가. 폰트 무관하게 고정 여백. 적용 대상: 컬러/캐릭터 팩 버튼·캐릭터 상세시트 큰 버튼(`pack-btn-lg`)·텍스트 테마 버튼·카드 잠금태그(`theme-card-lock-tag`). ⚠️ 최초엔 5px로 넣었으나 원래 있던 공백(~4px)과 거의 같아 티가 안 나서 **9px로 상향**(눈에 띄게).
16. ✅ **일정 드래그 부드럽게(성능+집기/놓기 애니메이션)** — 드래그 이동 끊김 개선 + iOS식 집기/놓기 감각. (1) 고스트를 `left/top`(리플로우) → **`transform:translate3d`(GPU 합성)** 이동. (2) `pointermove`마다 하던 고스트 이동+`elementFromPoint` 히트테스트를 **`requestAnimationFrame` 배칭(프레임당 1회)** 으로 — 최신 좌표만 `_evPtrX/Y`에 저장, `_evDragFrame()`에서 처리. (3) 고스트를 **outer(위치, `_evGhostTransform`=translate3d만, 드래그 중 트랜지션 없음→손가락 1:1)** + **inner(`.ev-drag-ghost-inner`: 시각 pill·스케일·애니메이션)** 로 분리. 집는 순간 `@keyframes evGhostPop`(작게→오버슈트 1.2→안정 1.08, 스프링 cubic-bezier)로 **튀어나오는 느낌**, 놓는 순간 `_evSettleGhost()`가 `.dropping` 클래스로 목표 칸(또는 원위치)으로 미끄러지며 **축소+페이드로 쏙 들어가는 느낌**. 원본 바(`.ev-drag-src`)도 opacity .28+scale .94로 부드럽게 가라앉음. `prefers-reduced-motion` 존중. 관련: `_evMakeGhost()`, `_evSettleGhost()`, `_evDragFrame()`, `_finish()`(rAF 취소+settle 호출). 그리드 드래그 + 날짜시트 롱프레스 드래그 모두 적용.
17. ✅ **드래그 이동 후 새로고침 깜빡임 제거(낙관적 업데이트)** — 다른 날짜로 옮길 때 `loadEvents()`가 `_events={}`로 비우고 빈 캘린더를 그린 뒤 네트워크로 재로드해서 **전체 새로고침되는 깜빡임**이 있었음. `_evMove`를 낙관적 업데이트로 변경: 로컬 `_events` 맵에서 해당 일정을 od→nd로 즉시 이동(`_evLocalMove`)+`renderMonthly()`로 바로 반영하고, **DB 업데이트는 백그라운드**로. 실패 시에만 `loadEvents()`로 서버 기준 롤백(부분 실패는 성공분을 원복해 그룹 일관성 유지). `_tdMove`(할 일)도 동일 패턴 — todo 객체(=`_todos` 참조) 필드만 바꾸고 `safeRender()`, 실패 시 `prev`로 즉시 원복. 드래그 대상은 실제 DB row(반복·가상 제외)라 반복 인덱스는 무관.
18. ✅ **드래그 중 텍스트 선택 방지** — 일정을 롱프레스로 옮길 때 드래그 시작(220ms 홀드) 전에 iOS가 텍스트 선택을 잡아 파란 선택 핸들이 캘린더에 남던 문제. 기존 `user-select:none`은 `body.ev-dragging`(드래그 시작 후)에만 적용돼 홀드 구간이 무방비였음. `.cal-grid,.cal-grid *`에 `-webkit-user-select:none/user-select:none/-webkit-touch-callout:none`을 **항상** 적용(그리드엔 입력창이 없어 안전).
19. ✅ **멀티방 일정 드래그 이동 전파** — 여러 방에 동시 추가한 일정(`multi_group_id` 공유)을 드래그로 옮기면 **현재 방 복사본만** 이동하던 문제. `_evMove`에서 `anchorRow.multi_group_id`가 있으면, 현재 방 group의 각 distinct 날짜 이동(od→nd)마다 `update({date:nd}).eq('multi_group_id',mgid).eq('user_id',_currentUser.id).eq('date',od)`로 **모든 방의 내 복사본을 한 번에 이동**(단일일·기간·여러날 모두 날짜별로 커버). 사용자 선택으로 **모달 없이 자동 전체 이동**(수정은 기존 `askMultiRoomScope` 모달 유지). 낙관적 로컬 반영은 현재 방만(다른 방은 재진입 시 반영). 그리드 드래그+날짜시트 드래그 둘 다 `_evMove` 공유라 함께 적용. ⚠️ 참고: **수정 경로는 이미 멀티방 지원**(`saveEditedEvent` Case 4 등에서 `multi_group_id` 있으면 `askMultiRoomScope`로 전체/방선택/이방만) — 사용자 요청에 따라 그대로 둠. 편집 진입점은 날짜시트(dpCard)의 '수정' 버튼 하나뿐(`openEditEvent`). `multi_group_id`는 방 간 연결, `range_group_id`=연속 기간(방별 독립), `is_multi`=불연속 여러 날.
20. ✅ **멀티방 할일도 동일 적용** — `todos`도 `multi_group_id`로 방 간 연결(추가 시 `_doSaveTodo`가 현재+다른 방에 stamp). (a) **수정**: 이미 `askMultiRoomScope` 모달 지원(line ~17898) — 그대로 유지. (b) **드래그**(`_tdMove`): `todo.multi_group_id` 있으면 `update(upd).eq('multi_group_id').eq('user_id').eq('start_date',prev.start_date)`로 **모든 방 복사본을 함께 이동**(모달 없이 자동). (c) **삭제**(`deleteTodo`): `multi_group_id` 있으면 이벤트처럼 `askMultiRoomScope(...,true)` 모달(전체/이 방만). ⚠️ **함정**: todos의 `multi_group_id`는 '여러 날' 할일(추가 시 날짜별 개별 `single` 행으로 저장, 같은 mgid 공유)도 묶으므로, 드래그·전체삭제 시 **반드시 `start_date`로도 필터**해야 함(안 그러면 다른 날짜 행까지 같은 날로 뭉개지거나 통째 삭제됨). range/single은 방당 1행이라 start_date 필터로 방별 형제가 정확히 매칭.
21. ✅ **할 일 복사(다른 방으로)** — 일정 복사(`copyEventToRooms`/`_doCopyEvent`)와 동일한 흐름의 `copyTodoToRooms`/`_doCopyTodo` 추가. 할일 상세 액션(날짜시트 dp-actions + 우리할일 카드)에 **복사** 버튼(함께 할일은 제외). `_roomPickMode='copytodo'`로 방 선택 모달 재사용(`confirmRoomPick`/`cancelRoomPick`에 분기 추가). 복사본은 원본과 같은 `multi_group_id`로 묶음(원본에 없으면 새로 부여 후 원본에도 update → 이후 수정/이동/삭제 동기화). 완료상태·member_keys는 초기화. `window.copyTodoToRooms` 노출.
22. ✅ **할일 드래그 게이트 확장(여러날 허용)** — 할일 드래그가 '여러날(multi)' 타입에서 안 되던 문제. `canTdDrag`(`__todoCellHTML`)가 `single`/`range`만 허용해 multi는 `data-tddrag="0"`으로 렌더 → 드래그 시작 자체가 안 됨. 이동 로직(`_tdMove`/`_tdGroupDates`)은 이미 multi 지원하므로 게이트에 `todo_type==='multi'` 추가. 반복 할일은 단일 DB행 이동이 애매해 계속 제외. (단일·비반복 기간 할일은 원래 정상 드래그됨.)
23. 🚨 **[중대] 할일 드래그/복사가 아예 안 되던 근본 원인 = IIFE 스코프 경계** — index.html은 **line 16786 `(function(){ 'use strict'; ... })();` IIFE 안에 할일 시스템 전체**(`_todos`, `safeRender`, `loadTodos`, `__getTodos`, `deleteTodo`, `renderTodoScreen` 등)를 담고 있음. 그런데 드래그/복사 헬퍼(`_tdFindById`·`_tdMove`·`_tdGroupDates`(16262~)·`copyTodoToRooms`·`_doCopyTodo`(10159~))는 **이 IIFE 바깥(메인 스코프)**에 있어서 안쪽 `_todos`·`safeRender`에 접근 불가 → `_tdFindById`가 항상 `null` 반환 → **할일 드래그가 시작조차 안 됨**(이벤트는 `_events`/`_evFindRowById`가 같은 바깥 스코프라 정상). 복사도 `(_todos||[])`가 `[]`라 "할 일을 찾을 수 없어요". **수정**: 바깥 헬퍼들이 `window.__getTodos()`(할일 목록)·`window.safeRender`·`window.loadTodos`(window 노출된 안쪽 함수)로 접근하게 변경. Playwright(headless Chromium)로 실제 렌더+드래그 시뮬레이션해 `_tdFindById` false→true, 드래그 활성화·`_tdMove` 이동까지 검증. ⚠️ **앞으로 할일 관련 함수를 추가할 땐 반드시 16786 IIFE 안쪽에 두거나, 바깥이면 `window.` 노출 함수로 접근할 것.** (`renderMonthly`·`invalidateDayEventsCache`·`shiftDateKey`·`_events`·`supa`·`_currentUser`·`toast`는 바깥 스코프라 직접 호출 OK.)
24. ✅ **기간 할일 전체 하이라이트 + 목록(날짜시트) 드래그** — (a) 기간/여러날 할일을 드래그로 집으면 누른 조각만 활성화되던 문제. `_start`(todo)에서 같은 `data-tdid`의 모든 `.cal-tditem`/`.cal-tdline` 조각에 `ev-drag-src` 부여(이벤트 group 방식과 동일) → 전체 범위가 들림. (b) 일정처럼 **날짜시트 목록(dpList)에서도 꾹 눌러 이동**: 시트 할일 `dp-item`에 `data-tddrag/data-tdid/data-tddate` 부여(비반복 단일·기간·여러날), `setupDateSheetDrag` pointerdown이 할일도 감지, `_beginSheetTdDrag()`(=`_beginSheetEvDrag`의 할일판)가 시트 닫고 `_evDrag`를 `kind:'todo'`로 세팅해 캘린더 드래그로 이어받음. Playwright로 srcBars 전체 하이라이트·`_beginSheetTdDrag` 세팅 검증.
25. ✅ **방목록 순서 편집(드래그 핸들+드래그)** — '내 방' 옆 **'순서 편집' 버튼**(`toggleRoomEdit`, `_roomEditMode`) → 켜면 각 카드에 **드래그 핸들(≡ SVG, `.r-card-grip`)** 이 나타나고(`›` 화살표는 숨김) 드래그로 재정렬, 버튼은 '완료'로 바뀜. ⚠️ **흔들림 애니메이션은 처음엔 iOS식 `@keyframes rWiggle`였으나 사용자가 어지럽다고 해서 제거**하고 핸들+살짝 뜬 그림자(`box-shadow`)로 대체. **편집 모드에서만** 드래그 가능(홀드 불필요—`_roomPD`가 `_roomEditMode` 아니면 무시, `_roomPM`에서 4px 이동 시 즉시 `_roomDragStart`). 순서는 **사용자별**이라 `members.sort_order`(int)에 저장(마이그레이션 `add_members_sort_order`, 기기 간 동기화). `loadRooms`가 `members.sort_order`도 조회해 오름차순(없으면 created_at 최신순 폴백, 안정정렬) 정렬 + 진입 시 `_roomEditMode=false`. `renderRooms` 카드에 `data-roomid`+섹션행에 편집버튼(방≥2)+`room-edit` 클래스 토글. 드래그: 집은 카드 `translateY`(흔들림 정지=`body.room-reordering .r-card{animation:none}`—translate 정상적용 위함), 나머지 `translateY(±h)` 자리양보, 드롭 시 `_applyRoomReorder`→`_myRooms` 재배열+낙관재렌더+`_saveRoomOrder`(sort_order 0..n 백그라운드). 편집 모드 중 카드 click(방입장) 차단+드래그 직후 1회 차단(`_roomSuppressClick`). 편집 모드는 `.room-edit .r-card{touch-action:none}`으로 드래그가 스크롤 대신 동작. 리스너는 `#roomsContent` 컨테이너 위임. ⚠️ 전부 **바깥 스코프**(<16786). Playwright로 편집OFF=드래그무시·흔들림없음, 편집ON=rWiggle+[A,B,C]→[B,C,A]+sort_order 저장 검증.
26. ❌ **D-day/기념일 (방별 공유) — 만들었다가 제거함** — 캘린더 상단 슬림 스트립(`#calDdayBar`)+관리/추가시트로 방별 공유 D-day를 구현했으나(PR #43·#44·#45), 사용자가 "느낌 없음"으로 **전체 삭제 요청**. `index.html`을 D-day 도입 직전 커밋(`9ee82f6`, 코드 25) 상태로 되돌려 클라이언트 코드(`#calDdayBar`, `#ddayListOverlay/Card`, `#ddayEditOverlay/Card`, `_ddays`·`loadDdays`·`renderDdayStrip`·`openDdayEdit`·`renderDdayCal` 등 JS, `.cal-dday-strip`·`.dday-*` CSS) 전부 제거. ⚠️ **주의**: 삭제한 건 '방별 공유 D-day'만이며, **기존 무관 기능은 유지**됨 — 할일 마감 D-day 배지(`.td-dday`/`getDdayClass`/`getDdayLabel`), 캘린더 셀의 공휴일·절기·기념일 라벨, 다음 생일까지 D-day. DB 테이블 `anniversaries`는 빈 채로 남겨둠(위 Supabase 섹션 참고). 재도입 시 git에서 PR #43~#45 diff 복원 가능.
27. ✅ **방 프로필 = 컬러 씰 (프리미엄)** — 방 목록 씰(첫 글자)을 **색 + 심볼** 조합으로 꾸밈. ⚠️ **디자인 변천**: 처음 캐릭터(모찌/곰돌/달콩)로 만들었다가(넷플릭스식) 사용자가 "허접/별로"라 함 → 왁스씰 고퀄 시안·5가지 컨셉 아티팩트까지 갔다가, 최종적으로 **초기 '컬러 씰'로 확정**(사용자 선택). **캐릭터 코드(`svgEmoji`/`CHAR_META`/`ROOM_MASCOTS` 등) 전부 제거.** 구성: **배경 색 12종**(`SEAL_COLORS`: 진한 6종 terra/rose/gold/sage/navy/plum + **파스텔 6종 butter/mint/sky/lavender/peach/blush**(2026-07 추가), 각 radial-gradient 왁스도장 느낌 `.seal-<key>`. ⚠️ **심볼·이니셜 색은 CSS 변수 `--seal-ink`로 배경 밝기에 맞춰 자동 전환** — 진한 색은 크림(#f3e6cf 기본값), 파스텔은 진한 먹색(각 `.seal-<key>`가 `--seal-ink`+`--seal-ring` 지정). `.has-seal{color:var(--seal-ink,#f3e6cf)}`·`.seal-ini{color:inherit}`. 스와치는 `.rm-swatches` 6열 그리드(2줄)) + **심볼 14종+이니셜**(`SEAL_SYMS`/`SEAL_SYM_ORDER`: ini(방 이니셜)·taegeuk(태극)·plum(매화)·lotus(연꽃)·chrys(국화)·leaf(나뭇잎)·butterfly(나비)·fish(잉어)·gourd(호리병)·fan(부채)·cloud(구름)·mountain(산)·moon(달)·pine(소나무)·kite(방패연), **꽉 찬 먹칠 실루엣** svg. ⚠️ **디자인 변천(사용자 피드백 연속 반려)**: 이모지형 아이콘→"이모지 티" / 추상 기하 마크→"AI 티" / 얇은 선 전통모티프 v1→"퀄리티 떨어짐, 태극·방패연·달 안 같음" → 최종 **꽉 찬 실루엣 한국 전통 문양**(2026-07). 핵심 개선: taegeuk=**외곽 원(stroke 1.8, fill none) + 곡옥 반쪽 채움** — 외곽선 덕에 아래(배경색) 로브도 또렷("자루 안 보임" 피드백 수정), moon=꽉 찬 초승달, kite=사각+연살 십자프레임+방구멍, pine=층층 실루엣, plum=중심 먹점, butterfly=더듬이, gourd=매듭선 추가. **디테일 선 패스(2026-07)**: 문양 안쪽에 가는 선 추가 — plum 꽃술 5가닥·leaf 곁잎맥 4개·fish 아가미/비늘 곡선·gourd 세로 골·lotus 아래 물결(크림, currentColor). 내부 선은 전부 `rgba(0,0,0,.22~.26) stroke-width 1`. **6종 v2 업그레이드(2026-07)**: butterfly=몸통 타원+머리+더듬이+상하 날개 4장 분리+날개 점 / pine=크리스마스트리 티 나던 층층형 폐기→**민화 우산소나무**(납작 수관 3덩이+가는 굽은 줄기+가지선) / chrys=꽃잎 2겹(외 8+내 8, 22.5° 오프셋) / cloud=뭉게구름+안쪽 말림 2개+**아래 꼬리 선 2줄**(크림) / mountain=삼각형 폐기→**둥근 두 봉우리 곡선**+해+능선 빗금 / moon=매끈한 초승달+**반짝 별**(크레이터 점 폐기). `_sealSvg`는 `fill="currentColor" stroke="currentColor" stroke-width=2.3`(실루엣 채움+살짝 획, 아웃라인 요소만 `fill="none"`), 내부 디테일(잎맥·눈·부챗살)은 `rgba(0,0,0,.24~.34)`로 어떤 배경색에도 대응. `.has-seal{color:#f3e6cf}`(씰 크림)·`.rm-sym{color:accent}`(피커). 값은 `mascot`='color:sym'(예 'navy:heart','rose:ini'). **프리미엄 게이팅은 저장 시점**(2026-07 변경)이었으나 → ⚠️ **2026-07 완전 무료 전환**(코드 34): `saveRoomMascot`/`clearRoomMascot`의 `isEffectivePremium()` 게이트 삭제, 누구나 저장 가능. **개인별 설정(2026-07 최종)**: 표시가 방 공유가 아니라 **개인별** — `members.seal`에 저장(내 행 직접 update, RLS `members_update_self`), `loadRooms`가 `select('room_id,sort_order,seal')`로 `_mySealByRoom` 맵 구성, `renderRooms`/`openRoomMascotForRoom`/`_syncRoomSetMascot` 전부 이 맵 기준. `rooms.mascot`·RPC `set_room_mascot`은 미사용 잔존. 시트 부제 "내 화면에만 적용돼요". **심볼 그리드 5열**(`.rm-symrow{grid-template-columns:repeat(5,1fr)}`, 15개=3줄 꽉 참 — "오른쪽 여백" 피드백 수정). **태극 좌우 방향 수정**(크림 로브가 왼쪽 아래로 — 국기 기준, sweep 플래그 교체). **프리미엄 구독 시트 기능 목록에 '방 프로필'(🪷) 항목 추가**. **스토어 쇼케이스 → 히어로 배너(2026-07 격상)**: 처음엔 테마 탭 하단 작은 행 카드였으나 "눈에 안 들어옴" 피드백 → **테마 탭 최상단(PICK 카드 바로 아래) 히어로 배너**(`.store-seal-hero`)로 교체. 진한 왁스 그라데이션 배경+골드 eyebrow(NEW·ROOM SEAL)+세리프 타이틀+크림 CTA pill(꾸미러 가기)+우측에 큰 씰 3개(태극74px 스탬프 pop 애니 `sshPop`·달·나비, 회전 배치). `openSealShowcase()`=스토어 닫고 **방목록 탭으로 이동+편집모드 자동 ON**(연필 노출, `_pendingRoomEdit` 플래그를 `renderRooms`가 소비 — loadRooms가 편집모드를 리셋하므로 로드 후 켜야 함)+토스트 안내. 원래는 피커를 바로 열었으나 스토어가 설정 탭에서 열려 "설정탭으로 떨어진다" 피드백으로 변경(2026-07). `prefers-reduced-motion` 존중. **표시는 방 공유** — `renderRooms`가 `_sealInner(r.mascot,r.name)`(파싱되면 `.r-card-seal.has-seal.seal-<color>` + 심볼/이니셜, 아니면 null→기본 첫글자). **진입 2군데**: (a) 방 목록 **'편집' 모드**(옛 '순서 편집' 개명, `rooms>=1`) → 씰의 **연필 배지(`.r-card-pencil`)** 탭 (연필은 pointerdown 전파차단+capture 클릭리스너 예외). (b) 방 설정 '방 정보'의 '방 프로필' 행. 시트 `#roomMascotCard`: 큰 미리보기(`#rmBigPrev`, `.has-seal.seal-<color>`) + 스펙(`#rmBpName`='로즈 · 하트') + `#rmGroups`에 JS로 **색 스와치(`.rm-sw`)+심볼(`.rm-sym`)** 주입. 상태 `_sealColor`/`_sealSym`/`_sealHad`, 저장 RPC `set_room_mascot`(값 'color:sym'), 낙관 반영+`renderRooms` 재렌더(편집모드 유지). '기본으로 되돌리기'=`clearRoomMascot`(null). 공용 헬퍼 `_parseSeal`/`_sealGlyph`/`_sealInner`/`_sealSvg`. 함수 전부 **전역**(<16786). Playwright 6종 검증(목록씰·무료게이트·6색7심볼·선택·저장·기본복원). DB 컬럼명은 `mascot` 유지(값 의미만 컬러씰로).
29. ✅ **온보딩(웰컴 슬라이드)에 7일 무료체험·꾸미기 항목** — 신규 가입 온보딩 3번째 슬라이드(`#onbOverlay` '이렇게 사용해요' `.onb-feats`)에 🎨 "7일 무료체험 · 꾸미기 — 스토어의 테마·글꼴·전통 문양 씰로 나만의 캘린더를 꾸며보세요. 첫 7일은 전부 무료예요." 추가(전환 유도).
30. ✅ **푸시 소프트 프리퍼미션** — 옵트인 16% 병목 수리. **콜드 팝업 제거**: `autoRegisterPush()`가 미결정(prompt/default) 상태에서 첫 진입에 자동으로 OS 팝업을 쏘던 로직 삭제(옛 `pushPrompted` 1회 키 폐기, granted 유저 재등록은 유지). 대신 **가치 순간에 앱 모달(vConfirm '알림 켜기')로 먼저 물어보고**, '알림 받기' 수락자에게만 OS 팝업(`registerNativePush(false)`/`registerPushNotification(false)`). 트리거 3개: (a) **로그인 → 방목록 도착**(`renderRooms`, 방≥1, 1s 지연 — 1차 정석 시점, 사용자 요청) (b) `renderMemberTabs`에서 2인+ 방 진입(1.2s) (c) 일정 저장 성공 직후(0.9s). ⚠️ 로그인 전엔 절대 안 뜸(`_pushAskEligible`이 `!_currentUser` 차단). 정책: '다음에'/OS 거절 → **7일 스누즈**, 평생 최대 3회, 세션당 1회(`_pushAskShown`). 상태는 `localStorage['uricalv2.pushAsk']={c,n}`. 함수: `maybeAskPush(trigger)`/`_pushAskEligible`/`_pushAskSnooze`(전역, autoRegisterPush 아래). iOS는 OS 팝업이 1회용 카드라 이 구조가 필수. Playwright 5종 검증(콜드팝업 0회·모달 표시·스누즈 기록·스누즈 차단·수락 시 OS 요청).
39. ✅ **바텀시트 액션 버튼이 광고·탭바에 가리던 문제(안드로이드)** — 갤럭시에서 '새 방 만들기'(`createCard`)·'방 코드로 입장'(`joinCard`) 시트의 하단 '만들기'/'입장' 버튼이 **네이티브 AdMob 배너 + 탭바**에 가려 못 누르고 스크롤도 안 되던 문제. 원인: `.mo-card` 하단 패딩이 `max(28px,safe-area)`뿐이라 광고 높이(`--ad-h`)를 미확보 → 버튼이 네이티브 배너(웹뷰 위 별도 뷰) 아래에 깔림. **수정 2가지**: (a) CSS — `body.has-ad .mo-card`/`.dp-card`에 `padding-bottom:calc(var(--ad-h)+28px+safe-area)` 추가(모든 바텀시트 공통, 콘텐츠 길면 기존 `overflow-y:auto`로 스크롤). (b) `openCreateRoom`/`openJoinRoom`에 `hideTabbar()`, `closeCreateRoom`/`closeJoinRoom`에 `showTabbar()` 추가(성공 시 create/joinRoom이 close 호출→복원, 실패 시 시트 유지). 탭바 z-index(30)<시트(51)지만 확실히 하려 숨김. index.html 변경이라 재빌드 필요.
38. ✅ **기간 일정 바 정렬(공휴일 라벨 밀림) 수리** — 여러 날 기간 일정 바가 공휴일/절기 라벨(`.cal-cell-label`)이 있는 칸에서만 아래로 밀려 계단처럼 끊겨 보이던 문제(예: 7/17 제헌절 칸의 '놀러감' 바). 원인: 셀 라벨이 `min-height:9px`이라 텍스트가 있는 칸에서 라벨 높이가 커질 수 있어 이벤트 시작 Y가 칸마다 달라짐(빈 라벨 placeholder는 이미 있었으나 min-height는 상한이 없음). **수정**: `.cal-cell-label`을 `height:11px`(고정)+`line-height:11px`+`box-sizing:border-box`+`overflow:hidden`으로 → 라벨 유무·텍스트 길이·폰트 무관하게 라벨 영역이 항상 정확히 11px, 이벤트 시작 Y가 모든 칸에서 동일. Playwright로 기간 일정 16~18 + 17일 제헌절 재현해 바 Y 차이 0px·텍스트 클리핑 없음 검증. ※ 현재 배포본(1.1.2)엔 이 min-height 상한 미보정 상태라 실기기에서 보였던 것으로 추정 — 이번 빌드부터 해소.
37. 🚧 **홈 화면 위젯 — 앱측 브릿지 이식 완료, 네이티브(Swift/CI/Apple) 미완** — **index.html 통합 완료(2026-07-11)**: 옛 `widget_today` 단일방 syncWidgetData를 **브릿지 v2로 교체**(전역 `syncWidgetData`/`buildWidgetPayload`/`_wgDoSync`/`_wgShift` @~8205) — 내가 속한 모든 방의 [오늘-7~+40] 일정+멤버(프로필명/가상)+할일+seal을 App Group(`group.com.lsung.uricalendar`) `widget.data` 키에 JSON 기록. `isNativeApp()` 아니면 no-op(웹/PWA 영향 0). loadEvents·일정이동 2곳에서 호출(디바운스 700ms). **딥링크 핸들러**: `com.lsung.uricalendar://add?room=<id>` → `appUrlOpen`(~7349)에서 `enterRoom`+`openDateSheetForToday`(위젯 ＋버튼). **할일 flush**: `setupWidgetForegroundFlush`가 `appStateChange(isActive)`마다 `_wgFlushPendingTodos()` — App Group `widget.pendingTodoToggles`(위젯이 append한 todoId 배열)를 **id별 홀짝**(홀수만 최종 토글)으로 DB `is_done` 반영(done_by/done_at/expires_at, toggleTodoDone과 동일)→대기열 비움→재동기화. ⚠️ **버그 수정**: DB 컬럼은 `is_done`인데 브릿지가 `done`으로 읽어 항상 미완료로 나오던 것 → `select is_done`+`done:!!t.is_done`(index.html+widget/bridge.js 둘 다). Playwright 검증(bridgelive.mjs JSON 정확·widgetflush.mjs is_done매핑+flush 홀짝). — **아직 남은 것(네이티브)**: (1) `widget/ios/UriCalendarWidget.swift`(4종 뷰·AppIntentConfiguration 방선택·멤버필터·ToggleTodoIntent·＋딥링크) **Xcode 미컴파일**(첫 빌드 때 API 버전차 잡기). (2) Apple Developer: App Group `group.com.lsung.uricalendar` + 위젯 App ID `com.lsung.uricalendar.widget` 생성. (3) Codemagic: cap sync 후 `add_widget_target.rb` 주입 스텝+위젯 서명. ⚠️ **빌드 저장소는 이 repo가 아니라 `lsung179-stack/appstore`**(package.json·assets/·google-services.json·ios/·www/ 보유; www/index.html이 실제 배포본). **2026-07-11 appstore 브랜치 `claude/ios-widget`에 실제 반영 완료**: (a) www/index.html ← 이 repo index.html 전체 복사(위젯 브릿지+range-bar fix 포함), (b) widget/ 폴더 복사, (c) package.json에 **`capacitor-widgetsbridge-plugin`** 추가, (d) codemagic.yaml의 ios-compile-check(무서명 컴파일검증)+ios-testflight에 위젯 주입 스텝(Sign in with Apple 엔타이틀먼트 뒤), (e) 서명은 업로드된 배포 인증서(`uricalendar_dist`, 개인키 포함)를 `ios_signing.certificates`로 키체인에 설치한 뒤, `app-store-connect fetch-signing-files`로 앱+위젯 두 번들 ID '프로파일'만 자동 발급(고정 프로파일 참조 제거). ⚠️ 인증서까지 fetch로 자동 발급 시도하면 "Cannot save Signing Certificates without certificate private key"로 실패함(기존 배포 인증서 개인키를 Apple이 안 내려줌) → 반드시 업로드 인증서 유지. ⚠️ **@capacitor/preferences 못 씀**: group 옵션 줘도 UserDefaults.standard에 키 프리픽스만 붙여 저장→별도 프로세스 위젯이 못 읽음. **widgetsbridge**가 `UserDefaults(suiteName:group)` 직접 기록(프리픽스 無)→위젯이 `widget.data`/`widget.pendingTodoToggles` 그대로 읽음. ⚠️⚠️ **플러그인 JS 이름은 `Capacitor.Plugins.WidgetsBridgePlugin`**(registerPlugin('WidgetsBridgePlugin')) — 축약형 `WidgetsBridge`가 아님! 처음에 `WidgetsBridge`로 접근해 undefined→syncWidgetData가 조용히 return→App Group에 아무것도 안 써져 위젯 방 목록이 텅 비었음(TestFlight 1.1.5(29)에서 발견). `_wgDoSync`·`_wgFlushPendingTodos` 둘 다 `WidgetsBridgePlugin` 사용. **호출 지점**: loadEvents·loadRooms·일정이동 후 syncWidgetData(디바운스 700ms). **네이티브 등록**: CAP_PLUGIN 매크로(구식 .m)라 CocoaPods 빌드(`cap add ios --packagemanager CocoaPods`)에서 자동 등록됨. ⚠️ 위젯 배포타깃 iOS17(AppIntentConfiguration 필수, 호스트앱은 16 유지). 다음: appstore 브랜치로 Codemagic **ios-compile-check** 먼저 돌려 Swift 컴파일 검증→통과 시 ios-testflight→green이면 main 머지. 상세=`widget/README.md`. **방향 확정(2026-07-10)**: iOS+Android 둘 다(Android Glance는 iOS 안정화 후) / 4종(오늘 small·2주 medium·콤보 large·월그리드) / 방(프로필) 선택형·멤버 필터. **위젯 UI 실기기 피드백 반영(2026-07-11)**: 4종 분리(토글 아님)·씰 PNG 래스터라이즈 표시·씰 탭=방 순환(CycleRoomIntent)·프로필 탭=멤버 필터(SetFilterIntent)·small=내 일정만·프로필 사진 로드(base64 PNG)·기간 일정 연속 바(런 lane-packing, 주 경계서 끊김 방지)·월 `< >` 이동(ShiftMonthIntent)·다음달 흐림·함께 일정 dedup(4명→1바)·상하 잘림 방지(행 높이 균등분배)·모든 달 **5주 고정**(6주 달은 마지막 1~2일 미표시—사용자 수용)·새로고침(RefreshIntent)·격자선 앱설정 연동(gridV/gridH). **콤보(large) '다가오는 일정' 기간 묶기(2026-07-11)**: `ComboView.upcoming`을 `[UpRun]`로 변경 — 같은 제목+색의 연속 날짜를 하나로 묶어 `rangeLabel`로 '7월1일 ~ 7월5일' 칩 표시(하루는 오늘/내일/M.d+시간), GridView 런 그룹핑과 동일 로직. **헤더 '전체' 칩 + '나 먼저' 정렬(2026-07-11)**: 멤버 4명↑일 때 prefix로 내 아바타가 잘리던 문제 → `orderedMembers`가 `myUserId`를 맨 앞 고정(GridView/ComboView에 myUserId 전달). 아바타 줄 맨 앞에 **'전체' 칩**(SetFilterIntent 빈값=필터 해제, active==nil일 때 강조, `lineLimit(1)+fixedSize`로 '전'으로 안 잘림). maxAvatars=large 4. **2주(medium) 위젯 주 이동(2026-07-11)**: `ShiftWeekIntent`(App Group `widget.weekOffset`, 1페이지=14일)+`readWeekOffset()` 추가, `GridView.weekNav`가 startDate에 `weekOffset*14`일 적용, 헤더에 `< 7.6~19 >`(weekLabel, 라벨 탭=현재로 복귀=delta -weekOffset, 오프셋≠0이면 terra색). 2주 헤더는 주이동 라벨이 넓어 maxAvatars=3(나 먼저라 내 것은 항상 보임). 월 위젯 ShiftMonthIntent와 동형(인터랙티브 위젯이 perform 후 자동 리로드). **콤보 미니달력 달 이동(2026-07-11)**: `ShiftComboMonthIntent`(App Group `widget.comboMonthOffset`, 월 위젯과 독립)+`readComboMonthOffset()`, `MiniMonth`가 base=today+off월 적용·헤더 `< 8월 >`(라벨 탭=복귀). 다가오는 일정 리스트는 오늘 기준 유지(미니달력만 브라우즈). **🚨 기간 바 오연결 수정(2026-07-11)**: 위젯이 **제목+색이 같으면 연속 날짜를 자동으로 기간 바로 이어붙이던 버그**(예 '놀러감' 7/17·7/18 단독 2건이 한 바로 연결). 앱은 `isSameRangeGroup`으로 **`shared_id`(함께 기간)/`range_group_id`(단독 기간)가 일치할 때만** 연결하고 단일·여러날(is_multi)은 절대 안 붙임. → 브릿지 payload의 events에 **`gid`** 필드 추가(`buildWidgetPayload`: is_multi→null, shared_id→'s:'+id, range_group_id→'r:'+id, 그 외 null / select에 `range_group_id,shared_id,is_multi` 추가). Swift `WGEvent.gid`, `GridView.runs`·`ComboView.upcoming` 그룹핑을 title|color→**gid 기준**으로 교체(gid null이면 각각 단독 칩). index.html·widget 둘 다 변경 → **재빌드 필요**.
36. ✅ **관리자 광고 코호트 + 초대 전환 지표** — (a) `admin_ads_cohorts()` RPC 신설 + 대시보드에 '주간 가입 코호트(광고 효과 추적)' 표: 가입 주별 가입→7일 생존→초대 합류→체험→유료→해지예약. Apple Ads 채널의 실효율(설치가 유저가 되는지) 판독용. 7일 미경과 코호트는 '측정중', 일부 경과는 '(n/m 경과분)'. (b) `admin_growth_stats()`에 초대 루프 지표 추가: '초대 입장(7일/30일)'·'초대 동반가입률'(30일 신규 중 가입 7일 내 타인 방 입장) 카드 2개 + 구독 전환율 카드에 해지예약 수. 판별 기준: 초대 입장=타인 소유 방의 members 행, 체험=period_type TRIAL, 유료=NORMAL, SANDBOX/is_manual 제외. admin.html은 정적 파일이라 머지 즉시 라이브. ⚠️ 첫 실데이터: 6/29주 광고 유입 70명 중 7일 경과분 27명의 생존 1명(4%) — 광고 설치의 대부분이 첫 주 내 이탈(코호트 표가 이걸 계속 추적).
35. ✅ **광고 배너-탭바 10px 이격(AdMob 오클릭 정책)** — 구글 AdMob '의도치 않은 클릭 방지' 정책 안내 메일 대응. 배너는 화면 최하단 고정 그대로 두고, `body.has-ad`의 탭바 offset을 `--ad-h`→`--ad-h+10px`로(콘텐츠 padding 64→74, FAB·오늘버튼·토스트 74→84 함께 보정). `--ad-h`는 `bannerAdSizeChanged` 이벤트로 기기별 실제 배너 높이가 들어오므로 어떤 화면 크기에서도 겹침 불가 — +10px은 순수 여백 추가라 가림 위험 0. Playwright로 --ad-h 56/90px 각각 탭바 bottom 66/100px·해제 시 원복 검증.
34. ✅ **방 프로필(컬러 씰) 무료 전환** — 사용자 결정으로 프리미엄 게이트 전면 해제. `saveRoomMascot`/`clearRoomMascot`의 `isEffectivePremium()` 체크+`showUpgradeModal` 삭제(누구나 저장), 프리미엄 구독 시트의 '방 프로필'(🪷) 기능 행 제거, 온보딩 꾸미기 문구를 "전통 문양 씰로 방을 꾸미고, 스토어의 테마·글꼴로 캘린더를…"로 조정(씰을 유료 그룹에서 분리). 스토어 히어로 배너·연필 진입·개인별 저장(members.seal) 로직은 그대로. Playwright 검증(무료 유저 저장 성공·업그레이드 모달 0회·프리미엄 시트 행 제거).
33. ✅ **알림 설정 화면(종류별 토글) + 카테고리별 발송 필터** — 설정→기타에 **'알림 설정' 행** 추가(고급 설정 아래). ⚠️ **설정 탭 화면은 `uni-nav` 마크업(~line 3150, `openAdvSettings` 버튼 근처)** — 처음에 옛 방설정 시트(`roomSetCard`의 rs-nav-row)에만 넣어서 실기기에서 안 보였음(#61에서 uni-nav에 추가로 수정, roomSetCard 쪽 행도 잔존) → 전용 시트 `#notifSetCard`(⚠️ id 주의: `notifCard`는 기존 알림함 시트가 이미 사용 — 충돌로 개명함). 구성: (a) **기기 권한 상태 카드**(`_ntfRenderSys`) — `getPushStatus()` 기준 granted=✅안내 / default=🔕+'알림 켜기' 버튼(`ntfEnablePush`) / denied=🔕+**휴대폰 설정에서 켜는 법 플랫폼별 안내**(iOS: 설정→알림→우리 캘린더 / Android: 설정→앱→알림) — 거절 유저 복구 경로(공지 대신 상시 UI로 해결). (b) **앱 전체 알림 마스터 토글**(`toggleNotifMaster`) — 구 고급설정 '알림 받기'를 이동·iOS 스위치화(`.ntg`), `members.notifications_enabled` 전체 update + 켤 때 권한 요청(구 `toggleRoomNotification`/`syncRoomNotifButton`은 삭제). 마스터 OFF면 하위 카테고리 dim(`.ntf-dim`). (c) **카테고리 토글 7종**(`NOTIF_CATS`: event 캘린더/todo 할일/reminder 미리알림/daily 아침요약/nudge 일정등록안내/birthday 생일/social 반응·멤버) — **`profiles.notif_prefs` jsonb**(마이그레이션 `add_profiles_notif_prefs`, 키없음/true=켜짐·false=꺼짐)에 저장, 낙관 반영+실패 롤백(`toggleNotifCat`). **발송 필터는 서버 중앙 처리**: `send-push` **v8** — body에 `kind`(선택) 받아 수신자 notif_prefs[kind]===false 제외(kind 미전달=구버전 앱은 기존대로 전체 발송, 하위호환) / `push-cron` **v13** — `prefOffSet()` 헬퍼로 daily→'daily'·empty-rooms→'nudge'·reminder→'reminder'·birthday→'birthday'(수신자+본인) 필터(인앱 notifications row도 함께 스킵). 클라이언트 6개 발송 지점에 kind 추가(event×3·social=member_joined/reaction·todo). Edge Function 원본은 Supabase 버전 히스토리(send-push v7/push-cron v12)로 롤백 가능. Playwright 6종 검증(시트+prefs 로드·카테고리 토글 저장/재토글·마스터 OFF/ON(권한요청)·거절자 안내 카드), v13 크론 실행 200 확인.
32. ✅ **빈 방목록 '초대 코드로 입장' CTA — 초대 루프 개선 2단계** — 신규 가입자(초대받고 온 사람 포함)가 처음 보는 빈 방목록(`renderEmpty`)에 CTA가 '+ 방 만들기' 하나뿐이라 초대받은 사람의 입장 동선이 없었음(우상단 + 메뉴를 스스로 찾아야 함 → 혼자방 생성/이탈 원인). 고스트 버튼 **'초대 코드로 입장'**(`openJoinRoom()`) 추가 + 안내 문구를 "방을 만들거나, 초대받았다면 코드로 바로 입장"으로 교체. 코드입장 시트의 '복사한 코드 붙여넣기'(`pasteJoinCode`)와 이어져 r.html에서 자동 복사된 코드가 여기서 소비됨. index.html 변경이라 재빌드 필요. Playwright 2종 검증(버튼 2개 렌더·CTA→joinCard 열림).
31. ✅ **초대 랜딩(r.html) 스마트화 — 초대 루프 개선 1단계** — 혼자방 60% 누수의 초입 수리. 기존 r.html은 코드 수동복사+스토어 배지뿐이라, 앱에 이미 있던 딥링크(iOS `com.lsung.uricalendar://join?code=`, index.html ~7286)와 Android intent 패턴(~7515)을 랜딩이 전혀 안 썼음. 추가: (a) **'앱으로 바로 열기' 버튼**(모바일+코드 있을 때만) — iOS=커스텀 스킴, Android=`intent://…package=…twa`(fallback=r.html?noauto=1), 탭 시 **코드 자동 클립보드 복사**(미설치자가 설치 후 앱 안 '붙여넣기' 버튼으로 이어짐 = SDK 없는 deferred deep link), 1.6s 후에도 페이지가 보이면 설치 안내 힌트(`#appHint`) 표시. (b) **방 이름 개인화** — `lookup_room_by_code` RPC가 SECURITY DEFINER+anon EXECUTE라 r.html에서 익명 fetch로 호출해 "'가족방'에 초대받았어요" 표시(XSS 방지 위해 textContent로만 조립, 실패 시 기본 문구 유지). (c) Android 인앱브라우저(카톡 등)는 진입 시 자동 intent 시도(세션당 1회 `sessionStorage['r.autoOpen']`, `noauto=1`로 루프 방지). r.html은 정적 파일이라 **재빌드 불필요, 머지 즉시 라이브**. Playwright 6종 검증(iOS 버튼+개인화·미설치 힌트+클립보드·intent URL 형식·인앱 자동시도·PC/코드없음 숨김·RPC 실패 폴백). ※ 코드입장 시트의 '복사한 코드 붙여넣기'(`pasteJoinCode`, 갤럭시 클립보드 거부 폴백 포함)는 원래 있던 기능.
28. ✅ **바텀시트 스와이프 다운 닫기** — `.mo-card` 시트 공통(방 프로필·방 설정·검색·날짜이동 등): **상단(핸들 `.mo-handle`/제목 `.mo-title`/부제 `.mo-sub` 직계 자식)을 잡고 아래로 내리면 닫힘**. 구현: document 위임 pointerdown/move/up(전역 `_shDrag`, `_shEnd`) — 8px 이상 내리면 드래그 활성(시트가 손가락 1:1 추적, `transition:none`+inline `translateY`), 놓을 때 `min(120px, 높이 30%)` 넘으면 `translateY(110%)`로 마저 내린 후 **오버레이 클릭으로 원래 close 함수 호출**(상태 정리 보장, 오버레이 미연결 시 `.on` 직접 제거 폴백), 미달이면 스프링백. 드래그 존 CSS `touch-action:none;user-select:none`(상단 3요소만 — 시트 본문 스크롤은 그대로). ⚠️ 데스크톱 프레임 모드(line ~165 `.mo-card.on{...!important}`)에선 시각 추적은 안 되지만 닫기 동작은 정상. Playwright 3종 검증(큰 스와이프 닫힘·작은 스와이프 유지·타 시트 동작).
- 저장소 `main`의 `index.html`은 **build 1.1.4 (28)** — 2026-07-08 출시본(항목 8~36 + 파스텔 씰 반영). 직전 준비본 1.1.3(27), 직전 실배포 1.1.2(26). **위젯 빌드는 1.1.5 (29)** — 1.1.4 train이 App Store Connect에서 닫혀(이미 배포됨) 새 빌드는 1.1.5로 올림(codemagic APP_VERSION=1.1.5, buildInfo=1.1.5 (29)). 단 개발자가 이후 로컬에서 다시 수정·업로드하면 main보다 앞설 수 있으니, 새 세션은 항상 현재 배포/로컬 버전을 확인할 것.
- 옛 버전 기반 브랜치/PR(예: `claude/calendar-ads-free-users-l7jsbt`의 PR #2)은 **머지 금지** — 삭제한 관리자 버튼 부활 + 최신 리팩터 덮어쓰기 위험.
- 배포 버전 확인: 앱 설정 화면 푸터의 `build ...` 문자열(런타임에 JS가 세팅, HTML 정적값과 다를 수 있음).

## 새 세션 체크리스트
1. 이 `CLAUDE.md` 읽기.
2. 작업 대상이 프리미엄/광고면: `my_premium_status()` 정의 + `subscriptions` 조회로 DB 진실 먼저 확인(추측 금지).
3. 클라이언트 코드는 `main`의 `index.html` 기준으로 작업(현재 최신 동기화됨 — 단 개발자 로컬이 더 앞설 수 있으니 확인).
4. 광고 관련이면 네이티브 여부와 `_admobDebug` 값부터 확인.
