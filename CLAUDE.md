# 우리 캘린더 (Our Calendar) — 프로젝트 인수인계 노트

> 이 파일은 새 세션이 시작될 때 자동으로 읽힙니다. 매번 설명하지 않아도 됩니다.
> 새 대화를 열면 먼저 이 파일 + 관련 코드/DB를 확인한 뒤 작업하세요.

## ⚠️ 유지보수 규칙 (Claude에게 — 반드시 지킬 것)
**이 저장소의 파일(특히 `index.html`)이나 DB(RPC/마이그레이션)를 변경할 때마다, 같은 커밋에서 이 `CLAUDE.md`도 갱신한다.**
- 무엇을 바꿨는지 아래 “이미 완료된 수정” 목록에 한 줄로 추가(또는 기존 항목 수정).
- 프리미엄/광고/결제 로직, 함수명, 계정, DB 스키마, 워크플로우가 바뀌면 해당 섹션도 함께 고친다.
- 날짜/버전 기준이 바뀌면 “이미 완료된 수정”의 날짜 표기도 갱신한다.
- 즉 코드 변경과 이 문서를 **항상 함께 커밋**한다 (문서가 뒤처지지 않게).
- 🎨 **컬러 팩 신규 제작 시 중복 색상 금지** (사용자 지시, 2026-07-22). 새 컬러 팩을 만들 때 팩 안에서든 기존 팩(`STORE_COLORS` 전체)과든 **너무 똑같은(거의 동일한) 색상이 있으면 다르게 조정**할 것 — 제작 전에 기존 전 팩 hex 값과 대조해 사실상 같은 색이면 명도/채도/색조를 바꿔 구분되게 한다.
- 🖼️ **테마 배경씬(bgScene·카드)은 테마마다 완전히 다른 컨셉으로** (사용자 지시, 2026-07-25). 캐릭터 테마 배경을 만들 때 "동물이 뒤돌아 자연 속을 걸어가는" 같은 **똑같은 구도를 재탕 금지** — 각 테마마다 시점·구도·분위기·시간대·활동을 다르게(예: 토끼=달밤 올려다보기, 곰=굴속 낮잠, 원숭이=덩굴 그네, 쥐=땅속집 단면, 닭=새벽 헛간). 아이콘은 이미 테마별 오브젝트가 달라 OK, **배경만 이 규칙 적용**. 여우비만 원조(뒤돌아 걷기) 유지.
- 🧩 **위젯 수정은 iOS와 Android(갤럭시)를 반드시 동시에 수정한다** (사용자 지시, 2026-07-20). 위젯 관련 변경(새 기능·버그수정·UI 조정 등)은 항상 **`widget/ios/UriCalendarWidget.swift`(SwiftUI)** 와 **`widget/android/*`(RemoteViews Java + res)** 둘 다에 반영하고, appstore repo(`www/`·`widget/`)에도 동기한다. 한쪽만 고치면 안 됨. Android는 `/tmp/awstub`로 stub 컴파일 검증, iOS는 오프라인이라 브레이스 균형만 확인.

## 프로젝트 개요
- 한국어 공유 캘린더 앱. **단일 `index.html` SPA**를 Capacitor 8(WebView)로 감싼 구조. 모든 JS/CSS/HTML이 이 한 파일 안에 있음.
- 백엔드: **Supabase** (Auth, Postgres, RPC). 구독 결제: **RevenueCat** (@revenuecat/purchases-capacitor). 광고: **AdMob** (@capacitor-community/admob) — 하단 배너 1개.
- 빌드: **Codemagic**이 매 빌드마다 네이티브 프로젝트 재생성 → 실기기(iOS + Android) 설치.

## 배포 워크플로우 (중요)
- 개발자는 보통 **`index.html`을 GitHub에 직접 업로드**(“Add files via upload”)하는 방식으로 작업. 그래서 **로컬 최신본이 저장소(main)보다 앞서 있을 수 있음** — 새 세션은 반드시 현재 배포/로컬 버전이 무엇인지 확인할 것.
- 클라이언트 코드 변경은 **Codemagic 재빌드** 후에야 기기에 반영됨.
- **DB(RPC/마이그레이션) 변경은 재빌드와 무관하게 즉시 라이브.**
- ⚠️ 광고(AdMob)는 **네이티브 앱에서만** 동작. 웹/PWA에서는 절대 안 뜸(정상). 광고 확인은 반드시 네이티브 앱에서. 앱 설정 화면 하단 푸터에 `네이티브`/`웹` 및 `광고:<_admobDebug>` 문자열이 표시됨.
- 🚦 **[매우 중요] 배포 게이트 = `deploy_version` (WebView/index.html 변경이 실제 사용자에게 보이려면 이걸 올려야 함)**: 이 앱은 서비스워커(`sw.js`) + **제어된 배포(gated rollout)** 를 씀. index.html을 새로 빌드/번들해도, **Supabase `app_config` 테이블의 `deploy_version`(값=`'YYYY-MM-DD HH:MM:SS'` KST) 타임스탬프를 올리지 않으면 일반 사용자는 옛 SW 캐시 index.html을 계속 봄**(=옛날 파일 적용됨 증상). 동작: 앱 시작 시 `checkDeployVersionOnStart()`+SW 발견 시 `_checkAndApplySWUpdate()`가 서버 `deploy_version` vs 로컬 `localStorage['uricalv2.deployVersion']` 비교 → **다르면** 캐시 비우고 새 SW 활성화+새로고침, **같으면** 옛 버전 유지. ⚠️ **관리자(`isAdmin()`, naver 계정)는 게이트 우회**해 항상 최신 → "관리자는 최신인데 일반계정은 옛날" 증상의 원인. **배포 방법 2가지**: (a) 앱 관리자 메뉴 **'배포하기' 버튼**(`openDeploySheet` → `deploy_version`=현재 KST 시각 저장), (b) SQL 직접: `update app_config set value=to_char(now() at time zone 'Asia/Seoul','YYYY-MM-DD HH24:MI:SS'), updated_at=now() where key='deploy_version';`. ⚠️ **위젯(네이티브 RemoteViews/WidgetKit)은 이 게이트와 무관** — 위젯 변경은 AAB/IPA 재빌드+설치로만 반영(deploy_version 불필요). ⚠️ **2026-08-11 `deploy_version`을 `2026-08-11 18:30:22`(KST)로 재갱신함**(iOS 네이티브 푸시 플랫폼별 플러그인 분기 수정 직후, 사용자가 바로 확인하려 재갱신 — JS 전용이라 IPA 재빌드 없이 SW 업데이트로 즉시 반영됨) — 이 시점까지 누적된 index.html 변경(방 프로필 리셋 3종·씰-테마 동시저장 버그·일정 수정/복사/삭제 방 멤버 전체 허용·마케팅 버전 1.1.7 승격·월헤더/공휴일라벨 큰글씨 대응·반복 종료일[초안]+리디자인+드롭다운 재교체+커스텀 드롭다운+상자 기준 앵커링·시간 피커 시작/종료 라벨+키보드 동시노출 방지·iOS 네이티브 푸시 라우팅[부분수정]+플랫폼별 플러그인 분기 등)이 PWA/WebView/네이티브 앱 사용자에게 즉시 반영됨. ⚠️ **단, iOS 푸시 실제 전달은 GoogleService-Info.plist 커밋 + 다음 iOS 빌드(IPA) 전까지는 여전히 안 됨**(JS 라우팅만으론 부족, 위 항목 참고). 네이티브 위젯 자체(RemoteViews/WidgetKit UI)는 이 게이트와 무관 — AAB/IPA 재빌드+설치 별도 필요.

## Supabase
- 프로젝트 id: **`bgqzkkaslqchbovzrkao`**
- 핵심 테이블: `public.subscriptions` (user_id, status, expires_at, plan_type, is_manual)
- `public.members` (room_id, user_id, color, notifications_enabled, role, is_virtual, virtual_*, **`sort_order` int**=방목록 사용자별 순서, **`seal` text**=개인별 방 프로필 'color:sym', 마이그레이션 `add_members_seal`). RLS `members_update_self`(user_id=auth.uid())로 본인 행 수정 가능.
- `public.rooms` (id, name, code, owner_id, ~~`mascot` text~~). ⚠️ **방 프로필은 2026-07에 방 공유(rooms.mascot)→개인별(members.seal)로 전환** — `rooms.mascot` 컬럼과 RPC `set_room_mascot`은 **미사용 잔존**(마이그레이션 `add_room_mascot`). 클라이언트는 members.seal만 읽고 씀.
- ~~`public.anniversaries`~~ — **방별 공유 D-day/기념일 기능은 제거됨**(사용자 요청, "느낌 없음"). 클라이언트 코드(스트립/시트/JS/CSS) 전부 삭제. 단 DB 테이블 `anniversaries`(마이그레이션 `create_anniversaries`)는 **그대로 남아있음**(빈 테이블, 미사용). 나중에 재도입하거나 정리할 때 참고.
- 관리자 RPC: `admin_users_detailed()` — 회원별 활동지표(SECURITY DEFINER, 관리자만).
- 관리자 RPC: `admin_growth_stats()` — WAU/MAU·활성화율·7일 리텐션·구독전환·공유방 비율 + **초대 지표**(invite_joins_7d/30d, invite_conv_num/den=30일 신규 중 가입 7일 내 타인 방 입장, cancel_pending=해지예약)(대시보드 성장 섹션).
- 관리자 RPC: `admin_ads_cohorts()` — 최근 8주 주간 가입 코호트: 가입→7일 생존(일정·last_sign_in 기준, 7일 경과분만 분모)→초대 합류→체험(TRIAL)→유료(NORMAL)→해지예약. 체험/유료 판별=subscriptions.period_type, SANDBOX 제외(마이그레이션 `add_invite_metrics_and_ads_cohorts`).
- Edge Functions: `push-cron`(mode: daily/reminder/empty-rooms/**birthday**, **v14**), `send-push`(**v8**, body `kind` 선택 파라미터), `revenuecat-webhook`. **⚠️ push-cron v14(2026-07-25, 사용자 "복사한 일정 알림 중복 방지")**: 미리알림·데일리 다이제스트가 각 `events` 행 소유자(`ev.user_id`)에게 발송되는데, 일정을 여러 방에 복사하면 같은 `multi_group_id` 행이 방마다 생겨 복사한 사람에게 **N번 중복 알림**이 가던 문제 → `handleReminder`/`handleDaily`에서 **유저별 `multi_group_id` 1회만 발송**(Set dedup). reminder는 `reminder_sent_at`을 복사본 전체(`targets`)에 찍어 스킵된 복사본이 다음 실행에 재알림 안 되게, 응답에 `reminded`(실발송 수) 추가. 런타임 검증: reminder 200 OK. multi_group_id 없는 일반 일정은 기존대로 개별 발송(무영향). pg_cron: daily 08:00·empty-rooms 08:05·**birthday 09:00(KST=0 0 UTC)**·reminder 5분마다·오래된 notifications 정리. 생일=`profiles.birthday`('MM-DD') 매칭, 같은 방 멤버+본인에게 축하 푸시. **알림 카테고리 설정**: `profiles.notif_prefs` jsonb(마이그레이션 `add_profiles_notif_prefs`) — send-push는 kind로, push-cron은 모드별 키(daily/nudge/reminder/birthday)로 false인 수신자 제외(코드 33 참고).
- 핵심 RPC: `my_premium_status()` — SECURITY DEFINER. `status='active' AND expires_at>now()` 인 구독이 있을 때만 `is_premium=true` 반환.
  - ⚠️ 과거 버그: 집계함수(max/array_agg/bool_or)를 GROUP BY 없이 써서 구독 0건이어도 항상 1행+상수 `is_premium:true`를 반환 → **모든 유저가 프리미엄으로 오판**. `HAVING count(*)>0` 추가로 수정 완료(라이브).

## 계정
- 관리자: `lsung179@naver.com` — `ANN_ADMIN_USER_ID = '3bc13a8b-618d-4d47-abea-1eec4d682ec0'`. 실구독 보유. 앱에선 `_adminPlanOverride`(무료/유료 플랜 체험 토글)로 프리미엄 표시를 제어(실구독과 무관).
- 무료 테스트: `lsung179@gmail.com` — `64c6502a-6365-40cb-ad05-49ec8cb6439e`. 구독 없음 → 무료여야 정상(광고O, 유료기능X).

## 프리미엄 / 광고 로직 지도 (index.html 함수명)
- 상태: `_isPremium`(= `_serverPremium`), `_serverPremium`/`_serverPremiumInfo`(서버 기준), `_rcPremiumActive`(기기 RevenueCat 기준 — 판정엔 미사용).
- `_recomputePremium()` → `_isPremium = _serverPremium` (**서버가 유일한 진실**). 기기 엔타이틀먼트로 프리미엄 부여 금지.
- `loadPremiumStatus()` → RPC `my_premium_status` 호출 → `_serverPremium` 설정.
- `isEffectivePremium()` → 관리자는 `_adminPlanOverride`, 일반 유저는 `_isPremium`.
- 결제: `onPremiumSubscribe()` → `_applyRcEntitlement()`(구매 직후 낙관 반영 후 서버 재동기화). **구매 복원 `onRestorePurchases()`는 `_applyRcEntitlement` 안 씀** — `loadPremiumStatus()` 후 `_serverPremium`일 때만 프리미엄(기기 Apple ID 구독 신뢰 금지 → 계정 간 프리미엄 누수 방지).
- `syncPremiumFromRevenueCat()` — **자동 호출 금지**(기기 구독을 직접 읽어 누수 위험). 정의만 두고 호출하지 않음.
- 광고: `showAdBannerIfNeeded()`, `hideAdBanner()`, `_anySubSheetOpen()`(열린 시트 있으면 광고 숨김), `_elVisible(el)`(실제 보이는 요소만 카운트 — 잔여 `.on` 오판 방지), `_admobDebug`(설정 화면 푸터에 노출).

## 이미 완료된 수정 (2026-07 기준)
1. ✅ **RPC `my_premium_status()` 수정** — `HAVING count(*)>0` 추가. DB 라이브. (무료 유저가 프리미엄으로 뜨던 근본 원인)
2. ✅ **광고 게이팅** — `_anySubSheetOpen()`이 `_elVisible()`로 실제 보이는 시트만 카운트(닫힌 시트 잔여 `.on`이 광고를 영구히 숨기던 버그).
3. ✅ **구매 복원 누수** — `onRestorePurchases()` 서버 단일 기준. 기기 엔타이틀먼트로 프리미엄 안 켜짐.
4. ✅ **멤버 없을 때 캘린더가 위로 올라가던 현상** — 노치 여백(`env(safe-area-inset-top)`)을 조건부로 표시되던 `.cal-member-tabs`에서 항상 존재하는 부모 `.screen.cal`로 이동.
5. ✅ 관리자 버튼 `전체 이용자 프리미엄`(globalTrialBtn)·`프리미엄 개별 관리`(premMgmtBtn) UI 삭제(충돌 우려). **(2026-07 추가 정리)** 남아있던 죽은 코드 완전 제거 — index.html의 `premMgmtOverlay/Card` 모달 마크업 + `loadGlobalTrialState/toggleGlobalTrial/openPremiumManage/premSearchUsers/premSetOverride` 등 함수 일체 삭제. 이 override 방식(`premium_overrides` 테이블·`app_config.global_premium_trial`)은 **애초에 무효**였음 — `my_premium_status()`가 `subscriptions`만 읽어서 override를 반영하지 않음. DB의 RPC(`admin_set_premium_override`/`set_global_premium_trial`)는 그대로 두되 미사용. ⚠️ **진짜 수동부여는 admin.html의 '수동 구독 부여'(`admin_grant_subscription` RPC → `subscriptions`에 `is_manual=true` 행 insert)이며 정상 작동함.** 이건 유지. 급하면 SQL로 직접 부여 가능: `insert into subscriptions(user_id,plan_type,status,started_at,expires_at,is_manual,manual_reason,granted_by,granted_at,source) values(<uid>,'manual','active',now(),now()+interval '1 year',true,'사유',<admin_uid>,now(),'manual');`
6. ✅ **7일 무료체험 노출(전환)** — 전 플랜 첫 결제 무료체험이 UI에 안 보이던 문제. `showUpgradeModal`(잠금 안내: "무료로 체험하기"), `openPremiumSheet`/CTA 버튼("7일 무료로 시작하기"), `_updateTrialCta()`(선택 플랜 가격 기준 "첫 7일 무료·이후 자동결제" 캡션 `#premTrialNote`), `renderTrialBanner`("첫 7일 무료")에 반영.
7. ✅ **혼자 방 초대 넛지(바이럴)** — 실멤버 1명인 방 캘린더 상단(`#calInviteNudge`)에 초대 배너 표시. `renderInviteNudge()`가 `_myMembers` 실멤버<2일 때 노출, `shareKakao()` 호출. `renderMemberTabs()` 시작부에서 호출. 배너에 ✕ 닫기 버튼(`dismissInviteNudge`) — 닫으면 그 방 id를 `localStorage['uricalv2.inviteNudgeDismissed']`에 저장해 다시 안 띄움(방별 기억). ✕ 클릭 시 `vConfirm`('다시 보지 않기'/'취소') 확인 후 저장(실수 방지).
8. ✅ **프리미엄 시트 탭바 숨김 버그** — 일정추가(탭바 숨김)→프리미엄 시트 진입 후 닫으면 하단 탭바가 안 돌아오던 문제. `openPremiumSheet`에 `hideTabbar()`, `closePremiumSheet`에 `showTabbar()` 추가(다른 풀스크린 시트와 대칭). 겸사겸사 프리미엄 안내 모달(`showUpgradeModal`의 `vConfirm`) 타이틀의 ✨ 제거.
9. ✅ **관리자 페이지 자동 로그인(세션 전달)** — `openAdminFullPage()`가 admin.html로 세션 토큰(`#at=&rt=`)을 안 실어 보내, 외부 브라우저/새 탭에서 세션이 없어 비번 폼으로 빠지던 버그. 관리자 계정이 **카카오/애플 소셜 로그인(비번 없음, `has_password:false`)**이라 비번 폼으로는 진입 불가였음. `getSession()` 토큰을 URL 해시로 실어 보내 admin이 `setSession`으로 자동 로그인하도록 수정.
10. ✅ **관리자 회원관리 상세화** — DB에 `admin_users_detailed()`(SECURITY DEFINER, 관리자 가드) RPC 추가: 유저별 최근활동(last_sign_in/이벤트/가입 GREATEST)·일정수·방수·구독·푸시토큰 유무. admin.html 회원관리를 **카드 대시보드**로 개편: 상단 KPI 타일(전체·7일/2주 활성·휴면·구독), 필터칩(+구독자), 회원 카드(활동색 아바타 링·이름·최근활동·📅일정·🏠방·가입경로·💎구독/휴면/🔔push 배지), 활동많은순·최근일정순 정렬, KPI에 7일 신규 추가. 카드=좌측 활동색 바+상태라벨(활발/보통/뜸함/휴면)+3칸 그리드(일정/방/최근접속)+'🗓 마지막 일정' 줄(last_event_at). '휴면(30일+ 미활동)'=앱삭제/이탈 추정(정확한 삭제 신호 없음). ※ '최근 활동'은 last_activity(=GREATEST(last_sign_in,last_event,가입)) 기준 — last_sign_in만 쓰면 로그인 유지 유저가 오래전 접속으로 오표시됨. 🔔=fcm_tokens OR push_subscriptions(알림 허용). 필터에 '최근 가입순' 있음.
11. ✅ **관리자 성장 대시보드** — `admin_growth_stats()` RPC + 대시보드에 '성장·리텐션·전환' 섹션(WAU/MAU, 활성화율, 7일 리텐션, 구독 전환율, 공유방 비율). Edge Function `push-cron`/`send-push`로 리텐션 푸시는 이미 가동 중.
58. ✅ **붓글씨 유행어 팩(calli)** (2026-07-22) — ⚠️ **변천**: 처음 하회탈(tal)+붓글씨(감성문구: 최고·사랑해·화이팅·축하해·고생했어·ㅋㅋㅋ) 2팩을 만들었으나 사용자 피드백 "탈은 별로, 붓글씨는 유행어로" → **하회탈 팩 완전 삭제**(STORE_EMOJI·CHAR_EXPRESSIONS·CHAR_META·ANIMAL_STICKER에서 제거, 이미지 CDN에만 잔존) + 붓글씨를 **요즘 유행어 6종으로 교체**: yar(야르)·yaho(야호)·crazy(미쳤다)·godlife(갓생)·lucky(럭키비키)·injeong(인정). '최고' 붓글씨(job 90c5e83b)를 서체 레퍼런스로 통일 생성(야르는 1회 nsfw 오탐→의미 설명 추가로 재생성 성공, 미쳤다도 1회 실패→재생성). 스토어명 '붓으로 쓴 요즘 유행어, 야르!', CHAR_META.name='붓글씨', identity='먹붓 유행어 캘리'. URL=hf_20260722_060*, 이모지 배너 '12가지'→'11가지 캐릭터 팩'(동물10+붓글씨1). ⚠️ AI 생성 한글이라 획 어색/오타 리스크 — PWA에서 확대 확인 권장. Playwright 검증(6키·tal 제거·행11·배너·문법0). **날씨 팩(weather) 추가(2026-07-22)**: 사용자 요청(눈·비·햇빛 등) — key `weather`, 6종 sun(햇빛)·rain(비)·snow(눈)·cloud(흐림)·storm(번개)·rainbow(무지개). 웃는 해(job 6410132e)를 스타일 레퍼런스로 카와이 날씨 캐릭터 통일 생성(무광 스토리북·파스텔·볼터치), URL=hf_20260722_0606*. 스토어명 '오늘 날씨 어때? 몽글몽글 날씨 친구들'(calli 아래 배치), CHAR_META.name='날씨'. 배너 '11가지'→'12가지 캐릭터 팩'(동물10+붓글씨+날씨). Playwright 검증(6키·행12·배너·문법0). **비/눈 구분 강조 재생성(2026-07-22)**: 비·눈이 구름 색만 달라 구분 안 된다는 피드백 → 해(레퍼런스)만 두고 5종 재생성: 비=굵은 파란 빗줄기+튀는 물방울, 눈=**빨간 니트모자 쓴 구름**+큰 육각 눈결정+쌓인 눈, 흐림=낙하물 없는 뿌연 회색구름+안개, 번개=큰 황금 지그재그 번개+옆 빗줄기, 무지개=프레임 채우는 큰 아치. URL=hf_20260722_0614*.
(구) 하회탈+감성 붓글씨 기록: (2026-07-22) — 동물 외 확장 1탄. (a) **하회탈 탈이**(key `tal`): 하회 양반탈 치비 캐릭터(한복+민화 스토리북풍), 6감정 like(좋아요)·laugh(빵터짐)·love(사랑)·surprised(깜짝)·sulk(삐짐)·cheer(화이팅). 대표(job 9fa31c0a) 생성 후 레퍼런스 물림으로 5장 통일. 스토어명 '어깨춤 덩실덩실 신명나는 탈이', CHAR_META.name='탈이'. (b) **붓글씨**(key `calli`): 먹붓 캘리그라피 텍스트 스티커+붉은 낙관, 6문구 best(최고)·love(사랑해)·cheer(화이팅)·congrats(축하해)·effort(고생했어)·laugh(ㅋㅋㅋ). '최고'(job 90c5e83b) 레퍼런스로 서체 통일('고생했어'는 1회 실패→재생성). 스토어명 '마음을 전하는 붓글씨 한 줄', CHAR_META.name='붓글씨'. 전부 remove_background 투명 PNG(hf_20260722_055*), ANIMAL_STICKER·CHAR_EXPRESSIONS·CHAR_META 추가, STORE_EMOJI **맨 위 2행**(신규 우선), 이모지 배너 부제 '10가지 동물'→'12가지 캐릭터 팩'. 리액션 피커·게이팅은 제네릭이라 자동 반영. ⚠️ 캘리는 텍스트 이미지라 한글 오타 리스크 — 확대 확인 권장(생성 모델 텍스트). Playwright 검증(12키 img·행12·tal 최상단·배너·상세시트·문법0). index.html 변경 → 재빌드 필요(PWA 무관).
57. ✅ **여름 과일 컬러 팩 4종 + 출시 히어로 배너** — 스토어 컬러탭에 여름 제철 과일 팩 4종 추가·정렬·홍보. (a) `STORE_COLORS` 상단에 `grape`(포도)·`watermelon`(수박)·`boksung`(복숭아)·`melon`(메론) 4팩. **컬러는 hue-family별 그룹+명도 그라데이션으로 정렬**(수박식): 포도=보라 8단(밝→진)+초록 4단(줄기·잎), 수박=핑크/레드 4+라임 4+그린 4, 복숭아=피치오렌지 8+핑크 4(블러시), 메론=그린 8+칸탈로프 오렌지 4(과육). 뒤죽박죽 배열 금지(사용자 요청). (b) **팩 이름 옆 이모지(🍇🍉🍑🍈) 전부 제거** — `name`에서 삭제(렌더는 `renderStoreColors`가 `item.name` 직접 사용, 별도 이모지 소스 없음). (c) **출시 히어로 배너**(`.store-fruit-hero`) — 컬러탭 최상단(방 프로필 씰 히어로 `.store-seal-hero`와 대칭 구조), 컬러팩 섹션 위. **힉스필드 생성 실사 하늘 사진 배경**(21:9 포토리얼: 푸른 여름 하늘+흰 뭉게구름, 오른쪽에 실제 과일 클러스터 수박·포도·복숭아·메론이 하늘에 자연스럽게 녹아든 원샷 — 잘라 붙인 경계 없음)을 `background:center right/cover`로 풀블리드. **좌측 스크림**(`::before` linear-gradient 파란 어둠 좌→우 투명)+텍스트 text-shadow로 하늘 위 글자 가독성 확보(흰구름에 글자 묻히던 문제 해결). 텍스트는 `padding-right:40%`로 좌측에 가둬 과일과 안 겹침. 골드 eyebrow(NEW·SUMMER COLORS)+흰 세리프 타이틀+과일칩 4개(포도·수박·복숭아·메론, 반투명 pill). `FRUIT_HERO_IMG`=힉스 CDN URL(job dc7a5f69) — 인라인 `background-image`로 주입. ⚠️ **변천**: 무광 일러스트(63922560, 투명 컷)→실사 컷아웃(9c60b23e)→CSS 하늘 그라데이션+CSS 흰구름→**실사 하늘+과일 원샷 사진 배경**(사용자 3연속 피드백: 상큼하게→흰구름 글자 가림+실제 하늘사진+과일 경계 분리감 → 통짜 사진으로 해결). CSS 흰구름·`.sfh-art` 플로팅 이미지·remove_background 제거. ⚠️ 씰 히어로와 달리 **onclick 없음**(컬러팩이 바로 아래 같은 탭에 있어 홍보 헤더 역할만). ⚠️ 이미지 힉스 CDN 직참조라 만료 시 깨질 수 있음(추후 자체 호스팅 권장, 동물 스티커와 동일 이슈). Playwright 검증(배너 렌더·타이틀·이미지 URL·칩4·이모지0·정렬). (d) **이모지 탭에도 동일 히어로**(`.store-emoji-hero`) — 컬러 배너와 같은 구조(풀블리드 이미지 배경+좌측 스크림+`.sfh-*` 텍스트 클래스 재사용), 이모지 탭 최상단(`renderStoreEmoji`), 캐릭터팩 섹션 위. **힉스필드 생성 따뜻한 동물 일러스트 배경**(21:9: 오른쪽에 강아지·고양이·토끼·곰·여우 무리, 왼쪽 크림 여백)+**따뜻한 갈색 스크림**(파란 대신, 크림/허니 배경 대응). 골드 eyebrow(NEW·ANIMAL STICKERS)+'동물 친구들의 귀여운 반응 이모지'+동물칩 4개(강아지·고양이·호랑이·토끼). `EMOJI_HERO_IMG`=힉스 CDN URL(job 97d4a1b2), 인라인 background-image. onclick 없음(홍보 헤더). index.html 변경 → 재빌드 필요(PWA는 무관).
12. ✅ **생일 축하 푸시** — `push-cron`에 `handleBirthday()`+`mode='birthday'` 추가(재배포 v12). 오늘 생일(`profiles.birthday`='MM-DD')인 유저와 같은 방 멤버에게 '🎂 OO님의 생일이에요', 본인에게 '🎂 생일 축하해요'. pg_cron `push-birthday`(매일 09:00 KST) 추가. Edge Function은 git 아닌 Supabase에 있음(수정 시 원본 백업 후 재배포).
13. ✅ **페이월 문구 전환 최적화(#2)** — 잠금 안내(`showUpgradeModal`) 문구를 '~는 프리미엄 전용/구독하면'(제한·결제 프레임)에서 '혜택+하고 싶은 것'(사진·테마·컬러/캐릭터팩·글꼴·할일) 프레임으로 교체. 모달이 이미 '무료로 체험하기' 버튼+7일 무료 문구를 붙이므로 전환 유도 강화.
14. ✅ **컬러 팩 카드 전체 클릭** — `renderStoreColors()`에서 기존엔 `.pack-btn` 버튼에만 onclick이 있어 버튼을 정확히 눌러야 안내모달이 떴음. 카드 전체(`.theme-card`)에 `onclick`(`_cardClick`: 잠금=`onUnlockColorClick`, 보유=`onLockColorClick`)+`cursor:pointer` 부여, 버튼에서는 onclick 제거(카드로 버블링 → 단일 발화, 중복 없음). 상자 아무 곳이나 눌러도 프리미엄 안내모달이 뜸.
15. ✅ **🔒 프리미엄 버튼 여백** — 자물쇠 이모지와 '프리미엄' 텍스트가 붙어 보이던 문제. 이모지를 `<span class="pack-btn-lock">🔒</span>`로 감싸고 `.pack-btn-lock{margin-inline-end:9px}`(카드 잠금태그는 5px) CSS 추가. 폰트 무관하게 고정 여백. 적용 대상: 컬러/캐릭터 팩 버튼·캐릭터 상세시트 큰 버튼(`pack-btn-lg`)·텍스트 테마 버튼·카드 잠금태그(`theme-card-lock-tag`). ⚠️ 최초엔 5px로 넣었으나 원래 있던 공백(~4px)과 거의 같아 티가 안 나서 **9px로 상향**(눈에 띄게).
16. ✅ **일정 드래그 부드럽게(성능+집기/놓기 애니메이션)** — 드래그 이동 끊김 개선 + iOS식 집기/놓기 감각. (1) 고스트를 `left/top`(리플로우) → **`transform:translate3d`(GPU 합성)** 이동. (2) `pointermove`마다 하던 고스트 이동+`elementFromPoint` 히트테스트를 **`requestAnimationFrame` 배칭(프레임당 1회)** 으로 — 최신 좌표만 `_evPtrX/Y`에 저장, `_evDragFrame()`에서 처리. (3) 고스트를 **outer(위치, `_evGhostTransform`=translate3d만, 드래그 중 트랜지션 없음→손가락 1:1)** + **inner(`.ev-drag-ghost-inner`: 시각 pill·스케일·애니메이션)** 로 분리. 집는 순간 `@keyframes evGhostPop`(작게→오버슈트 1.2→안정 1.08, 스프링 cubic-bezier)로 **튀어나오는 느낌**, 놓는 순간 `_evSettleGhost()`가 `.dropping` 클래스로 목표 칸(또는 원위치)으로 미끄러지며 **축소+페이드로 쏙 들어가는 느낌**. 원본 바(`.ev-drag-src`)도 opacity .28+scale .94로 부드럽게 가라앉음. `prefers-reduced-motion` 존중. 관련: `_evMakeGhost()`, `_evSettleGhost()`, `_evDragFrame()`, `_finish()`(rAF 취소+settle 호출). 그리드 드래그 + 날짜시트 롱프레스 드래그 모두 적용.
17. ✅ **드래그 이동 후 새로고침 깜빡임 제거(낙관적 업데이트)** — 다른 날짜로 옮길 때 `loadEvents()`가 `_events={}`로 비우고 빈 캘린더를 그린 뒤 네트워크로 재로드해서 **전체 새로고침되는 깜빡임**이 있었음. `_evMove`를 낙관적 업데이트로 변경: 로컬 `_events` 맵에서 해당 일정을 od→nd로 즉시 이동(`_evLocalMove`)+`renderMonthly()`로 바로 반영하고, **DB 업데이트는 백그라운드**로. 실패 시에만 `loadEvents()`로 서버 기준 롤백(부분 실패는 성공분을 원복해 그룹 일관성 유지). `_tdMove`(할 일)도 동일 패턴 — todo 객체(=`_todos` 참조) 필드만 바꾸고 `safeRender()`, 실패 시 `prev`로 즉시 원복. 드래그 대상은 실제 DB row(반복·가상 제외)라 반복 인덱스는 무관.
18. ✅ **드래그 중 텍스트 선택 방지** — 일정을 롱프레스로 옮길 때 드래그 시작(220ms 홀드) 전에 iOS가 텍스트 선택을 잡아 파란 선택 핸들이 캘린더에 남던 문제. 기존 `user-select:none`은 `body.ev-dragging`(드래그 시작 후)에만 적용돼 홀드 구간이 무방비였음. `.cal-grid,.cal-grid *`에 `-webkit-user-select:none/user-select:none/-webkit-touch-callout:none`을 **항상** 적용(그리드엔 입력창이 없어 안전).
19. ✅ **멀티방 일정 드래그 이동 전파** — 여러 방에 동시 추가한 일정(`multi_group_id` 공유)을 드래그로 옮기면 **현재 방 복사본만** 이동하던 문제. `_evMove`에서 `anchorRow.multi_group_id`가 있으면, 현재 방 group의 각 distinct 날짜 이동(od→nd)마다 `update({date:nd}).eq('multi_group_id',mgid).eq('user_id',_currentUser.id).eq('date',od)`로 **모든 방의 내 복사본을 한 번에 이동**(단일일·기간·여러날 모두 날짜별로 커버). 사용자 선택으로 **모달 없이 자동 전체 이동**(수정은 기존 `askMultiRoomScope` 모달 유지). 낙관적 로컬 반영은 현재 방만(다른 방은 재진입 시 반영). 그리드 드래그+날짜시트 드래그 둘 다 `_evMove` 공유라 함께 적용. ⚠️ 참고: **수정 경로는 이미 멀티방 지원**(`saveEditedEvent` Case 4 등에서 `multi_group_id` 있으면 `askMultiRoomScope`로 전체/방선택/이방만) — 사용자 요청에 따라 그대로 둠. 편집 진입점은 날짜시트(dpCard)의 '수정' 버튼 하나뿐(`openEditEvent`). `multi_group_id`는 방 간 연결, `range_group_id`=연속 기간(방별 독립), `is_multi`=불연속 여러 날.
20. ✅ **멀티방 할일도 동일 적용** — `todos`도 `multi_group_id`로 방 간 연결(추가 시 `_doSaveTodo`가 현재+다른 방에 stamp). (a) **수정**: 이미 `askMultiRoomScope` 모달 지원(line ~17898) — 그대로 유지. (b) **드래그**(`_tdMove`): `todo.multi_group_id` 있으면 `update(upd).eq('multi_group_id').eq('user_id').eq('start_date',prev.start_date)`로 **모든 방 복사본을 함께 이동**(모달 없이 자동). (c) **삭제**(`deleteTodo`): `multi_group_id` 있으면 이벤트처럼 `askMultiRoomScope(...,true)` 모달(전체/이 방만). ⚠️ **함정**: todos의 `multi_group_id`는 '여러 날' 할일(추가 시 날짜별 개별 `single` 행으로 저장, 같은 mgid 공유)도 묶으므로, 드래그·전체삭제 시 **반드시 `start_date`로도 필터**해야 함(안 그러면 다른 날짜 행까지 같은 날로 뭉개지거나 통째 삭제됨). range/single은 방당 1행이라 start_date 필터로 방별 형제가 정확히 매칭.
21. ✅ **할 일 복사(다른 방으로)** — 일정 복사(`copyEventToRooms`/`_doCopyEvent`)와 동일한 흐름의 `copyTodoToRooms`/`_doCopyTodo` 추가. 할일 상세 액션(날짜시트 dp-actions + 우리할일 카드)에 **복사** 버튼(함께 할일은 제외). `_roomPickMode='copytodo'`로 방 선택 모달 재사용(`confirmRoomPick`/`cancelRoomPick`에 분기 추가). 복사본은 원본과 같은 `multi_group_id`로 묶음(원본에 없으면 새로 부여 후 원본에도 update → 이후 수정/이동/삭제 동기화). 완료상태·member_keys는 초기화. `window.copyTodoToRooms` 노출.
22. ✅ **할일 드래그 게이트 확장(여러날 허용)** — 할일 드래그가 '여러날(multi)' 타입에서 안 되던 문제. `canTdDrag`(`__todoCellHTML`)가 `single`/`range`만 허용해 multi는 `data-tddrag="0"`으로 렌더 → 드래그 시작 자체가 안 됨. 이동 로직(`_tdMove`/`_tdGroupDates`)은 이미 multi 지원하므로 게이트에 `todo_type==='multi'` 추가. 반복 할일은 단일 DB행 이동이 애매해 계속 제외. (단일·비반복 기간 할일은 원래 정상 드래그됨.)
23. 🚨 **[중대] 할일 드래그/복사가 아예 안 되던 근본 원인 = IIFE 스코프 경계** — index.html은 **line 16786 `(function(){ 'use strict'; ... })();` IIFE 안에 할일 시스템 전체**(`_todos`, `safeRender`, `loadTodos`, `__getTodos`, `deleteTodo`, `renderTodoScreen` 등)를 담고 있음. 그런데 드래그/복사 헬퍼(`_tdFindById`·`_tdMove`·`_tdGroupDates`(16262~)·`copyTodoToRooms`·`_doCopyTodo`(10159~))는 **이 IIFE 바깥(메인 스코프)**에 있어서 안쪽 `_todos`·`safeRender`에 접근 불가 → `_tdFindById`가 항상 `null` 반환 → **할일 드래그가 시작조차 안 됨**(이벤트는 `_events`/`_evFindRowById`가 같은 바깥 스코프라 정상). 복사도 `(_todos||[])`가 `[]`라 "할 일을 찾을 수 없어요". **수정**: 바깥 헬퍼들이 `window.__getTodos()`(할일 목록)·`window.safeRender`·`window.loadTodos`(window 노출된 안쪽 함수)로 접근하게 변경. Playwright(headless Chromium)로 실제 렌더+드래그 시뮬레이션해 `_tdFindById` false→true, 드래그 활성화·`_tdMove` 이동까지 검증. ⚠️ **앞으로 할일 관련 함수를 추가할 땐 반드시 16786 IIFE 안쪽에 두거나, 바깥이면 `window.` 노출 함수로 접근할 것.** (`renderMonthly`·`invalidateDayEventsCache`·`shiftDateKey`·`_events`·`supa`·`_currentUser`·`toast`는 바깥 스코프라 직접 호출 OK.)
24. ✅ **기간 할일 전체 하이라이트 + 목록(날짜시트) 드래그** — (a) 기간/여러날 할일을 드래그로 집으면 누른 조각만 활성화되던 문제. `_start`(todo)에서 같은 `data-tdid`의 모든 `.cal-tditem`/`.cal-tdline` 조각에 `ev-drag-src` 부여(이벤트 group 방식과 동일) → 전체 범위가 들림. (b) 일정처럼 **날짜시트 목록(dpList)에서도 꾹 눌러 이동**: 시트 할일 `dp-item`에 `data-tddrag/data-tdid/data-tddate` 부여(비반복 단일·기간·여러날), `setupDateSheetDrag` pointerdown이 할일도 감지, `_beginSheetTdDrag()`(=`_beginSheetEvDrag`의 할일판)가 시트 닫고 `_evDrag`를 `kind:'todo'`로 세팅해 캘린더 드래그로 이어받음. Playwright로 srcBars 전체 하이라이트·`_beginSheetTdDrag` 세팅 검증.
25. ✅ **방목록 순서 편집(드래그 핸들+드래그)** — '내 방' 옆 **'순서 편집' 버튼**(`toggleRoomEdit`, `_roomEditMode`) → 켜면 각 카드에 **드래그 핸들(≡ SVG, `.r-card-grip`)** 이 나타나고(`›` 화살표는 숨김) 드래그로 재정렬, 버튼은 '완료'로 바뀜. ⚠️ **흔들림 애니메이션은 처음엔 iOS식 `@keyframes rWiggle`였으나 사용자가 어지럽다고 해서 제거**하고 핸들+살짝 뜬 그림자(`box-shadow`)로 대체. **편집 모드에서만** 드래그 가능(홀드 불필요—`_roomPD`가 `_roomEditMode` 아니면 무시, `_roomPM`에서 4px 이동 시 즉시 `_roomDragStart`). 순서는 **사용자별**이라 `members.sort_order`(int)에 저장(마이그레이션 `add_members_sort_order`, 기기 간 동기화). `loadRooms`가 `members.sort_order`도 조회해 오름차순(없으면 created_at 최신순 폴백, 안정정렬) 정렬 + 진입 시 `_roomEditMode=false`. `renderRooms` 카드에 `data-roomid`+섹션행에 편집버튼(방≥2)+`room-edit` 클래스 토글. 드래그: 집은 카드 `translateY`(흔들림 정지=`body.room-reordering .r-card{animation:none}`—translate 정상적용 위함), 나머지 `translateY(±h)` 자리양보, 드롭 시 `_applyRoomReorder`→`_myRooms` 재배열+낙관재렌더+`_saveRoomOrder`(sort_order 0..n 백그라운드). 편집 모드 중 카드 click(방입장) 차단+드래그 직후 1회 차단(`_roomSuppressClick`). 편집 모드는 `.room-edit .r-card{touch-action:none}`으로 드래그가 스크롤 대신 동작. 리스너는 `#roomsContent` 컨테이너 위임. ⚠️ 전부 **바깥 스코프**(<16786). Playwright로 편집OFF=드래그무시·흔들림없음, 편집ON=rWiggle+[A,B,C]→[B,C,A]+sort_order 저장 검증.
26. ❌ **D-day/기념일 (방별 공유) — 만들었다가 제거함** — 캘린더 상단 슬림 스트립(`#calDdayBar`)+관리/추가시트로 방별 공유 D-day를 구현했으나(PR #43·#44·#45), 사용자가 "느낌 없음"으로 **전체 삭제 요청**. `index.html`을 D-day 도입 직전 커밋(`9ee82f6`, 코드 25) 상태로 되돌려 클라이언트 코드(`#calDdayBar`, `#ddayListOverlay/Card`, `#ddayEditOverlay/Card`, `_ddays`·`loadDdays`·`renderDdayStrip`·`openDdayEdit`·`renderDdayCal` 등 JS, `.cal-dday-strip`·`.dday-*` CSS) 전부 제거. ⚠️ **주의**: 삭제한 건 '방별 공유 D-day'만이며, **기존 무관 기능은 유지**됨 — 할일 마감 D-day 배지(`.td-dday`/`getDdayClass`/`getDdayLabel`), 캘린더 셀의 공휴일·절기·기념일 라벨, 다음 생일까지 D-day. DB 테이블 `anniversaries`는 빈 채로 남겨둠(위 Supabase 섹션 참고). 재도입 시 git에서 PR #43~#45 diff 복원 가능.
27-3. ✅ **씰·카드배경 라인업 영하게 리프레시** (2026-07-22, 사용자 "올드해 보인다는 평 많음, 영하게") — 전통 모티프 5종(plum 매화·lotus 연꽃·crane 학·camellia 동백·chrysw 국화)을 라인업에서 빼고 **트렌디 5종으로 교체**: ribbon(리본, 코케트 새틴 보우)·cherryf(체리, Y2K)·daisy(데이지)·star(별똥별)·heart(하트, 러브코어 날개하트). 씰(왁스 레퍼런스 45ce8f1a 물림, remove_background, zoom 1.3, URL=hf_20260722_124*) + 매칭 카드 배경(수채 레퍼런스 01b398d9 물림, URL=hf_20260722_124*) 세트로 생성. **새 SEAL_SET_ORDER/CARD_SKIN_PREM**=[nabi,rosewreath,ribbon,moon,daisy,star,tulip,snow,heart,koi,cherryf,cherry](12종 유지). ⚠️ **구 전통 5종은 SEAL_SETS/CARD_SKINS 데이터에 잔존**(레거시 저장값 렌더용, 피커/스토어에만 미노출 — 삭제 금지). 신규 세트는 svg 폴백 없음 → `_wgSealSvg('set')`에 st.svg 가드 추가(위젯은 PNG 실패 시 이니셜 폴백). 배율은 1.3 일괄 시작(실기기 스크린샷 보고 세부 보정 예정).
27-2. ✅ **씰 세트 = 힉스필드 고화질 왁스씰 PNG로 격상** (2026-07-22, 사용자 "너무 이쁘다 퀄 + 입체감") — 코드 27-1의 손그림 SVG가 부족하다는 피드백 → **12종 전부 힉스필드 생성 실사풍 3D 부조 왁스씰**(대표 nabi 생성(job 45ce8f1a) 후 레퍼런스 물림 "EXACT SAME style/재질/조명/림"으로 11종 파생, remove_background 투명 PNG, URL=hf_20260722_0836~0837*). `SEAL_SETS[key].img` 필드 추가, **`_sealSetArt(key)`=img 우선(`<img class="sealimg">`)·svg 폴백** — 표시 전 지점(_sealInner·피커 big/grid·rs썸네일·스토어 히어로)이 art 사용. CSS `.has-seal img.sealimg`=중앙 118% object-fit:contain(씰 원이 컨테이너 원을 꽉 채움), `.seal-set` 배경=왁스 빈판 그라데이션(로딩 전 플레이스홀더). `_preloadSealSets()`(피커 첫 오픈 시 12장 프리캐시). **위젯**: `_wgRasterizeUrl(url)` 신설 — CDN PNG를 crossOrigin으로 72px 래스터 시도, 실패(CORS/타임아웃) 시 기존 SVG 세트 래스터 폴백. **동물 씰≠카드 포즈 다양화 + 사람 캐릭터 정리(2026-07-22)**: 씰이 전부 '엄지척'(like)으로 겹치던 문제 → 동물마다 성격에 맞는 감정 스티커를 **씰·카드에 서로 다르게** 배정(강아지 씰cheer/카드excited·고양이 love/smug·호랑이 roar/proud·토끼 bashful/love·곰 hug/comfort·펭귄 excited/laugh·여우 scheme/wink·원숭이 laugh/love·쥐 sneaky/cheer·닭 proud/excited) — 캐릭터별 개성+씰과 카드가 같은 캐릭터의 다른 포즈. 소년 씰 URL 오타(141945→141925) 수정. 개구쟁이·심술이·깔깔이가 같은 캐릭터라 **심술이(c_simsul)·깔깔이(c_kkalkal) 순서에서 제거**(개구쟁이 하나만 유지, 데이터 잔존). **소녀·소년·개구쟁이 씰↔카드 캐릭터 일치 완료(2026-07-22)**: 씰이 카드와 다른 사람으로 보이던 문제 → 각 씰을 **해당 카드 이미지를 레퍼런스로 물려 재생성**("Use the EXACT SAME character ... bust portrait") 후 remove_background → SEAL_SETS.c_girl(ref e6474037→씰 150339)·c_boy(ref e7e5bfbd→씰 150359)·c_gaegu(ref 4c65fcf5→씰 150343) URL 교체. Playwright로 3종 seal=new·art=true 검증. **동물 카드 전용 배경 씬 생성 완료(2026-07-22, 사용자 "전용 배경도 만들어줘")**: 10동물 카드가 char+grad(그라데이션+스티커)라 밋밋하다는 피드백 → **각 동물 화풍에 맞는 캐릭터-없는 21:9 배경 씬**을 힉스 생성(강아지 크레용 공원·고양이 플랫벡터 창가·호랑이 민화 산수·토끼 수채 달밤·곰 구아슈 꿀숲·펭귄 클레이 빙하·여우 카툰 가을숲·원숭이 크레파스 정글·쥐 색연필 선반·닭 카툰 농장, 좌측은 텍스트용 여백). `CARD_SKINS.a_*.scene` 필드 추가, `_cardSkinStyle` char분기가 **scene 있으면 그라데이션 대신 씬 이미지**(`url(char) right/94%, url(scene) cover`) — **스티커는 그대로라 씰=카드 캐릭터 불변**(씬만 뒤에 깔림). 프리로드(_preloadSealSets·preloadStoreAssets)에 char·scene 추가. URL=hf_20260722_1514*. ⚠️ 사람 카드(c_girl/c_boy)는 이미 풀블리드 씬 일러스트(img)라 미해당. Playwright 검증(10동물 scene·style·씰매칭·문법0). **미리보기 씬 미표시 버그 수정 + 개구쟁이 카드 씬화(2026-07-22 후속, 사용자 "미리보기에 안 보여 + 개구쟁이 배경카드 다시")**: (1) `_rmSyncPreview`의 char 분기가 `_cardSkinStyle`과 달리 그라데이션만 하드코딩해 **씬이 상단 미리보기(#rmCardPrev)에만 안 뜨던 버그**(그리드 버튼은 정상) → scene 있으면 씬 이미지 cover로 수정. (2) **개구쟁이 카드를 풀블리드 img → char+scene 방식으로 전환**(동물과 동일): 기존 씰 캐릭터(ref 4c65fcf5)를 물려 투명 스티커(1ab76ca8) + 크레용 놀이터 씬(78e85e35) 생성 → `CARD_SKINS.c_gaegu={char,scene}`. 씰(803e9739도 4c65fcf5 파생)과 같은 캐릭터라 씰=카드 유지. Playwright 검증(개구쟁이 char+scene·미리보기 씬 합성·씰매칭·문법0). **씬+캐릭터 통짜 일러스트로 재작업(2026-07-22 후속, 사용자 "배경이랑 따로노는 느낌, 자연스럽게 녹여줘")**: 스티커를 씬 위에 얹는 char+scene 합성이 '붙여넣은' 느낌이라 → **각 캐릭터를 씬 안에 그려 넣은 21:9 통짜 일러스트로 재생성**(캐릭터 스티커+씬 2개를 레퍼런스로 물려 "integrate ... grounded with soft shadow, cohesive lighting, matching <medium> art style, NOT a pasted sticker"). 10동물+개구쟁이(c_gaegu) 전부 `CARD_SKINS.*={name,img}` 풀블리드(rcs-img)로 전환(char/scene/grad 제거) — 소녀·소년과 동일 방식. 씰(SEAL_SETS.a_*=플랫 스티커)은 그대로라 씰=같은 캐릭터·다른 포즈(사용자가 원한 방식). URL=hf_20260722_1529~1530*. `_cardSkinStyle`/`_rmSyncPreview`의 scene 분기는 폴백으로 잔존(무해). Playwright 검증(11종 img-only·rcs-img·미리보기·씰매칭·문법0). **소녀·소년 사진 같은(3D 반실사) 스타일로 변경(2026-07-22 후속, 사용자 "소녀 소년 디자인을 사진같은 느낌으로")**: 납작 카툰 → **반실사 3D CGI 렌더**(Pixar/DreamWorks풍, 부드러운 시네마틱 조명·질감, 실제 아동 사진 아님·명확한 렌더 캐릭터)로 카드 재생성(기존 카드 e6474037/e7e5bfbd를 정체성 레퍼런스로 물려 헤어·성격 유지, 소녀=파스텔 라벤더 씬·소년=파스텔 민트 씬). **씰도 새 카드에서 흉상 재생성→remove_background**로 일치: CARD_SKINS.c_girl(8dbb166e)·c_boy(134cf44c), SEAL_SETS.c_girl(씰 93219145)·c_boy(씰 804d0b36). Playwright 검증(카드/씰 new·rcs-img·sealArt·문법0). **→ 2D 깜찍 스타일로 재변경(2026-07-22, 사용자 "3D 사진느낌 아니라 2D로 깜찍하게")**: 반실사 3D가 원하는 느낌이 아니라 함 → **부드러운 카와이 2D 카툰(카카오 이모티콘풍, 동글동글 클린 라인·큰 반짝 눈·볼터치, flat 2D)**로 카드 재생성(원 카드 e6474037/e7e5bfbd 정체성 유지). 씰도 새 2D 카드에서 흉상 재생성→remove_background. CARD_SKINS.c_girl(c12826f9)·c_boy(ddcef1ce), SEAL_SETS.c_girl(씰 b8233388)·c_boy(씰 0c151ddc). Playwright 검증. **스토어 동물 캐릭터 → 씰+카드 재활용(2026-07-22, 사용자 "스토어 이모지 동물들을 씰·카드로, 그 외 동물 삭제")**: 아기(c_baby)+내가 생성한 동물 6종(c_cat~c_chick) 제거. **스토어 이모지 10동물**(보리·코코·호담·달이·꿀곰이·뽀글이·여우비·몽당이·콩이·꼬꼬)의 like-스티커를 재활용 — 씰(`SEAL_SETS.a_*`, img=스티커 URL, zoom 1.05)+카드(`CARD_SKINS.a_*`, **char+grad 방식**=그라데이션 배경+캐릭터 스티커 합성, 생성 0장·스티커 재활용으로 씰↔카드 완전 동일). `_cardSkinCls`/`_cardSkinStyle`에 char 분기(multi-background: 오른쪽 캐릭터+왼쪽 그라데이션), `_rmSyncPreview`·CSS `.rm-skin.rcs-char` 추가. SEAL_SET_ORDER·CARD_SKIN_PREM 맨 앞 10동물, 뒤에 사람 소녀·소년+장난꾸러기 3종+컨셉. 캐릭터별 파스텔 그라데이션(강아지 크림·호랑이 주황·토끼 핑크·펭귄 하늘 등). **장난꾸러기 캐릭터 3종 + 튤립·체리·장미 제거(2026-07-22, 사용자 "짱구 같은 장난꾸러기가 매력, 이쁜거 말고 웃긴거")**: 크레파스 낙서 화풍(4c65fcf5 레퍼런스)으로 개구쟁이(c_gaegu, 혀 내밀기)·심술이(c_simsul, 심통)·깔깔이(c_kkalkal, 빵터짐) 씰+카드 세트 생성(씰=크레용 얼굴 bust bg제거, 카드=21:9). SEAL_SET_ORDER·CARD_SKIN_PREM에서 tulip·cherryf(체리)·rosewreath(장미) 제거(데이터는 레거시 저장값용 잔존), 사람 캐릭터 뒤(index 9)에 장난꾸러기 3종 삽입. URL=hf_20260722_143*·134644. **사람 캐릭터 3종 추가(2026-07-22, 사용자 "동물만 안 가도, 사람 캐릭터도")**: 같은 부드러운 그라데이션 화풍으로 소녀(c_girl)·소년(c_boy)·아기(c_baby) 씰+카드 세트 생성(고양이 카드/씰 레퍼런스 물림). SEAL_SET_ORDER·CARD_SKIN_PREM에서 동물 6종 뒤(index 6)에 삽입 → 총 캐릭터 9종+컨셉 12종. URL=hf_20260722_1418~1419*. **캐릭터 씰+카드 6종 세트(2026-07-22, 사용자 "선이 딱딱함→그라데이션 자연스럽게, 6가지, 방프로필도")**: 초안 3종(하드라인 스티커)을 폐기하고 **부드러운 에어브러시 그라데이션 화풍**(no hard outline)으로 캐릭터 6종 재생성: 냥이(고양이)·멍뭉이(강아지)·토끼·펭귄·여우·병아리. 각 캐릭터 = **매칭 씰**(왁스씰에 캐릭터 얼굴 부조, remove_background 투명 PNG, SEAL_SETS c_* 키, zoom 1.25)+**카드 배경**(21:9 풀블리드, 오른쪽 캐릭터+왼쪽 그라데이션, CARD_SKINS c_* 키). 대표 고양이 카드(ebdad9a1)·씰(d671d4ce) 레퍼런스 물림으로 5종 파생. SEAL_SET_ORDER·CARD_SKIN_PREM 맨 앞에 c_cat~c_chick 6개 삽입(기존 12 컨셉 뒤). URL=hf_20260722_1413~1415*. 씰은 svg 없어 위젯은 `_wgSealSvg` st.svg 가드로 이니셜 폴백. 옛 c_kid 제거. **카드 배경 캐릭터 컨셉 초안(2026-07-22, 사용자 요청 "짱구·고양이 같은 캐릭터 컨셉")**: 꽃 모티프가 올드하다는 피드백 → 힉스필드로 캐릭터 카드 배경 4종 초안 생성(웹툰 스티커 고양이·크레파스 낙서 꼬마·말랑 클레이 곰·산리오 토끼) → 사용자 "곰 빼고 3가지" → `CARD_SKINS.c_cat`(냥이)·`c_kid`(엉뚱이)·`c_bunny`(토끼) 3종을 CARD_SKIN_PREM 맨 앞에 추가(21:9 풀블리드, rcs-img). 기존 12 컨셉 배경은 뒤에 유지. URL=hf_20260722_1346*. 스타일 확정되면 12종 세트로 확장 예정. **카드 배경 = 씰 세트와 동일 컨셉으로 재작업(같은 날, 사용자 "세트로, 잉어면 잉어")**: CSS 컨셉 스킨 8종 폐기 → **힉스필드 생성 21:9 수채 구아슈 카드 배경 12종**(씰 세트와 같은 키 nabi~cherry, 대표 koi(job 01b398d9) 레퍼런스 물림으로 통일 — 오른쪽에 모티프(잉어·국화·달·학 등)+왼쪽은 텍스트용 민무늬 워시, moon만 dark). `CARD_SKINS[k].img`+`_cardSkinCls`(img면 rcs-img[+rcs-dark])+`_cardSkinStyle`(인라인 background-image, r-card·피커 버튼·미리보기 공용), URL=hf_20260722_0929~0931*. 프리로드(preloadStoreAssets+_preloadSealSets)에 12장 추가. 무료 7 파스텔 CSS는 유지. 옛 rcs-moonlight~aurora CSS는 잔존(미사용, 저장값 없음). (구 CSS판) **카드 배경 꾸미기 구현(같은 날)**: 확장 지점에 실제 기능 탑재 — **마이그레이션 `add_members_card_skin`**(members.card_skin text, 라이브). `CARD_SKINS` 15종=무료 7(butter·blush·mint·sky·lavender·peach·sage 파스텔 그라데이션, +'기본'=null) + 프리미엄 8(moonlight 달빛별밤·silk 딥실크·hanji 한지·sakura 벚꽃·ocean 바다·forest 숲·goldfoil 골드(inset 골드 테두리)·aurora 오로라 — 전부 CSS 레이어드 그라데이션, `.rcs-<key>` 클래스+어두운 스킨은 `.rcs-dark`로 카드 텍스트 밝게). 피커 '카드 배경' 카테고리 활성(기본/프리미엄 탭 재사용, 무료는 프리미엄 스킨 🔒 보기만·선택 시 업그레이드 모달, `pickCardSkin` 클래스 토글=깜빡임 없음, 미리보기 카드 `#rmCardPrev`에 실시간 반영). **저장은 seal+card_skin 동시**(`_saveMascotValue(val,skin)`, 저장 시 세트/프리미엄 스킨 각각 게이트), '기본으로 되돌리기'=둘 다 null. loadRooms가 card_skin도 조회(`_myCardSkinByRoom`), renderRooms가 `.r-card`에 `_cardSkinCls()` 부여. 위젯은 미적용(카드 스킨은 앱 방목록 전용). **확장형 꾸미기 레이아웃(같은 날, 사용자 예고 "추후 방 카드 전체 꾸미기 확장")**: 방 프로필 편집을 카테고리 구조로 재편 — 상단 **카테고리 칩 `#rmCats`**('씰' 활성 + '카드 배경 · 준비 중' dim 칩, `setRmCat`/`_rmCat`, renderRoomMascotPicker가 카테고리 디스패치·비씰 카테고리는 '준비 중' 표시), 미리보기를 씰 단독 원 → **방 카드 목업 `.rm-cardprev`**(씰 54px+방 이름+스펙+›, 방 목록 카드가 실제로 어떻게 보일지)로 교체. ⚠️ **추후 카드 배경 확장 시**: `_rmCat==='cardbg'` 분기에 스킨 그리드 추가 + 저장은 members에 새 컬럼(예: card_skin) 권장(seal 값 형식 오염 금지) + `_sealInner`/renderRooms가 카드 스킨 클래스 소비. **씰 탭 힉스 실사 광고 배너(같은 날)**: 스토어 씰 탭 히어로를 CSS 그라데이션+씰 3개 → **힉스필드 생성 21:9 실사 배너**(딥 브라운 실크+파스텔 왁스씰 클러스터+황동 스탬프, job c7694e03, `SEAL_HERO_IMG`)로 교체 — 과일/이모지 배너와 같은 풀블리드+좌측 딥브라운 스크림(`.seal-hero-lux`) 구조, 골드 eyebrow(PREMIUM · WAX SEAL)+크림 CTA. **깜빡임/로딩 지연 방지**: `preloadStoreAssets()`에 씰 배너+세트 12장 추가(스토어 열 때 미리 캐시 → 탭 진입 즉시 표시). 옛 `.store-seal-hero` CSS는 잔존(무해). **방 프로필 편집 풀스크린+기본/프리미엄 탭(같은 날, 사용자 요청)**: `#roomMascotCard`를 `.mo-card` 바텀시트 → **`.ann-card` 풀스크린**(공지/방만들기 스타일: ann-head ‹뒤로 + ann-body, `roomMascotOverlay` 삭제, open/close에 hideTabbar/showTabbar+_subScreenEnter/Exit)으로 전환. 상단 세그먼트 `#rmSeg`(`.rm-seg-btn`, `setRmTab`/`_rmTab`/`_rmSyncSeg`) — **기본 탭**=무료 직접 꾸미기(색 12+문양 19+이니셜), **프리미엄 탭**=고급 씰 12종. **무료 유저는 프리미엄 탭 구경 가능하지만 선택 불가**(탭 시 업그레이드 모달, 선택 상태 안 바뀜)+하단 안내문 '.rm-prem-note'(첫 7일 무료). 열 때 저장값이 세트면 프리미엄 탭에서 시작. **스토어 '씰' 전용 탭 분리(같은 날, 사용자 요청)**: 테마 탭에 있던 씰 히어로+고급 씰 섹션을 신설 `renderStoreSeal()`로 이동, 스토어 탭바에 '씰' 탭 추가(테마·씰·컬러·텍스트·이모지 순, setStoreTab 라우팅). 테마 탭은 테마 카드만 남음. **꽉 채움 2차(같은 날)**: 줌을 올려도 크림 링이 남던 원인=`.seal-set` 왁스 플레이스홀더 그라데이션+컨테이너 box-shadow/링이 씰 뒤로 비침 → 세트 씰은 배경·box-shadow·::before/::after 링 전부 제거(r-card-seal/rm-bigprev/rs썸네일/ssh-seal/rm-set-prev)하고 줌 1.14~1.52로 상향(씰이 원을 살짝 넘치게, 위젯 래스터도 1.18×zoom 연동). 이제 씰 이미지만 보임(스티커 방식). **유료화(같은 날, 사용자 지시 "기존 꾸미기 무료·고급씰 유료+스토어 배치")**: 피커를 2단 구성으로 — 상단 '고급 씰 · 프리미엄'(12세트, 비프리미엄은 🔒 배지+탭/저장 시 `showUpgradeModal`) + 하단 '직접 꾸미기 · 무료'(기존 색 12종 스와치+문양 19종+이니셜 그리드 복원, `pickSealColor`/`pickSealSym`/`_rmPickCustom`, 저장 무료). `saveRoomMascot`=세트면 `isEffectivePremium()` 게이트, 커스텀이면 그대로 저장. `_rmSyncPreview()`가 세트/커스텀 겸용 미리보기. **스토어 테마 탭**에 '고급 씰 PREMIUM SEAL' 섹션(히어로 아래, 12세트 그리드+잠금 배지+CTA `onStoreSealClick`: 프리미엄=openSealShowcase/무료=업그레이드 모달). 프리미엄 시트 기능 목록에 '고급 방 프로필 씰'(🪷) 행 재추가(코드 34에서 뺐던 것과 달리 이번엔 고급씰만 유료). 이미 저장된 세트는 계속 렌더(표시는 게이트 없음, 재저장만 게이트). **후속 수정(같은 날)**: (a) 씰이 원을 꽉 안 채우던 문제 — 세트별 `zoom` 필드(1.1~1.44, 생성 이미지마다 씰 점유율이 달라 실기기 스크린샷 기준 보정) + CSS `scale(var(--sz))`·위젯 래스터에도 동일 줌. (b) **선택 깜빡임 제거** — `pickSealSet`이 `renderRoomMascotPicker` 전체 재렌더(이미지 12장 재생성) 대신 기존 그리드에서 `.on` 클래스만 토글+큰 미리보기 img src만 교체(data-set 속성 기반). ⚠️ 27-1의 SVG 세트는 폴백으로 잔존(삭제 금지). ⚠️ 힉스 CDN 직참조(만료 시 깨짐 — 동물 스티커와 동일, 자체 호스팅 권장). ⚠️ 에이전트 프록시가 CDN 차단이라 로컬 스크린샷 검증 불가 — 구조 검증(Playwright: img12·art·문법0)만, 시각 확인은 사용자 기기. index.html 변경 → 재빌드 필요.
27-1. ✅ **방 프로필 씰 = 완성형 세트로 전면 개편** (2026-07-22, 사용자 지시 "색상·문양 따로 말고 조화로운 한 세트씩") — 왁스씰 레퍼런스 사진 기반. **`SEAL_SETS` 12종**(nabi 나비정원·rosewreath 장미리스·plum 매화·moon 달빛·lotus 연꽃·crane 학·tulip 튤립·snow 눈꽃·camellia 동백·koi 잉어·chrysw 국화·cherry 벚꽃): 각 세트 = 파스텔 왁스 배경(radialGradient+이중 림) + **다색 부조 문양**(면색+동일색조 진한 외곽선 relief, 하이라이트·스파클) 자체 완결 SVG(viewBox 48, `_sealSetSvg(key)`가 `svg.sealset`로 래핑). **저장값 `set:<key>`**(members.seal, `_parseSeal`이 {set:key} 반환). **피커(`renderRoomMascotPicker`) 전면 교체**: 색 스와치+심볼 그리드 삭제 → 세트 3열 그리드(`.rm-setgrid`/`.rm-set`, 74px 미리보기+이름, 선택=accent 링), `pickSealSet`, 저장 시 세트 미선택이면 토스트. **레거시 호환**: 기존 'color:sym' 값은 `_sealInner`/`_sealGlyph` 구경로로 계속 렌더(SEAL_SYMS·SEAL_COLORS 유지, 코드 27의 19문양 포함), 새 저장만 세트. 렌더 전 지점(`_sealInner` 방목록·캘린더 월줄·방전환 토스트·씰피커 팝업·가져오기 목록 + `_syncRoomSetMascot` + rmBigPrev)이 세트 분기. **위젯**: `_wgSealSvg('set',key)`가 세트 SVG 그대로 래스터화(자체 배경 포함, iOS/Android 페이로드 자동 반영). **스토어 씰 히어로 3개도 세트로 교체**(nabi/moon/rosewreath). CSS `.seal-set{--seal-ring:transparent}`·`.has-seal svg.sealset{inset:0 100%}`. Playwright 검증(파스 신/구·인너 신/구·위젯 svg·그리드 12·선택 링·소형 렌더·문법 0) + 스크린샷 시각 반복(장미 소용돌이·리스 잎 접선 배치·학 구도(붉은 해+물결)·연꽃 v2). 제작 플레이그라운드=스크래치 `sealsets.html`(부품 헬퍼 rose/leaf/blossom/butterfly/wax). index.html 변경 → 재빌드 필요.
27. ✅ **방 프로필 = 컬러 씰 (프리미엄)** — 방 목록 씰(첫 글자)을 **색 + 심볼** 조합으로 꾸밈. ⚠️ **디자인 변천**: 처음 캐릭터(모찌/곰돌/달콩)로 만들었다가(넷플릭스식) 사용자가 "허접/별로"라 함 → 왁스씰 고퀄 시안·5가지 컨셉 아티팩트까지 갔다가, 최종적으로 **초기 '컬러 씰'로 확정**(사용자 선택). **캐릭터 코드(`svgEmoji`/`CHAR_META`/`ROOM_MASCOTS` 등) 전부 제거.** 구성: **배경 색 12종**(`SEAL_COLORS`: 진한 6종 terra/rose/gold/sage/navy/plum + **파스텔 6종 butter/mint/sky/lavender/peach/blush**(2026-07 추가), 각 radial-gradient 왁스도장 느낌 `.seal-<key>`. ⚠️ **심볼·이니셜 색은 CSS 변수 `--seal-ink`로 배경 밝기에 맞춰 자동 전환** — 진한 색은 크림(#f3e6cf 기본값), 파스텔은 진한 먹색(각 `.seal-<key>`가 `--seal-ink`+`--seal-ring` 지정). `.has-seal{color:var(--seal-ink,#f3e6cf)}`·`.seal-ini{color:inherit}`. 스와치는 `.rm-swatches` 6열 그리드(2줄)) + **심볼 14종+이니셜**(`SEAL_SYMS`/`SEAL_SYM_ORDER`: ini(방 이니셜)·taegeuk(태극)·plum(매화)·lotus(연꽃)·chrys(국화)·leaf(나뭇잎)·butterfly(나비)·fish(잉어)·gourd(호리병)·fan(부채)·cloud(구름)·mountain(산)·moon(달)·pine(소나무)·kite(방패연), **꽉 찬 먹칠 실루엣** svg. ⚠️ **디자인 변천(사용자 피드백 연속 반려)**: 이모지형 아이콘→"이모지 티" / 추상 기하 마크→"AI 티" / 얇은 선 전통모티프 v1→"퀄리티 떨어짐, 태극·방패연·달 안 같음" → 최종 **꽉 찬 실루엣 한국 전통 문양**(2026-07). 핵심 개선: taegeuk=**외곽 원(stroke 1.8, fill none) + 곡옥 반쪽 채움** — 외곽선 덕에 아래(배경색) 로브도 또렷("자루 안 보임" 피드백 수정), moon=꽉 찬 초승달, kite=사각+연살 십자프레임+방구멍, pine=층층 실루엣, plum=중심 먹점, butterfly=더듬이, gourd=매듭선 추가. **디테일 선 패스(2026-07)**: 문양 안쪽에 가는 선 추가 — plum 꽃술 5가닥·leaf 곁잎맥 4개·fish 아가미/비늘 곡선·gourd 세로 골·lotus 아래 물결(크림, currentColor). 내부 선은 전부 `rgba(0,0,0,.22~.26) stroke-width 1`. **6종 v2 업그레이드(2026-07)**: butterfly=몸통 타원+머리+더듬이+상하 날개 4장 분리+날개 점 / pine=크리스마스트리 티 나던 층층형 폐기→**민화 우산소나무**(납작 수관 3덩이+가는 굽은 줄기+가지선) / chrys=꽃잎 2겹(외 8+내 8, 22.5° 오프셋) / cloud=뭉게구름+안쪽 말림 2개+**아래 꼬리 선 2줄**(크림) / mountain=삼각형 폐기→**둥근 두 봉우리 곡선**+해+능선 빗금 / moon=매끈한 초승달+**반짝 별**(크레이터 점 폐기). `_sealSvg`는 `fill="currentColor" stroke="currentColor" stroke-width=2.3`(실루엣 채움+살짝 획, 아웃라인 요소만 `fill="none"`), 내부 디테일(잎맥·눈·부챗살)은 `rgba(0,0,0,.24~.34)`로 어떤 배경색에도 대응. `.has-seal{color:#f3e6cf}`(씰 크림)·`.rm-sym{color:accent}`(피커). 값은 `mascot`='color:sym'(예 'navy:heart','rose:ini'). **프리미엄 게이팅은 저장 시점**(2026-07 변경)이었으나 → ⚠️ **2026-07 완전 무료 전환**(코드 34): `saveRoomMascot`/`clearRoomMascot`의 `isEffectivePremium()` 게이트 삭제, 누구나 저장 가능. **개인별 설정(2026-07 최종)**: 표시가 방 공유가 아니라 **개인별** — `members.seal`에 저장(내 행 직접 update, RLS `members_update_self`), `loadRooms`가 `select('room_id,sort_order,seal')`로 `_mySealByRoom` 맵 구성, `renderRooms`/`openRoomMascotForRoom`/`_syncRoomSetMascot` 전부 이 맵 기준. `rooms.mascot`·RPC `set_room_mascot`은 미사용 잔존. 시트 부제 "내 화면에만 적용돼요". **심볼 그리드 5열**(`.rm-symrow{grid-template-columns:repeat(5,1fr)}`) — **2026-07-22 십장생 5종 추가로 총 19종+이니셜=20개(4줄 꽉 참)**: crane(학, 수직 목+처진 꽁지깃)·turtle(거북, 옆모습 등딱지+무늬선)·bamboo(대나무, 마디 줄기+잎 3장)·sun(해, 원판+뾰족 스파이크 12개 — 스파이크는 stroke="none"으로 가늘게, 국화와 구분)·wave(물결, 삼봉 반원 물결문+동심 물결선+물보라). 같은 실루엣+rgba 먹선 문법, Playwright 시각 반복(학=오리→학 4회·해=꽃→해 3회·물결=왕관→물결문 2회 교정). **태극 좌우 방향 수정**(크림 로브가 왼쪽 아래로 — 국기 기준, sweep 플래그 교체). **프리미엄 구독 시트 기능 목록에 '방 프로필'(🪷) 항목 추가**. **스토어 쇼케이스 → 히어로 배너(2026-07 격상)**: 처음엔 테마 탭 하단 작은 행 카드였으나 "눈에 안 들어옴" 피드백 → **테마 탭 최상단(PICK 카드 바로 아래) 히어로 배너**(`.store-seal-hero`)로 교체. 진한 왁스 그라데이션 배경+골드 eyebrow(NEW·ROOM SEAL)+세리프 타이틀+크림 CTA pill(꾸미러 가기)+우측에 큰 씰 3개(태극74px 스탬프 pop 애니 `sshPop`·달·나비, 회전 배치). `openSealShowcase()`=스토어 닫고 **방목록 탭으로 이동+편집모드 자동 ON**(연필 노출, `_pendingRoomEdit` 플래그를 `renderRooms`가 소비 — loadRooms가 편집모드를 리셋하므로 로드 후 켜야 함)+토스트 안내. 원래는 피커를 바로 열었으나 스토어가 설정 탭에서 열려 "설정탭으로 떨어진다" 피드백으로 변경(2026-07). `prefers-reduced-motion` 존중. **표시는 방 공유** — `renderRooms`가 `_sealInner(r.mascot,r.name)`(파싱되면 `.r-card-seal.has-seal.seal-<color>` + 심볼/이니셜, 아니면 null→기본 첫글자). **진입 2군데**: (a) 방 목록 **'편집' 모드**(옛 '순서 편집' 개명, `rooms>=1`) → 씰의 **연필 배지(`.r-card-pencil`)** 탭 (연필은 pointerdown 전파차단+capture 클릭리스너 예외). (b) 방 설정 '방 정보'의 '방 프로필' 행. 시트 `#roomMascotCard`: 큰 미리보기(`#rmBigPrev`, `.has-seal.seal-<color>`) + 스펙(`#rmBpName`='로즈 · 하트') + `#rmGroups`에 JS로 **색 스와치(`.rm-sw`)+심볼(`.rm-sym`)** 주입. 상태 `_sealColor`/`_sealSym`/`_sealHad`, 저장 RPC `set_room_mascot`(값 'color:sym'), 낙관 반영+`renderRooms` 재렌더(편집모드 유지). '기본으로 되돌리기'=`clearRoomMascot`(null). 공용 헬퍼 `_parseSeal`/`_sealGlyph`/`_sealInner`/`_sealSvg`. 함수 전부 **전역**(<16786). Playwright 6종 검증(목록씰·무료게이트·6색7심볼·선택·저장·기본복원). DB 컬럼명은 `mascot` 유지(값 의미만 컬러씰로).
29. ✅ **온보딩(웰컴 슬라이드)에 7일 무료체험·꾸미기 항목** — 신규 가입 온보딩 3번째 슬라이드(`#onbOverlay` '이렇게 사용해요' `.onb-feats`)에 🎨 "7일 무료체험 · 꾸미기 — 스토어의 테마·글꼴·전통 문양 씰로 나만의 캘린더를 꾸며보세요. 첫 7일은 전부 무료예요." 추가(전환 유도).
30. ✅ **푸시 소프트 프리퍼미션** — 옵트인 16% 병목 수리. **콜드 팝업 제거**: `autoRegisterPush()`가 미결정(prompt/default) 상태에서 첫 진입에 자동으로 OS 팝업을 쏘던 로직 삭제(옛 `pushPrompted` 1회 키 폐기, granted 유저 재등록은 유지). 대신 **가치 순간에 앱 모달(vConfirm '알림 켜기')로 먼저 물어보고**, '알림 받기' 수락자에게만 OS 팝업(`registerNativePush(false)`/`registerPushNotification(false)`). 트리거 3개: (a) **로그인 → 방목록 도착**(`renderRooms`, 방≥1, 1s 지연 — 1차 정석 시점, 사용자 요청) (b) `renderMemberTabs`에서 2인+ 방 진입(1.2s) (c) 일정 저장 성공 직후(0.9s). ⚠️ 로그인 전엔 절대 안 뜸(`_pushAskEligible`이 `!_currentUser` 차단). 정책: '다음에'/OS 거절 → **7일 스누즈**, 평생 최대 3회, 세션당 1회(`_pushAskShown`). 상태는 `localStorage['uricalv2.pushAsk']={c,n}`. 함수: `maybeAskPush(trigger)`/`_pushAskEligible`/`_pushAskSnooze`(전역, autoRegisterPush 아래). iOS는 OS 팝업이 1회용 카드라 이 구조가 필수. Playwright 5종 검증(콜드팝업 0회·모달 표시·스누즈 기록·스누즈 차단·수락 시 OS 요청).
41. ✅ **방 만들기·코드 입장을 바텀시트→전체 화면(고급설정/공지사항 스타일)** — 사용자 요청으로 `createCard`/`joinCard`를 `.mo-card` 바텀시트에서 **`.ann-card` 풀스크린**(공지·고급설정과 동일: `.ann-head`(‹ 뒤로 + 타이틀) + `.ann-body > .ann-form`)으로 전환. `#createOverlay`/`#joinOverlay` dim 오버레이 제거(풀스크린이라 불필요), 액션은 하단 풀폭 primary 버튼 1개(취소=뒤로). open/close에 `_subScreenEnter/Exit` 추가 → `.ann-card.on`이라 `_anySubSheetOpen`이 자동 감지해 **광고 숨김**(기존 #39 바텀시트 광고가림 문제도 근본 해소). closeMainModals·뒤로가기 레지스트리(`createCard`/`joinCard`)·closeRoom 성공경로는 id 기반이라 그대로 동작. `.ann-form{padding:18px 20px max(28px,safe-area)}`.
39. ✅ **바텀시트 액션 버튼이 광고·탭바에 가리던 문제(안드로이드)** — 갤럭시에서 '새 방 만들기'(`createCard`)·'방 코드로 입장'(`joinCard`) 시트의 하단 '만들기'/'입장' 버튼이 **네이티브 AdMob 배너 + 탭바**에 가려 못 누르고 스크롤도 안 되던 문제. 원인: `.mo-card` 하단 패딩이 `max(28px,safe-area)`뿐이라 광고 높이(`--ad-h`)를 미확보 → 버튼이 네이티브 배너(웹뷰 위 별도 뷰) 아래에 깔림. **수정 2가지**: (a) CSS — `body.has-ad .mo-card`/`.dp-card`에 `padding-bottom:calc(var(--ad-h)+28px+safe-area)` 추가(모든 바텀시트 공통, 콘텐츠 길면 기존 `overflow-y:auto`로 스크롤). (b) `openCreateRoom`/`openJoinRoom`에 `hideTabbar()`, `closeCreateRoom`/`closeJoinRoom`에 `showTabbar()` 추가(성공 시 create/joinRoom이 close 호출→복원, 실패 시 시트 유지). 탭바 z-index(30)<시트(51)지만 확실히 하려 숨김. index.html 변경이라 재빌드 필요.
38. ✅ **기간 일정 바 정렬(공휴일 라벨 밀림) 수리** — 여러 날 기간 일정 바가 공휴일/절기 라벨(`.cal-cell-label`)이 있는 칸에서만 아래로 밀려 계단처럼 끊겨 보이던 문제(예: 7/17 제헌절 칸의 '놀러감' 바). 원인: 셀 라벨이 `min-height:9px`이라 텍스트가 있는 칸에서 라벨 높이가 커질 수 있어 이벤트 시작 Y가 칸마다 달라짐(빈 라벨 placeholder는 이미 있었으나 min-height는 상한이 없음). **수정**: `.cal-cell-label`을 `height:11px`(고정)+`line-height:11px`+`box-sizing:border-box`+`overflow:hidden`으로 → 라벨 유무·텍스트 길이·폰트 무관하게 라벨 영역이 항상 정확히 11px, 이벤트 시작 Y가 모든 칸에서 동일. Playwright로 기간 일정 16~18 + 17일 제헌절 재현해 바 Y 차이 0px·텍스트 클리핑 없음 검증. ※ 현재 배포본(1.1.2)엔 이 min-height 상한 미보정 상태라 실기기에서 보였던 것으로 추정 — 이번 빌드부터 해소.
37. 🚧 **홈 화면 위젯 — 앱측 브릿지 이식 완료, 네이티브(Swift/CI/Apple) 미완** — **index.html 통합 완료(2026-07-11)**: 옛 `widget_today` 단일방 syncWidgetData를 **브릿지 v2로 교체**(전역 `syncWidgetData`/`buildWidgetPayload`/`_wgDoSync`/`_wgShift` @~8205) — 내가 속한 모든 방의 [오늘-7~+40] 일정+멤버(프로필명/가상)+할일+seal을 App Group(`group.com.lsung.uricalendar`) `widget.data` 키에 JSON 기록. `isNativeApp()` 아니면 no-op(웹/PWA 영향 0). loadEvents·일정이동 2곳에서 호출(디바운스 700ms). **딥링크 핸들러**: `com.lsung.uricalendar://add?room=<id>` → `appUrlOpen`(~7349)에서 `enterRoom`+`openDateSheetForToday`(위젯 ＋버튼). **할일 flush**: `setupWidgetForegroundFlush`가 `appStateChange(isActive)`마다 `_wgFlushPendingTodos()` — App Group `widget.pendingTodoToggles`(위젯이 append한 todoId 배열)를 **id별 홀짝**(홀수만 최종 토글)으로 DB `is_done` 반영(done_by/done_at/expires_at, toggleTodoDone과 동일)→대기열 비움→재동기화. ⚠️ **버그 수정**: DB 컬럼은 `is_done`인데 브릿지가 `done`으로 읽어 항상 미완료로 나오던 것 → `select is_done`+`done:!!t.is_done`(index.html+widget/bridge.js 둘 다). Playwright 검증(bridgelive.mjs JSON 정확·widgetflush.mjs is_done매핑+flush 홀짝). — **아직 남은 것(네이티브)**: (1) `widget/ios/UriCalendarWidget.swift`(4종 뷰·AppIntentConfiguration 방선택·멤버필터·ToggleTodoIntent·＋딥링크) **Xcode 미컴파일**(첫 빌드 때 API 버전차 잡기). (2) Apple Developer: App Group `group.com.lsung.uricalendar` + 위젯 App ID `com.lsung.uricalendar.widget` 생성. (3) Codemagic: cap sync 후 `add_widget_target.rb` 주입 스텝+위젯 서명. ⚠️ **빌드 저장소는 이 repo가 아니라 `lsung179-stack/appstore`**(package.json·assets/·google-services.json·ios/·www/ 보유; www/index.html이 실제 배포본). **2026-07-11 appstore 브랜치 `claude/ios-widget`에 실제 반영 완료**: (a) www/index.html ← 이 repo index.html 전체 복사(위젯 브릿지+range-bar fix 포함), (b) widget/ 폴더 복사, (c) package.json에 **`capacitor-widgetsbridge-plugin`** 추가, (d) codemagic.yaml의 ios-compile-check(무서명 컴파일검증)+ios-testflight에 위젯 주입 스텝(Sign in with Apple 엔타이틀먼트 뒤), (e) 서명은 업로드된 배포 인증서(`uricalendar_dist`, 개인키 포함)를 `ios_signing.certificates`로 키체인에 설치한 뒤, `app-store-connect fetch-signing-files`로 앱+위젯 두 번들 ID '프로파일'만 자동 발급(고정 프로파일 참조 제거). ⚠️ 인증서까지 fetch로 자동 발급 시도하면 "Cannot save Signing Certificates without certificate private key"로 실패함(기존 배포 인증서 개인키를 Apple이 안 내려줌) → 반드시 업로드 인증서 유지. ⚠️ **@capacitor/preferences 못 씀**: group 옵션 줘도 UserDefaults.standard에 키 프리픽스만 붙여 저장→별도 프로세스 위젯이 못 읽음. **widgetsbridge**가 `UserDefaults(suiteName:group)` 직접 기록(프리픽스 無)→위젯이 `widget.data`/`widget.pendingTodoToggles` 그대로 읽음. ⚠️⚠️ **플러그인 JS 이름은 `Capacitor.Plugins.WidgetsBridgePlugin`**(registerPlugin('WidgetsBridgePlugin')) — 축약형 `WidgetsBridge`가 아님! 처음에 `WidgetsBridge`로 접근해 undefined→syncWidgetData가 조용히 return→App Group에 아무것도 안 써져 위젯 방 목록이 텅 비었음(TestFlight 1.1.5(29)에서 발견). `_wgDoSync`·`_wgFlushPendingTodos` 둘 다 `WidgetsBridgePlugin` 사용. **호출 지점**: loadEvents·loadRooms·일정이동 후 syncWidgetData(디바운스 700ms). **네이티브 등록**: CAP_PLUGIN 매크로(구식 .m)라 CocoaPods 빌드(`cap add ios --packagemanager CocoaPods`)에서 자동 등록됨. ⚠️ 위젯 배포타깃 iOS17(AppIntentConfiguration 필수, 호스트앱은 16 유지). 다음: appstore 브랜치로 Codemagic **ios-compile-check** 먼저 돌려 Swift 컴파일 검증→통과 시 ios-testflight→green이면 main 머지. 상세=`widget/README.md`. **방향 확정(2026-07-10)**: iOS+Android 둘 다(Android Glance는 iOS 안정화 후) **🤖 Android 위젯 v1 착수(2026-07-12)**: ⚠️ Capacitor android가 **Java 전용**(MainActivity.java, Kotlin 플러그인 無)이라 Glance(Kotlin+Compose 툴체인) 대신 **RemoteViews(순수 Java, gradle 의존성 0)** 로 감—매 빌드 재생성 프로젝트에서 안정적. 데이터 브릿지: iOS는 widgetsbridge(App Group), **Android는 `@capacitor/preferences`가 SharedPreferences 'CapacitorStorage'에 `widget.data` 기록** → 위젯이 `getSharedPreferences('CapacitorStorage').getString('widget.data')`로 읽음(`_wgDoSync`가 `Capacitor.getPlatform()`으로 분기). 파일: `widget/android/`(UriCalendarWidgetProvider·MonthWidgetService·MonthWidgetFactory·WidgetData .java + res/layout·xml·drawable + inject_android_widget.sh). **월 그리드 위젯**(GridView 7열 컬렉션, 일자별 이벤트 칩 최대2+넘침, 오늘/일요일 색, 탭=앱 열기, ↻ 새로고침, 주기 30분 갱신). v1 간소화: iOS의 gid 연속 기간 바 대신 일자별 칩, 멤버 필터·방 전환·씰 이미지는 다음 반복. Codemagic android-aab에 `inject_android_widget.sh` 스텝 추가(cap sync 후 파일복사+Manifest receiver/service 주입). package.json에 `@capacitor/preferences ^8.0.0` 추가. **다음: android-aab 빌드 돌려 컴파일 검증→실기기 위젯 추가 테스트→반복(iOS처럼).** / 4종(오늘 small·2주 medium·콤보 large·월그리드) / 방(프로필) 선택형·멤버 필터. **위젯 UI 실기기 피드백 반영(2026-07-11)**: 4종 분리(토글 아님)·씰 PNG 래스터라이즈 표시·씰 탭=방 순환(CycleRoomIntent)·프로필 탭=멤버 필터(SetFilterIntent)·small=내 일정만·프로필 사진 로드(base64 PNG)·기간 일정 연속 바(런 lane-packing, 주 경계서 끊김 방지)·월 `< >` 이동(ShiftMonthIntent)·다음달 흐림·함께 일정 dedup(4명→1바)·상하 잘림 방지(행 높이 균등분배)·월 위젯 **주 수 동적**(그 달이 실제 걸치는 5·6주 자동, 6주 달도 마지막 날 안 잘림). 6주 달은 `dense`로 숫자16·바10.5 축소해 **일정 2줄 유지**·새로고침(RefreshIntent)·격자선 앱설정 연동(gridV/gridH). **콤보(large) '다가오는 일정' 기간 묶기(2026-07-11)**: `ComboView.upcoming`을 `[UpRun]`로 변경 — 같은 제목+색의 연속 날짜를 하나로 묶어 `rangeLabel`로 '7월1일 ~ 7월5일' 칩 표시(하루는 오늘/내일/M.d+시간), GridView 런 그룹핑과 동일 로직. **헤더 '전체' 칩 + '나 먼저' 정렬(2026-07-11)**: 멤버 4명↑일 때 prefix로 내 아바타가 잘리던 문제 → `orderedMembers`가 `myUserId`를 맨 앞 고정(GridView/ComboView에 myUserId 전달). 아바타 줄 맨 앞에 **'전체' 칩**(SetFilterIntent 빈값=필터 해제, active==nil일 때 강조, `lineLimit(1)+fixedSize`로 '전'으로 안 잘림). maxAvatars=large 4. **일정바 텍스트 흑/백 자동(2026-07-12)**: 위젯 그리드 기간 바 텍스트를 배경색 명도에 따라 자동 전환(`contrastText(hex)` — 앱 `getContrastTextColor`와 동일: 가중 luminance `(0.299r+0.587g+0.114b)/255 > 0.62`면 어두운 글자 `#3a2418`, 아니면 흰색). `DayCell.barView`의 `.foregroundColor(.white)`→`contrastText(b.color)`. **새로고침=오늘로 복귀(2026-07-12)**: `RefreshIntent`가 데이터 갱신뿐 아니라 월/2주/콤보 오프셋(MONTH_KEY/WEEK_KEY/COMBO_MONTH_KEY)을 모두 0으로 리셋 → 어느 위젯에서든 새로고침 누르면 현재 날짜로 복귀. **위젯 편집 멤버 후보를 선택 방으로 한정(2026-07-12)**: `MemberQuery`가 모든 방 멤버를 나열해 다른 방 멤버까지 뜨던 문제 → `@IntentParameterDependency<CalConfigIntent>(\.$room)`로 선택된 방에 의존, `membersFor(roomId)`가 그 방(없으면 현재/첫 방) 멤버만 후보로. **멤버 오버플로 '+N'(2026-07-12)**: 멤버가 maxAvatars보다 많으면 마지막 칸을 `+N`(숨은 멤버 수)으로 표시(`shownMembers`=maxAvatars-1개+`+N`, `hiddenCount`). `+N` 탭=앱 그 방 열기(딥링크). 숨은 멤버 필터는 위젯 편집화면 '멤버' 드롭다운(CalConfigIntent, 전체 멤버 나열)에서 가능. '나 먼저'라 내 아바타는 항상 포함. **2주(medium) 위젯 주 이동(2026-07-11)**: `ShiftWeekIntent`(App Group `widget.weekOffset`, 1페이지=14일)+`readWeekOffset()` 추가, `GridView.weekNav`가 startDate에 `weekOffset*14`일 적용, 헤더에 `< 7.6~19 >`(weekLabel, 라벨 탭=현재로 복귀=delta -weekOffset, 오프셋≠0이면 terra색). 2주 헤더는 주이동 라벨이 넓어 maxAvatars=3(나 먼저라 내 것은 항상 보임). 월 위젯 ShiftMonthIntent와 동형(인터랙티브 위젯이 perform 후 자동 리로드). **콤보 미니달력 달 이동(2026-07-11)**: `ShiftComboMonthIntent`(App Group `widget.comboMonthOffset`, 월 위젯과 독립)+`readComboMonthOffset()`, `MiniMonth`가 base=today+off월 적용·헤더 `< 8월 >`(라벨 탭=복귀). 다가오는 일정 리스트는 오늘 기준 유지(미니달력만 브라우즈). **🚨 기간 바 오연결 수정(2026-07-11)**: 위젯이 **제목+색이 같으면 연속 날짜를 자동으로 기간 바로 이어붙이던 버그**(예 '놀러감' 7/17·7/18 단독 2건이 한 바로 연결). 앱은 `isSameRangeGroup`으로 **`shared_id`(함께 기간)/`range_group_id`(단독 기간)가 일치할 때만** 연결하고 단일·여러날(is_multi)은 절대 안 붙임. → 브릿지 payload의 events에 **`gid`** 필드 추가(`buildWidgetPayload`: **함께 일정 shared_id 있으면** is_multi면 `'s:'+id+'@'+date`(같은 날만 dedup·날짜 간 연결 안 함) 아니면 `'s:'+id`(연속 연결), shared_id 없으면 is_multi→null·range_group_id→`'r:'+id`·그 외 null / select에 `range_group_id,shared_id,is_multi` 추가). ⚠️ **함께 여러날 일정(놀러감: is_multi=true+멤버별 shared_id) 중복 수정**: 처음엔 is_multi를 무조건 null로 해서 같은 날 멤버 복사본이 dedup 안 돼 2개씩 겹침 → shared_id 있으면 per-day 키로 dedup(위젯 Swift가 같은 gid를 Set으로 합침). Swift `WGEvent.gid`, `GridView.runs`·`ComboView.upcoming` 그룹핑을 title|color→**gid 기준**으로 교체(gid null이면 각각 단독 칩). index.html·widget 둘 다 변경 → **재빌드 필요**. **캘린더 그리드에 할일 표시(2026-07-12)** **레인 배치 occupancy 기반 수정(2026-07-12)**: 기존 laneEnd 방식이 기간 일정 종료일이 멀면 그 사이 모든 일정을 높은 레인으로 밀어 '+N'만 뜨고 바 안 보이던 문제 → 앱 _computeMonthLanes처럼 실제 날짜 점유 집합(occ)으로 레인 배정(기간은 걸친 날만 점유). 월·2주 그리드 둘 다 적용. **위젯 할일 완료 표시(2026-07-12)**: ToggleTodoIntent가 widget.data 전체 재인코딩(취약)하던 것 제거→대기열만 기록, 표시는 `todoDone()`=서버done XOR 대기열홀짝으로 즉시 반영(누르면 회색 표시). 콤보 다가오는일정의 `!done` 필터 제거(완료 시 사라지지 않고 회색). **일정바 순서=사용자 배치(order_index)(2026-07-12)**: 위젯이 생성순으로 배치하던 것 → payload events에 `order_index`(ord) 추가, iOS runs 정렬을 앱 `_computeMonthLanes`와 동일(기간 상단→order_index→시작일→제목), Android는 날짜별 이벤트를 order_index로 정렬. **테두리 일정(outline) + 할일 노출 수정(2026-07-12)**: (a) `bar_style='outline'`(앱 .ev-outline: 투명 배경+색 테두리+먹색 글자)를 payload `style`로 실어 iOS는 `barView`가 채움 대신 stroke 테두리, Android는 투명 배경+색 글자로 렌더. (b) 할일이 안 뜨던 원인=빈 이벤트 줄(maxLanes 2)이 항상 예약돼 할일을 셀 밖으로 밀어냄 → `usedLanes`(실제 쓰인 최대 lane+1)만 예약해 겹치는 일정 없는 달은 할일이 보이게. Android는 셀에 할일 줄(☐/☑+제목, 완료=취소선) 추가.: 월/2주 그리드 셀에 이벤트 바 아래로 **할일 줄**(작은 체크박스 `square`/`checkmark.square.fill` + 제목, 완료=취소선+흐리게) 추가. `DayCell`에 `todos`/`maxTodos`(2주=3·월=1) 파라미터, `GridView`가 날짜별 `room.todos.filter{date==}` 전달(할일은 공유라 멤버 필터 없음). 넘침은 이벤트+할일 합산 `+N`. 할일 날짜는 payload의 `start_date` 기준(범위 할일은 시작일에만). widget-only 변경이나 재빌드 필요. **2주 위젯 잘림 수정(2026-07-12)**: 2주(medium) 위젯은 셀이 짧은데 이벤트 2줄+할일 3줄(최대 5개)이 들어가 위가 잘리던 문제 → `maxTodos`를 `weeks==2 ? max(0,3-usedLanes) : 1`로 변경(이벤트 줄 수 포함 **셀당 총 3개**로 제한, 초과분은 `+N`)+2주도 `dense`(숫자16·바10.5 축소) 적용. 월 위젯은 기존대로(이벤트+할일1). **할일도 멤버 필터 적용(2026-07-15)**: 위젯이 특정 멤버로 필터돼도 **할일은 무조건 전부 표시**해서, 남의 프로필(예: 정선주) 필터에 내가 등록한 할일이 뜨던 문제. 앱 `_todoVisibleForView`와 동일한 `todoVisibleFor(t,filter)` 헬퍼 추가(iOS Swift) — 전체(nil)=모두 / 함께 할일(memberKeys 있음)=그 멤버 포함 시 / 가상멤버(v-)=숨김 / 혼자 할일=작성자(userId) 일치 시. payload todos에 `userId`(작성자)+`memberKeys`(함께 대상) 추가(todos select에 `user_id,member_keys`), Swift `WGTodo`에 필드 추가. 적용 3곳: GridView(dTodos, filter), TodayView(todaysTodos, eff=filter??내ID), ComboView(다가오는 할일, filter). ⚠️ **Android 위젯은 멤버 필터 자체가 아직 없음**(이벤트도 전부 표시) → 할일 필터 불필요, payload 새 필드는 무시(하위호환). **위젯 즉시 반영 수정(2026-07-18)**: 일정/할일 추가·수정·삭제·이동해도 위젯에 바로 안 뜨고 새로고침해도 옛 데이터던 문제. 원인=`syncWidgetData`가 **700ms 디바운스**라, 편집 직후 앱을 홈으로 내려 위젯을 보면 WebView가 백그라운드로 얼어 디바운스 콜백/`buildWidgetPayload`(네트워크+PNG 래스터)가 완료 못 해 App Group에 안 써짐 → 위젯이 옛 데이터 유지(새로고침해도 App Group이 옛 상태). **수정 3가지**: (1) `syncWidgetData(immediate)` 추가 — 명시 편집(loadEvents·loadTodos·드래그이동·포그라운드복귀)은 `immediate=true`로 **디바운스 없이 즉시 기록**(포그라운드 중 완료 보장). 로드/연속변경만 700ms 디바운스 유지. (2) `_wgDoSync`에 **in-flight 병합 가드**(`_wgSyncing`/`_wgSyncAgain`) — loadEvents+loadTodos 동시 호출 중복 빌드 방지, 진행 중 재요청은 끝나고 1회만 재실행. (3) **PNG 캐시**(`_wgSealPngCache`/`_wgAvatarPngCache`) — 매 sync마다 씰 재굽기·아바타 재다운로드(최대 2.5s)하던 것 캐시(키=씰값·roomName/URL, 바뀌면 자동 재생성, 아바타는 성공만 캐시해 일시 실패 재시도 허용) → sync가 빨라져 백그라운드 전 완료율↑. **할일 위젯 반영 신규**: `loadTodos`가 위젯 sync를 아예 안 해서 할일 변경이 위젯에 반영 안 되던 별개 갭도 수정(끝에 `syncWidgetData(true)` 추가). ⚠️ **Android는 여전히 자동 푸시 없음**(`WidgetsBridgePlugin`이 iOS 전용 → 쓰기 후 위젯에 알림 못 보냄) — 다만 이제 쓰기가 즉시 완료돼 **수동 새로고침 시 최신 표시**. Android 자동 푸시는 네이티브 브로드캐스트 필요(후속). index.html 변경 → 재빌드 필요. **🤖 Android 월 위젯 iOS 파리티 1차(2026-07-19)**: 실기기 없이(테스트 불가) 빌드 최소화 위해 **로컬 stub 컴파일 하네스**(`/tmp/awstub/`: android.* 최소 스텁 + `check.sh`가 `widget/android/*.java`를 javac로 검증)로 문법·구조 검증 후 반영. (1) **연속 기간 바(continuous range bars)** — 기존 일자별 칩(날마다 독립 정렬, 제목 반복, 레인 안 맞음)을 iOS `GridView.runs`/`slotsFor`와 동일하게 재작성: `MonthWidgetFactory`가 payload의 **gid**로 연속 날짜를 런(run)으로 묶고(gid 없으면 단독 칩, 앱 isSameRangeGroup과 동일), 실제 날짜 점유(occupancy) 집합으로 레인 배정(앱 `_computeMonthLanes`와 동일 → 일정 없는 날은 낮은 레인 재사용, '모든 반복이 3번째 줄' 문제 위젯에서도 해소). 셀별 `DayBar[MAX_LANES=2]`(contLeft/contRight)로 이어지는 칸은 제목 숨기고 색만 이어 붙여 연속 바처럼 보임. outline 스타일 유지. `widget_cell.xml` 좌우 패딩 1dp→0dp(인접 셀 바 맞닿음). node 단위테스트로 런 그룹핑·레인·연속 플래그 검증. (2) **방 순환**(iOS CycleRoomIntent) — 헤더 방 이름 탭=다음 방(`ACTION_CYCLE_ROOM` 브로드캐스트, 위젯-로컬 prefs `uri_widget_state.roomId`에 저장, `WidgetData.pickRoom(overrideId)` 우선). (3) **달 이동**(iOS ShiftMonthIntent) — 헤더 `‹ M월 ›`(`ACTION_SHIFT_MONTH` delta ±1, prefs `monthOffset`), 달 라벨 탭=오늘 복귀, 오프셋≠0이면 라벨 terra 강조. 오늘 하이라이트는 실제 오늘 기준(표시 달과 분리). 새로고침/방순환 시 `monthOffset=0`. 브로드캐스트는 기존 `ACTION_REFRESH`와 동일 패턴(명시 인텐트, 요청코드 2~6 고유), inject 스크립트 intent-filter에 새 액션 2개 추가(갤럭시 ROM 대비). ⚠️ **아직 iOS 대비 미구현(후속)**: 멤버 필터 칩, 씰/아바타 PNG 표시, 3종 추가 위젯 크기(오늘 small·2주 medium·콤보 large), 편집 후 자동 푸시(WidgetsBridge iOS 전용). widget/android 변경 → **android-aab 재빌드 필요**(appstore 브랜치 `claude/ios-widget`에도 동기 복사). **🤖 Android 위젯 iOS 완전 파리티(2026-07-19, 사용자 "전부 한 빌드에" 선택)**: 실기기 없이 1빌드 목표라 **stub 컴파일 하네스**(`/tmp/awstub`, javac로 21클래스 검증)+**node 순수-로직 단위테스트**(런/레인·upcoming 기간묶기·멤버필터·todoVisibleFor·dayLabel)로 최대 검증 후 반영. **위젯 4종 전부**(iOS와 동일): ①오늘(small)·②2주(medium)·③콤보(large)·④월(large). **구조**: `WidgetCommon.java`(공용 상태·브로드캐스트·런/레인·upcoming·색/날짜·씰/아바타 비트맵) + `WidgetData.java`(payload 파싱, **sealPng·avatarPng·todo.{id,time,userId,memberKeys} 추가** + `decodeDataUrl`) + 그리드 팩토리 `GridWidgetFactory`(월·2주 공용, kind 파라미터) + `MiniMonthFactory`(콤보 미니월) + 서비스 3종(Month/TwoWeek/ComboMini) + 프로바이더 4종. **payload는 iOS와 동일**(buildWidgetPayload 플랫폼 무관, Android는 `@capacitor/preferences`가 CapacitorStorage/widget.data에 sealPng/avatarPng 포함 전체 JSON 기록 — 확인함). **멤버 필터**: 헤더에 씰(탭=방순환) + '전체' 칩 + 멤버 아바타 최대4(나 먼저, 넘치면 +N) → 탭하면 그 멤버만(`ACTION_SET_FILTER`, prefs `filterUser`), 이벤트+할일 모두 필터(`todoVisibleFor` iOS와 동일). **씰/아바타**: payload의 base64 PNG를 `WidgetData.decodeDataUrl`→Bitmap, 아바타는 `circleBitmap`으로 **원형 클립**(사진) 또는 **색원+이니셜**(폴백, iOS SealIcon 폴백과 동일) Canvas 생성, `setImageViewBitmap`. **인터랙티브(브로드캐스트→중앙수신 UriCalendarWidgetProvider→상태갱신→refreshAll 4종)**: 방순환(CYCLE_ROOM)·달이동(월 SHIFT_MONTH)·주이동(2주 SHIFT_WEEK)·콤보미니 달이동(SHIFT_COMBO)·멤버필터(SET_FILTER)·새로고침(REFRESH, 오프셋 전부 0). 요청코드 액션·슬롯별 고유(FLAG_UPDATE_CURRENT). **오늘(small)**: 컬렉션 없이 고정행(이벤트3+할일2), 기본 필터=나(myUserId), 카운트 배지. **콤보(large)**: 좌='다가오는 일정'(gid 연속 기간 묶기 `upcoming`, 오늘 진행중=terra 칩)+오늘 할일1, 우=미니월(달이동, 이벤트 점 최대3). **2주(medium)**: 그리드 14칸+주이동 라벨(M.d~M.d). **드로어블**: chip_on/off(필터칩)·av_sel/none(선택링)·mini_today_bg·dot_circle. **info xml 3개**(today 2x2·twoweek 4x2·combo 4x4) + inject 스크립트가 **receiver 4종+service 3종** 주입(월 receiver에 SHIFT_WEEK/SHIFT_COMBO/SET_FILTER/TOGGLE_TODO 액션 추가). **할일 토글 구현(2026-07-19 추가)**: 오늘/콤보 위젯 할일 행 탭=완료 토글 — 위젯이 `WidgetCommon.appendPending`으로 **CapacitorStorage `widget.pendingTodoToggles`(JSON 배열 문자열)에 todoId append**(ACTION_TOGGLE_TODO 브로드캐스트), 표시는 `todoDone`=서버done XOR 대기열홀짝으로 즉시 회색 반영. 앱은 `_wgFlushPendingTodos`에 **Android 분기 추가**(`@capacitor/preferences` Preferences.get으로 큐 읽고→홀짝 판정→DB is_done flush→Preferences.remove로 비움)로 포그라운드 복귀 시 반영(iOS는 WidgetsBridge, 로직 공유). 그리드(월·2주)·미니월 할일은 컬렉션이라 표시만(iOS도 동일). ⚠️ **RemoteViews 제약상 iOS와 미세차이**: 바 모서리 라운딩 없음(평면 연속 바), 아바타 선택 표시는 링(dimming 없음). **오늘(small) 위젯 씰+카운트(2026-07-20)**: 오늘 위젯 좌측 상단에 방 프로필 씰 추가(탭=방 순환, iOS `TodayView`에 `SealIcon`+`CycleRoomIntent`·Android `tw_seal`+`sealBitmap`+CYCLE_ROOM), 카운트 배지를 일정만→**일정+할일 합산**으로. 오늘 위젯은 이미 내 일정+내 할일 표시(eff=filter??myUserId). iOS·Android 동시 적용. **새로고침 깜빡임 피드백(2026-07-20)**: 위젯 ↻ 탭 시 시각 피드백. iOS=2-엔트리 타임라인(RefreshIntent가 App Group FLASH_KEY=now 기록→timeline이 0.8s 이내면 flash엔트리(opacity .4)+0.45s후 정상엔트리, 위젯 Group.opacity로 잠깐 흐려졌다 복귀=페이드 깜빡임). Android=goAsync+Handler 이중 업데이트(refresh 시 setFlash(true)+refreshAll로 ↻ 아이콘 terra 강조 렌더→300ms후 setFlash(false)+refreshAll 원복, wireGridHeader·TodayWidgetProvider가 isFlash로 ↻ 색 토글). ⚠️ 실제 데이터 새로고침은 그대로(notifyAppWidgetViewDataChanged), 깜빡임은 위에 얹은 피드백일 뿐. stub 컴파일 검증(iOS는 오프라인이라 브레이스 균형만). ⚠️ **실기기 검증 0**(갤럭시 없음) — stub 컴파일·XML 유효성·resId 매칭·매니페스트 주입·로직 단위테스트까지만 로컬 검증. widget/android 전면 변경 → **android-aab 재빌드 필수**.
40. ✅ **혼자 넛지 깜빡임 수정 + 캘린더 상단 현재 방 표시 바** — (a) `renderInviteNudge` 조건 `realCount<2`→`realCount===1`: 방 진입 순간 `_myMembers=[]`(count 0)일 때 멤버 있는 방에서도 넛지가 깜빡였음 → 로드 완료+진짜 혼자(1명)일 때만. (b) 방 많이 쓰는 사람이 첫 진입 시 '무슨 방'인지 헷갈리던 문제(캘린더에 방 이름이 어디에도 없었음, lastRoomId 자동 진입) → **월 표시줄(‹ 7월 ›) 좌측에 방 씰**(`#calRoomSealBtn`, `renderRoomSeal()`) 상시 표시(혼자 방·공유 방 모두, 세로 공간 0 — 월줄은 항상 있음). ⚠️ **변천**: 별도 상단 바→멤버 탭 줄(전체 옆)→"혼자 방엔 씰 안 뜸" 피드백으로 **월 표시줄 좌측 씰로 이동**(2026-07-12 최종). 검색버튼과 함께 `.cal-month-left`(absolute left) 그룹. 씰만 표시(이름 없음), 탭=cycleRoom. 탭=`cycleRoom()`(다음 방으로 바로 순환 전환+`showRoomSwitch()`로 씰+방이름 중앙 안내 1.1s 자동사라짐, **따뜻한 파치먼트 크림 배경**(#fdf7ec→#f3e6cd)+ink 글자(씰이 룸리스트처럼 자연스럽게 녹아듦, 링 제거), 방 1개면 `calTabSwitch('rooms')` 폴백). **첫 접속 안내**: loadRooms의 lastRoom 자동진입(`_autoEnteredOnce` 세션1회) 시 `_myRooms.length>=2`면 `showRoomSwitch`로 어느 방인지 1회 안내(방 1개는 제외). ⚠️ 처음엔 탭=방목록 이동이었으나 사용자 요청으로 **자동 순환 전환**으로 변경(위젯 CycleRoomIntent와 동일 감각). ⚠️ 처음엔 별도 상단 바(`#calRoomBar`)로 만들었으나 "달력 공간 줄어든다"는 피드백 → **멤버 탭 줄에 통합**(세로 공간 0 추가). `_sealInner(_mySealByRoom[id],name)`(없으면 첫글자 폴백)로 씰 렌더. `renderMemberTabs`가 방 탭을 **이미 떠 있던 멤버 줄 맨 앞**에만 얹음(공유 방 세로 공간 추가 0). ⚠️ **혼자 방(멤버<2)은 멤버 줄 자체를 숨김(원래대로)** → 달력 공간 안 줄어듦(사용자 피드백 "달력 공간 줄어든다"). 즉 방 표시는 멤버 있는 방에서만, 공짜로. index.html 변경 → 재빌드 필요.
36. ✅ **관리자 광고 코호트 + 초대 전환 지표** — (a) `admin_ads_cohorts()` RPC 신설 + 대시보드에 '주간 가입 코호트(광고 효과 추적)' 표: 가입 주별 가입→7일 생존→초대 합류→체험→유료→해지예약. Apple Ads 채널의 실효율(설치가 유저가 되는지) 판독용. 7일 미경과 코호트는 '측정중', 일부 경과는 '(n/m 경과분)'. (b) `admin_growth_stats()`에 초대 루프 지표 추가: '초대 입장(7일/30일)'·'초대 동반가입률'(30일 신규 중 가입 7일 내 타인 방 입장) 카드 2개 + 구독 전환율 카드에 해지예약 수. 판별 기준: 초대 입장=타인 소유 방의 members 행, 체험=period_type TRIAL, 유료=NORMAL, SANDBOX/is_manual 제외. admin.html은 정적 파일이라 머지 즉시 라이브. ⚠️ 첫 실데이터: 6/29주 광고 유입 70명 중 7일 경과분 27명의 생존 1명(4%) — 광고 설치의 대부분이 첫 주 내 이탈(코호트 표가 이걸 계속 추적).
35. ✅ **광고 배너-탭바 10px 이격(AdMob 오클릭 정책)** — 구글 AdMob '의도치 않은 클릭 방지' 정책 안내 메일 대응. 배너는 화면 최하단 고정 그대로 두고, `body.has-ad`의 탭바 offset을 `--ad-h`→`--ad-h+10px`로(콘텐츠 padding 64→74, FAB·오늘버튼·토스트 74→84 함께 보정). `--ad-h`는 `bannerAdSizeChanged` 이벤트로 기기별 실제 배너 높이가 들어오므로 어떤 화면 크기에서도 겹침 불가 — +10px은 순수 여백 추가라 가림 위험 0. Playwright로 --ad-h 56/90px 각각 탭바 bottom 66/100px·해제 시 원복 검증.
34. ✅ **방 프로필(컬러 씰) 무료 전환** — 사용자 결정으로 프리미엄 게이트 전면 해제. `saveRoomMascot`/`clearRoomMascot`의 `isEffectivePremium()` 체크+`showUpgradeModal` 삭제(누구나 저장), 프리미엄 구독 시트의 '방 프로필'(🪷) 기능 행 제거, 온보딩 꾸미기 문구를 "전통 문양 씰로 방을 꾸미고, 스토어의 테마·글꼴로 캘린더를…"로 조정(씰을 유료 그룹에서 분리). 스토어 히어로 배너·연필 진입·개인별 저장(members.seal) 로직은 그대로. Playwright 검증(무료 유저 저장 성공·업그레이드 모달 0회·프리미엄 시트 행 제거).
33. ✅ **알림 설정 화면(종류별 토글) + 카테고리별 발송 필터** — 설정→기타에 **'알림 설정' 행** 추가(고급 설정 아래). ⚠️ **설정 탭 화면은 `uni-nav` 마크업(~line 3150, `openAdvSettings` 버튼 근처)** — 처음에 옛 방설정 시트(`roomSetCard`의 rs-nav-row)에만 넣어서 실기기에서 안 보였음(#61에서 uni-nav에 추가로 수정, roomSetCard 쪽 행도 잔존) → 전용 시트 `#notifSetCard`(⚠️ id 주의: `notifCard`는 기존 알림함 시트가 이미 사용 — 충돌로 개명함). 구성: (a) **기기 권한 상태 카드**(`_ntfRenderSys`) — `getPushStatus()` 기준 granted=✅안내 / default=🔕+'알림 켜기' 버튼(`ntfEnablePush`) / denied=🔕+**휴대폰 설정에서 켜는 법 플랫폼별 안내**(iOS: 설정→알림→우리 캘린더 / Android: 설정→앱→알림) — 거절 유저 복구 경로(공지 대신 상시 UI로 해결). (b) **앱 전체 알림 마스터 토글**(`toggleNotifMaster`) — 구 고급설정 '알림 받기'를 이동·iOS 스위치화(`.ntg`), `members.notifications_enabled` 전체 update + 켤 때 권한 요청(구 `toggleRoomNotification`/`syncRoomNotifButton`은 삭제). 마스터 OFF면 하위 카테고리 dim(`.ntf-dim`). (c) **카테고리 토글 7종**(`NOTIF_CATS`: event 캘린더/todo 할일/reminder 미리알림/daily 아침요약/nudge 일정등록안내/birthday 생일/social 반응·멤버) — **`profiles.notif_prefs` jsonb**(마이그레이션 `add_profiles_notif_prefs`, 키없음/true=켜짐·false=꺼짐)에 저장, 낙관 반영+실패 롤백(`toggleNotifCat`). **발송 필터는 서버 중앙 처리**: `send-push` **v8** — body에 `kind`(선택) 받아 수신자 notif_prefs[kind]===false 제외(kind 미전달=구버전 앱은 기존대로 전체 발송, 하위호환) / `push-cron` **v13** — `prefOffSet()` 헬퍼로 daily→'daily'·empty-rooms→'nudge'·reminder→'reminder'·birthday→'birthday'(수신자+본인) 필터(인앱 notifications row도 함께 스킵). 클라이언트 6개 발송 지점에 kind 추가(event×3·social=member_joined/reaction·todo). Edge Function 원본은 Supabase 버전 히스토리(send-push v7/push-cron v12)로 롤백 가능. Playwright 6종 검증(시트+prefs 로드·카테고리 토글 저장/재토글·마스터 OFF/ON(권한요청)·거절자 안내 카드), v13 크론 실행 200 확인.
32. ✅ **빈 방목록 '초대 코드로 입장' CTA — 초대 루프 개선 2단계** — 신규 가입자(초대받고 온 사람 포함)가 처음 보는 빈 방목록(`renderEmpty`)에 CTA가 '+ 방 만들기' 하나뿐이라 초대받은 사람의 입장 동선이 없었음(우상단 + 메뉴를 스스로 찾아야 함 → 혼자방 생성/이탈 원인). 고스트 버튼 **'초대 코드로 입장'**(`openJoinRoom()`) 추가 + 안내 문구를 "방을 만들거나, 초대받았다면 코드로 바로 입장"으로 교체. 코드입장 시트의 '복사한 코드 붙여넣기'(`pasteJoinCode`)와 이어져 r.html에서 자동 복사된 코드가 여기서 소비됨. index.html 변경이라 재빌드 필요. Playwright 2종 검증(버튼 2개 렌더·CTA→joinCard 열림).
31. ✅ **초대 랜딩(r.html) 스마트화 — 초대 루프 개선 1단계** — 혼자방 60% 누수의 초입 수리. 기존 r.html은 코드 수동복사+스토어 배지뿐이라, 앱에 이미 있던 딥링크(iOS `com.lsung.uricalendar://join?code=`, index.html ~7286)와 Android intent 패턴(~7515)을 랜딩이 전혀 안 썼음. 추가: (a) **'앱으로 바로 열기' 버튼**(모바일+코드 있을 때만) — iOS=커스텀 스킴, Android=`intent://…package=…twa`(fallback=r.html?noauto=1), 탭 시 **코드 자동 클립보드 복사**(미설치자가 설치 후 앱 안 '붙여넣기' 버튼으로 이어짐 = SDK 없는 deferred deep link), 1.6s 후에도 페이지가 보이면 설치 안내 힌트(`#appHint`) 표시. (b) **방 이름 개인화** — `lookup_room_by_code` RPC가 SECURITY DEFINER+anon EXECUTE라 r.html에서 익명 fetch로 호출해 "'가족방'에 초대받았어요" 표시(XSS 방지 위해 textContent로만 조립, 실패 시 기본 문구 유지). (c) Android 인앱브라우저(카톡 등)는 진입 시 자동 intent 시도(세션당 1회 `sessionStorage['r.autoOpen']`, `noauto=1`로 루프 방지). r.html은 정적 파일이라 **재빌드 불필요, 머지 즉시 라이브**. Playwright 6종 검증(iOS 버튼+개인화·미설치 힌트+클립보드·intent URL 형식·인앱 자동시도·PC/코드없음 숨김·RPC 실패 폴백). ※ 코드입장 시트의 '복사한 코드 붙여넣기'(`pasteJoinCode`, 갤럭시 클립보드 거부 폴백 포함)는 원래 있던 기능.
28. ✅ **바텀시트 스와이프 다운 닫기** — `.mo-card` 시트 공통(방 프로필·방 설정·검색·날짜이동 등): **상단(핸들 `.mo-handle`/제목 `.mo-title`/부제 `.mo-sub` 직계 자식)을 잡고 아래로 내리면 닫힘**. 구현: document 위임 pointerdown/move/up(전역 `_shDrag`, `_shEnd`) — 8px 이상 내리면 드래그 활성(시트가 손가락 1:1 추적, `transition:none`+inline `translateY`), 놓을 때 `min(120px, 높이 30%)` 넘으면 `translateY(110%)`로 마저 내린 후 **오버레이 클릭으로 원래 close 함수 호출**(상태 정리 보장, 오버레이 미연결 시 `.on` 직접 제거 폴백), 미달이면 스프링백. 드래그 존 CSS `touch-action:none;user-select:none`(상단 3요소만 — 시트 본문 스크롤은 그대로). ⚠️ 데스크톱 프레임 모드(line ~165 `.mo-card.on{...!important}`)에선 시각 추적은 안 되지만 닫기 동작은 정상. Playwright 3종 검증(큰 스와이프 닫힘·작은 스와이프 유지·타 시트 동작).
42. ✅ **음력 날짜 KASI 보정(하루 어긋남 수정)** — `lunar-javascript`(중국 표준시 UTC+8 기준)가 신월(합삭)이 자정 근처에 뜨는 달에서 한국 음력(KASI, UTC+9)과 **하루 어긋나던 문제**(발견: 2026 중양절 — 앱은 10/18, 네이버/KASI는 10/19). 원인=음력 달의 시작일(음력 1일)이 중국·한국에서 다르게 배정돼 그 달 전체가 하루 밀림. **수정(하이브리드)**: `korean-lunar-calendar@0.4.0`(KASI 기준, 지원 1000~2050) CDN 스크립트 추가(전역 `KoreanLunarCalendar`) + 헬퍼 `solarToLunarKR(y,m,d)→{lm,ld,isLeap}`·`lunarToSolar`가 **KASI 우선, 실패 시 lunar-javascript 폴백**. **음력 날짜 표시 4곳 재배선**: `getDayLabel`(음력명절/음력1·15일)·`getCellLunar`(셀 작은 음력)·`getDayHeaderInfo`(날짜시트 '음 M.D')·`coLunar`(공유카드). ⚠️ **24절기(`getJieQi`)·삼복(`getDayGan`)은 lunar-javascript 그대로 유지**(KASI 라이브러리엔 절기 없음). ⚠️ **음력 생일(`L-MM-DD`) 변환도 KASI로** → 음력 생일 푸시가 하루 밀리던 잠재 버그도 해소. node 회귀검증: 설날/추석/대보름 2025·2026 전부 KASI=기존과 동일(무회귀), 중양절만 10/18→10/19로 교정. index.html 변경 → 재빌드 필요. 되돌리려면 스크립트 태그+헬퍼 제거하고 4곳을 `Solar.fromYmd().getLunar()`로 원복(백업 태그 `backup/before-repeat-lunar-20260718`).
43. 🚧 **반복 일정 커스텀화 (매주 요일 다중 / 매월 며칠·N째주 요일·말일)** — 기존 매주=일정 요일 1개 자동, 매월=며칠만 지원하던 걸 확장. **마이그레이션 `add_repeat_custom_columns`**(라이브): `events`·`todos`에 `repeat_monthly_mode`(dom|nth|last), `repeat_nth_week`(1~4, -1=마지막) 추가(nullable). **① 앱(events) 완료**: (a) UI — `#evRepeatDetail`(매주=요일칩 `#evWeekdayChips` 다중선택 / 매월=모드 세그 `#evMonthlyMode` 며칠·N째주요일·말일 + `#evNthWeekSel`(첫째~넷째·마지막)+`#evNthChips` 요일). 상태 `_evRepeatWeekdays[]`·`_evMonthlyMode`·`_evNthWeek`, 기본값=origin 날짜 요일/N째주. (b) `getEvRepeatPayload`가 `repeat_weekdays`(다중)·`repeat_monthly_mode`·`repeat_nth_week` 반환, 저장 16곳에 필드 스레딩(`repeatMonthlyMode`/`repeatNthWeek`), 편집 로드 복원. (c) **펼치기** — loadEvents 인덱스에 `_monthlyNthEvents{'N-dow'}`·`_monthlyLastEvents[]` 추가, `_getDayEventsRaw`에서 `nthOcc=floor((day-1)/7)+1`·`isLastOcc=(day+7>말일)`·말일 매칭. `_weeklyEvents`는 이미 배열 순회라 매주 다중요일 자동 지원. `repeat_except`(이 날짜만 삭제)는 기존 필터로 커스텀 반복도 커버. node 검증: 월수금·첫째주월화목·매달21일·마지막주금·말일 전부 정확. **② 위젯 펼치기 완료**: `buildWidgetPayload`가 반복 일정을 창[-40~+70] 안 구체 날짜로 전개(`_wgExpandEvent`, 앱 규칙과 동일 — node 회귀로 앱 발생일과 일치 확인). origin이 창 이전인 반복 일정은 별도 쿼리(`.lt('date',from).or('repeat_type.in.(weekly,monthly,yearly),recurring_yearly.eq.true')`)로 가져와 전개(try/catch로 실패해도 graceful). 반복 발생분은 **gid=null**(각 단독 칩 — 요일/월 반복이 연속 기간 바로 잘못 이어지지 않게), 비반복만 기존 gid(함께/기간 연결) 유지. events select에 반복 컬럼 6개 추가. **위젯은 재빌드 필요**(index.html+www 반영). **③ 할일(todos) 반복 완료**: 일정과 동일 UI/로직 이식. 마이그레이션 `add_todos_repeat_weekdays`(todos에 `repeat_weekdays int[]` 추가 — 최초 `add_repeat_custom_columns`가 mode/nth만 넣고 weekdays는 빠뜨렸던 것 보정). UI=`#tdRepeatDetail`(요일칩 `#tdWeekdayChips`/매월모드 `#tdMonthlyMode`/N째주 `#tdNthWeekSel`/`#tdNthChips`), 상태 `_tdRepeatWeekdays`·`_tdMonthlyMode`·`_tdNthWeek`, 핸들러 `setTodoRepeat`(확장)·`_syncTdRepeatUI`·`onTdMonthlyMode`·`onTdNthWeek`(window 노출). `_doSaveTodo` fields에 `repeat_weekdays`/`repeat_monthly_mode`/`repeat_nth_week` 추가(모든 저장 경로가 `fields` 공유), 편집로드/리셋 복원. 펼치기=`todoOccursOnDate` 확장(weekly 다중요일·monthly dom/nth/last). 위젯=`buildWidgetPayload` todos도 `_wgExpandEvent` 재사용해 전개(창 이전 반복은 별도 쿼리). node 검증: 일정과 동일 발생일. ⚠️ 할일 반복은 `repeat_except`(이 날짜만 삭제) 미지원(todos 컬럼 없음, 기존 동작 유지). **④ 반복 일정 레인 per-day 배치 수정**: 반복 일정(예 매주 화목금)이 특정 날의 다른 일정(기간 일정 등) 때문에 밀린 레인이 **모든 발생일에 똑같이 적용**돼(일정 없는 날도 3번째 줄) 있던 문제. 원인=`_computeMonthLanes`가 반복 일정을 `_eventItemKey`='e:id' 한 아이템으로 묶어 가장 혼잡한 날 기준 단일 레인 배정. 수정=`_eventLaneKey(ev,dayKey)` 신설 — 반복(비기간) 일정은 `e:id@date` 날짜별 키로 spread(할일 반복이 이미 하던 방식). `_computeMonthLanes` 아이템 빌드(9344)+`_dayAbsSlots` 소비(9500) 둘 다 이 키 사용. node 검증: 특근range+시그니엘 있는 날만 아래 레인, 빈 날은 lane0. 위젯은 반복 gid=null이라 이미 per-day라 무관. **⑤ 매월 N째주 기준 명확화**: '첫째/둘째 주'가 달력 주인지 요일 등장 순서인지 애매하다는 피드백 → N째주 선택 시 **실제 날짜 힌트**(`#evNthHint`/`#tdNthHint`) 표시(`_nthHintText`/`_nthDatesInMonth` 전역 헬퍼) — 예 "매달 둘째 화·목·금요일 · 이번 달 10월 8·9·13일". 라벨 '몇째 주'→'몇째 · 요일', 며칠 힌트에 28일 초과 시 '(그 날이 없는 달은 건너뜀)' 추가. **⑥ 반복 UI 위치 이동**: 일정폼 `#evRepeatRow`(색상 아래→`#evModeRow` 안 모드토글과 미니달력 사이)+**할일폼도 동일**(반복 블록을 색상 아래→기간 토글과 `#todoMiniCal` 사이로, 사용자 요청 "기간이랑 캘린더 사이"). id 그대로라 JS 무영향(위치 무관). **⑦ 반복 시작일 자동 스냅**: "N째주 요일을 8월부터 시작하려면?" 질문 대응 — 반복 origin이 패턴과 안 맞으면(예 8/20에 "둘째 월요일" 생성) 8/20 자투리 일정이 남고 패턴은 다음 달부터던 문제. 헬퍼 `_repeatFirstDate(baseStr,type,weekdays,monthlyMode,nthWeek)` 신설 — **선택 패턴의 기준 달 첫 발생일** 반환(weekly=base 이후 첫 해당요일 / monthly nth=기준달 최소 발생일 / last=기준달 말일 / dom=그대로). `getEvRepeatPayload`가 `start_date`(스냅값) 반환, `_doSaveEvent`가 **새 일정·하루 모드**일 때 `_dayDate=start_date`로 origin 이동(편집·기간·여러날은 미적용). `_doSaveTodo`도 single+새 할일에 동일 적용(전역 `_repeatFirstDate` 호출). → 8월 아무 날 + "둘째 월요일" 고르면 8월 둘째 월요일부터 자투리 없이 시작. node 검증 8종(요일/nth/last/dom/음수nth). **⑧ N째주 = 달력 주(줄) 기준으로 전환**: "둘째주 수목"이 수(4/14)·목(4/8)으로 서로 다른 줄에 갈라지던 문제(4월1일이 목요일이라 목의 2번째 등장과 수의 2번째 등장이 다른 주). 원인=N째주를 **'그 요일의 N번째 등장(occurrence)'**으로 계산. → **'N번째 달력 줄(주 시작 설정 반영)'** 기준으로 변경해 같은 N째주의 여러 요일이 같은 줄에 붙게. 핵심 헬퍼 `_nthWeekdayDate(y,m0,nth,dow)`=N째 줄의 그 요일 날짜(월 밖이면 0), `_weekStartPref()`. **5곳 일괄 전환**: `_nthDatesInMonth`(힌트)·`_repeatFirstDate`(스냅)·`_getDayEventsRaw`(앱 펼치기, `_nthWk=floor((day-1+firstCol)/7)+1`)·`todoOccursOnDate`(할일)·`_wgExpandEvent`(위젯) 전부 `_nthWeekdayDate` 기준. -1(마지막주)=마지막 등장(=마지막 줄) 유지. ⚠️ 부작용: 월 첫 줄이 부분주면 '첫째주 [요일]'이 그 달에 없을 수 있음(정직한 달력주 의미, 힌트가 실제 날짜/없음 표시). node 검증: 2027-04 둘째주 수목=7·8(같은 줄), 셋째주=14·15. **⑨ 미니 달력 ↔ 반복 힌트 연동**: '며칠' 모드에서 폼 미니 달력 날짜를 탭해도 "매달 N일" 힌트가 옛 날짜 그대로던 문제(_dayDate만 바뀌고 반복 UI 미갱신). `selectDayDate`(일정)·`todoMiniPick`(할일)에 `syncEvRepeatUI`/`_syncTdRepeatUI` 호출 추가 → 날짜 탭 시 며칠 힌트·N째주 날짜 힌트 즉시 갱신. **⑩ 달력 월 이동 시 N째주 힌트 연동 + 힌트 색상 개선**: (a) 날짜 탭뿐 아니라 미니 달력 **월 이동**(`dayCalNav`/`todoMiniNav`)에도 힌트 갱신 추가, N째주 힌트가 **표시된 달**(`_dayCalDate`/`_todoMiniY·M`) 기준으로 계산돼 달력 넘기면 그 달 날짜로 바뀜(힌트 '이번 달'→'N월'로 문구 변경, 표시 달≠현재 달 대응). (b) `.rep-hint` 색상 `--sub`(흐림)→`--accent`+`font-weight:600`(잘 보이게). **⑪ 말일 힌트에 실제 날짜 표시**: '말일' 힌트가 "매달 마지막 날에 반복돼요"뿐이던 것 → 표시된 달의 실제 말일을 괄호로 "(10월 31일)" 추가(`new Date(y,m+1,0)`, 달력 월 이동 따라감). 일정·할일 둘 다. index.html 변경 → 재빌드 필요.
44. ✅ **방 씰 롱프레스/드래그 방 선택기** — 캘린더 월줄 좌측 방 씰(`#calRoomSealBtn`) 탭=`cycleRoom`(순환)이던 것을 **방 목록 팝업**으로 교체(사용자 요청). 씰 `onpointerdown`(`_sealPointerDown`) → `#roomSealPicker` 팝업(모든 방 씰+이름, 현재 방 `.cur` 표시) 열림. **두 가지 선택 방식**: (1) 누른 채 드래그 → `pointermove`가 `elementFromPoint`로 손 밑 항목 `.hl` 강조 → `pointerup`이 그 방으로 `enterRoom`+`showRoomSwitch`, (2) 항목 탭(`onclick=_sealPickRoom`). 탭(안 움직임)으로 씰 놓으면 목록 유지→항목 탭 대기, 드래그 후 항목 밖에서 놓으면 닫힘. 바깥 탭(`_sealPickerOutside` pointerdown capture)으로 닫힘. **씰 재탭 토글 닫기**: `_sealPointerDown`이 이미 열려 있으면(`.on`) `closeRoomSealPicker`+return(드래그 세션 안 시작). 방 1개면 `calTabSwitch('rooms')` 폴백. 씰 버튼 `touch-action:none`(드래그 시 스크롤 방지). 함수 전부 전역+window 노출(`_sealPointerDown`/`_sealPickRoom`). jsdom 6종 검증(열림·탭유지·항목탭·드래그릴리즈·빈곳드롭·솔로폴백). ⚠️ **cycleRoom은 유지**(미사용이나 window 노출 그대로). index.html 변경 → 재빌드 필요.
45. ✅ **다른 방 일정 가져오기(캘린더 상시, 선택+미리보기)** — 방 만들 때만 있던 '기존 방 일정 가져오기'를 **캘린더에서 언제든**. 월줄 우측 `.cal-month-right`(오늘칩+`#calImportBtn` **복사 아이콘** flex 그룹, 오늘칩 absolute→flex child) → `openImportEvents()`. UI 문구 '가져오기' 통일(함수명만 import). **2단계 시트(`#importCard`)**: (1) 소스 방 목록(`#importRoomList`, 씰+이름) 탭 → (2) `_importPickRoom(srcId)`가 소스 방 내 일정을 **논리 일정으로 그룹핑**(함께=shared_id/단독기간=range_group_id로 묶어 1항목, 나머지 개별) 후 **체크리스트**(`#importEventList`, 제목·색점·날짜라벨·체크박스, 이미 있는 건 회색+체크해제) 표시 → `#importConfirmBtn` '확인'으로 `_confirmImport()`. `_importSelectAll`(전체선택/해제), `_importToggle`(개별). **년/월 좌우 페이지**: 체크리스트를 첫 날짜 기준 `YYYY-MM`으로 묶어 **한 달씩** 표시, `‹ 2026년 5월 ›` 화살표(`_importPageNav`)+**좌우 스와이프**(`_importSetupSwipe`, 이벤트 목록 pointerdown/up 가로 스와이프 48px↑)로 달 이동. `_importPageIdx`/`_importYMs`, 헤더에 'N/M달 · 선택 N/전체'. `_importSelectAll`=**현재 달만** 전체 토글. 체크는 달 넘겨도 유지, 확인은 전 달 체크분 전체 삽입. **기간일정 단일화 버그 수정**: 예전엔 소스 range_group_id를 그대로 복사했는데, 확인 시 **연속 기간 그룹마다 새 `rg_+genShared()` 재생성**해 모든 날 같은 id로 삽입 → 기간 바 유지(단일로 안 쪼개짐). 여러날(is_multi)은 is_multi=true로 각 단독. **중복 제외**=날짜\|제목\|시간 기준 현재 방 내 일정과 대조(그룹 내 일부 중복이면 그 날만 스킵). 함께 일정도 내 행만 solo(shared_id 제외)로. `_attachVirtualMember`로 관리자모드. 함수 전역+window 노출. node 검증(기간=새 rg로 3일 묶임·여러날 is_multi·단일·dedup). index.html 변경 → 재빌드 필요.
46. ✅ **월 일정 '이미지' 공유 전송(실제 화면 캡처)** — 캘린더 월줄 우측 `.cal-month-right`에 **공유 버튼**(`#calShareBtn`) → `shareMonthImage()`. ⚠️ **처음엔 캔버스 직접 그리기였으나 "실제 화면과 다르다, 완전 똑같이" 피드백** → **`html2canvas`(이미 CDN 로드됨, line 4746)로 실제 캘린더 화면(`#calendarScreen`) 그대로 캡처**(`_captureCalendarImage`). **하단 탭바는 `#calendarScreen`의 형제 요소라 자동 제외**, 캡처 전 `calInviteNudge`·`calFab`·`roomSealPicker`만 잠시 숨김. **멤버 필터 자동 반영**(현재 렌더된 화면 그대로 → 전체=전체일정, 멤버 선택 상태면 그 멤버 일정만). **방 프로필 씰도 화면 그대로 포함**(월줄 좌측 `#calRoomSealBtn`). **캡처 이미지 정리(피드백 반영, `_prepMonthCapture` in html2canvas `onclone` — 실제 화면은 안 건드림)**: (a) 상단 멤버 프로필은 **현재 선택된 것(누구의 일정)만** 표시(`.cal-member-tab.on`만 남기고 나머지 숨김), (b) 월줄의 **가져오기·공유·돋보기·오늘 버튼 + 양쪽 ‹›화살표 숨김**, 방 씰 + 년월만, (c) **년월 한 줄**('2026년 7월', `.cal-month-title`를 row+단일 div로 재구성), (d) **이모지 제거**(`_stripEmoji` — 그리드 텍스트노드에서 이모지 유니코드 스트립, 이모지가 일정 제목 가리는 문제). 크롭은 bbox 대신 **배경색 자동 트림**(`_trimBg` — 캔버스 위아래 균일 배경행 제거, 클론 레이아웃 변화에 안전). 위아래 여백은 배경색으로 명시적으로 채움(상단 W*5.5%·하단 4.5%·좌우 3%, 여백부족·좌우잘림 피드백). ⚠️ **2차 수정(피드백)**: (e) **반응 버블 제거** — 멤버가 남긴 이모지/캐릭터 반응(`.cal-reaction-bubble`, 곰돌 등 `svgEmoji` SVG)이 렌더 때마다 애니메이션으로 떠 캡처에 잡히던 것(텍스트 아니라 SVG라 `_stripEmoji`로 안 지워짐) → onclone에서 `.cal-reaction-bubble` remove. (f) **프로필 잘림 수정** — `_trimBg` x샘플이 15~85%뿐이라 **좌측 끝(아바타·이름·씰)만 있는 상단 행이 배경으로 오인돼 잘리던** 것 → x샘플을 4~95%로 넓힘. (g) **프로필을 방 씰 옆으로 이동**(피드백) — 별도 멤버탭 줄에 있던 '누구의 일정' 프로필을 `.cal-month-left`(씰 옆)로 이동(활성 멤버 아바타 30px+이름을 컴팩트 chip으로 씰 뒤에 append), 원래 `#calMemberTabs` 줄은 숨김. 결과: 월줄 좌측 `[씰][아바타 이름]` + 중앙 '2026년 8월'. ⚠️ 아바타 짜부 수정: html2canvas가 img object-fit:cover를 못 그려 사진이 눌리던 것 → background-image cover(원형 유지)로 대체. **실제 앱**: `.cal-month-arrow` padding `6px 10px`→`6px 4px`(‹› 너비 축소). Playwright로 onclone 변환(버튼숨김·년월한줄·활성멤버만·이모지제거) 검증. scale=min(4,dpr+1.5)(공유 이미지 해상도 최대화, 기존 2.5→3.5~4배). `_shareImageBlob`이 File→`navigator.share({files})`(카톡 포함), 미지원 시 `_showMonthImagePreview`(길게 눌러 저장) 폴백. **직접 그리기 `_renderMonthImage`(캔버스 W=1080, 주시작·칩색·대비글자·+N·잘림)는 html2canvas 실패 시 폴백으로 유지.** 함수 전역+`window.shareMonthImage` 노출. 실제 화면 캡처라 오프라인 검증 불가(html2canvas+CDN 필요) — Playwright로 문법·함수 정의 확인, 폴백 캔버스는 PNG 시각검증 완료. — (구버전 캔버스 직접그리기 세부) 현재 보는 달(`_calYear`/`_calMonth`)을 **캔버스에 달력 이미지(PNG)로 그려** Web Share API(`navigator.share({files})`, iOS15+·최신 안드로이드 지원)로 전송(카톡 포함 공유 시트). ⚠️ **플러그인 없음**(@capacitor/share·filesystem 미설치)이라 파일 공유는 navigator.share files에 의존, 미지원이면 **이미지 미리보기 오버레이**(`_showMonthImagePreview`, 길게 눌러 저장 안내+저장 링크)로 폴백. `_renderMonthImage()`: W=1080 캔버스, 주 시작 요일(`getWeekStartPref`) 반영한 offset, 헤더(YYYY년 M월+방이름)+요일헤더(일요일 빨강)+6주 그리드(다른달 회색). 각 셀=날짜 숫자+일정 칩 최대3(+N 넘침), 칩 색=`colorHex`, 글자색=`getContrastTextColor`(명도 대비), 긴 제목 `…` 잘림, 시간 있으면 'HH:MM 제목'. 일정 소스=`_getDayEventsRaw`(반복·생일 포함), 제목+시간 dedup(함께 복사본 1개). `cv.toBlob`→File→share. **캡션(사진과 함께 가는 텍스트, A안)**: 전체="우리 YYYY년 M월 일정 🗓️", 멤버 선택 시 활성 멤버탭 이름으로 "OO님의 YYYY년 M월 일정 🗓️"(_shareImageBlob이 #calMemberTabs .on에서 판별). 📅→🗓️(카톡이 📅를 날짜 스티커로 렌더하는 문제 회피), 방이름 이모지 중복 제거. 캡션(최종)=**'[우리 캘린더 : 함께 쓰는 공유 캘린더]' 맨 위**(대괄호+콜론, 앱 이름) + 얇은 구분선(─) + 일정 문구('OO님의 YYYY년 M월 일정 🗓️'). ⚠️ **변천**: 태그라인 헤더 맨 위→일정 먼저(주인공)로 뒤집었다가 → 사용자가 "캘린더 문구를 위로"해서 다시 앱 이름을 맨 위로 / 구분자는 `·`(가운뎃점)→`:`(콜론)+대괄호로 확정 / 스토어 링크(App Store id6773996160 + Google Play app.vercel.first_uri_calender.twa) 둘 다 붙였다가 "링크는 그냥 빼줘(좋으면 검색해서 유입)" 피드백으로 **스토어 링크 전부 제거**, 홍보는 앱 이름 한 줄만. **`_trimBg` 하단 크롭 = '콘텐츠 밴드' 기준**(2026-07-20): 예전엔 격자선까지 콘텐츠로 봐서 마지막 주 **빈 셀 아래 여백**('한칸 빈칸' 피드백)이 남았음 → 하단은 실제 일정 바/글자 밴드(`frac(y)>0.05 && frac(y-1)>0.025`, 최소 2px 두께)의 맨 아래까지만 남기고 그 밑 빈 셀·1px 가로 테두리는 잘라냄(1px 선은 윗행이 비어 밴드 불인정→안 걸림). 상단은 기존 엄격 `rowBg`(씰·아바타 호 보존). `padT=W*0.0275`·`padB=W*0.012`(하단 최소 숨결)·좌우 0. Playwright 합성 캔버스로 하단이 마지막 바 밑(빈 셀/테두리 제외)에 크롭됨 검증. **텍스트 버전**(`_buildMonthShareText`/`shareMonthKakao`)은 이미지 생성 실패 시 폴백으로 유지. 함수 전역+`window.shareMonthImage` 노출(<16786). **Playwright(headless Chromium)로 실제 PNG 렌더해 시각 검증**(레이아웃·칩 색/대비·잘림·+N·주시작 정상). index.html 변경 → 재빌드 필요.
56. 🧪 **[v30·TEST] 동물 리액션 이모지 실사용 테스트 임베드** — 기존 캐릭터팩(모찌·곰돌·달콩) 이모지가 "허접·감정 애매"하다는 피드백 → 힉스필드로 **동물 캐릭터 재디자인** 착수. 앱 전통 감성에 맞춰 **아기호랑이·달토끼·해태** 캐스트 잡고, 톤 비교용으로 **대표 호랑이 2종**(밝은 3D 클레이 vs 무광 앱팔레트)을 생성→배경 투명 처리. **리액션 시스템에 임시 임베드**해 PWA 실사용 테스트: `svgEmoji(key)`가 `test:` 키면 `<img src=CDN>` 반환(힉스 cloudfront URL 직참조 — 이미지 바이트는 프록시 차단으로 다운로드 불가, 사용자 폰 브라우저는 정상 로드), 리액션 피커(`renderEmojiPager`)에 **관리자(`ANN_ADMIN_USER_ID`)에게만** '🐯 동물 테스트' 페이지 노출(본인만 선택 가능, 렌더는 모두에게 보임). ⚠️ **명확한 삭제 마커 `[TEST-ANIMAL]` 4곳**(① `TEST_ANIMAL_EMOJI` var 상수 + ② `svgEmoji` 내 1줄 + ③ `renderEmojiPager` 페이지푸시 + ④ CSS `.rx-animal-img`)만 제거하면 완전 원복. TDZ 방지 위해 `var`+널가드. Playwright로 svgEmoji 출력(3D/무광→img·모찌→svg 유지·미지정→'')·문법0 검증. index.html만 변경(PWA는 재빌드 무관, 브랜치 배포로 확인). **크기**: 컨텍스트별 `.rx-animal-img` 크기 지정(날짜시트 칩 18px·캘린더 버블 38px·피커 버튼 채움·기본 20px) — 처음 base `100%`라 날짜시트에서 카드만큼 커지던 것 수정. **사용자 선호: 무광 앱팔레트 > 밝은 3D**(2026-07-21, "저게 더 이쁘네"). **캐스트 확정(2026-07-21)**: 강아지·고양이·호랑이 3종 × **6감정**(좋아요·사랑해요·축하해요·빵터짐(😆 눈물X)·속상해요·응원해요), 무광 앱팔레트, **상반신 통일**(전신은 얼굴 작아짐), 고양이는 사용자 실제 반려묘(흰몸+귀/머리 태비+회색꼬리) 모티브. 스타일 일관성=무광 호랑이(job 335156e9)를 nano_banana_pro 레퍼런스로 물림. **미리보기 임베드**: `TEST_ANIMAL_SHEETS`(3종 시트 CDN URL) + 리액션 피커 관리자 페이지 '🐯 동물 시안'을 pickable 이모지 대신 **시트 이미지 갤러리**(`p.preview==='animalSheets'`, `.rx-animal-preview`)로 표시 — 크레딧 0으로 폰에서 톤/모양 확인용. **방향 전환(2026-07-21)**: 3동물 균일 스타일 → **8동물, 동물마다 그림체·감정 전부 다르게**(사용자 "각각 특색있게, 자유분방하게"). 강아지=크레용·고양이=플랫벡터·호랑이=민화·토끼=수채화·곰=색종이콜라주·펭귄=양모펠트·여우=모던카툰·원숭이=막그림(쿨다운). 동물마다 감정 세트도 다름(고양이 관심없어·삐짐·째려봄, 곰 졸려·토닥토닥, 여우 계략·시치미, 원숭이 시무룩·놀람 등). 각 6감정, 상반신. 산만함은 "취향껏 골라 쓰면 그만"으로 OK(사용자 결정). ✅ **정식 반영 완료(2026-07-21)**: 기존 캐릭터팩(모찌·곰돌·달콩) 삭제하고 **8동물×6감정=48개 개별 투명 스티커**(힉스 생성→remove_background→CDN URL)로 교체. `ANIMAL_STICKER`{'dog:like':url…48개} 맵 + `svgEmoji`가 동물키면 `<img class=rx-animal-img>` 반환(레거시 `CHAR_SVG` typeof 가드 폴백). `CHAR_EXPRESSIONS`·`CHAR_META`·`STORE_EMOJI`를 8동물로 교체 → 스토어 이모지탭·리액션 피커·상세모달·캘린더 버블·날짜시트 칩 전부 자동 반영(전부 svgEmoji 경유). 프리미엄 게이팅(`getUnlockedCharPacks`)·해제 로직 그대로. [TEST-ANIMAL] 미리보기 페이지는 제거(실제 8팩이 뜨므로), 죽은 `TEST_ANIMAL_*` 상수·`CHAR_SVG` 옛 SVG는 무해하게 잔존. ⚠️ 이미지가 힉스 CDN 직참조라 CDN 만료 시 깨질 수 있음(추후 자체 호스팅 권장). Playwright 검증(svgEmoji 동물→img·미지정→''·charEmojiKeys 6·문법0). index.html 변경 → 재빌드 필요(PWA는 무관). **⚠️ 2026-07-21 추가: 동물 2종 추가 → 총 10동물×6감정=60 스티커.** `mouse`(쥐, 색연필 스타일: like·love·sneaky·surprised·sad·cheer)+`chicken`(닭, 귀여운 카툰 성체 닭 스타일: like·proud·excited·laugh·sulk·cheer). ⚠️ **닭 디자인 변천**(사용자 연속 피드백): 클레이 병아리→실사 깃털 병아리→"병아리 말고 닭"→실사 깃털 성체닭→"징그러움"→**귀여운 카툰 성체 닭**(최종, 무광 스토리북·둥근 형태, job cf3b57a8 등). ANIMAL_STICKER·CHAR_EXPRESSIONS·CHAR_META·STORE_EMOJI 4곳 모두 mouse/chicken 추가(STORE_EMOJI 이름 '🐭 쥐'·'🐔 닭'). 이모지 배너도 mouse+chick 포함 버전으로 재생성(job 162b2d37). Playwright 10동물 검증(mouse/chicken 6키 img·팩 렌더·배너 URL·문법0). **⚠️ 2026-07-22 디자인 통일 + 크기 통일**: 닭 6종이 서로 다른 닭(색·그림체 제각각)이던 문제 + 곰 6종도 제각각 → **레퍼런스 물림 재생성으로 통일**(닭=승인된 '좋아요' 황갈색 카툰 암탉 job 4463a195 참조, 곰='좋아' 곰 job 62ba49b6 참조, nano_banana_pro medias 참조 + "EXACT SAME character" 프롬프트). **크기 통일**: 모든 신규 생성에 "프레임 높이 70% 점유+균등 여백" 지시. 기존 팩 크기 편차는 **동물별 CSS 다이얼**(`.rx-a-<동물>{transform:scale(...)}`, svgEmoji가 rx-a-* 클래스 부여)로 보정 — cat .86·mouse .9(캔버스 꽉 채우는 팩만 축소), 닭 .78 임시 보정은 재생성으로 불필요해져 제거. 닭·곰 URL 12개 hf_20260722_*로 교체. 나머지 8동물은 팩 내 일관성 확인됨(강아지·여우 등은 흰 다이컷 테두리 있음 = 동물별 그림체 차이는 의도). Playwright 검증(신규 URL 12·CSS 룰·문법0). **2026-07-22 캐릭터 이름 부여**: 팩 이름 옆 동물 이모티콘(🐶🐱…) 삭제하고 캐릭터 이름으로 교체 — 보리(강아지)·나비(고양이)·호담(호랑이)·달이(토끼)·꿀곰(곰)·뽀글(펭귄)·여우비(여우)·몽당(원숭이)·콩이(쥐)·꼬꼬(닭). STORE_EMOJI name/desc + CHAR_META name/identity 20곳 교체(identity='<그림체> <동물>'로 동물 식별 유지 — 상세시트 부제·리액션 이름 '보리 (좋아요)' 형태). **→ 2026-07-22 2차: 카카오 이모티콘식 풀 타이틀로 재변경**(사용자가 카카오 이모티콘샵 스크린샷 제시) — STORE_EMOJI.name='오늘도 신나개! 크레용 멍뭉이 보리'·'관심 없는 척, 츤츤 고양이 나비'·'민화에서 튀어나온 기운찬 호담이'·'수줍음 가득 말랑 토끼 달이'·'토닥토닥 포근포근 꿀곰이'·'뒤뚱뒤뚱 몽글몽글 펭귄 뽀글이'·'오늘도 계략 중! 꾀쟁이 여우비'·'대충 그렸는데 귀여운 몽당이'·'살금살금 꼬마 생쥐 콩이'·'우당탕탕 동네 대장 암탉 꼬꼬'. CHAR_META.name은 짧은 이름 유지(리액션 표기 '보리 (좋아요)'), openCharDetail 타이틀은 STORE_EMOJI 풀 타이틀 우선. **→ 2026-07-22 3차: 밈 톤으로 재작명**(사용자 "4050 아저씨가 유행 따라가는 이름 같다, 영하게") — 힘 뺀 밈 문형(~함/~에 진심/담당/맛집/다 계획이 있는)으로 전면 교체: '산책 두 글자에 진심인 보리'·'애교는 만렙, 눈치는 빵점 코코'(2026-07-22 고양이 이름 나비→코코 변경 + 타이틀 시크→바보·애교쟁이 톤 변경)·'작아도 기세는 호랑이, 호담'·'부끄러움 담당 토끼 달이'·'포근함 맛집, 꿀곰이'·'급할 거 없는 펭귄 뽀글이'·'다 계획이 있는 여우, 여우비'·'대충 그렸는데 귀여운 몽당이'(유지)·'작아도 할 건 다 하는 콩이'·'우당탕탕 암탉 꼬꼬'·'감정은 붓글씨로 대신함'(calli)·'오늘 날씨: 대체로 귀여움'(weather, 일기예보 패러디). **2026-07-22 이모지 탭 카카오샵식 리스트 개편(스토어 재설계 C안)**: 2열 그리드(2×2 미리보기)가 풀 타이틀에 안 맞아 → `renderStoreEmoji`가 **한 팩=한 행**(`.ep-row`: 대표 스티커 74px `.ep-th`=preview[0] + 풀 타이틀 `.ep-t1` + '표정 6종 · <그림체 동물>' `.ep-t2` + 상태 pill `.ep-act`) 리스트로 렌더. 행 탭=openCharDetail, 버튼은 stopPropagation으로 기존 사용/해제 로직 유지. 히어로 배너·섹션 타이틀 그대로. 옛 `.emoji-pack-preview` CSS는 잔존(무해). 재설계 A(히어로 캐러셀)·B(테마·컬러 카드 통일)는 미적용(사용자가 C·D만 선택, D=텍스트 세로형은 이미 적용돼 있었음). Playwright 검증(행10·썸네일10·버튼10·행탭→상세). **2026-07-22 곰·펭귄·원숭이 팩 재디자인**(사용자 요청): 3팩 18스티커 전부 힉스 재생성 — 곰=허니 스토리북(허니팟 안은 골드 곰, 구아슈 동화책), 펭귄=말랑 클레이(파스텔 아기펭귄 클레이), 원숭이=크레파스 낙서('대충 그렸는데 귀여운' 컨셉 유지, 두꺼운 크레용 선). 각 팩 대표 1장 생성→레퍼런스 물림(EXACT SAME character+70% 프레이밍)으로 5장 파생→remove_background→URL 18개 교체(hf_20260722_0625~0626). CHAR_META identity·STORE_EMOJI desc도 '허니 스토리북 곰'/'말랑 클레이 펭귄'/'크레파스 낙서 원숭이'로 갱신(팩 타이틀·이모션 키·labels는 그대로). Playwright 검증(18키 img+새 URL+rx-a-* 클래스·identity 3종·팩12·문법0). **2026-07-21 추가 조정**: (a) 이모지 팩 미리보기 상단 여백 — `.emoji-pack-preview` padding `8px`→`16px 8px 10px`(상단 행이 카드 위에 딱 붙던 것 해소). (b) 이모지 배너 칩 → ⚠️ **최종 전부 제거**(사용자 요청): 동물 이름칩(`.sfh-chips`) 삭제하고 부제 '일정에 콕 찍는 10가지 동물 캐릭터 팩' 텍스트만 남김(칩 4종→10종→제거 순 변천). **⚠️ 2026-07-22 우스꽝스러운 공룡 '두두' 신규 팩 추가(사용자 카카오 '이빨공룡' 스크린샷 제시)**: 크레용/마커 낙서풍 초록 공룡(두두), **씰·카드배경·이모지 풀세트**. 이모지 6감정 like·roar(으르렁)·laugh·love·sulk·cheer — 대표 like(job 3ba06108) 생성 후 레퍼런스 물림으로 5감정 파생→remove_background(hf_20260722_2313*). 씰(SEAL_SETS.a_dino=like 스티커 재활용, zoom 1.05)+카드(CARD_SKINS.a_dino=공룡이 크레용 원시림 씬에 녹아든 통짜 일러스트 a8899169). ANIMAL_STICKER 6·CHAR_EXPRESSIONS.dino·CHAR_META.dino(name '두두', identity '크레용 낙서 공룡')·STORE_EMOJI(name '이빨은 사나운데 하는 짓은 허당, 두두')·SEAL_SET_ORDER·CARD_SKIN_PREM(a_chicken 뒤) 추가. 배너 부제 '12가지'→'13가지 캐릭터 팩'(10동물+calli+weather+dino=13). Playwright 검증(6키 img·팩13·씰/카드·orders·rcs-img·문법0). **🎨 방 캐릭터 테마 프로토타입(2026-07-22, 사용자 "카드배경 캐릭터 방에 들어가면 캘린더·할일·설정도 그 테마로 꾸며줘, 대표 1개만")**: 방 카드 스킨(캐릭터)에 맞춰 **입장 시 앱 전체가 그 캐릭터 테마로 자동 변신**. 기존 per-room 테마 인프라(`THEMES`/`applyTheme`/`user_room_themes`) 위에 구현. (1) `THEMES.dino` 초록 팔레트 추가(스토어 목록엔 없음, 자동 전용). (2) `ROOM_CHAR_THEME` 맵(카드스킨키→{theme,cls,mascot}) — **대표로 `a_dino`(두두)만** 구현, 다른 캐릭터는 여기 추가하면 자동 확장. (3) `applyRoomThemeFor`가 방의 내 카드스킨이 매핑되면(사용자가 별도 방테마 안 골랐을 때) 그 캐릭터 색 테마 적용 → `--accent` 등 CSS 변수로 캘린더·할일·설정 버튼/탭/체크박스/FAB가 초록으로 통일. (4) `_applyRoomCharDecor`/`_clearRoomCharDecor`가 `body.rc-on/.rc-dino` 클래스 + **콘텐츠 뒤 큰 캐릭터 워터마크**(`#rcWatermark`, 타입블럭 스타일). 방 나가면(`exitRoom`)·인증화면(`showScreen`)에서 제거. **타입블럭(TimeBlocks) 스타일 반영(2026-07-22, 사용자 스크린샷 참고)**: 초기 좌하단 작은 마스코트 → **화면 중앙 하단 큰 캐릭터 워터마크**로 변경. `.screen`·`.cal-grid`·`.cal-cell`이 전부 투명이라 `body{background:var(--bg)}` 위·화면 콘텐츠 아래(`z-index:-1`)에 `#rcWatermark`(width min(84vw,380px)·opacity .15·pointer-events none) 깔면 날짜/일정 뒤로 은은하게 비침(터치 방해 0). 캘린더·할일·설정 전 화면에서 배경 워터마크 유지(방 리스트/인증 진입 시 제거). **🚨 워터마크 안 보이던 버그 수정(2026-07-23, 사용자 "그냥 색만 변한거잖아")**: 처음 `#rcWatermark`를 `document.body`에 `z-index:-1`로 붙였는데 **`.app`이 `position:fixed`라 스택 컨텍스트를 만들어 body의 z-index:-1을 완전히 가림**(빨강 테스트로 확인 — 안 보임). → **데코를 `.app`의 첫 자식(`#rcDecor`)으로 삽입**(`position:absolute inset:0 z-index:0 pointer-events:none overflow:hidden`). 투명한 `.screen`/그리드가 DOM상 뒤에 있어 그 위에 그려지고, 데코는 먼저 그려져 날짜/일정 뒤로 비침(Playwright 스크린샷으로 캘린더 뒤 캐릭터+구름 확인). 구성: **큰 캐릭터 워터마크**(`.rc-char` opacity .17, min(88vw,410px)) + **소프트 구름 2개**(`.rc-cloud1/2` 흰 radial, 우상단·좌하단). 초록 캐릭터가 초록 배경에서 대비 약해 opacity .15→.17. jsdom 검증(#rcDecor .app 첫자식·char bg·구름2·z0·pe none·accent·클리어·비매핑 무장식). **탭바·설정 SVG 캐릭터화(2026-07-23, 사용자 "하단 탭바·설정 svg를 캐릭터로 꾸며줘")**: (1) 탭바(`#calTabbar`, .app 밖 body 자식)—`body.rc-on`이면 아이콘·라벨 전체 초록 틴트(비활성 `--aL`·활성 `--accent`), **활성 탭은 SVG 숨기고 캐릭터 스티커 표시**(`.cal-tab.on .cal-tab-ico::after`=`var(--rc-char-url)`, `_applyRoomCharDecor`가 body에 `--rc-char-url` 세팅·클리어 시 제거). `.on` 클래스 반응이라 탭 전환 시 JS 불필요. (2) 설정 목록 아이콘(`.uni-nav-l svg`, currentColor)—`body.rc-on`이면 `--accent` 초록 틴트. Playwright 스크린샷 확인(탭바 활성=두두 얼굴·나머지 초록 라인·설정 초록+워터마크). jsdom 검증(--rc-char-url 세팅/클리어·문법0). **색↔워터마크 불일치 수정(2026-07-23, 사용자 "같은 방인데 한쪽 빈티지 한쪽 그린")**: 캐릭터 워터마크는 뜨는데 색은 방마다 달랐음 — 원인=`applyRoomThemeFor`가 캐릭터 색 테마를 **"저장된 방 테마(`user_room_themes`)가 없을 때만"** 적용(`!explicit` 게이트)해서, 예전에 방 테마를 저장한 방은 워터마크(캐릭터)만 붙고 색은 옛 테마 유지 → 데코와 색 불일치. → **캐릭터 카드 스킨이 있으면 색 테마도 무조건 그 캐릭터로 우선**(저장된 방 테마보다 우선). 캐릭터 스킨 없는 방만 저장된 방 테마/개인 테마 사용. jsdom 검증(스킨방=저장 lavender 무시하고 green+워터마크·비스킨방=lavender 유지+무데코). **버튼마다 다른 선사시대 아이콘 세트(2026-07-23, 사용자 "탭바·설정 svg를 하나하나 다른 걸로: 화산·나무·공룡·호수 등")**: 초록 틴트만이 아니라 **탭바 4 + 설정 7 버튼을 각각 다른 두두 테마 아이콘**으로 교체. 힉스 크레용 doodle 아이콘 11종 생성→remove_background(hf_20260723_0022~0023*): 동굴(방목록)·야자수(캘린더)·공룡발자국(우리할일)·화산(설정)·왕관공룡(프리미엄)·바구니(스토어)·알둥지(주문)·돌톱니(고급설정)·종(알림)·소라고둥(공지)·호수(고객센터). 각 버튼에 `data-rci="..."` 속성 추가 + `body.rc-dino [data-rci]` CSS로 원 SVG 숨기고 background-image 스왑(탭=`.cal-tab-ico` bg 128%, 설정=`.uni-nav-l::before` 23px). 아이콘 세트 없는 캐릭터는 제네릭 폴백(활성 탭 캐릭터 얼굴+초록 틴트) 유지. jsdom 검증(탭4·설정7 전부 svg display:none+테마 bg-image·문법0). ⚠️ 아이콘 CDN 직참조(에이전트 프록시 차단이라 로컬 스크린샷 불가, 구조검증만·시각확인은 사용자 기기). **→ 기능 맞춤 아이콘으로 재작업(2026-07-23, 사용자 "해당 컨셉(=버튼 기능)에 맞는 아이콘으로")**: 임의 선사시대 물건(동굴·야자수·발자국·화산·알둥지·돌톱니·소라·호수)이 버튼 기능과 안 맞아 → **기능 맞춤 아이콘 8종 재생성**(같은 크레용 화풍): 집(방목록)·달력(캘린더)·체크리스트(우리할일)·톱니(설정)·택배상자(주문)·슬라이더(고급설정)·확성기(공지)·물음표말풍선(고객센터). 기능에 이미 맞던 3종은 유지—왕관공룡(프리미엄)·바구니(스토어)·종(알림). URL만 교체(data-rci·CSS 구조 동일). jsdom 검증(11 data-rci→기능아이콘 매핑·leftover 0·문법0). **옅은 선사시대 배경 패턴 추가(2026-07-23, 사용자 "캘린더 배경에 선사시대 배경 조금만, 일정확인 방해 없게")**: `ROOM_CHAR_THEME.a_dino.bg`=힉스 생성 옅은 doodle 패턴(고사리·야자잎·공룡발자국·작은 화산·잎·조약돌 sparse 세이지그린 라인아트, remove_background hf_20260723_003650). `_applyRoomCharDecor`가 ct.bg 있으면 `.rc-decor` 최배면에 `.rc-bg` 타일 레이어 추가(`background-repeat:repeat·size 56%·opacity .07·pointer-events none`) → 캐릭터 워터마크 뒤·날짜 위 가독성 유지. Playwright 스크린샷으로 날짜/요일 위 옅게 깔림+가독성 확인(문법0). **패턴 진하기 상향(2026-07-23, 사용자 "아직 그대로")**: 세이지그린 라인아트가 초록 배경 위 7%라 거의 안 보임 → opacity .07→.16·size 56%→50%. **방삭제·방나가기 아이콘 추가(사용자 "설정 방삭제 아이콘 안 바뀜")**: `data-rci="delroom"`(쓰레기통 hf_005029_856fab43)·`data-rci="leave"`(문+화살표 hf_004937_c36bc55f) 추가—처음엔 이 두 버튼에 data-rci가 없어 제네릭 svg-hide 대상도 아니었음. ⚠️ **주의**: `body.rc-dino .uni-nav[data-rci] .uni-nav-l > svg{display:none}`가 블랭킷이라 data-rci만 붙이고 배경이미지 CSS 규칙을 안 넣으면 아이콘이 빈칸이 됨 → 반드시 data-rci+CSS 규칙 쌍으로 추가. **패턴 재생성(또렷하게)+아이콘 확대(2026-07-23, 사용자 "아직도 안 보여·아이콘 작아")**: 얇은 저대비 패턴이 .16으로도 거의 안 보여 → **볼드 미디엄 세이지그린 doodle 패턴 재생성**(hf_005520_b8f69e6d)·opacity .16→.22. 설정 아이콘 `.uni-nav-l::before` 23px/contain→**26px/background-size:122%**(여백 넘어 확대). ⚠️ 사용자 빌드에 패턴 커밋이 아직 없을 수 있음(재빌드 필요)—탭 아이콘은 보이는데 패턴만 안 보이면 빌드 순서 문제. **CDN→인라인 SVG 패턴으로 전환(2026-07-23, 사용자 "그래도 안 보여")**: 힉스 CDN 패턴은 ①에이전트가 로컬 검증 불가 ②배경제거하면 얇은 선만 남아 흐림 → **`ROOM_CHAR_THEME.a_dino.bgSvg`=인라인 SVG doodle 패턴**(고사리·공룡발자국·화산·잎, #4f9d3a stroke)로 교체. `_applyRoomCharDecor`가 bgSvg면 `data:image/svg+xml,`+encodeURIComponent로 data-uri 생성(bg URL보다 우선). CDN 무의존→**PWA 즉시 반영**+로컬 스크린샷 검증 가능. `.rc-bg` opacity .24·타일 178px(가시성 확보하되 subtle). Playwright 스크린샷으로 패턴 또렷+날짜 가독성 확인. **→ 자연스러운 풀 씬 배경으로 재전환(2026-07-23, 사용자 "허접한 패턴 말고 캐릭터·카드배경처럼 자연스럽게")**: 타일 doodle이 허접하다는 피드백 → **힉스 세로형(9:16) 옅은 선사시대 풍경 씬**(파스텔 세이지·크림, 하단 언덕·고사리·야자수·먼 화산, 상단 옅은 하늘 여백, 저대비, 캐릭터無) 생성(hf_20260723_012945_6721778a, 배경제거 안 함=풀 씬). `ROOM_CHAR_THEME.a_dino.bgScene` 필드+`_applyRoomCharDecor`가 bgScene이면 `.rc-bg.rc-bg-scene`(cover·bottom·no-repeat·opacity .5). ⚠️ CDN 직참조라 에이전트 로컬 검증 불가—구조검증만, 시각확인은 PWA/기기. opacity(.5)는 CSS라 안 보고도 조정 가능. **→ 캐릭터를 씬 안에 통합 + 풍경 위로(2026-07-23, 사용자 "캐릭터도 풍경에 자연스럽게 배치, 너무 하단이라 안 보임 더 높게")**: 씬(캐릭터 없는 풍경)+별도 워터마크가 따로 놀고 풍경이 화면 맨 아래에만 깔려 잘 안 보이던 문제 → **두두 캐릭터를 풍경 한가운데 녹여 그린 통짜 9:16 씬 재생성**(hf_20260723_015928_1b83402b, 언덕·고사리·야자수·화산이 중앙~하단 채우고 캐릭터 중앙 배치, 레퍼런스 3ba06108). `ROOM_CHAR_THEME.a_dino.bgScene` URL 교체 + `charInScene:true` 추가 → `_applyRoomCharDecor`가 `charInScene`(또는 bgScene)면 **별도 `.rc-char` 워터마크·구름 생략**(캐릭터·하늘이 씬에 이미 있음). `.rc-bg-scene` `background-position:bottom center`→`center`(캐릭터가 중앙이라). **위치 미세조정(사용자 "위치 조금만 더")**: 톤 폰은 씬이 세로로 꽉 차(height-cover) background-position Y가 안 먹어서 → `background-size:auto 116%`(살짝 오버스케일해 세로 크롭 여지 생성)+`background-position:center 38%`(캐릭터를 조금 위로). Playwright 문법0. ⚠️ 나머지 12캐릭터는 각자 아이콘 세트+ROOM_CHAR_THEME 항목 추가로 확장 예정. 사용자가 프리미엄 방테마를 명시 선택하면 색은 그것 우선(마스코트는 스킨 기준 유지). jsdom 검증(dino 테마 accent #4f9d3a·rc-on/rc-dino·마스코트 주입·클리어·비매핑방 무장식·문법0). **10동물 색 테마 확장(2026-07-23, 사용자 "다른 테마들도 두두처럼" + 크레딧 충전 예정)**: 크레딧 0으로 가능한 층부터 — `THEMES.a_dog~a_chicken` 10종 파스텔 팔레트(각 동물 대표색: 강아지 탄·고양이 슬레이트·호랑이 주황·토끼 핑크·곰 허니골드·펭귄 하늘·여우 주홍·원숭이 갈색·쥐 모브그레이·닭 노랑) + `ROOM_CHAR_THEME.a_dog~a_chicken`(theme+cls+mascot=기존 `ANIMAL_STICKER['<동물>:like']` 재활용, **생성 0장**). 각 동물 방 입장 시 앱 색+캐릭터 워터마크+아이콘 색틴트+활성탭 캐릭터 자동 적용(두두 방식, bgScene·전용아이콘 없이 워터마크만). **배경 씬(캐릭터당 ~5크레딧)·전용 아이콘 13종(캐릭터당 ~40크레딧)은 충전 후 유료 단계로 얹을 예정**(두두만 현재 씬+아이콘 완비). 힉스 단가=이미지 2·배경제거 1 크레딧. Playwright 문법0·10팔레트+11매핑 일치 검증. **캐릭터별 다른 자세 + 5개 삭제(2026-07-23, 사용자 "코코도 엄지척이네 캐릭터마다 다른 자세로 + 보리·호담·눈꽃·소녀·소년 테마·프로필·이모지 삭제")**: (a) ROOM_CHAR_THEME 워터마크가 전부 `:like`(엄지척)이던 것 → 캐릭터 개성별 다른 감정으로: 코코=love·달이=bashful·꿀곰이=hug·뽀글이=excited·여우비=scheme·몽당이=laugh·콩이=sneaky·꼬꼬=proud(기존 감정 스티커 재활용, 생성 0). (b) **보리(a_dog)·호담(a_tiger)** = 테마(ROOM_CHAR_THEME·THEMES)+프로필(CARD_SKIN_PREM·SEAL_SET_ORDER)+이모지(STORE_EMOJI) 전부 제거 / **소녀(c_girl)·소년(c_boy)·눈꽃(snow)** = 프로필(CARD_SKIN_PREM·SEAL_SET_ORDER)에서 제거. 전부 데이터(CARD_SKINS·SEAL_SETS·ANIMAL_STICKER)는 잔존(레거시 저장값 렌더용). 이모지 배너 '13가지'→'11가지'(8동물+calli+weather+dino). 남은 테마 동물 8종. **무늬형 프로필 나비정원·별똥별 테마 추가(2026-07-23, 사용자 "나비정원 별똥별 씰도 똑같이")**: `THEMES.nabi`(로즈·라벤더 플로럴)·`THEMES.star`(페리윙클 인디고) 팔레트 + `ROOM_CHAR_THEME.nabi/star`(mascot=기존 씰 이미지 재활용, 크레딧 0). 이미 CARD_SKIN_PREM(테마 탭)에 있어 그 방 카드 배경으로 고르면 앱이 그 모티프 색+씰 워터마크로 변신(동물과 동일 방식, card_skin 기준). **개구쟁이(c_gaegu) 테마 추가(2026-07-23)**: `THEMES.c_gaegu`(밝은 코랄)+`ROOM_CHAR_THEME.c_gaegu`(mascot=씰 803e9739 재활용, 크레딧 0).  **나머지 무늬형 테마 추가(2026-07-23)**: moon(슬레이트블루)·daisy(선샤인옐로)·heart(로즈)·koi(주홍)·cherry(사쿠라핑크) THEMES+ROOM_CHAR_THEME(씰 이미지 워터마크 재활용, 크레딧 0). 리본은 삭제됨. 이제 전 무늬형 프로필이 테마 탭에서 앱 색 변신. **뽀글이·하트·벚꽃 삭제(2026-07-23, 예산 절감)**: a_penguin(테마+프로필+이모지)·heart·cherry(테마+프로필) 제거, 이모지 배너 11→10. 풀셋팅 대상 13종 남음. index.html 변경 → 재빌드 필요.
55. 🚧 **[v30] 스토어 '달력 주문' 탭 임시 숨김** — 사용자 요청으로 스토어 탭바의 `달력 주문` 버튼(index.html ~4399)에 `style="display:none"` 추가해 진입점만 가림. `renderCalOrder`·주문 폼(`openCalOrderForm`)·관리자 주문관리·`내 주문 내역`(프로필, 기존 주문자 배송조회용) 코드는 **전부 그대로 유지** — 되돌리려면 그 한 줄의 style만 제거. 스토어 기본탭은 '테마'라 숨겨도 라우팅 문제 없음. **주문 진입점도 숨김(2026-07-24, 사용자 "달력주문 보류라 내 주문내역·배송조회 숨겨")**: 설정 uni-nav `data-rci="orders"` 버튼 + 프로필 `#profileOrdersRow` 둘 다 style display:none. openMyOrders·myOrdersCard 시트 코드는 잔존(복구=display:none 제거). index.html 변경 → 재빌드 필요(PWA는 재빌드 무관). ⚠️ **동물 캐릭터 스토어 이모지 재디자인 진행 중** → 코드 56 참고(힉스필드 3D 클레이, 대표 호랑이 2종 리액션 실사용 테스트 임베드 완료).
54. 🚧 **[v30] 성능 점검 + 렌더/로딩 최적화** — 사용자 "로딩 오래·렉" 보고. **DB는 정상**(events 1MB·방당 최대243, `idx_events_room_date` 인덱스 양호) → 병목은 클라이언트. 핫패스 감사로 상위 5병목 확인. **적용(안전·고효과)**: (1) **음력 변환 캐싱** — `_klc()`가 매 호출 `new KoreanLunarCalendar()` 하던 것 단일 인스턴스 재사용(`_klcInst`), `solarToLunarKR`을 (y-m-d) 메모(`_s2lCache`), `getDayLabel`도 (key|`_holidayVer`) 메모(`_dayLabelCache`, HOLIDAYS 변경 시 `_holidayVer++` 무효화) → 달력 1회 열 때 42셀×4~6렌더=170~250회 음력 재계산을 42회로 축소(순수함수, 결과 동일). (2) **jspdf·html2canvas 지연 로드** — head 블로킹 `<script>`(~200KB) 제거, `ensureHtml2Canvas()`/`ensureJsPDF()`(`_loadScriptOnce`)로 첫 공유/PDF 때만 로드(`_captureCalendarImage`·`coGeneratePDF`에서 await). **미적용(위험·후속)**: loadEvents/loadTodos `select('*')` 무바운드(반복·검색이 전체집합 가정 → date-bound 시 회귀 위험), 위젯 sync immediate(네이티브 신선도 트레이드), renderMonthly innerHTML 통째 교체+강제 reflow(diff 대공사). index.html 변경 → 재빌드 필요. Playwright 문법 검증. **(3) 스토어 이모지·배너 로딩 지연 개선(2026-07-21)**: 이모지 스티커(60장)·히어로 배너가 힉스 CDN 원격 이미지(1024px PNG)라 처음 볼 때 다운로드 지연. `svgEmoji`의 `loading="lazy"`→`eager`+`decoding="async"`, `preloadStoreAssets()` 신설(ANIMAL_STICKER 60개+FRUIT/EMOJI_HERO_IMG를 `new Image()`로 미리 캐시, `openStore`에서 1회 호출 `_storeAssetsPreloaded`). 탭 전환 시 즉시 표시. ⚠️ **근본 해결은 자체 호스팅+리사이즈**(60장을 128px webp 등으로 줄여 앱/전용 CDN에)이나, 힉스 CDN이 프록시 차단이라 에이전트가 다운로드 불가 → 개발자 직접 export 필요.
53. 🚧 **[v30] iOS 위젯 드래그 리사이즈로 종류 전환** — 사용자 요청(아이폰에서 위젯 모서리 드래그 시 종류가 바뀌길). **원인**: iOS18은 홈에서 위젯 오른쪽아래 핸들을 드래그해 크기 변경 지원하는데, 그러려면 **한 위젯이 여러 패밀리를 지원**해야 함. 기존엔 오늘/2주/월/콤보를 **각각 단일 패밀리 별개 위젯**(TodayWidget=small·TwoWeekWidget=medium·MonthWidget/ComboWidget=large)으로 만들어 드래그 리사이즈 대상이 아니었음. **수정**: `AdaptiveCalView`(@Environment(\.widgetFamily) 분기) + `CalendarWidget`(kind 'UriCalendar', `supportedFamilies([.systemSmall,.systemMedium,.systemLarge])`)로 **하나의 위젯이 작게=오늘/중간=2주/크게=월**을 렌더 → iOS18 드래그로 크기 바꾸면 종류 자동 전환. 콤보는 별도 위젯 유지(large, 큰 슬롯 하나뿐이라 월과 공존 불가). 기존 4개 위젯 struct(Today/TwoWeek/Month) 제거, 번들=CalendarWidget+ComboWidget. ⚠️ 기존 old kind(UriToday 등) 추가해둔 유저는 업데이트 후 비게 됨(거의 미배포라 무방). ⚠️ **Android 파리티(onAppWidgetOptionsChanged로 리사이즈시 종류전환)는 후속**. 브레이스 균형 확인(오프라인). widget 변경 → 재빌드 필요.
52. 🚧 **[v30] 갤럭시 위젯 리사이즈 확장(드래그로 크게)** — 사용자 요청("화면 꽉차는 위젯 + 오른쪽 아래 드래그로 변경"). Android 위젯은 이미 `resizeMode="horizontal|vertical"`이라 드래그 리사이즈 자체는 됐으나, `maxResize`가 없어 런처가 작게 캡함 → info XML 4종에 `android:maxResizeWidth/Height` 추가(월·콤보 640×760dp, 2주 640×520dp, 오늘 420×420dp)로 **홈 화면 대부분을 채우도록 크게 늘릴 수 있게** 함(그리드 제약상 완전 전체화면은 불가). ⚠️ **iOS는 OS상 위젯 크기 고정**(추가 시 Small/Medium/Large 선택, 배치 후 리사이즈 불가)이라 해당 없음 — iPhone 최대는 Large(4×4). '전체화면 위젯 한 개 더'는 별도 타입 대신 기존 large 위젯을 크게 리사이즈하는 것으로 대체. widget 변경 → android-aab 재빌드 필요.
51. 🚧 **[v30] 위젯 보내기(⤴) 버튼 제거** — 사용자 요청으로 위젯 헤더의 보내기(월 일정 공유) 버튼을 iOS+Android 모두에서 삭제. iOS: 그리드헤더/오늘 위젯의 `://share` Link 2개 제거(새로고침만 남김). Android: `wireGridHeader`의 `wg_add` 배선 + `TodayWidgetProvider`의 `tw_add` 배선 제거, 레이아웃 4종에서 `wg_add`/`tw_add` TextView 삭제. `openScheme` 헬퍼·`://share` appUrlOpen 핸들러는 미사용으로 잔존(무해, 추후 재사용 가능). stub 컴파일 OK(22클래스)·XML 유효. ⚠️ **이 이후 변경분은 build 30(다음 빌드) 대상.** widget 변경 → 재빌드 필요.
47. ✅ **위젯 ＋버튼 → 보내기(월 일정 공유) 버튼** — 위젯의 빠른추가(＋)를 **보내기 버튼**으로 교체(사용자 요청, iOS+Android 동시). 탭하면 앱을 열어 **그 방의 이 달 일정 이미지 공유**(#46 `shareMonthImage`와 동일 흐름). **딥링크** `com.lsung.uricalendar://share?room=<id>` 신설 — `appUrlOpen`(index.html ~7593)에서 방 진입(다르면 `enterRoom`)→`calTabSwitch('cal')`(html2canvas가 보이는 캘린더 화면 캡처하도록 보장)→렌더 대기 후 `shareMonthImage()`. **iOS**(`widget/ios/UriCalendarWidget.swift`): 그리드헤더 원형 ＋(`plus`)→`square.and.arrow.up`, 오늘 위젯 `plus.circle.fill`→`square.and.arrow.up.circle.fill`, 둘 다 Link destination을 `://add`→`://share`. **Android**(`widget/android`): 새 헬퍼 `WidgetCommon.openScheme(c,rc,uri)` — **MainActivity 명시 대상 ACTION_VIEW 인텐트**(매니페스트 intent-filter 없이도 Capacitor BridgeActivity가 `intent.getData()`→appUrlOpen 발화, 기존 `openApp`의 ACTION_MAIN+extra는 JS에 전달 안 돼 무동작이었음). `wireGridHeader`(월·2주·콤보 `wg_add`)+`TodayWidgetProvider`(`tw_add`)가 `openScheme(...,"com.lsung.uricalendar://share?room="+roomId)` 사용. 레이아웃 4종 글리프 `＋`→`⤴`(보내기, textSize 15~16sp). ⚠️ id는 `wg_add`/`tw_add` 그대로(용도만 변경). stub 컴파일 OK(Intent 스텁에 ACTION_VIEW/CATEGORY_DEFAULT/FLAG_ACTIVITY_* 추가), 레이아웃 XML 유효성 확인. **재빌드 필요**(iOS 위젯+index.html+Android 위젯 전부). appstore `claude/ios-widget`에도 동기.
48. ✅ **함께 일정 다른 방으로 복사(멤버 매핑)** — 기존엔 `copyEventToRooms`가 함께 일정(`shared_id`)을 "복사할 수 없어요"로 막았음 → 허용. **규칙(사용자 요청)**: 대상 방마다 원본 함께 멤버 중 **그 방에도 있는 '같은 사람'(실제 회원 user_id 일치)만** 유지 → **2명 이상이면 함께 유지**(방마다 새 `shared_id`, 기간은 날짜 걸쳐 동일 shared_id로 연속 바), **1명 이하면 단독 전환**(단독 기간은 새 `range_group_id`). 원래 함께가 아니던 사람은 애초에 대상 아님(자동 제외). **가상(프로필) 멤버는 방 로컬이라 매핑 불가 → 제외**(실제 회원만 캐리). 구현: `canCopy=canEdit`(dp 복사 버튼 게이트에서 shared 제외 삭제), `copyEventToRooms`의 shared 차단 블록 제거, `_doCopyEvent`가 `ev.shared_id`면 신설 `_doCopySharedEvent(ev,rooms)`로 분기. 대상 방 실제 멤버는 `members`(room_id,user_id,is_virtual) 조회로 판정. 함께-소스 복사는 방마다 독립(multi_group_id 미연결 — 방별 멤버 집합이 달라 sync 부적합). 토스트로 함께/단독 전환 개수 안내. node 10종 검증(멤버 교집합·D제외·가상제외·기간 shared_id 연속·단독 range_group_id·여러날 is_multi·복사자 비멤버). index.html만 변경 → 재빌드 필요. ⚠️ **단독 일정 복사 경로(`_doCopyEvent` 기존 로직)는 그대로**(multi_group_id로 방 간 sync 유지).
49. 🚧 **관리자 푸시 발송(공지·이벤트) + 갤럭시 알림 진단** — 사용자 요청("임의로 알림 보내는 기능 + 테스트 겸용"). **신규 Edge Function `admin-broadcast`**(Supabase 배포됨, verify_jwt:true, 관리자 `ADMIN_ID`만): 대상 `me`(본인)·`all`(전체 실제 멤버)·`{room_id}`·`{user_ids}`에게 FCM+WebPush 동시 발송. send-push와 동일 FCM v1 경로(uri_high 채널·high priority). **응답에 진단 정보** 반환: `saPresent`(FCM_SERVICE_ACCOUNT 시크릿 설정 여부 — false면 안드로이드 미발송 근본원인)·`fcmSent/fcmTotal`·`webSent/webTotal`·`sampleErrors`(실패 샘플 5). **admin.html '공지' 탭 상단에 '푸시 알림 보내기' 카드**(`renderAnnounce` 내, `#pushTitle/#pushBody/#pushUrl/#pushTarget`+`sendAdminPush()` → `supa.functions.invoke('admin-broadcast')`) — 결과에 도달 수치·시크릿 경고·성공 시 "그래도 안 뜨면 갤럭시 배터리최적화/절전(앱 잠자기) 확인" 안내. 소스 백업 `supabase/functions/admin-broadcast/index.ts`(양 repo). **진단 결과(2026-07-20)**: 커밋된 admin SDK 서비스계정(`uri-calendar-firebase-adminsdk-*.json`, project `uri-calendar`)으로 실제 토큰에 validate_only FCM 전송 → **200 정상 수락**(SA·토큰·프로젝트·payload 전부 유효). fcm_tokens 22개(안드로이드)·최신 오늘, last_used_at 배치 클러스터(5개 동시 등)로 **서버→FCM 전송은 동작 중**으로 판단. 남은 원인 후보=①Supabase 시크릿 미설정(툴의 saPresent로 즉시 판별) ②갤럭시 기기측 절전/알림권한. admin.html은 정적파일이라 머지 즉시 라이브(재빌드 불필요). Edge Function은 이미 배포됨.
50. 🚧 **갤럭시 위젯 미표시 검토 + 피커 개선** — 사용자 보고("갤럭시 위젯 안 뜬다"). **정적 전수 검토 결과 위젯 코드/설정은 정상**: Java stub 컴파일 OK(22클래스)·resId 참조 30개 전부 layout id와 매칭·layout/xml/drawable 참조 전부 해소·info XML 유효·inject_android_widget.sh 매니페스트 주입 유효(receiver 4+service 3)·codemagic android-aab에 inject 스텝 존재·package.json 위젯 의존성(@capacitor/preferences·widgetsbridge 등) 전부 존재. ⚠️ **`android:exported="false"`는 정상**(공식 문서 확인 — 시스템이 APPWIDGET_UPDATE를 특별 전달, 위젯 receiver는 false 권장). 즉 정적 버그 없음 → 원인 후보=①위젯 포함 android-aab가 아직 Play 미출시(설치본이 위젯 이전 빌드) ②갤럭시 기기 런타임(실기기 logcat 필요, 갤럭시 없어 검증 불가). **피커 개선(저위험)**: info XML 4종에 `android:previewLayout` 추가(삼성 피커 미리보기) → ⚠️ **철회**(아래): previewLayout이 GridView(컬렉션)를 포함해 삼성이 미리보기 생성 실패→'위젯을 추가할 수 없습니다'로 추가 자체가 막힘. **previewLayout 전부 제거**(2026-07-20 실기기 확인). label은 유지, inject 스크립트 receiver 4종에 `android:label` 추가("우리 캘린더 · 월/2주/오늘/콤보" — 원래 라벨 없어 4개가 다 "우리 캘린더"로 떠 구분 안 됐음). 매니페스트 주입 시뮬레이션으로 XML 유효 확인. widget/android 변경 → **android-aab 재빌드 필요**(이 빌드가 실제 Play 출시돼야 갤럭시에 위젯 등장). ✅ **알림(FCM)은 사용자가 도달 확인함**(admin-broadcast 도구로 테스트 → 정상 수신). **🚨 갤럭시 위젯 빈 화면 근본 원인 발견·수정(2026-07-22)**: 실기기에서 위젯이 피커엔 뜨는데 홈에 놓으면 투명/"위젯을 추가할 수 없습니다" → 원인=레이아웃 4종에 순수 `<View>` 스페이서/구분선 16개. **RemoteViews는 @RemoteView 허용 목록 뷰만 인플레이트 가능하고 순수 View는 목록에 없어** 레이아웃 인플레이트가 통째로 실패(위젯 4종 전부). 수정=`<View ` → `<FrameLayout `(전부 self-closing이라 태그명 치환만으로 안전, XML 유효성 검증). ⚠️ 이전에 시도한 resId 원본 패키지 폴백(getIdentifier 이중 조회)은 무해하나 **원인 아니었음**(aapt가 리소스 테이블을 applicationId로 리네임하므로 원래 조회도 정상) — 잔존 유지. 스텁 컴파일 하네스 없이 XML 검증만(레이아웃-only 변경, Java 무변경). appstore 두 브랜치+이 repo 동기 완료. **android-aab 재빌드 필요.** **🛡️ 빈 화면 방어 폴백 추가(2026-07-23, 사용자 "아직 홈에 안 뜸/빈 화면")**: 재차 정적 전수 감사(레이아웃 허용뷰만·월레이아웃 id 전수 일치 wg_av0~3 포함·resId 네임스페이스 폴백·매니페스트·codemagic inject+bundleRelease)에서 **결정적 코드 버그 없음** → 원인=①빌드/출시 지연 ②기기 런타임. 방어책 `WidgetCommon.fallbackRV`(widget_fallback.xml=FrameLayout+TextView '탭하여 열기'+열기 인텐트) 신설, 4 프로바이더 updateAll을 try/catch로 감싸 예외 시 빈 화면 대신 폴백 렌더. stub 22클래스 컴파일 OK·XML 유효. ⚠️ 폴백은 런타임 예외만 커버(피커 미표시/빌드 미포함은 별개=재빌드+Play 출시).
59. ✅ **관리자 플랜 체험 토글 고정(유지)** (2026-07-22) — 설정→관리자 전용의 유료/무료 플랜 체험 토글(`_adminPlanOverride`)이 메모리 변수뿐이라 앱을 나갔다 오면(재시작) 무료로 초기화되던 문제. `localStorage['uricalv2.adminPlanOverride']`('1'/'0')에 저장하고 선언부에서 복원, `toggleAdminPlan`을 상태전환+저장 / `_applyAdminPlanUI`(버튼 클래스·라벨·스위치·광고 반영, 시작 시에도 호출)로 분리. 광고는 `showAdBannerIfNeeded`가 `isEffectivePremium()`을 이미 참조하므로 복원만으로 자동 반영. 관리자 외 유저는 `isEffectivePremium`이 override를 안 읽어 영향 0. Playwright 4종 검증(기본 OFF·토글 ON+저장·리로드 유지·OFF 리로드 유지). index.html 변경 → 재빌드 필요.
60. ✅ **'이 방 테마' → 방 프로필 카드 배경으로 통합(색 섞임 해소)** (2026-07-23, 사용자 "설정 이방테마를 방프로필과 통합, 지금 둘 다 적용하면 색이 섞임") — 설정의 '이 방 테마'(6색 스와치, `user_room_themes`)와 방 프로필 카드 배경(`members.card_skin`)이 별개라 둘 다 걸면 앱 색(테마)과 카드 색이 따로 놀던 문제. **카드 배경이 테마를 결정하도록 통합**. (a) **기본 배경 = 테마 6종**: `CARD_SKINS.t_vintage/t_lavender/t_peach/t_mint/t_rose/t_sky`(각 `{free:true,theme:'<key>'}`) 신설, `CARD_SKIN_FREE`를 옛 파스텔 7종(butter·blush·mint·sky·lavender·peach·sage)→이 6종으로 교체(옛 7종 데이터는 레거시 저장값 렌더용 잔존). CSS `.rcs-t_*` 각 테마 hue 파스텔 그라데이션. (b) **카드 스킨→테마 매핑** `_cardSkinTheme(skin)`(캐릭터=ROOM_CHAR_THEME.theme, 기본배경=CARD_SKINS[k].theme). `applyRoomThemeFor`가 카드 스킨 테마를 **저장된 방 테마보다 우선** 적용 → 카드 배경 고르면 앱 전체 색이 그 테마로 통일(색 섞임 원천 차단). 카드 배경 없으면 기존 `user_room_themes`/개인 `_myTheme` 폴백(하위호환). (c) **라이브 미리보기**: `pickCardSkin`이 현재 들어온 방 편집 중이면 선택 즉시 `applyTheme`로 앱 색 미리보기, `closeRoomMascot`가 `applyRoomThemeFor`로 저장값 기준 확정/복원(저장=새 테마, 취소=기존). `_rmBaseTheme`(편집 시작 테마) 캡처. (d) **설정 '이 방 테마' 섹션 제거**: `#uniRoomThemeRow`/`#uniRoomThemeGrid` 마크업 삭제. `renderRoomThemeGrid`·`setThisRoomTheme`·`user_room_themes`는 잔존(가드로 no-op, 폴백 렌더용). 스토어 '테마' 탭(개인 기본 테마)은 그대로(별개 스코프, 카드 배경이 항상 우선이라 무충돌). Playwright 검증(테마행 제거·6종 그라데이션 렌더·문법0). **UI 용어 '씰'→'프로필' 변경(2026-07-23, 사용자 요청)**: 사용자 노출 텍스트의 '씰'을 전부 '프로필'로 교체(방 프로필 카테고리 칩·스토어 탭·프리미엄 기능명·필드 라벨·미리보기 서브·스토어 히어로/섹션 타이틀·토스트·온보딩; 영문 eyebrow 'WAX SEAL'→'PROFILE', 'PREMIUM SEAL'→'PREMIUM PROFILE'). ⚠️ **코드 식별자·클래스·data-속성·주석은 그대로**(SEAL_SETS·_sealInner·.seal-set·data-rmcat="seal"·data-tab="seal" 등 미변경 — 내부 키). **'기본'+'빈티지' 통합(2026-07-23, 사용자 "기본이랑 빈티지 따로 있는대 기본으로 합쳐")**: 카드 배경 기본 탭에 있던 빈 '기본'(none, card_skin='') 버튼 제거 + `t_vintage.name` '빈티지'→'기본'(빈티지=앱 기본 테마라 사실상 중복). 이제 '기본' 버튼 하나=빈티지 테마. 미선택(null)도 '기본' 선택 표시(`k==='t_vintage'&&!_cardSkin`). 완전 초기화는 '기본(첫 글자)으로 되돌리기'(clearRoomMascot)가 담당. **UI 라벨 '카드 배경'→'테마'(2026-07-23, 사용자 "카드배경을 테마로 변경")**: 카드 배경이 앱 테마를 결정하므로 방 프로필 카테고리 칩 '카드 배경'→'테마', 필드 라벨 '기본 배경'→'기본 테마'·'컨셉 배경'→'컨셉 테마', 모달 '컨셉 카드 배경'→'컨셉 테마'. `data-rmcat="cardbg"` 등 코드 키는 그대로. **방편집·스토어 프로필+테마 통합(2026-07-23, 사용자 "방편집 프로필·테마 통합, 스토어도 통합, 무료 표시X 프리미엄만, 레이아웃 이모지처럼")**: (a) 방편집 [프로필][테마] 카테고리 칩(#rmCats) 제거. [기본][프리미엄] 세그먼트 유지 — 기본=색+문양 커스텀 프로필 + 기본 테마 6색(무료 유지), 프리미엄=통합 세트 이모지식 행(`.ep-row`·`.rm-uset`). `pickUnifiedSet`이 `_sealSet=_cardSkin=key`로 씰+카드+앱테마 동시 적용. (b) 스토어 프로필(씰) 탭 삭제, 테마 탭 하나로 병합. `renderStoreThemes`=프리미엄 세트만 이모지식 행(무료 6색 카드 제거). (c) `_setDisplayName`(CARD_SKINS/SEAL_SETS name), pickCardSkin/`_rmPickCustom`에 무료↔프리미엄 전환 정리(무료 선택 시 _sealSet·프리미엄 _cardSkin 해제). `saveRoomMascot`은 그대로 seal+card_skin 저장(통합=`set:key`+`key`). 옛 pickSealSet·renderStoreSeal·setRmCat·STORE_THEMES는 잔존(미사용). Playwright 문법0·구조 검증. index.html 변경 → 재빌드 필요. **기본 테마 5종 채도 상향(2026-07-23, 사용자 "피치·민트 등 너무 밝다, 채도 조금만 올려")**: 파스텔이 너무 옅다는 평 → lavender·peach·mint·rose·sky의 `THEMES` `bg`/`aP` + CSS `.rcs-t_*` 그라데이션 3스톱을 HSL 채도 ×1.35(배경은 근백색이라 ×1.55)+명도 살짝↓로 재계산해 화사하게(vintage=기본 따뜻한색이라 유지). 밝기/텍스트 대비는 그대로(어둡게 아님). Playwright 로드검증(문법0·peach --bg=#fff2e6).
61. ✅ **기간 일정 바 칸 경계 이음새(끊겨 보임) 방지 + PWA 캐시 갱신** (2026-07-23, 갤럭시 사용자 제보 "근속휴가 4·5일이 연결 안 되고 끊어져 보임") — (a) **현재 코드 검증**: 실 DB 데이터(근속휴가 rg_9b7b… 8/3~8/11 단독 기간, 8/2~8/4 함께 '유럽 여행'과 겹침)로 실제 `renderMonthly` 렌더해 측정 → 기간 바가 같은 레인(top 동일)에 인접 셀 우변=좌변으로 **정확히 맞닿아 렌더됨**(현재 코드엔 끊김/계단 버그 없음). 제보자는 옛 빌드(실배포 build 28=7/8) 또는 갱신 전 PWA로 추정. (b) **방어적 이음새 제거**: 그래도 삼성 웹뷰 등 특정 DPR에서 두 채움이 소수 픽셀 경계에서 만나면 안티에일리어싱 실틈이 보일 수 있어, `.cal-evbar.ev-l-join`/`.ev-r-join` margin을 0→**-1px**로(연결 변만, 같은 기간 바끼리 1px 겹침 → 이음새 원천 차단). 같은 색·직각변이라 겹침 안 보임. 주 경계(일/토)는 `ev-l-open`/`ev-r-open`이라 미적용(둥근 wrap 유지). 그리드 바깥열(일·토)은 항상 open이라 `overflow-x:hidden` 클리핑 무관. Playwright 실렌더로 겹침 2px·top 동일 검증. (c) **PWA 캐시**: `sw.js` v9→**v10**, HTML 문서 요청은 `cache:'reload'`로 항상 네트워크 우선(오프라인만 캐시 셸 폴백) → 스토어·테마·이 수정 등 코드 변경이 새로고침 즉시 반영. index.html+sw.js 변경 → 재빌드/재배포 필요(PWA는 sw 갱신 후 새로고침).
62. 🚧 **갤럭시 위젯 실기기 피드백 3종(2026-07-24, build 33 설치 후)** — 위젯이 홈에 정상 표시됨(코드 50 FrameLayout fix 효과). 후속 수정: (a) **오늘 날짜 표시 없음(iOS엔 있음)** → `GridWidgetFactory`가 오늘을 텍스트색(#8B3A2A)만 바꿔 티가 안 났음 → **채운 원 배경(`cell_today_bg.xml` 오val+padding)+흰 숫자**로 iOS 파리티(월·2주 공통). (b) **2주 위젯 하단 여백 과다** → GridView가 2행만 렌더하고 셀(44dp)이 짧아 아래가 비어 보임. 2주 전용 셀 레이아웃 `widget_cell_tw.xml`(minHeight 44→60dp)을 factory가 `kind==TWOWEEK`일 때 사용해 2행이 위젯 높이를 더 채우게. ⚠️ RemoteViews GridView는 행을 높이에 맞춰 stretch 못 함(안드로이드 제약) → 완전 제거 아니라 완화, 너무 크게 리사이즈하면 여백 남음(정적 weighted 2행 레이아웃 전환이 근본해결이나 리스크로 보류). (c) **앱 내 광고배너↔하단탭 겹침** → Android `ADAPTIVE_BANNER`가 기본 `--ad-h`(56px)보다 높을 수 있는데 `bannerAdSizeChanged` 미측정 시 탭바(`--ad-h+10px`)가 배너 안으로 들어감 → 기본 `--ad-h` 56→62px, 탭바 여백 +10→+16px, 측정값 클램프(>200이면 device px로 보고 dpr 나눔, [52,160] 클램프). ⚠️ 네이티브 광고라 로컬 재현 불가 — 실기기 검증 필요(설정 푸터 `광고:<_admobDebug>`+`--ad-h`로 실제값 확인). stub 컴파일 OK(22클래스). widget/android+index.html 변경 → android-aab 재빌드 필요. **위젯 하단 여백 → 적응형 셀 높이+표시 줄 수(2026-07-24, 사용자 "여백만큼 +N 말고 일정 더 보여줘")**: RemoteViews GridView가 행을 stretch 못 해 월·2주 위젯 하단이 비던 문제 근본 수정. (1) 프로바이더(`UriCalendarWidgetProvider`·`TwoWeekWidgetProvider`)에 `onAppWidgetOptionsChanged`+build 시 `OPTION_APPWIDGET_MAX_HEIGHT`(dp) 읽어 `WidgetCommon.setGridHeightDp(kind)` 저장. (2) `GridWidgetFactory`가 저장된 높이÷행수로 **셀 최소높이(px) 산정→`setInt(cell_root,"setMinimumHeight")`로 각 셀을 키워 GridView 행이 위젯을 꽉 채움**(하단 여백 제거). (3) 남는 셀 높이만큼 **표시 줄 수(laneCap 2~5) 동적 산정** → `cellBars(...,laneCap,...)`로 `+N` 대신 실제 일정 더 노출(`buildRuns`는 레인 무제한 배정, `cellBars`가 maxLanes로 필터라 그대로 지원). 셀 레이아웃 `widget_cell.xml`에 `cell_ev3~5` 추가(최대 5줄), 옛 `widget_cell_tw.xml` 삭제(적응형이 대체). 높이 미확보(0) 시 laneCap=2·min높이 미적용=기존 동작(graceful). headerDp=56 보수 추정으로 오버슈트(스크롤) 대신 소폭 여백 쪽으로 바이어스. stub 컴파일 22클래스 OK(스텁에 Bundle·getAppWidgetOptions·OPTION_APPWIDGET_MAX_HEIGHT·onAppWidgetOptionsChanged 추가). ⚠️ 실기기 검증 0(갤럭시 없음) — 로직·컴파일만. iOS는 SwiftUI라 무관(이미 꽉 참). **최소 크기 하한 + 칩 여백/둥글기(2026-07-24, 사용자 "많이 줄이면 마지막주 사라짐 + 일정이 기간처럼 붙어보임")**: (1) info XML `minResizeHeight` 월 110→**250dp**·2주 90→**130dp**(6주 다 보이는 하한), factory 셀 최소높이 clamp 44→30dp+`gridDp<rows*44` 강제 제거(작은 위젯도 전주 표시, 오버플로 스크롤 방지). (2) **단일 일정 = 양끝 1.5dp 여백 + 3dp 둥근 칩**(`ev_chip_single`), 기간 시작/끝은 바깥쪽만 둥글게(`ev_chip_start`/`_end`), 기간 중간은 flat(연결 유지) — 주 경계(일 dow0·토 dow6)도 끝으로 처리. 색은 `setColorStateList(setBackgroundTintList)` **API 31+ 전용**(inset+corners drawable 틴트), minSdk 24라 **API<31은 기존 flat 색 폴백**(무회귀). 스텁에 Build.VERSION·ColorStateList·setColorStateList 추가, 컴파일 22클래스 OK. **재조정(2026-07-24, 사용자 "iOS도 여백 거의 없음 — 여백 기존대로 두고 둥글기만"+"27일 바가 26일보다 큼")**: 칩 드로어블 inset(양끝 1.5dp 여백) 제거 → 꽉 찬 폭 유지+모서리 4dp 둥글기만(iOS 파리티). 이벤트 바 높이 편차 방지 위해 cell_ev1~5에 minHeight 13dp+gravity center_vertical+includeFontPadding false → 모든 일정바 동일 높이. **여백 재추가(2026-07-24, 사용자 iOS 스샷 "여백 다 없애진 말고 저정도 실여백은")**: inset 0→양끝 2dp 얇게 복원(iOS 실여백 수준)+4dp 둥글기 유지. 기간 시작/끝만 바깥 여백, 중간 flat 연결. **iOS 완전 파리티(2026-07-24, 사용자 "갤럭시·iOS 육안 크기 동일하게")**: iOS Swift 확인 결과 iOS는 칸 spacing 0+칩이 칸 꽉 채움, 보이는 틈은 실제 마진이 아니라 3pt 둥근 모서리가 만든 착시. → 안드로이드도 동일하게 inset 제거(0)+모서리 3dp(iOS 3pt)로, 월 그리드 horizontalSpacing 0dp 확인. 단일=꽉 찬 칩+3dp 둥글기(둥근 모서리로 옆칩과 자연 분리), 기간=이어지는 변 각지게 연속. **양쪽 1.5dp 여백 통일(2026-07-24, 사용자 요청)**: 안드로이드 칩 inset 1.5dp+iOS barView에 `.padding(.leading/trailing, roundL/R ? 1.5 : 0)` 추가(둥근 끝쪽만, 기간 중간 0=연속) → 갤럭시·iOS 육안 여백 동일. iOS는 오프라인이라 브레이스/괄호 균형만 검증(0/0). **→ 1dp로 축소(2026-07-25, 사용자 요청)**: 칩 여백을 1.5→1로 줄임. 안드로이드 `ev_chip_single/start/end.xml` inset 1.5dp→1dp, iOS 755줄 `.padding(roundL/R ? 1 : 0)`+마커 leading `roundL ? 3 : 1`. 기간 중간 0 유지. XML 유효·brace 균형(317/317, 1033/1033) 확인.
63. ✅ **프리미엄 만료 시 테마 기본 복귀 / 컬러·이모지는 유지+신규차단** (2026-07-24, 사용자 요청) — 프리미엄 테마(캐릭터·무늬·컨셉 카드배경) 쓰다 **만료되면 자동 기본 테마 복귀**. 헬퍼: `_isPremiumSkin`(무료=CARD_SKIN_FREE 6종+`free:true` 레거시, 그 외 프리미엄)·`_isFreeTheme`(vintage/lavender/peach/mint/rose/sky)·`_premOn`·`_effSkin`(만료 시 프리미엄 스킨→null). `applyRoomThemeFor`가 만료면 프리미엄 스킨 미적용+저장 방테마/개인테마가 프리미엄이어도 vintage로 강제+데코(ct) 숨김. `renderRooms` 카드배경도 `_effSkin` 게이트. ⚠️ 저장값(members.card_skin) 안 지움→재구독 시 자동 복원. **컬러·이모지 무변경**: 저장값은 렌더 유지·신규만 `getUnlockedColorPacks`/`getUnlockedCharPacks`가 `[]`로 차단(사용자 요청과 이미 일치). Playwright 검증(만료→dino방 vintage·데코off·#8b3a2a / 라벤더 유지 / 프리미엄→dino #4f9d3a·데코on). index.html 변경→재빌드.
**위젯 함께 일정 왼쪽 띠 표시(2026-07-24, 사용자 요청)**: 앱 `.cal-evbar-marker`처럼 위젯 기간/단일 바 시작 칸 왼쪽에 얇은 띠. payload events에 `shared`(=shared_id 유무) 추가, iOS WGEvent/EvRun/DayBar에 shared 스레딩 + `barView`가 `shared&&!contLeft`면 대비색 RoundedRectangle(2.5pt) 오버레이. Android는 RemoteViews 제약상 TextView에 내부 스트립 못 얹어 시작 칸 텍스트 앞에 `▎`(대비색 글자) prepend로 띠 표현. 컴파일 22클래스/문법0. 재빌드 필요.
**두두 테마 상단 버튼(검색·카톡보내기·가져오기) 아이콘 적용(2026-07-25, 사용자 "상단 버튼도 테마 적용 + 피그마 초안→힉스 마무리로 크레딧 절약")**: 두두(dino) 테마는 하단 탭바 4·설정 11 아이콘만 캐릭터화돼 있었고 **캘린더 월줄 상단 버튼(검색 `.cal-search-btn`·공유 `#calShareBtn`·가져오기 `#calImportBtn`)은 미적용**이었음. **피그마로 초안 아이콘 3종 제작**(파일 key `9yAz5d1dIbatwEu8lYXfvI`, 팀 `니치 폼 빌더` team::1648241131928014235): 두두 초록 팔레트(accent #4f9d3a·pale #d9efc9·light #77c05e·deep #3f7f2e)의 **컬러 필드 아이콘** — 돋보기(유리+하이라이트+반짝이)·종이비행기(2톤+모션점)·트레이+내림화살표(+잎 악센트). `createNodeFromSvg`로 피그마에 배치. **→ 최종: 힉스 크레용 PNG로 마무리(2026-07-25, 환불 완료 후 힉스 재개)**: 피그마 벡터가 "색만 바뀐 밋밋한 벡터"라 기존 크레용 아이콘과 화풍 불일치 지적 → **기존 두두 달력 아이콘(hf_20260723_002823, media 779885df)을 레퍼런스로 물려 nano_banana_pro로 크레용 화풍 3종 생성**(굵은 삐뚤 검정 외곽선+크레용 질감): 돋보기(job 7c5a6cc5→bg제거 376ae2a8)·종이비행기(5a3e9813→78d5fee1)·트레이+내림화살표(976af858→a3425333). remove_background 투명 PNG URL=hf_20260724_2316*. 3버튼에 `data-rci="search"/"cshare"/"cimport"` 추가 + CSS `body.rc-dino .cal-search-btn[data-rci]/.cal-import-btn[data-rci]`가 내부 `<svg>`를 `visibility:hidden`(레이아웃 박스 유지)하고 `background-image`(크레용 PNG, size 25px) 표시(탭/설정 아이콘과 동일 패턴). ⚠️ 힉스 CDN 직참조(만료 시 깨짐 — 탭/설정 아이콘과 동일 이슈). ⚠️ 이 패턴(상단 버튼 data-rci)을 **나머지 테마 확장 시 재사용** — 테마별 색/화풍 아이콘을 같은 3키에 넣으면 됨. ⚠️ 에이전트 프록시가 힉스 CDN 차단이라 로컬 시각검증 불가 → 사용자 승인 후 적용(구조검증: rc-dino에서 3버튼 background-image=cloudfront·svg hidden·size 25px). index.html 변경 → 재빌드 필요(PWA 무관).
**테마별 아이콘 확장 착수 — 코코(고양이) 탭바 파일럿(2026-07-25, 사용자 "다른 테마도 적용, 테마마다 특유 컨셉, 돌려막기 X, 피그마 초안→힉스 마무리로 크레딧 절약")**: 두두(dino) 외 나머지 13종 테마도 두두처럼 풀세트(상단3+탭4+설정11+배경씬)로 꾸미는 작업 착수. **테마별 컨셉 확정**(각 캐릭터 화풍+색+고유 모티프 다르게): 코코(고양이)=플랫벡터 슬레이트+발바닥·털실 / 달이(토끼)=수채 핑크+당근 / 꿀곰이=구아슈 골드+꿀단지 / 여우비=모던카툰 주홍+꼬리 / 몽당이=크레파스 브라운+바나나 / 콩이(쥐)=색연필 모브+치즈 / 꼬꼬(닭)=카툰 옐로+달걀 / 나비정원=플로럴 로즈 / 별똥별=셀레셜 인디고 / 개구쟁이=크레파스 코랄 / 달=밤 블루 / 데이지=선샤인 옐로 / 잉어=수묵 주홍+물결. **파일럿=코코 탭바 4아이콘**: 사용자 피드백 "기능 틀에 박히지 말고 자유분방하게(홈=고양이얼굴·캘린더=발바닥·설정=생선뼈), 크게 할 거 없으면 기존 모양도 OK" → **방목록=고양이 얼굴·캘린더=발바닥·우리할일=클립보드(기능 유지)·설정=생선뼈**. 피그마(파일 key `9yAz5d1dIbatwEu8lYXfvI`)로 플랫벡터 초안 제작→SVG data-uri 인라인, `body.rc-a_cat .cal-tab[data-rci=…]`가 두두 탭 패턴과 동일하게 svg 숨기고 background-image(size 120%) 표시. ⚠️ **아이콘=기능literal 고집 X, 테마 캐릭터 오브젝트로 자유롭게**(라벨이 기능 설명하므로) — 나머지 테마도 이 원칙(잉어=물고기/물결, 별=유성 등). **→ 코코 풀세트 완성(2026-07-25, 사용자 "캣타워·방석도 있고" + "코코 풀세트 완성")**: 방목록=고양이얼굴 **→ 캣타워로 교체**(층=방 메타포). 상단3=검색(**고양이 눈**)·공유(**날치=날아가는 물고기**)·가져오기(사료그릇+화살표). ※검색/공유는 뻔한 돋보기·비행기 대신 코코 오브젝트로 재작업(2026-07-25). 설정목록8=프리미엄(왕관 gold)·스토어(쇼핑백+발바닥)·고급설정(**털실+뜨개바늘**=만지작, 두두 슬라이더와 차별화)·알림(**방울 gold**)·공지(확성기)·고객센터(**실제 코코 캐릭터 얼굴**, 사용자 요청—생성 얼굴 대신 코코 마스코트 PNG)·방삭제(쓰레기통 red)·방나가기(문+화살표). 전부 슬레이트 `#8496ac`+코랄 `#ef9d84` 팔레트 통일, SVG data-uri 인라인. `body.rc-a_cat`에 두두와 동일 패턴(탭 `.cal-tab[data-rci]`·상단 `.cal-search-btn/.cal-import-btn[data-rci]`·설정 `.uni-nav[data-rci] .uni-nav-l::before`). 아이콘 소스=스크래치 `/tmp/cat_icons.js`(15개). Playwright 검증(탭4+상단3+설정8 전부 data-uri 적용·문법0). ⚠️ orders(주문내역)는 display:none이라 아이콘 생략. **→ 힉스 질감 마무리 완료(2026-07-25)**: 플랫 SVG 초안 14개(고객센터=코코 캐릭터라 제외)를 힉스 nano_banana_pro로 **플랫벡터 큐트 화풍(clean rounded outline+soft cel shading)** 재생성→remove_background 투명 PNG. 통일성 위해 **발바닥 1개를 스타일 앵커**로 먼저 생성(job ea5b44f6) 후 나머지 13개를 그 앵커 레퍼런스로 파생(고양이 캐릭터 레퍼런스는 오브젝트마다 고양이 섞일 위험이라 오브젝트 앵커 방식). **→ 색 재작업(2026-07-25, 사용자 "코코랑 색상 안 맞아")**: 슬레이트 청회색이 실제 코코(크림+따뜻한 갈색 태비+회색꼬리+핑크코)와 안 맞아 **따뜻한 팔레트(크림·타우니 그레이브라운·더스티 로즈핑크·따뜻한 갈색 외곽선)로 14개 전량 재생성**(새 따뜻한 발바닥 앵커 d707d331→13개 파생, 사용자 색 승인). URL=hf_20260725_0051~0053*. (구 슬레이트 URL=hf_20260725_0043~0044*, 미사용). **앱 테마 틴트도 코코 톤으로(2026-07-25)**: THEMES.a_cat을 청회색(accent #7f8fa6·bg #f2f4f8)→**따뜻한 카라멜/크림**(accent #a8794e·bg #f7f2ea·text #33291f·sub #8a7358)으로 교체(힉스 0, 색값만). **캣타워 아이콘=원목 캣타워 사진 모티브 재생성**(사용자 제공 사진: 원목 기둥·둥근 발판·투명 버블돔·큐브하우스): 힉스로 워밍 앵커 물려 재생성→bg제거→방목록 교체(URL=hf_20260725_005715). CSS data-uri→PNG URL 교체(탭 size 120→128%). ⚠️ 힉스 CDN 직참조(만료 위험). ⚠️ 프록시가 CDN 차단이라 로컬 시각검증 불가—구조검증만(14개 background-image=cloudfront·문법0), 시각은 사용자 기기. 배경 씬은 추후. index.html 변경 → 재빌드 필요(PWA 무관). **이건 초안**(플랫)이며 승인 후 힉스로 각 테마 화풍(수채·구아슈 등) 질감 마무리 예정. ⚠️ 워크플로: **피그마 초안(무료로 모양·모티프·색 확정) → 힉스에 레퍼런스로 넣어 질감만 입힘**(힉스 재생성 낭비↓=크레딧 절약). ⚠️ 확장 시 나머지 12테마도 같은 `body.rc-<cls> .cal-tab[data-rci]/.cal-search-btn[data-rci]/.uni-nav[data-rci]` 패턴 재사용. Playwright 검증(rc-a_cat 4탭 data-uri·svg none·문법0). index.html 변경 → 재빌드 필요(PWA 무관).
**위젯 날짜 탭 → 그 달 캘린더로 이동(안드로이드 딥링크 라우팅 수정)(2026-07-24, 사용자 "위젯 날짜 누르면 마지막 화면=설정으로 뜸, 누른 달로 가게")**: 갤럭시 위젯에서 날짜 셀을 탭하면 앱이 **마지막 화면(설정 등)으로 그냥 복귀**하고 누른 날짜가 무시되던 버그. 원인=Android 셀 탭 템플릿 `WidgetCommon.openAppTemplate`이 **ACTION_MAIN+CATEGORY_LAUNCHER**라 런처처럼 '마지막 상태 복귀'만 했고, fill-in의 `widgetDate` extra는 앱 JS가 읽지도 않았음(iOS는 원래 `://open?room=&date=` 딥링크라 정상). **수정**: (1) `openAppTemplate`을 **ACTION_VIEW(데이터 URI 없음)+CATEGORY_DEFAULT+NEW_TASK|SINGLE_TOP, MUTABLE**로 변경(보내기 버튼 `openScheme`과 동일 경로 → BridgeActivity가 `intent.getData()`로 appUrlOpen 발화). 템플릿 data가 null이라 셀 fill-in의 data URI가 `Intent.fillIn`으로 적용됨. (2) `GridWidgetFactory`·`MiniMonthFactory`가 `room.id` 저장 후 셀 fill-in에 `fill.setData(Uri.parse("com.lsung.uricalendar://open?room=<id>&date=<key>"))` 세팅(월·2주·콤보미니 전부). (3) JS `://open` 핸들러(index.html ~7855) 강건화 — `calTabSwitch('cal')`(=showScreen('calendarScreen')로 설정 화면 벗어남+모달 정리) 후 `_calYear/_calMonth` 재세팅+`renderMonthly`(enterRoom/showScreen이 이번 달로 되돌리는 것 방지), 비전환 지연 0→80ms. stub 컴파일 22클래스 OK(Intent(String) 스텁 생성자 추가). iOS는 이미 정상(변경 없음). widget/android+index.html 변경 → android-aab 재빌드 필요.
**방목록에서도 마지막 입장 방 테마 유지(2026-07-25, 사용자 요청)**: `exitRoom`이 방목록 갈 때 개인 기본테마(`_myTheme`/vintage)로 리셋+`_clearRoomCharDecor`하던 것을 → **마지막 입장 방의 테마(색+캐릭터 꾸미기) 유지**로 변경. 시작 시 `_currentRoom.id`(`_lastId`)+`_currentRoomTheme`(`_lastTheme`) 캡처 → showScreen/loadRooms 후 `_lastTheme`로 즉시 applyTheme(깜빡임 방지)+`applyRoomThemeFor(_lastId)`로 색·꾸미기 정확 재적용(enterRoom과 동일 경로). 마지막 방 없으면 기존대로 기본테마+데코제거. 프리미엄 만료 시 applyRoomThemeFor 내부 게이트가 기본으로 복귀(기존 동작 유지). 문법0.
- 🦊 **여우비(a_fox) 캐릭터 리프레시 + 테마 아이콘 힉스 적용** (2026-07-25, 사용자 "이모지에 있는 여우도 전부 똑같이 만들어줘 다르게 생겼어" + "적용시켜줘") — 기존 여우비 이모지 6종(좋아·윙크·계략·킥킥·시치미·자신만만)이 서로 제각각이라 **새 통일 여우 캐릭터**(굵은 검정 외곽선·주황몸·크림가슴·청록 귀안쪽, 힉스 job `af293ed4`, 기존 여우비 `8ad71c7b` 물려 생성)로 **6종 전부 재생성**(레퍼런스 물림→remove_background). `ANIMAL_STICKER['fox:*']` 6개 URL 교체(hf_20260725_0518*)+`SEAL_SETS.a_fox`(좋아 포즈)+`ROOM_CHAR_THEME.a_fox` mascot(계략) 새 URL. **여우비 테마 아이콘 15개**(탭4·상단3·설정8)도 같은 새 여우/모던카툰 화풍으로 힉스 생성(af293ed4 물림, 오브젝트는 "no fox" 프롬프트)→remove_background→**`body.rc-a_fox` CSS 블록**(코코 rc-a_cat 패턴 복제, index.html ~900행, data-rci 공용 속성 재사용) 삽입. 아이콘 매핑: rooms=여우얼굴·cal=단풍잎·todo=체크클립보드·settings=버섯·search=발자국돋보기·cshare=낙엽날림·cimport=낙엽바구니·premium=로제트훈장·store=노점·adv=톱니·notif=여우귀종·notice=여우울음·support=여우헤드셋·delroom=쓰레기통·leave=여우굴. ⚠️ 나머지 11개 테마(토끼·곰·원숭이·쥐·닭·나비정원·별똥별·개구쟁이·달·데이지·잉어)는 **피그마/SVG 초안 승인 완료, 힉스 생성 대기**(각 테마 캐릭터 물려 15개씩 생성 예정). ⚠️ 힉스 CDN 직참조(만료 위험, 두두·코코와 동일). ⚠️ 에이전트 프록시가 힉스 CDN 차단이라 로컬 시각검증 불가 — 구조검증(rc-a_fox 22규칙)만, 시각은 사용자 기기. **배경도 새 여우로 교체(2026-07-25 추가)**: (1) 방 카드 배경 `CARD_SKINS.a_fox.img`=**여우가 단풍숲에 녹아든 뒤모습 산책 씬 21:9**(ad8c1917, 사용자 "화면 안 보고 자연스럽게 걸어다니는, 자연과 하나 된 모습" 요청, 20f9bcfa→cb611544(등산)→5f73f6f1→297b43ba(정면산책)→**ad8c1917(뒤모습·작게 풍경화)**), (2) 방 입장 배경 씬 `ROOM_CHAR_THEME.a_fox`에 `bgScene`=**뒤모습 산책 씬 9:16**(5be9e16a)+`charInScene:true`(두두 방식). ⚠️ 힉스 잡이 가끔 멈춤(재생성으로 대응). index.html 변경 → 재빌드 필요.
- 🐰 **달이(a_rabbit) 테마 풀세트 (여우비 방식, 2026-07-25)** — 11개 남은 테마 롤아웃 중 첫 번째. 수채화 토끼 화풍(기존 토끼 스티커 `1abae10c` 물려 생성)으로 **테마 아이콘 15개**(탭4 rooms=토끼얼굴·cal=당근·todo=클로버·settings=달 / 상단3 search=돋보기+클로버·cshare=풍선·cimport=당근바구니 / 설정8 premium=티아라·store=채소노점·adv=톱니·notif=토끼귀종·notice=깃발·support=토끼+헤드셋·delroom=쓰레기통·leave=토끼굴) 힉스 생성→remove_background→**`body.rc-a_rabbit` CSS 블록**(rc-a_fox 패턴 복제) 삽입. URL=hf_20260725_0809~0810*. 배경 2종: `CARD_SKINS.a_rabbit.img`=**수채 토끼 산책 씬 21:9**(6aad4a66), `ROOM_CHAR_THEME.a_rabbit`에 `bgScene`(9:16 9343f5c1)+`charInScene:true`. 빌드번호 49→50. ⚠️ 남은 10개 테마(곰·원숭이·쥐·닭·나비정원·별똥별·개구쟁이·달·데이지·잉어) 대기 — 한 개씩 롤아웃(사용자 지시 "한개한개 만들어가자"). ⚠️ 힉스 CDN 직참조·로컬 시각검증 불가(구조검증만). index.html 변경 → 재빌드 필요.
- 🐻 **꿀곰이(a_bear) 테마 풀세트 (여우비 방식, 2026-07-25)** — 두 번째 롤아웃. 구아슈 허니 스토리북 화풍(기존 곰 스티커 `1ae49953` 물려 생성)으로 **테마 아이콘 15개**(탭4 rooms=곰얼굴·cal=꿀벌·todo=체크클립보드·settings=꿀단지 / 상단3 search=돋보기+꿀방울·cshare=날아가는 꿀벌·cimport=꿀단지 바구니 / 설정8 premium=벌집훈장·store=꿀 노점·adv=톱니·notif=종+꿀벌·notice=확성기·support=곰+헤드셋·delroom=쓰레기통·leave=곰동굴) 힉스 생성→remove_background→**`body.rc-a_bear` CSS 블록**(rc-a_fox 패턴 복제) 삽입. URL=hf_20260725_082*. 배경 2종: `CARD_SKINS.a_bear.img`=**허니숲 뒤모습 산책 씬 21:9**(3b995e40), `ROOM_CHAR_THEME.a_bear`에 `bgScene`(9:16 c8690701)+`charInScene:true`. 빌드번호 50→51. ⚠️ 남은 9개 테마(원숭이·쥐·닭·나비정원·별똥별·개구쟁이·달·데이지·잉어) 대기. index.html 변경 → 재빌드 필요.
- 🐵 **몽당이(a_monkey) 테마 풀세트 (여우비 방식, 2026-07-25)** — 세 번째 롤아웃. 크레파스 낙서 화풍(기존 원숭이 스티커 `7f21fbf8` 물려 생성)으로 **테마 아이콘 15개**(탭4 rooms=원숭이얼굴·cal=바나나·todo=체크클립보드·settings=야자나무 / 상단3 search=돋보기+바나나·cshare=바나나부메랑·cimport=바나나바구니 / 설정8 premium=정글왕관·store=바나나노점·adv=톱니·notif=종·notice=확성기·support=원숭이+헤드셋·delroom=쓰레기통·leave=나무구멍) 힉스 생성→remove_background→**`body.rc-a_monkey` CSS 블록** 삽입. URL=hf_20260725_0828~0829*. 배경 2종: `CARD_SKINS.a_monkey.img`=**크레용 정글 뒤모습 산책 씬 21:9**(3d24fe93), `ROOM_CHAR_THEME.a_monkey`에 `bgScene`(9:16 689dd5dd)+`charInScene:true`. 빌드번호 51→52. ⚠️ 남은 8개 테마(쥐·닭·나비정원·별똥별·개구쟁이·달·데이지·잉어) 대기. index.html 변경 → 재빌드 필요.
- 🐭 **콩이(a_mouse) 테마 풀세트 (여우비 방식, 2026-07-25)** — 네 번째 롤아웃. 색연필 화풍(기존 쥐 스티커 `adb117fe` 물려 생성)으로 **테마 아이콘 15개**(탭4 rooms=쥐얼굴·cal=치즈·todo=체크클립보드·settings=도토리 / 상단3 search=돋보기+치즈·cshare=편지날기·cimport=치즈바구니 / 설정8 premium=로제트·store=치즈노점·adv=톱니·notif=종·notice=확성기·support=쥐+헤드셋·delroom=쓰레기통·leave=쥐구멍) 힉스 생성→remove_background→**`body.rc-a_mouse` CSS 블록** 삽입. URL=hf_20260725_0834~0835*. 배경 2종: `CARD_SKINS.a_mouse.img`=**색연필 초원 뒤모습 산책 씬 21:9**(645ac380), `ROOM_CHAR_THEME.a_mouse`에 `bgScene`(9:16 afef81e5)+`charInScene:true`. 빌드번호 52→53. ⚠️ 남은 7개 테마(닭·나비정원·별똥별·개구쟁이·달·데이지·잉어) 대기. index.html 변경 → 재빌드 필요.
- 🐔 **꼬꼬(a_chicken) 테마 풀세트 + 배경 컨셉 전면 다양화 (2026-07-25)** — 다섯 번째 롤아웃 + ⚠️ **사용자 피드백 반영: 배경씬이 전부 여우 템플릿(뒤돌아 걷기)으로 똑같다 → 테마마다 완전히 다른 컨셉으로 재생성**(위 유지보수 규칙 신설). (a) **꼬꼬 아이콘 15개**: 카툰 동화책 화풍(닭 스티커 `dbc53b41` 물려 생성) rooms=닭얼굴·cal=달걀·todo=체크클립보드·settings=밀이삭 / search=돋보기+달걀·cshare=깃털날기·cimport=달걀바구니 / premium=금달걀·store=달걀노점·adv=톱니·notif=종·notice=확성기·support=닭+헤드셋·delroom=쓰레기통·leave=헛간문. 힉스→bg제거→`body.rc-a_chicken` CSS. URL=hf_20260725_0844~0845*. (b) **배경 5테마 distinct 재생성**(카드 21:9 + bgScene 9:16, charInScene): 달이=**밤 언덕 큰 보름달 올려다보기**(084219/084224)·꿀곰이=**꿀단지 옆 굴속 낮잠**(084231/084237)·몽당이=**덩굴 타고 그네뛰기**(084244/084249)·콩이=**땅속 보금자리 단면**(084256/084301)·꼬꼬=**새벽 헛간 울타리 홰**(084308/084313). 각 테마 스티커 레퍼런스 물림. 여우비만 원조(단풍숲 뒤돌아 걷기) 유지. 옛 걷기 배경 URL 교체됨. 빌드번호 53→54. ⚠️ 남은 6개 테마(나비정원·별똥별·개구쟁이·달·데이지·잉어)도 각자 고유 배경 컨셉으로. index.html 변경 → 재빌드 필요.
- 🦋 **나비정원(nabi) 테마 풀세트 (2026-07-25)** — 무늬 테마 첫 풀세트(사용자 "전부 풀세트 계속"). 수채 꽃 화풍(nabi 카드 `558b4b84` 물려 생성) **아이콘 15개**(탭4 rooms=나비·cal=꽃·todo=클립보드·settings=화분 / 상단3 search=돋보기+꽃·cshare=나비날기·cimport=꽃바구니 / 설정8 premium=꽃화관·store=꽃노점·adv=톱니·notif=꽃종·notice=확성기·support=헤드셋+꽃·delroom=쓰레기통·leave=정원문) 힉스→bg제거→**`body.rc-nabi` CSS**. URL=hf_20260725_0904~0906*. 배경(고유 컨셉): **나비 가득한 수채 꽃 정원** 카드(6427601b)+bgScene(9c13abac, charInScene). ⚠️ cls는 `rc-nabi`(rc-a_ 아님). 빌드번호 54→55. ⚠️ 남은 5개(별똥별·개구쟁이·달·데이지·잉어). index.html 변경 → 재빌드 필요.
- ⭐ **별똥별(star) 테마 풀세트 (2026-07-25)** — 셀레셜 밤하늘 화풍(star 카드 `3d4f9461` 물려 생성) **아이콘 15개**(rooms=별·cal=별똥별·todo=클립보드·settings=망원경 / search=돋보기+별·cshare=별똥별날기·cimport=별병 / premium=별왕관·store=별노점·adv=톱니·notif=별종·notice=확성기·support=헤드셋+별·delroom=쓰레기통·leave=빛문) 힉스→bg제거→**`body.rc-star` CSS**. URL=hf_20260725_091*. 배경(고유): **별똥별 쏟아지는 밤하늘** 카드(aec5db6e)+bgScene(bf6a0a3c, charInScene). 빌드번호 55→56. ⚠️ 남은 4개(개구쟁이·달·데이지·잉어). index.html 변경 → 재빌드 필요.
- 😝 **개구쟁이(c_gaegu) 테마 풀세트 (2026-07-25)** — 크레파스 낙서 화풍(c_gaegu 카드 `a4c62025` 물려 생성) **아이콘 15개**(rooms=개구쟁이얼굴·cal=크레용·todo=클립보드·settings=팽이 / search=돋보기·cshare=종이비행기·cimport=장난감바구니 / premium=종이왕관·store=장난감가게·adv=톱니·notif=종·notice=확성기·support=개구쟁이+헤드셋·delroom=쓰레기통·leave=문) 힉스→bg제거→**`body.rc-c_gaegu` CSS**. URL=hf_20260725_0916~0917*. 배경(고유): **크레용 놀이터에서 신나게 노는 개구쟁이** 카드(1ebf4f6c)+bgScene(2794a671, charInScene). 빌드번호 56→57. ⚠️ 남은 3개(달·데이지·잉어). index.html 변경 → 재빌드 필요.
- 🌙 **달(moon) 테마 풀세트 (2026-07-25)** — 달빛 밤 화풍(moon 카드 `2deb5b76` 물려 생성) **아이콘 15개**(rooms=보름달·cal=초승달·todo=클립보드·settings=밤구름 / search=돋보기+별·cshare=유성·cimport=별바구니 / premium=달왕관·store=밤노점·adv=톱니·notif=달종·notice=확성기·support=헤드셋+달·delroom=쓰레기통·leave=문) 힉스→bg제거→**`body.rc-moon` CSS**. URL=hf_20260725_0922~0923*. 배경(고유, 별·토끼와 차별): **호수에 비친 큰 보름달**(반사) 카드(4118ca1b, dark:true 유지)+bgScene(2f27d6eb, charInScene). 빌드번호 57→58. ⚠️ 남은 2개(데이지·잉어). index.html 변경 → 재빌드 필요.
- 🌼 **데이지(daisy) 테마 풀세트 (2026-07-25)** — 화창한 노랑/흰 수채 화풍(daisy 카드 `21f3b017` 물려 생성) **아이콘 15개**(rooms=데이지·cal=해·todo=클립보드·settings=물뿌리개 / search=돋보기+데이지·cshare=꽃잎날기·cimport=데이지바구니 / premium=데이지화관·store=꽃노점·adv=톱니·notif=데이지종·notice=확성기·support=헤드셋+데이지·delroom=쓰레기통·leave=정원문) 힉스→bg제거→**`body.rc-daisy` CSS**. URL=hf_20260725_0928~0930*(해 아이콘은 잡 멈춤으로 재생성 bcfc4fab). 배경(고유, 나비정원과 차별): **햇살 가득 데이지 들판**(탁 트인 초원) 카드(5d9d3aee)+bgScene(cb67c266, charInScene). 빌드번호 58→59. ⚠️ 남은 1개(잉어). index.html 변경 → 재빌드 필요.
- 🐟 **잉어(koi) 테마 풀세트 (2026-07-25) — 14개 테마 롤아웃 완료!** — 수묵/민화 화풍(koi 카드 `01b398d9` 물려 생성) **아이콘 15개**(rooms=잉어·cal=물결·todo=클립보드·settings=연꽃 / search=돋보기+물방울·cshare=종이배·cimport=물고기바구니 / premium=금잉어·store=노점·adv=톱니·notif=종·notice=확성기·support=헤드셋+잉어·delroom=쓰레기통·leave=문) 힉스→bg제거→**`body.rc-koi` CSS**. URL=hf_20260725_0935~0936*. 배경(고유): **연꽃 연못에서 헤엄치는 잉어**(수묵) 카드(7eb10dc9)+bgScene(e4ab36db, charInScene). 빌드번호 59→60. ✅ **전 14개 테마(여우비·달이·꿀곰이·몽당이·콩이·꼬꼬·나비정원·별똥별·개구쟁이·달·데이지·잉어 + 두두·코코)가 아이콘 15+고유 배경 완비.** 각 테마 배경은 서로 다른 컨셉(걷기·달밤·굴속낮잠·덩굴그네·땅속집·새벽헛간·나비정원·별하늘·놀이터·호수달·데이지들판·연꽃연못). index.html 변경 → 재빌드 필요.
- 저장소 `main`의 `index.html`은 **build 1.1.5 (36)** — 2026-07-23 갤럭시 위젯 빌드용(항목 8~61 + 스토어 프로필·테마 통합·기간바 이음새 fix·테마 채도↑·민트 재설계 반영). buildInfo 문자열(정적 line 4848 + 런타임 line 8000) 둘 다 `1.1.6 (73)`(위젯 빨간날 표시 반영). **2026-07-25 새 빌드용으로 버전 1.1.5→1.1.6 승격 + 버전코드 하한 각 +1**(사용자 요청): appstore `codemagic.yaml` — iOS `APP_VERSION="1.1.6"`·빌드번호 하한 34→35, Android `versionName 1.1.6`·versionCode 하한 79→80. buildInfo도 1.1.6 (72). 빌드번호(72)는 앱 내 표시 라벨일 뿐, 실제 스토어 versionCode는 codemagic 자동산정(iOS=TestFlight최신+1 floor35, Android=50+BUILD_NUMBER floor80)이라 별개.
- 🛒 **테마별 스토어 아이콘 빈칸 수정** (2026-07-25, 사용자 실기기 제보 "중간중간 아이콘이 없는게 있네", nabi·daisy 설정화면 스크린샷) — 14개 테마의 설정목록 '스토어'(`data-rci="store"`) 아이콘이 빈칸으로 뜨던 버그. 원인=스토어 아이콘 생성 시 '가판대(market stall)' 프롬프트가 옅고 흰 이미지를 만들어 remove_background가 피사체까지 과다 제거 → 거의 투명한 PNG. 스토어 DOM 행은 프리미엄 행과 구조 동일(정상), CDN도 정상(탭 아이콘은 보임) → 이미지 자체가 문제로 확진. 수정: 12개 테마(fox~koi, cat·dino는 기존 정상)의 스토어 아이콘을 **'컬러풀 줄무늬 차양 상점(colorful striped awning storefront), 굵고 채도 높은 채움색, 옅음 금지'** 프롬프트로 각 테마 화풍에 맞춰 재생성→remove_background→URL 교체(hf_20260725_0945*). CSS 규칙(rc-<cls> 스토어 background-image)은 그대로, URL만 교체. Playwright로 14테마 전체 아이콘 세트 완비 검증(설정8·탭4·상단3=15개 규칙 각각 present)+문법0. ⚠️ 힉스 CDN 직참조(만료 위험)·프록시 차단으로 로컬 시각검증 불가(구조검증만·시각은 기기). index.html 변경 → 재빌드 필요.
- ⚡ **테마 배경씬 로딩 개선 — 2K PNG → `_min.webp` 프리뷰** (2026-07-25, 사용자 "테마 선택 시 배경씬 로딩 너무 오래") — 방 입장 시 `.rc-bg-scene`(전체화면 배경)이 2K 업스케일 PNG(수 MB)라 CSS 첫 로드가 느려 바로 안 뜨던 문제. 배경씬은 **opacity 0.4로 흐리게 깔리는 배경**이라 고해상도 불필요 → `ROOM_CHAR_THEME.*.bgScene` 14개 URL을 각 이미지의 **`_min.webp`(Higgs 자동생성 웹 프리뷰, 훨씬 작음)** 로 교체(`.png`→`_min.webp`, 파일명 규칙만 변경). 로딩 크게 빨라지고 0.4 투명이라 화질 체감차 거의 없음. **+ 프리로드(2026-07-25, 사용자 "업스케일 전에도 느렸음")**: 첫 입장 때 네트워크로 처음 받아 느린 게 근본 원인 → `_preloadSceneUrl`/`_preloadSkinScene`/`_preloadRoomScenes`(전역, `_scenesPreloaded` 중복가드) 신설, **방목록 로드 시(loadRooms, `_myCardSkinByRoom` 채운 직후) 각 방의 bgScene을 `new Image()`로 미리 캐시** + **테마 선택 즉시(pickCardSkin·pickUnifiedSet) 해당 씬 프리로드** → 저장·입장 시 즉시 표시. webp라 작아서 프리로드 부담 적음. ⚠️ 카드 배경(CARD_SKINS.img)은 이미 1584 PNG(작은 썸네일)라 그대로. ⚠️ 만약 실기기에서 배경이 너무 흐리거나 안 뜨면 `_min.webp`→`.png` 원복(패턴 치환). index.html 변경 → 재빌드 필요.
- ✅ **프리미엄 만료 시 씰·테마 색 완전 자동 복귀** (2026-07-25, 사용자 "무료 전환하면 씰·테마색이 남아있음, 모두 기본 자동 전환") — 코드 63이 카드배경(`_effSkin`)+방테마(`applyRoomThemeFor`)만 복귀시켜서, **① 씰(프로필)** 과 **② 개인 전역 테마(`_myTheme`, profiles.theme)** 는 만료 후에도 프리미엄이 남아있던 갭 수정. **① 씰**: `_sealInner`가 `set:<key>` 세트 씰을 무조건 렌더하던 것 → **프리미엄 세트(`_isPremiumSkin`)이고 비프리미엄(`!_premOn()`)이면 null 반환** → 5개 호출부(방목록·월줄 씰버튼·방전환·가져오기) 전부 `|| 첫글자` 폴백이라 자동으로 기본 첫글자로. 커스텀 씰(`color:sym`)은 무료라 유지. **② 테마 색**: `loadMyTheme`의 `applyTheme(_myTheme)`+exitRoom 즉시적용이 게이트 없이 프리미엄 개인테마를 적용하던 것 → 헬퍼 `_effTheme(theme)`(비프리미엄+프리미엄테마면 무료 `_myTheme` 또는 vintage 반환) 신설해 적용 지점 3곳에 반영. **③ 상태 변화 즉시 반영**: `_refreshPremiumGatedUI()`(renderRooms+테마 재적용) 신설 → `loadPremiumStatus`(프리미엄 상태 flip 시)+`_applyAdminPlanUI`(관리자 무료/유료 토글 시) 연결. ⚠️ **저장값(members.seal·card_skin·profiles.theme)은 안 지움 → 재구독 시 자동 복원**(게이트만 통과). ⚠️ 컬러팩·이모지는 기존 정책대로 저장값 렌더 유지+신규만 차단(무변경). node 로직검증 8종(무료:세트씰→null·커스텀유지·프리미엄테마→vintage/무료테마유지, 유료:전부유지). index.html 변경 → 재빌드 필요. **⚠️ WebView라 deploy_version 올려야 일반 사용자 반영**(배포게이트 참고). **후속 수정(2026-07-25, 사용자 "씰은 기본 되는데 배경색이 라벤더 유지, vintage로"): `applyRoomThemeFor`가 프리미엄 카드스킨 만료 시 `_cardSkinTheme(skin=null)`→null→**옛 `user_room_themes`(무료 lavender 등)로 폴백**해 안 돌아가던 버그.** DB 확인=프리미엄 카드스킨 방들에 옛 방테마(lavender·mint)가 따로 저장돼 있었음. 수정=게이트 전 **`rawSkin`으로 `_cardSkinTheme` 판정** → 카드 배경 테마 방이면 그 테마만 쓰고(만료 시 `_effTheme`→vintage) **옛 user_room_themes는 무시**, 카드 배경 없는 방만 방/개인 테마 사용(그것도 `_effTheme` 게이트). node 6종 검증(별·곰·여우 방→vintage·옛방테마무시, 무료 방테마 mint 유지, 유료 star+데코). **후속 수정(2026-07-27, 사용자 "무료 전환 후 위젯에 프리미엄 프로필이 그대로"): 위젯 씰은 앱 `_sealInner`가 아니라 `buildWidgetPayload`의 `sealPng`(=`_wgSealPng(r.seal,...)`) 별도 경로라 프리미엄 게이트가 안 걸려 여우비 등 프리미엄 세트 씰이 위젯에 계속 남던 갭.** → payload seal 생성 시 `_parseSeal`로 `set:<key>` 판정 후 `_isPremiumSkin`+`!_premOn()`이면 `seal=null` → `_wgSealPng(null)`=null → 네이티브 위젯이 색+첫글자 폴백 렌더(앱과 동일). 캐시키(seal값 기반)라 재구독 시 자동 복원. index.html 변경 → 재빌드 필요(위젯은 payload 갱신 후 새로고침).
- 🩹 **갤럭시 위젯 오늘(날짜) 타원 + 일정 줄 어긋남 수정** (2026-07-25, 개발자 VS Code Claude Opus 5 작업 — appstore `7efb339`, 소스 저장소엔 이 세션이 backport) — 갤럭시 월/2주 위젯에서 오늘 날짜가 **가로로 긴 타원**으로 뜨고, 그 칸의 일정 줄이 아래로 밀려 같은 주 다른 날짜와 정렬이 깨지던 문제. 원인=`cell_today_bg.xml`(오늘 배경 oval drawable)의 **`<padding>`(좌우 6dp/상하 2dp)** — 좌우 패딩으로 폭>높이가 돼 원이 타원으로 늘어나고, 상하 패딩이 '오늘' 칸 TextView에만 붙어 그 칸이 4dp 높아져 일정 줄이 아래로 밀림. **수정**: (a) `cell_today_bg.xml`에서 `<padding>` 제거(정원 유지·주석으로 이유 명시), (b) `widget_cell.xml`의 `cell_day` TextView를 `wrap_content`→**17dp×17dp 정사각 고정**+`gravity=center`+`includeFontPadding=false`(iOS `DayCell` numBox 고정 프레임과 동일 방식) → 오늘/평일 칸 높이가 항상 같아 줄 어긋남 해소, oval이 정원으로 렌더. 월·2주 위젯이 `GridWidgetFactory`/`widget_cell` 공유라 함께 적용. ⚠️ 위젯 변경이라 **android-aab 재빌드+설치**로만 반영(deploy_version 무관). ⚠️ **이 수정은 appstore(빌드 저장소)에서 먼저 이뤄짐** — 소스 저장소(`first-uri-calender`) widget/android 2파일을 이 세션이 appstore에서 backport해 일치시킴. ⚠️ **주의: `widget/bridge.js`는 반대로 소스가 최신·정답**(`WidgetsBridgePlugin`, 코드 37 버그수정)이고 appstore가 옛 버그판(`WidgetsBridge`) — 향후 위젯 동기 시 bridge.js는 소스→appstore 방향.
- 🖼️ **위젯 추가화면(피커) 미리보기 이미지 + 오늘칸 정렬 재확인** (2026-07-27, 사용자 실기기 "위젯 추가화면에 예시가 안 보임 + 오늘(2일)칸 일정이 아래로 밀려 정렬 깨짐") — (a) **피커 미리보기**: 삼성/안드로이드 위젯 추가화면이 실제 미리보기 대신 앱 아이콘(OUR) 플레이스홀더만 띄우던 문제. ⚠️ **`previewLayout`은 코드 50에서 GridView 컬렉션 때문에 삼성 미리보기 생성 실패로 철회**했으므로, 대신 **정적 `android:previewImage`(PNG 썸네일)** 방식 사용(컬렉션 없음=안전, 구글 권장). PIL로 앱 팔레트(크림카드·terra 씰·전체칩·요일헤더 일=빨강·기간 컬러 일정바·오늘 원)에 맞춘 미리보기 PNG 4종 생성(월·2주·오늘·콤보) → `res/drawable-nodpi/widget_preview_*.png` + info XML 4종에 `android:previewImage` 추가. inject 스크립트에 drawable-nodpi PNG 복사 스텝 추가(기존엔 *.xml만 복사해 PNG 누락됐음). (b) **오늘칸 정렬**: 실기기 스샷의 '오늘(2일) 일정이 아래로 밀림'은 **이미 5656b9a(2026-07-25 backport)에서 수정된 그 버그**(cell_today_bg 패딩 제거 + cell_day 17dp 정사각 고정) — 사용자 설치본(build 33, ~7/24)이 그 수정 이전이라 아직 보이는 것. 코드는 정상, **다음 빌드에서 해소**(추가 코드변경 불필요). widget/android res·inject 변경 → **android-aab 재빌드 필요**. ⚠️ iOS 피커는 WidgetKit이 실제 렌더 스냅샷을 자동 표시하므로 previewImage 불필요(해당 없음).
- 🟥 **위젯 빨간날(공휴일) 표시 — 빨간 날짜 + 빨간 일정바 자동 생성** (2026-07-27, 사용자 "위젯에도 빨간날은 날짜를 빨간색으로, 추석·설 같은 빨간날 + 앱엔 텍스트만인데 위젯엔 빨간 일정바 하나 자동생성해 어떤날인지 표시, 사진참조") — 앱은 공휴일을 셀 라벨 텍스트로만 표시하는데, 위젯(월·2주·콤보 미니월)에서 **공휴일 날짜 숫자를 빨간색**으로 + **그 이름을 빨간 일정바로 자동 생성**해 표시. **payload**: `buildWidgetPayload`가 `holidays:{'YYYY-MM-DD':이름}` 맵 추가(앱 `HOLIDAYS` 맵=공휴일만, 절기·기념일 제외; 창 `from`~`to` 범위, 두 연도 `loadHolidaysForYear` 보장). **iOS**(`UriCalendarWidget.swift`): `WGData.holidays`·`CalEntry.holidays`·`GridView.holidays`·`DayCell.holiday`·`ComboView/MiniMonth.holidays` 스레딩. DayCell 숫자색 `dow==0 || holiday!=nil → .sunRed`, 숫자 아래 **빨간 바(`Color.sunRed` 채움+흰 글자)** 프리펜드. MiniMonth 숫자도 일요일/공휴일 빨강. **Android**(`WidgetData.java` holidays 파싱+`GridWidgetFactory`/`MiniMonthFactory`): 셀 숫자 빨강(일요일/공휴일) + `widget_cell.xml`에 **`cell_holiday` 빨간 TextView 바**(#C0503F) 추가해 이름 표시. 콤보 미니월도 숫자 빨강. 컴파일 22클래스 OK·XML 유효·JS 문법0·brace 균형. ⚠️ 위젯 변경이라 **android-aab/IPA 재빌드+설치**로만 반영(payload는 index.html이라 앱 재빌드도 필요). appstore 두 브랜치+www 동기. buildInfo 72→73.
- 🧹 **[정리] 죽은 레거시 할일-셀 렌더 서브시스템 제거** (2026-07-25, 사용자 "좀비코드 확인·정리해서 렉 줄여줘") — ⚠️ **먼저 정직하게**: 죽은 함수는 실행 안 돼서 렉과 무관(JS 컴파일 22ms 측정). 좀비 스캔 결과 무서워 보이던 `setupWidgetForegroundFlush`·`setupNativeBackButton`·`setupNativeAuthDeepLink`는 **자기실행 IIFE**(`(function X(){...})()`)라 살아있음(오탐, 건드리면 안 됨). 리스너 누수도 없음(할일 데이트피커 scroll 리스너는 `if(children.length>0)return` 가드로 1회만 부착). **실제로 제거한 것**: 통합 레인 시스템(cellHtml)으로 대체돼 아무도 안 부르는 **자기완결 죽은 클러스터 8심볼 ~130줄** — `_renderTodosIntoCells_legacy`·`computeTodoRangeMeta`·`buildRangeCellHTML`·`buildSingleCellHTML`·`renderCellTodos`·`rangeLabelInfo`·`isRangeType`·`_todoRangeMeta`. 제거 후 전부 0참조 확인, 활성 무동작 스텁 `renderTodosIntoCells`(배지만 갱신)는 유지. 파일 ~4KB 감소. **효과=코드 청결·크기↓(렉은 그대로)**. 나머지 죽은 소함수(~19개, 각 1KB↓)는 CLAUDE.md 의도적 잔존(`syncPremiumFromRevenueCat`·`pickSealSet` 등)이거나 위험 대비 이득 없어 보류. index.html 변경 → 재빌드 필요.
- ⚡ **[성능] 방 입장 로딩 개선 — 프로필 세션 캐싱 + 테마 조회 생략** (2026-07-25, 사용자 "방 입장 로딩도 있어") — enterRoom 경로 분석: 데이터 로드는 이미 병렬(loadEvents/Members/Profile/Announcements), `select('*')`도 방당 최대 243건(~100KB)이라 병목 아님. **진짜 지연 = 방 입장마다 반복되는 네트워크 왕복**. **수정 2가지**: (1) **`loadCurrentProfile` 세션 캐싱** — 프로필+DB unlocks는 사용자 전역(방마다 안 바뀜)인데 매 입장마다 재조회하고 enterRoom이 이걸 기다려 `renderMonthly`를 호출했음 → `_profileLoaded` 플래그로 세션 1회만 조회(force 인자로 명시 새로고침 가능). 프로필 수정은 `_currentProfile` 직접 갱신이라 캐시 안전. **로그아웃/신규 로그인 시 플래그 리셋**(다른 계정 프로필 유지 방지, onAuthStateChange SIGNED_OUT·SIGNED_IN). (2) **`applyRoomThemeFor`의 `user_room_themes` 조회 생략** — 카드 스킨이 테마를 정하면(코드 60 통합) 저장된 방 테마는 어차피 무시되므로, `_cardSkinTheme(skin)` 있으면 DB 조회 건너뜀(테마 방 입장 시 왕복 1회 제거, 동작 동일). ⚠️ 테마 자산(아이콘 15장+bgScene) CDN 로드는 async라 렌더 안 막음(별개, 추후 min.webp 검토 가능). index.html 변경 → 재빌드 필요.
- ⚡ **[성능] 콜드스타트 로딩 개선 — 렌더 차단 폰트 비동기화** (2026-07-25, 사용자 "로딩·렉 심함, 업스케일 전부터") — 업스케일 이전부터 있던 렉이라 이미지 아닌 런타임 조사. **측정 결과**: JS 컴파일 데스크톱 22ms(모바일 ~100ms, 병목 아님)·힙 9.5MB·인라인 base64 752KB(문자열이라 파싱 저렴)·폴링/realtime 구독 없음·`getDayEvents` 캐시 정상. **진짜 병목 = `<head>` 렌더 차단 리소스**: (a) Noto Serif KR 폰트 CSS(line 8) + (b) Pretendard 가변폰트 CSS(line 9)가 **동기 로드**라 첫 페인트를 막고 있었음(line 11 장식폰트는 이미 `media=print` 비동기였는데 8·9만 누락). ⚠️ 특히 **body 폰트는 `--ff-sans`=`-apple-system` 우선**이라 Pretendard는 애플 기기에서 거의 안 쓰이는데도 큰 가변폰트를 차단 다운로드 중이었음. **수정**: line 8·9를 `media="print" onload="this.media='all'"` + `<noscript>` 폴백으로 전환 → 첫 화면이 시스템 폰트로 즉시 뜨고 폰트는 준비되면 스왑. ⚠️ **CDN 스크립트(supabase·lunar)는 defer 불가** — `createClient`가 line 5271 파싱 시점에 실행돼 `window.supabase` 선로드 필요(동기 유지). ⚠️ 런타임 '렉'(특정 화면 잔버벅)은 별개 — 어느 화면인지 확인 후 추가 최적화 예정(스토어 60스티커 프리로드·테마 아이콘 1024px CDN 다중로드·renderMonthly 전체 재빌드 등 후보). index.html 변경 → 재빌드 필요.
- 🔎 **테마 배경 28장 2K 업스케일 + 아이콘 크기 상향** (2026-07-25, 사용자 "아이콘·배경씬 해상도 올려줘, 원본보다 질 나쁨 + 아이콘 조금 키워") — (a) **배경 업스케일**: 14테마 카드(21:9)+bgScene(9:16) 원본이 nano_banana_2 **1k**(씬 768×1376·카드 1584×672)라 전체화면에 부족 → 힉스 `upscale_image`(bytedance, 2K)로 28장 전부 업스케일(씬→2160×3856·카드→5088×2160). 결과 URL을 `CARD_SKINS.*.img`+`ROOM_CHAR_THEME.*.bgScene` 28곳에 교체(hf_20260725_1131~1138_* 배치). 비용 56cr(212→156). **⚠️ 성능 후속(2026-07-25, 사용자 "로딩·렉 심함"): 카드 배경(CARD_SKINS.img)은 원본 1584px로 되돌림** — 카드는 방목록·프로필편집기 그리드에서 **작게 썸네일**로 표시되는데 2K(5088×2160, 장당 수MB)면 프로필편집기 프리미엄 탭이 14장을 동시 로드해 큰 렉 유발 → 카드는 1584 원본 유지, **전체화면 bgScene(방 입장 시 1장)만 2K 유지**(사용자가 요청한 '배경씬' 해상도). 즉 14 bgScene=2K, 14 card=1584원본. ⚠️ **아이콘(탭·상단·설정)은 업스케일 안 함** — 화면에서 ~30px로 작게 표시돼 원본 1024px가 이미 축소 렌더라 픽셀 해상도 병목 아님(크레딧 낭비 방지, 사용자 승인). (b) **아이콘 크기 상향**: 14테마 공통 크기규칙 — 탭 `background-size:128%→138%`, 설정 `::before 122%→132%`, 상단 `25px→28px`(박스 크기 26px 유지, 아트만 키움). ⚠️ 업스케일 결과는 CDN 차단으로 로컬 시각검증 불가 — 구조검증(28 URL 반영·문법0)만, 시각은 기기. index.html 변경 → 재빌드 필요.
- 🐛 **다음달(흐림) 기간 일정 바 중간이 겹쳐 진해 보이던 버그 수정** (2026-07-25, 사용자 실기기 제보) — `.cal-cell.other{opacity:.35}`(다음달 셀 흐림)에서 기간 바 조인이 코드 61의 `margin:-1px`(이음새 방지 겹침)라 **반투명 상태에서 1px 겹침이 두 겹으로 합성 → 칸 경계마다 진한 세로선 → 기간 바 중간이 진해 보임**. 현재달은 바가 불투명이라 안 보였음. 수정: `.cal-cell.other .cal-evbar.ev-l-join{margin-left:0}`/`ev-r-join{margin-right:0}` — 다음달 셀에서만 조인 겹침 0(바는 이어지되 진해짐 제거, 현재달 -1px 이음새 방지는 유지). Playwright 재현으로 버그(진한 세로선)→수정(균일) 확인. index.html 변경 → 재빌드 필요. ⚠️ 표시 빌드번호(46)는 라벨일 뿐 — 실제 Android versionCode는 codemagic `50+BUILD_NUMBER`(floor 79)로 자동 산정(~101+, Play 마지막 78보다 항상 큼), iOS 빌드번호는 TestFlight최신+1. 즉 화면 표시 33 ≠ 스토어 versionCode. 직전: build 1.1.4 (28)=2026-07-08 실배포. codemagic APP_VERSION=1.1.5. **⚠️ Android versionCode는 32 불가**(Play가 78까지 이미 소진 — 재사용 불가·항상 증가 필수). Android `VC=50+BUILD_NUMBER`(offset 28→50로 상향, floor 79), 다음 빌드 ~101부터. iOS `NEXT`=TestFlight최신+1(floor 32). 즉 iOS 빌드번호=32대, Android versionCode=100대(별개 체계, Play/AppStore는 각자 증가만 하면 OK). buildInfo 표시는 둘 다 1.1.5 (32). 단 개발자가 이후 로컬에서 다시 수정·업로드하면 main보다 앞설 수 있으니, 새 세션은 항상 현재 배포/로컬 버전을 확인할 것.
- **⚠️⚠️⚠️ [상시 규칙] 빌드 반영 필수 = appstore 동기화**: Codemagic은 이 repo가 아니라 **`lsung179-stack/appstore`의 `www/index.html`** 을 번들해 iOS·Android를 빌드함 — 이 repo `index.html`을 고쳐도 appstore www에 복사 안 하면 **빌드에 절대 안 나옴**(2026-07-21 이모지 변경이 빌드에 안 뜨는 사고 실제 발생). **사용자 지시(2026-07-21): 앞으로 이 repo `index.html`을 변경할 때마다 반드시 appstore도 함께 동기화**. 방법: `first-uri-calender/index.html` → `appstore/www/index.html` 전체 복사 후 **appstore의 `main` + `claude/ios-widget` 두 브랜치 모두 commit+push**(add_repo로 appstore 추가, `/workspace/appstore`). 빌드번호 변경 시 appstore `codemagic.yaml`의 iOS floor(`NEXT -lt N`)·Android floor(`VC -lt N`)도 함께 수정. 2026-07-21 build32 동기화 완료(appstore main 8897eb1, ios-widget 84710bd).
- 옛 버전 기반 브랜치/PR(예: `claude/calendar-ads-free-users-l7jsbt`의 PR #2)은 **머지 금지** — 삭제한 관리자 버튼 부활 + 최신 리팩터 덮어쓰기 위험.
- 배포 버전 확인: 앱 설정 화면 푸터의 `build ...` 문자열(런타임에 JS가 세팅, HTML 정적값과 다를 수 있음).

## 📝 출시 자료 자동 작성 규칙 (사용자 지시 2026-07-25)
- **사용자가 "빌드한다/빌드할게/새 빌드" 등 빌드 의사를 밝히면, 요청 없이도 자동으로 아래 3종을 함께 작성해 준다** (한국어, 사용자 대상 문구, 기술용어 금지):
  1. **출시명(릴리스명)**: `버전 — 한 줄 요약` (예: `1.1.6 — 캐릭터 테마 대업데이트`)
  2. **프로모션 텍스트**(App Store, ≤170자): 앱 핵심 가치 + 이번 하이라이트 한 문장.
  3. **출시노트/What's New**(양 스토어 공용): 이모지 불릿 3~5개, 사용자 체감 변경만. Play Store는 언어당 500자 제한이라 짧게.
- 범위 = **마지막 실제 출시(현재 1.1.4=28) 이후 누적된 사용자 체감 변경** 전부(1.1.5는 EULA로 거부돼 미출시). buildInfo/CLAUDE.md '이미 완료된 수정'에서 사용자 눈에 보이는 것만 골라 씀(내부 리팩터·성능 세부·deploy_version 등은 제외하거나 "속도 개선/안정성 향상"으로 뭉뚱그림).
- v1.1.6 자료는 이미 작성함(캐릭터 테마 대추가·위젯 개선·속도·버그수정).
- 🐛 **방 프로필 '기본으로 되돌리기' 미완전 리셋 3종 수정** (2026-07-27, 사용자 실기기 "❤️방은 기본으로 돌려도 민트 유지 + 기본 선택시 카드가 흰색 아니라 베이지크림 + 기본으로 되돌려도 프로필씰이 기본색으로 안 돌아감") — (1) **레거시 방 테마 잔존으로 인한 색 안 바뀜**: `applyRoomThemeFor`는 카드 배경(`card_skin`)이 없는 방만 옛 `user_room_themes`(2026-07 이전 '이 방 테마' 기능의 잔존 테이블) 값을 폴백으로 읽는데, `clearRoomMascot()`(기본으로 되돌리기)은 `card_skin`만 null로 만들고 이 레거시 테이블 행은 안 지워서 옛 값(예: 민트)이 계속 적용됨. `clearRoomMascot()`에 `user_room_themes` 행 삭제(`.delete().eq('user_id',...).eq('room_id',...)`) 추가해 완전 초기화. (2) **'기본' 카드 배경이 크림색이던 것 → 흰색**: `.rcs-t_vintage`(기본 카드 배경 CSS)를 `linear-gradient(...,#fbf2e0,#ecdcc2,#e0cba9)`(베이지 그라데이션) → `#ffffff`(순백)로 교체. (3) **방 프로필 씰 편집 화면의 큰 미리보기가 항상 테라코타(빨강)로 고정 렌더돼 리셋이 안 된 것처럼 보이던 문제**: `_rmBigPrev`가 `_sealColor`/`_sealSym`(기본값 terra/ini) 기준으로 늘 색이 있는 씰을 그렸음(진짜 저장 여부와 무관) — 새 상태 `_sealCustomPicked`(사용자가 실제로 색/문양을 클릭했는지) 도입: `openRoomMascotForRoom`에서 저장값이 커스텀(색:문양)일 때만 true, `_rmPickCustom`(색/문양 클릭 시) 즉시 true, `clearRoomMascot`에서 false로 리셋. `_rmSyncPreview`가 `_sealCustomPicked` false면 **중립 미리보기**(`.rm-bp-none`, 방목록 기본 씰과 동일 톤: `var(--card)` 배경+`var(--border)` 테두리+`var(--accent)` 색 첫 글자, '기본 (첫 글자)' 라벨) 렌더, 스와치/문양 그리드의 `.on` 하이라이트도 동일 플래그로 게이트(아무것도 안 고른 상태에서 테라코타 스와치가 미리 선택된 것처럼 보이던 것도 해소). index.html 변경 → 재빌드 필요.
- ✅ **일정 수정·복사·삭제 = 방 멤버 누구나 가능(다른 사람이 만든 것도)** (2026-07-27, 사용자 "다른사람이 만든 일정,할일도 수정,복사,삭제 가능하게, 현재는 자신이 만든것만 가능") — DB 조사 결과 **RLS는 이미 방 멤버 전체 허용**(`events_update_v2`/`events_delete_v2`/`todos_update_v2`/`todos_delete_v2` 정책이 `user_id` 무관 방 멤버 여부만 체크, 더 제한적인 구 정책들과 OR로 병존하지만 느슨한 정책이 있으면 통과됨) — **실제 막던 곳은 클라이언트 UI 한 곳**: 날짜시트(dpCard) 일정 카드의 `canEdit=!isVirtual&&(isMine||ev._isShared||_adminBypass())`(index.html ~10703)가 본인 소유·함께 일정·관리자가 아니면 수정/복사/삭제 버튼 자체를 안 그렸음(코드는 여기 한 곳뿐 — 편집 진입점이 dpCard '수정' 버튼 하나라 이 게이트만 풀면 전체 해결). **수정**: `canEdit=!isVirtual`(가상 일정=본인 생일 등만 제외, 소유자 무관 전원 허용). ⚠️ **할일(todos)은 조사 결과 애초에 게이트가 없었음** — `buildTodoCard`(방목록 우리할일 탭)·`refreshDaySheetTodos`(날짜시트)·`openTodoEditSheet`·`deleteTodo` 전부 소유자 체크 없이 이미 방 멤버 누구나 수정/복사(`_tdCanCopy`는 '함께 할일'만 예외, 소유자 무관)/삭제 가능했음(RLS도 동일하게 허용) — 사용자가 일정과 함께 겪었던 문제로 착각했을 가능성. index.html 변경 → 재빌드 필요.
- 🐛 **방 프로필: 테마만 바꿔도 씰 색이 같이 저장되던 버그 수정** (2026-07-27, 사용자 실기기 "테마 색상만 변경했는데 프로필씰 색상도 변경되고있어") — 방금 코드 항목(방 프로필 리셋 3종 수정)에서 `_sealCustomPicked` 플래그를 도입해 **미리보기**는 고쳤지만, 실제 **저장 로직**(`_autoSaveMascot()`/`saveRoomMascot()`)이 여전히 `sealVal=_sealSet?('set:'+_sealSet):(_sealColor+':'+_sealSym)`로 **무조건** 씰 값을 계산해 저장하고 있었음 — '기본 테마' 카드 배경(예: 라벤더)만 클릭해도(씰은 안 건드렸는데도) `_sealColor/_sealSym`의 기본값('terra'/'ini')이 씰로 함께 저장돼, "기본으로 되돌리기"로 지워둔 씰이 테마 선택 한 번에 다시 채워짐(방 이름이 이모지면 그 이모지가 '이니셜'로 보여 색깔 있는 씰처럼 보임). **수정**: 두 함수 모두 `sealVal=_sealSet?('set:'+_sealSet):(_sealCustomPicked?(_sealColor+':'+_sealSym):null)`로 — 사용자가 색/문양을 실제로 클릭했을 때만 씰을 저장, 테마/카드배경만 바꾸면 씰은 `null` 유지. **실데이터 정정**: 관리자 계정(`3bc13a8b-...`)의 ❤️방(`X9NN8E`, room_id `fb748ce4-0e25-4041-b59e-8198ab9d868b`)이 이 버그로 `seal='terra:ini'`+`card_skin='t_lavender'`로 저장돼 있던 것을 SQL로 `seal=null`만 정정(card_skin은 사용자가 실제로 원한 라벤더라 유지). index.html 변경 → 재빌드 필요. **✅ WebView는 deploy_version을 `2026-08-09 15:46:29`로 재갱신해 이미 일반 사용자에게 반영됨.**
- 🔢 **새 빌드용 버전코드 하한 각 +1** (2026-08-09, 사용자 "이제 새빌드하게 버전코드 하나씩 올려줘") — 마케팅 버전(1.1.6)은 그대로 유지, 버전코드(빌드번호)만 상향. appstore `codemagic.yaml` — iOS 빌드번호 하한 `35`→`36`(`NEXT -lt 36`), Android `versionCode` 하한 `80`→`81`(`VC -lt 81`). buildInfo(index.html 정적+런타임 두 곳)도 `1.1.6 (73)`→`1.1.6 (74)`. 이 시점까지 신규 반영분: 방 프로필 리셋 3종 버그(민트 잔존·크림색·씰 미복귀)·테마-씰 동시저장 버그·일정/할일 방 멤버 전체 수정·복사·삭제 허용·위젯 빨간날(공휴일) 빨간 숫자+빨간 바·위젯 프리미엄 씰 무료전환 게이트·위젯 추가화면 미리보기 이미지·위젯 오늘칸 정렬(이전 커밋에서 이미 반영, 이번 빌드로 실기기 확인 가능). index.html+codemagic.yaml 변경 → 재빌드 필요.
- 🔢 **마케팅 버전 1.1.6 → 1.1.7** (2026-08-09, 사용자 "1.1.7로 올리고 프로모션 텍스트·업그레이드 사항·출시명·출시노트도 같이 작성해줘") — appstore `codemagic.yaml`(main+ios-widget 두 브랜치) `APP_VERSION`·`ANDROID_VERSION_NAME` 기본값 `1.1.6`→`1.1.7`. buildInfo(index.html) `1.1.6 (74)`→`1.1.7 (75)`. 버전코드 하한(iOS 36·Android 81)은 직전 항목에서 이미 올려둔 값 그대로 유지(마케팅 버전만 별개로 증가, 코드 자체는 계속 오름차순이라 재조정 불필요). 출시명/프로모션 텍스트/업그레이드 사항/출시노트는 채팅으로 작성해 전달(파일 미저장) — 표준 규칙(⚠️ 출시 자료 자동 작성 규칙)에 따라 다음 빌드 때도 마지막 실제 출시(1.1.4=28) 이후 누적분 기준으로 자동 갱신. index.html+codemagic.yaml 변경 → 재빌드 필요.
- 🐛 **갤럭시 큰 글씨 설정에서 월 헤더 겹침 + 공휴일 라벨 잘림 수정** (2026-08-09, 사용자 실기기 "사용자 글자크기가 너무 커서 추석 글자가 잘려 보이고 ›버튼이랑 이미지 생성버튼이랑 겹쳐 보여") — 안드로이드 접근성 '글자 크기 크게' 설정은 WebView 텍스트를 전체적으로 확대하는데(레이아웃 폭은 그대로), 두 곳이 이걸 못 버텼음. (1) **월 헤더(‹ 연월 › + 우측 공유/가져오기/오늘 버튼) 겹침**: `.cal-month-left`/`.cal-month-right`가 `position:absolute` 오버레이라 `‹ 연월 ›`(가운데 flex 그룹, `justify-content:center`)이 글자가 커지며 넓어지면 고정된 좌우 아이콘과 그냥 겹쳐 그려졌음. **수정**: `.cal-month-row`를 `display:grid;grid-template-columns:auto 1fr auto`(좌/중앙/우 3열)로 전환, `‹ .cal-month-title ›`를 새 `.cal-month-center` 래퍼로 감싸 가운데 열(`1fr`)에 배치 + `min-width:0;overflow:hidden`+연/월 텍스트에 `text-overflow:ellipsis`로 넘치면 겹치는 대신 스스로 줄어들게. 좌우 버튼 그룹은 `grid-column:1/3`으로 항상 자기 칸에 고정돼 절대 침범 안 됨(`.cal-month-left`엔 씰 팝업이 얹혀 있어 `position:relative`는 유지, `position:absolute`만 제거). (2) **공휴일 라벨('추석' 등) 세로 잘림**: `.cal-cell-label`이 `height:11px;line-height:11px` 고정 px였는데, 글자가 커지면 그 박스보다 글자가 커져 위아래가 잘려 알아볼 수 없는 모양이 됐음(기간 일정 바 정렬을 위해 라벨 유무 무관하게 높이를 고정해야 했던 이유는 유지하되) → `height:1.375em;line-height:1.375em`(em 단위, 기존 11px/8px 비율)로 전환 — 폰트가 커지면 박스도 같이 커져 안 잘리고, 같은 렌더에서는 모든 셀이 동일 배율이라 정렬도 그대로 유지됨. Playwright로 1.0x/1.3x/1.6x 텍스트 확대 3단계 렌더 비교 검증(헤더 안 겹침·라벨 안 잘림 확인). index.html 변경 → 재빌드 필요.
- 🚧 **[초안] 반복 일정/할일 '종료일' 설정 기능** (2026-08-10, 사용자 "반복일정기능에 언제까지 할건지 설정할 수 있는 기능을 만들고 싶어 pwa로 확인하게 초안 만들어서 머지시켜줘") — 기존 반복(매주/매월/매년)은 끝없이 반복되기만 했음. **DB**: `events`·`todos`에 `repeat_until date`(nullable) 컬럼 추가(마이그레이션 `add_repeat_until_columns`, 라이브). **UI**: 일정/할일 폼의 반복 상세 영역(`#evRepeatDetail`/`#tdRepeatDetail`) 아래에 신규 `#evUntilWrap`/`#tdUntilWrap` — 반복이 '안 함'이 아니면(매주/매월/매년 공통) 표시되는 `<input type="date">`+'지우기' 버튼(비우면 계속 반복). 상태 `_evRepeatUntil`/`_tdRepeatUntil`, 핸들러 `onEvUntilDateChange`/`onTdUntilDateChange`, 동기화 `_syncEvUntilUI`/`_syncTdUntilUI`(반복 종류 전환 시 `syncEvRepeatUI`/`_syncTdRepeatUI`가 함께 호출). **저장**: `getEvRepeatPayload()`가 `repeat_until` 반환(시작일보다 이르면 무효화→계속 반복으로 폴백), `_doSaveEvent`/`saveEditedEvent`의 **모든 이벤트 insert/update 호출 16곳**에 `repeat_until:repeatUntil` 스레딩(전부 `repeat_nth_week:repeatNthWeek,` 뒤에 일괄 추가). 할일은 `_doSaveTodo`의 **공유 `fields` 객체 한 곳**에만 추가(모든 저장 경로가 공유라 한 곳으로 충분). **펼치기(발생일 계산)**: `_getDayEventsRaw`(달력 렌더)·`todoOccursOnDate`(할일)·`_wgExpandEvent`(위젯 payload, 이벤트+할일 공용)에 컷오프 필터 추가(`date>repeat_until`이면 그 발생 제외, 종료일 당일은 포함). 위젯 payload용 `_evCols`/`_tdCols` select 목록에도 `repeat_until` 추가. **편집 로드**: `openEditEvent`/`openTodoEditSheet`가 `ev.repeat_until`/`t.repeat_until`을 상태로 복원. Node 격리 테스트 17종 검증(주/월/년 컷오프 경계값·컷오프 없을 때 무회귀·할일 매주/매월N째주 컷오프). ⚠️ **초안 범위**: 위젯 네이티브 UI(iOS/Android) 표시 자체는 미변경(payload 필터링만 적용돼 컷오프 이후 발생은 위젯에도 자동으로 안 보임 — 별도 네이티브 작업 불필요), 일정 복사(`copyEventToRooms` 등)·가져오기(`_confirmImport`) 경로는 `repeat_until` 미전파(스코프 밖, 후속 필요 시 추가). index.html 변경 + DB 마이그레이션 라이브 → **deploy_version 갱신 시 PWA 즉시 반영**(재빌드는 네이티브 위젯 미변경이라 급하지 않음, 다음 정기 빌드에 포함해도 무방). **→ 종료일 선택 UI를 앱 컨셉에 맞게 리디자인(2026-08-11, 사용자 "반복일정 종료일을 우리캘린더 컨셉과 맞게 만들어줘")**: 네이티브 `<input type="date">`가 iOS/Android OS 기본 다크·휠 피커라 앱의 크림톤 빈티지 UI와 이질감이 커서 교체. `#evUntilDate`/`#tdUntilDate` input을 제거하고, **기존 '하루 모드' 미니 달력(`renderDayCal`/`ev-multi-*` 클래스, 크림 카드+세리프 월타이틀+일요일 빨강)을 그대로 재사용**한 토글형 팝다운으로 교체 — `#evUntilBtn`/`#tdUntilBtn`(`.fi` 스타일 버튼, 미선택 시 "종료일 선택 안 함 · 탭해서 지정"/선택 시 "8월 25일 화요일까지" 굵게)을 탭하면 바로 아래 `#evUntilCal`/`#tdUntilCal`(`.ev-multi-cal` 카드: `‹ 2026년 8월 ›` 네비+요일헤더+날짜그리드)이 펼쳐짐, 날짜 탭 시 즉시 선택+접힘(`pickEvUntilDate`/`pickTdUntilDate`). 신규 함수 `toggleEvUntilCal`/`evUntilCalNav`/`renderEvUntilCal`(+ td 대응 3종), 상태 `_evUntilCalDate`/`_tdUntilCalDate`(달력이 보여주는 달 커서, `_syncEvUntilUI`/`_syncTdUntilUI`가 매번 선택값 또는 기준일 기준으로 재계산). 반복종류 전환·폼 재오픈 시 `_syncEvUntilUI`/`_syncTdUntilUI`가 팝다운을 항상 접어둠(스테일 상태 방지). 저장 로직(`getEvRepeatPayload`/`_doSaveTodo` fields)·컷오프 펼치기 로직은 무변경(순수 UI 교체). 옛 `#evUntilHint`/`#tdUntilHint`(중복 텍스트)는 버튼 라벨이 날짜를 직접 보여주므로 제거. Node로 JS 문법 검증(`new Function`, 3개 스크립트 블록 전부 OK) + Playwright로 렌더링 시각 확인(크림 카드·세리프 타이틀·오늘 테두리·선택일 accent 채움, 기존 하루모드 달력과 동일 톤). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **→ 미니 달력 팝다운 → 년/월/일 드롭다운으로 재교체(2026-08-11, 사용자 "미니캘린더를 하나 더 만들지말고 드롭다운형태로 년,월,일로 정할 수 있게 해줘")**: 방금 만든 `ev-multi-cal` 팝다운도 "새 미니 달력을 하나 더 만드는 것"이라는 사용자 지적 → **생일 선택(`uniBdMonth`/`uniBdDay`, `.fi.pf-bd-select` 커스텀 화살표 드롭다운) 컴포넌트를 그대로 재사용**해 년/월/일 3단 드롭다운으로 전면 교체. `#evUntilBtn`/`#evUntilCal`/`#tdUntilBtn`/`#tdUntilCal`(토글+미니달력) 마크업·함수(`toggleEvUntilCal`/`evUntilCalNav`/`renderEvUntilCal`/`pickEvUntilDate`+td 대응 3종, `_evUntilCalDate`/`_tdUntilCalDate` 상태) 전부 삭제. 새 마크업=`#evUntilY`(년, JS로 옵션 채움)+`#evUntilM`(월, 1~12 정적 옵션)+`#evUntilD`(일, JS로 옵션 채움)+지우기 버튼, 그 아래 `#evUntilHint`(예: "8월 25일 화요일까지 반복돼요")로 부활(드롭다운 3개라 선택 요약이 다시 필요해짐). 신규 함수: `_fillEvUntilYear()`(기준일 연도~+6년 범위, 저장된 종료일 연도가 범위 밖이면 확장), `_fillEvUntilDay(y,m)`(년/월 선택 시 **실제 그 달 말일까지만**(윤년 2월 등 정확 반영) 옵션 재생성, 미선택 시 31일 폴백), `onEvUntilYMDChange()`(세 셀렉트 중 하나라도 바뀌면 일 옵션 재계산+년월일 모두 채워졌을 때만 `_evRepeatUntil` 조합, 하나라도 비면 null=계속 반복), `clearEvUntil()`(세 셀렉트 리셋). `_syncEvUntilUI()`가 반복종류 전환/폼 열릴 때 년 옵션 재생성+저장값 있으면 세 셀렉트에 분해해 채움. td 쪽도 동일 구조(`_fillTdUntilYear`/`_fillTdUntilDay`/`onTdUntilYMDChange`/`clearTdUntil`, 기준일=`_tdBaseDateObj()`). 저장/컷오프 로직(`getEvRepeatPayload`/`_doSaveTodo` fields/발생일 필터)은 무변경. Node로 JS 문법 검증(3개 스크립트 블록 OK, 옛 함수/id 잔여 참조 0건 확인) + Playwright로 렌더링 시각 확인(생일 선택과 동일한 화살표 스타일 드롭다운 3개+지우기 버튼+요약 힌트 텍스트). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **→ 네이티브 select를 앱 자체 드롭다운으로 교체 + 년 범위 확대(2026-08-11, 사용자 "이것도 앱디자인과 동일하게 적용시키고 32년까지만 표시가되는대 더 먼 미래까지... 실제 날짜와 동일하게 적용시켜줘")**: 직전 버전은 `<select>`(네이티브 폼 컨트롤)라 iOS/Android에서 여닫을 때 OS 기본 팝업(다크 배경·흰 체크마크)이 뜨고 앱 CSS로 스타일링이 안 됨(스크린샷으로 확인) — `.fi.pf-bd-select`는 닫힌 상태 버튼만 꾸며줬을 뿐, 펼쳐지는 목록 자체는 항상 OS 네이티브라 방법이 없었음. **`<select>`를 완전히 버리고 앱 자체 드롭다운으로 교체**: 방 전환 드롭다운(`openRoomSwitcher`)이 이미 쓰던 `.rs-dropdown`/`.rs-dropdown-overlay`(크림 카드+그림자+외부탭 닫기)를 재사용하는 **공용 팝업 `#ymdSelOverlay`/`#ymdSelDropdown`**(body 최상단, roomSwitcher 마크업 옆에 배치) 신설. 행 스타일은 `.rs-row`를 그대로 쓰지 않고 **전용 `.ymd-row`/`.ymd-row-name`/`.ymd-row-check` CSS 신규 작성** — `.rs-row`가 2624줄에서 다른 용도(방 설정 카드형 리스트)로 재정의되어 있어 그대로 재사용하면 패딩/배경이 섞여버림을 발견, 충돌 방지로 별도 클래스 사용(주석 명시). 버튼(`#evUntilY`/`M`/`D`, td 동일)을 탭하면 `_openYmdSel(btnId, items, curVal, onPick)`(공용, 일정·할일 6버튼이 콜백만 다르게 공유)가 버튼 바로 아래 그 값 목록을 크림 카드로 띄움 — 현재 선택값 행은 `--aP` 배경 강조+✓ 체크, 화면 밖으로 넘치면 자동 좌우/상하 클램프, 열릴 때 선택 행으로 자동 스크롤. 항목 탭 시 `_pickYmdSel(value)`→콜백 실행(`_evUntilY` 등 상태 갱신+`_applyEvUntilYMD()`)→팝업 닫힘. **상태 저장 방식 전환**: `<select>.value` 대신 `_evUntilY`/`_evUntilM`/`_evUntilD`(문자열, td도 동일 변수 3개) 전역 상태로 관리, 버튼 텍스트는 `_renderEvUntilBtns()`가 갱신("2026년"/"8월"/"25일" 또는 미선택 플레이스홀더). 년/월을 바꿔 일(day)이 그 달에 없어지면(예: 2월 29일 선택 후 평년으로 변경) 말일로 자동 클램프. **년 범위 확대**: 기존 '+6년'(스크린샷의 2032 상한 원인)을 **'+50년'**으로 확대(`_evUntilYearRange`/`_tdUntilYearRange`, 저장값 연도가 범위 밖이면 자동 확장) — ⚠️ **참고로 이 계산엔 외부 데이터(네이버 등) 조회가 필요 없음**: 양력 미래 날짜는 그레고리력 산술(`new Date(y,m,0).getDate()` 등)만으로 몇 년이든 100% 정확하며(윤년 포함), 항상 기기의 실제 현재 시각(`new Date()`)을 기준점으로 계산하므로 이미 실제 달력과 정확히 일치함(앱이 KASI 데이터를 실제로 쓰는 곳은 음력 변환뿐 — `solarToLunarKR`, 여긴 양력 종료일이라 무관, 주석으로 명시). Node로 JS 문법 검증(3개 블록 OK, 옛 select/id 잔여 참조 0건) + Playwright로 렌더링 시각 확인(팝업이 앱 크림 카드+accent 체크마크로 뜸, 년 목록이 2076년까지 스크롤 가능). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **→ 팝업을 상자 기준 앵커링으로 재작업(2026-08-11, 사용자 "드롭다운이 년월일 상자기준 아래로 드롭다운되게 해줘")**: 직전 버전은 뷰포트 좌표(`getBoundingClientRect`+`position:fixed`)로 계산한 단일 공용 팝업(`#ymdSelOverlay`/`#ymdSelDropdown`)이라, 버튼 폭보다 넓은 `min-width` 등으로 상자와 어긋나 보일 여지가 있었음 — **각 년/월/일 버튼을 `position:relative` 박스로 감싸고, 그 안에 전용 `.ymd-pop`(`position:absolute;top:calc(100% + 6px);left:0;right:0`) 자식**을 둬서 뷰포트 좌표 계산 없이 항상 그 박스 바로 아래·같은 폭으로 뜨게 함(시트 슬라이드 애니메이션 등 transform 조상이 있어도 무관, 문서 흐름 기반이라 원천적으로 어긋날 수 없음). 일정·할일 폼 각 3버튼(`#evUntilY/M/D`, `#tdUntilY/M/D`)마다 자기 전용 `#evUntilYPop` 등 6개 팝업 div. 공용 로직 재작업: `_openYmdSel(btnId,...)`(뷰포트 계산+클램프) → **`_toggleYmdPop(popId,...)`**(popId로 해당 `.ymd-pop`을 직접 열고 닫기만, 클램프 로직 불필요해 삭제) + `_closeAllYmdPops()`(열려있는 팝업 전체 정리, 문서 최상위 `pointerdown` capture 리스너로 바깥 탭 시 자동 호출) + `_pickYmdPop(popId,value)`. 같은 상자를 다시 탭하면 토글로 닫힘. `_syncEvUntilUI`/`_syncTdUntilUI`가 반복종류 전환·폼 재오픈 시 `_closeAllYmdPops()` 호출(스테일 팝업 방지). 옛 전역 `#ymdSelOverlay`/`#ymdSelDropdown` 마크업 삭제(더 이상 필요 없음, `.rs-dropdown`류 재사용도 그만둠). `.ymd-pop{max-height:220px;overflow-y:auto;z-index:30}` 신규 CSS(`.ymd-row`류는 그대로 재사용). Node로 JS 문법 검증(3개 블록 OK, 옛 함수명 잔여 참조 0건) + Playwright로 렌더링 확인(년 팝업이 년 상자 바로 밑에, 폭도 그 상자와 동일하게 렌더). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영.

- 🕐 **시간 피커 시계 아이콘 — 시작/종료 대상 명시** (2026-08-11, 사용자 스크린샷 "이거 시계가 시작시간 선택인지 끝시간 선택인지 기준이 애매해 기준을 만들어줘") — 일정/할일 폼의 시간 입력(시작칸 `--:--` ~ 종료칸, 우측 시계 아이콘)에서 시계 아이콘이 어느 칸(시작/종료)을 편집하는지 **화면에 아무 표시도 없어서 애매했던 문제**. 내부적으로 `_activeTimeField`(일정)/`_todoActiveTimeField`(할일) 상태가 이미 있었지만, 이건 실제 입력칸(`evStartHour` 등 28px짜리 숫자 텍스트)에 **정확히 포커스가 가야만** 갱신되고 화면엔 어떤 표시도 없었음(휠을 시계로 직접 열면 어느 칸인지 전혀 안 보임 — 스크린샷의 근본 원인). **수정**: (1) **라벨 신설** `#tpTargetLabel`/`#todoTpTargetLabel`(휠 카드 맨 위, "시작 시간"/"종료 시간" accent 색 텍스트) — 시계를 열 때마다, 그리고 열려있는 동안 다른 칸으로 전환할 때마다 갱신. (2) **활성 칸 테두리 강조 유지** — 기존엔 실제 키보드 포커스가 있을 때만 `.focused`(accent 테두리)가 붙었는데, 시계로 열었을 땐(키보드 없이) 강조가 전혀 안 보였음 → `toggleTimePicker`/`toggleTodoTimePicker`가 열 때 활성 칸에 `.focused`를 명시적으로 부여(신규 `_syncEvTpTarget`/`_syncTodoTpTarget`, 라벨 갱신도 겸함). (3) **상자 어디를 눌러도 그 칸이 활성화** — 기존엔 정확히 숫자 위(`.time-input`, 폭 28px)만 눌러야 포커스가 잡혀서, 콜론이나 여백을 누르면 반응이 없어 사용자가 "안 눌린다"고 느꼈을 수 있음 → 상자 전체(`.time-input-box`)에 `onclick="_evTimeBoxTap(event,'start'/'end')"`(td는 `_todoTimeBoxTap`) 추가 — 입력칸 직접 탭은 기존 포커스 이벤트가 처리하고, 그 외 여백 탭이면 시(hour) 입력칸에 프로그램적으로 focus. (4) **휠이 열린 채로 다른 칸을 탭하면 실시간 전환** — `bindEvTimeInputs`/`bindTodoTimeInputs`의 focus 리스너가 `_syncEvTpTarget()`/`_syncTodoTpTarget()`을 호출해, 라벨·테두리·휠 스크롤 위치(그 칸의 현재 값)까지 즉시 재동기화. (5) 시계 닫을 때(`closeTimePicker`/`closeTodoTimePicker`) 강조 테두리 정리. **규칙(사용자에게 보이는 것)**: 상자를 탭하면 그 칸이 파란(accent) 테두리로 활성화되고, 시계를 열면 그 칸 이름이 휠 위에 "시작 시간"/"종료 시간"으로 항상 표시됨 — 어느 칸을 편집 중인지 더 이상 추측할 필요 없음. Playwright로 실제 index.html에서 추출한 코드로 상호작용 검증(여백 탭→활성 전환·라벨 텍스트·휠 초기값이 그 칸 값과 일치·열린 채 다른 칸 탭 시 재동기화·닫으면 강조 해제). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **→ 휠이 열려있는 동안 OS 키보드 동시 노출 방지(2026-08-11, 사용자 스크린샷 "만약 시계가 활성화 되어있을땐 키보드가 안 뜨게 해줘")**: 직전 수정이 상자 여백 탭 시 `.focus()`를 호출하도록 만든 부작용 — 휠이 이미 열린 상태에서 다른 칸(예: 시작칸)을 탭하면 실제 `<input>`이 포커스되어 **iOS 숫자 키보드가 휠 위에 겹쳐 뜸**(스크린샷 확인, 시작 칸 커서 깜빡임+하단 키패드). **수정**: 시계 아이콘으로 휠을 열 때 `#evStartHour/Min`·`#evEndHour/Min`(td 동일) 4개 입력칸에 **`readOnly=true`** 부여(`_setEvTimeInputsReadonly`/`_setTodoTimeInputsReadonly`), 휠을 닫을 때(`closeTimePicker`/`closeTodoTimePicker`) 다시 `false`로 복원(직접 타이핑 모드는 그대로 유지). readonly 입력은 탭하면 `focus` 이벤트는 정상 발생(기존 활성칸 전환·라벨·테두리·휠 재동기화 로직 무영향)하지만 **모바일 브라우저가 가상 키보드는 띄우지 않는 표준 동작**을 이용 — 별도 분기 로직 없이 한 줄짜리 토글로 해결. 추가로 `toggleTimePicker`/`toggleTodoTimePicker`가 휠을 여는 순간 **직접입력 중이던 포커스가 있으면 우선 `blur()`**(키보드가 이미 떠 있는 상태에서 시계를 눌렀을 때도 즉시 내려가도록, 일부 브라우저는 `readOnly` 전환만으로 이미 떠 있는 키보드를 안 내릴 수 있어 보험 처리). Playwright로 검증(휠 열기 전 readOnly=false·연 후 4칸 모두 true·휠 열린 채 다른 칸 탭해도 readOnly 유지+활성칸 전환 정상·닫으면 다시 false). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영.

- 🚨 **[중대 발견 · 부분수정] iOS 네이티브 앱 푸시 — 클라이언트가 처음부터 웹푸시 경로로 잘못 라우팅됨** (2026-08-11, 사용자가 실제 App Store 다운로드 앱에서 "이 브라우저는 알림을 지원하지 않아요" 토스트 확인 후 "저거 앱스토어에서 다운받은건대??") — **원인**: `getPushStatus()`/`autoRegisterPush()`/`_pushAskEligible()`/`maybeAskPush()`/알림설정 토글/`ntfEnablePush()` 총 6곳이 전부 `isAndroidNative()`로 분기해 **iOS는 네이티브 앱이어도 항상 웹푸시(`PushManager`) 경로**로 빠졌음(주석: "iOS는 이미 알림이 정상 동작하므로 건드리지 않는다" — 확인 안 된 잘못된 가정). Capacitor iOS의 WKWebView는 `PushManager`(Web Push API)를 지원하지 않음(Safari 홈화면 PWA 전용 기능, 서드파티 앱에 내장된 WKWebView는 미지원) → `getPushStatus()`가 항상 `'unsupported'` 반환 → 토글 켜면 정확히 이 토스트가 뜸. **증거**: `fcm_tokens` 테이블이 안드로이드 22개뿐(코드 49 진단, iOS 0개) — iOS가 애초에 이 경로를 탄 적이 없었다는 방증. **1차 수정(완료, index.html)**: 6개 지점 전부 `isAndroidNative()`→`isNativeApp()`으로 변경, iOS 네이티브 앱도 `registerNativePush()`(Capacitor `PushNotifications` 플러그인, `PN.requestPermissions()`+`PN.register()`)로 라우팅. `_ensurePushChannel()`(안드로이드 알림채널 생성)은 내부에서 이미 `_nativePlatform()!=='android'`면 skip해 iOS에 안전. 플랫폼별 안내 문구 분기(`isAnd`, 설정→알림 경로 텍스트)는 그대로 안드로이드 전용 유지(무관한 변경 아님). **⚠️ 2차 갭(미해결, 후속 필요)**: `registerNativePush()`가 iOS에서 실제로 받는 토큰은 **원본 APNs 디바이스 토큰**(hex)이지 FCM 토큰이 아님 — `send-push`/`push-cron`/`admin-broadcast`(백업 소스 확인) 발송 로직이 전부 **FCM v1 API**(`fcm.googleapis.com/v1/.../messages:send`)로 `fcm_tokens.token`을 그대로 보내는데, 이건 FCM 등록토큰 형식을 기대하므로 **원본 APNs 토큰을 넣으면 발송 실패**함. iOS 네이티브 프로젝트(`ios/App/App/AppDelegate.swift`) 확인 결과 Firebase iOS SDK 연동이 전혀 없음(`GoogleService-Info.plist` 없음, `import FirebaseMessaging` 없음, APNs↔FCM 브리징 코드 없음) — 즉 **1차 수정만으론 iOS에서 권한 요청 팝업은 정상적으로 뜨지만, 실제 푸시 메시지 발송(전달)까지는 아직 안 됨**. 완전한 해결엔 iOS 네이티브 쪽에 Firebase Messaging 연동이 추가로 필요(`@capacitor-firebase/messaging` 플러그인 또는 Firebase iOS SDK pod 추가 + `GoogleService-Info.plist`(Firebase 콘솔에서 iOS 앱 등록 후 다운로드 필요, 계정 소유자만 가능) + AppDelegate 브리징 코드 + Xcode Push Notifications capability 확인 + 이미 있는 `AuthKey_APNs_푸시.p8.p8`을 Firebase 콘솔 Cloud Messaging의 Apple 앱 설정에 업로드). 이 작업은 Xcode/Firebase 콘솔 접근이 필요해 이번 세션에서 미완료 — 사용자가 진행 원하면 GoogleService-Info.plist 확보부터 안내 예정. index.html 변경(1차 수정) → deploy_version 갱신 시 네이티브 앱도 SW 업데이트로 즉시 반영(별도 IPA 재빌드 불필요, JS 전용 변경이라). **→ 2차 갭(Firebase iOS 연동) 코드/파이프라인 준비 완료(2026-08-11, 사용자 "그럼 앱스토어 네이티브 앱도 되게 고쳐줘")**: iOS에서 실제로 FCM 호환 토큰을 받으려면 네이티브 프로젝트에 Firebase Messaging 연동이 필요한데, ⚠️ **`ios/`(App.xcodeproj·AppDelegate.swift 등)는 Codemagic이 `rm -rf ios && npx cap add ios --packagemanager CocoaPods`로 매 빌드 통째로 재생성**하므로 그 안의 파일을 직접 커밋해봐야 다음 빌드에서 사라짐(체크인된 `ios/App/App/AppDelegate.swift` 등은 실제 빌드에 안 쓰이는 죽은 사본, `CapApp-SPM/Package.swift`도 마찬가지 — 실빌드는 CocoaPods 기반인데 체크인본은 옛 SPM 스캐폴딩). google-services.json(안드로이드)이 이미 **repo 루트에 커밋 + 빌드 스크립트가 `cap add` 뒤에 복사**하는 패턴으로 이 문제를 해결해뒀길래 iOS도 동일 패턴 적용. **패키지**: `@capacitor-firebase/messaging`+`firebase` npm 의존성 추가(package.json) — 이 플러그인은 iOS에서 APNs 토큰을 Firebase가 자동으로 FCM 토큰으로 변환해줌(플러그인 공식 문서 fetch로 API 확인: `checkPermissions/requestPermissions`는 기존 `PushNotifications`와 동일하게 `{receive:...}` 형태라 그대로 호환, `getToken()`이 토큰을 직접 반환(안드로이드처럼 `register()`+`'registration'` 이벤트 비동기 수신이 아님), 이벤트명은 `tokenReceived`/`notificationActionPerformed`/`notificationReceived`로 다름). **신규 파일(repo 루트, `ios/` 밖이라 재생성에서 안전)**: `ios-native/AppDelegate.swift`(플러그인이 요구하는 APNs 콜백 3종 — `didRegisterForRemoteNotificationsWithDeviceToken`/`didFailToRegisterForRemoteNotificationsWithError`/`didReceiveRemoteNotification`, 공식 setup 문서 그대로), `ios-native/add_google_service_info.rb`(`xcodeproj` gem으로 GoogleService-Info.plist를 App 타깃 Copy Bundle Resources에 등록 — 안 하면 파일을 넣어도 앱 번들에 안 들어가 런타임에 Firebase가 설정을 못 찾음, `widget/ios/add_widget_target.rb`와 동일 패턴). **codemagic.yaml**: `ios-compile-check`·`ios-testflight` 두 워크플로 모두 `cap add ios`/`cap sync ios` 직후에 새 스텝 "Firebase Messaging 설정" 추가 — **repo 루트에 `GoogleService-Info.plist`가 없으면 경고만 찍고 그레이스풀 스킵**(google-services.json처럼 강제 실패시키지 않음 — 파일 준비 전에도 나머지 빌드가 안 막히게), 있으면 `ios/App/App/`로 복사 + `ios-native/AppDelegate.swift` 덮어쓰기 + `Info.plist`에 `FirebaseMessagingAutoInitEnabled=NO`(수동 `getToken()` 호출 시에만 발급, 기존 소프트 권한요청 UX와 일치) + ruby 스크립트로 리소스 등록. **JS(index.html)**: `_getPushPlugin()`이 플랫폼별로 다른 네이티브 객체 반환(`_nativePlatform()==='ios'`→`Capacitor.Plugins.FirebaseMessaging`, 그 외→기존 `PushNotifications`), `_setupNativePushListeners()`가 플랫폼별 이벤트명 분기, `registerNativePush()`가 iOS는 권한 승인 후 `PN.getToken()`으로 토큰을 직접 받아 저장(안드로이드는 기존 `PN.register()`+리스너 유지). `_ensurePushChannel()`은 기존 `_nativePlatform()!=='android'` 가드로 iOS 안전(무변경). **⚠️ YAML 삽질 기록(재현 방지용)**: 처음엔 codemagic.yaml의 `script: \|` 블록 안에 bash heredoc(`cat > file << 'SWIFT' ...`)으로 Swift 코드를 직접 박아 넣었다가 **YAML 파싱 실패**(literal block scalar는 모든 줄이 최소 들여쓰기를 유지해야 하는데 heredoc 본문은 원래 들여쓰기 없이 써야 코드가 깨끗하고, bash heredoc 종료자(`SWIFT`)는 반대로 들여쓰기가 있으면 안 됨(`<<-`도 탭만 벗겨내고 스페이스는 안 됨, 게다가 YAML 자체가 블록 들여쓰기에 탭 사용을 금지) — 구조적으로 양립 불가). PyYAML로 직접 재현·검증 후 **heredoc을 repo 파일(`ios-native/*`)+`cp`/`ruby 파일경로`로 교체**해 해결(위 "신규 파일" 참고). ⚠️ **미완료(사용자 액션 필요, 계정 소유자만 가능)**: (1) Firebase 콘솔에서 `uri-calendar` 프로젝트에 iOS 앱 추가(번들 ID `com.lsung.uricalendar`) → `GoogleService-Info.plist` 다운로드 → **appstore repo 루트**에 커밋(`google-services.json`과 같은 위치), (2) 이미 있는 `AuthKey_APNs_푸시.p8.p8`을 Firebase 콘솔 Cloud Messaging → Apple 앱 구성에 업로드(Key ID·Team ID 필요). 이 두 가지가 끝나야 실제 iOS 푸시 전달이 완성됨. **검증 범위**: JS 문법(`new Function`)·YAML 파싱(PyYAML)·Ruby 문법(`ruby -c`)·Swift 중괄호 균형 확인함. **Xcode 실컴파일은 미검증**(Mac 환경 없음) — GoogleService-Info.plist 없이도 `ios-compile-check` 워크플로가 통과하는지 먼저 돌려보길 권장(그레이스풀 스킵 경로 검증), 파일 준비 후 재실행하면 전체 경로 검증됨. index.html+appstore(package.json·codemagic.yaml·ios-native/) 변경 → deploy_version 갱신(JS)+다음 iOS 빌드(네이티브) 둘 다 필요.

- 🐛 **기간(range) 일정 + 반복 조합 시 중복 표시 버그 수정** (2026-08-12, 사용자가 카톡 고객 피드백 스크린샷 4장 제시 후 "기간일정을 반복으로 하면 일정생성이 첫날로 다 몰려버려 · 고객 요구사항이야 한번 봐바") — **원인**: 기간(며칠짜리) 일정은 DB에 **날짜마다 별도 물리 row**가 생성되고(`range_group_id`로 묶어 연속 바 렌더), 반복 저장 로직이 이 **모든 날짜 row에 동일한 `repeat_type`/`repeat_weekdays`(등) 값을 그대로 복제**해왔음. `loadEvents()`의 반복 인덱싱(`_weeklyEvents[dow]` 등)은 row 단위로 독립 origin을 만들기 때문에, 예를 들어 월~금 5일 기간 일정에 "매주 월~금" 반복을 걸면 **origin이 5개가 생겨** 다음 주 같은 요일 칸에 5개가 겹쳐 뜸(고객이 본 "일정생성이 첫날로 다 몰림"의 실제 모습 — 여러 개가 겹쳐 쌓여 보임). **수정**: 신규 헬퍼 `_rangeRepeatFields(d, firstD, repeatType, repeatWeekdays, repeatMonthlyMode, repeatNthWeek, repeatUntil, recurringYearly)`(`getEvRepeatPayload()` 바로 아래, ~line 11416) — **기간의 첫 날짜에만** 반복 필드를 그대로 두고, 나머지 날짜는 `repeat_type:'none'`(+관련 필드 전부 null)로 저장해 **origin이 항상 하나**가 되게 함. `Object.assign(...,_rangeRepeatFields(...))` 패턴으로 **총 7곳** 적용: `_doSaveEvent()`의 여러 날(multi, 불연속) 신규 생성(본인 방+다른 방 2곳)·기간(range) 신규 생성(본인 방+다른 방 2곳), `saveEditedEvent()`의 함께+기간 재생성·혼자→함께 전환(기간이면 전체 날짜)·단독 일정의 range/multi 모드 재생성. **할일(todos)은 무관**(기간 할일이 날짜마다 별도 row가 아니라 `start_date`/`end_date` 필드를 가진 **단일 row**라 애초에 이 중복 메커니즘이 발생할 수 없음 — 확인만 하고 변경 없음). Node 격리 테스트로 검증(월~금 5일+매주 월~금 반복 시나리오: 첫날만 `repeat_type='weekly'` 유지+나머지 4일 `'none'`, `loadEvents` 방식으로 시뮬레이션한 반복 인덱스에서 다음 주 화요일 origin이 정확히 1개, 비기간 단일 반복 일정은 기존 그대로 동작). ⚠️ **알려진 제약(고객에게 안내 필요)**: 이 수정은 **중복 버그만 제거**함 — 기간 전체(며칠 폭)가 통째로 매주/매월 반복되는 게 아니라 **기간의 첫날 하루만** 계속 반복됨(즉 "월~금 5일짜리 일정이 매주 반복"이 아니라 "월요일 하루가 매주 반복"이 되어 화~금 4일분은 다음 주기부터 사라짐). **"기간 전체를 통째로 반복"은 별도 기능 설계가 필요**(예: 반복 origin 자체를 기간 메타데이터로 확장해 펼치기 로직에서 N일짜리 블록을 통째로 매 주기 생성하는 구조 변경) — 이번 수정 범위 밖, 후속 논의 필요. **미착수 요청 2건**(같은 스크린샷에서 확인, 이번엔 다루지 않음): (1) 반복 일정 편집 시 "이 일정만 변경"/"이후 일정도 변경" 범위 선택 UI, (2) 반복 6개월(180일) 상한 완화/제거 요청. index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 08:37:28`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

- ✅ **반복 일정 편집 범위 선택 "이 일정만 변경"/"이후 일정도 변경" 구현** (2026-08-13, 위 항목의 미착수 요청 (1) 적용 — 사용자 "1번 적용하고") — 기존엔 반복(매주/매월/매년, 기간·함께·다중방 아닌 단독 일정) 일정의 **가상 발생일(origin이 아닌 미래/과거 칸에 펼쳐 보이는 occurrence)** 을 수정하면 항상 **origin row 자체**를 직접 편집해, 결과적으로 과거·미래 모든 발생에 소급 적용됐음(개별 발생 편집이라는 개념 자체가 없었음). **구현**: `openEditEvent(eventId)`를 `async`로 전환 — 함수 진입 시 `_selectedDate`를 **rebase 전에** `viewDate`로 캡처(=사용자가 실제로 열었던 날짜 시트의 날짜, origin이 아닌 발생일일 수 있음), `isRecurringOcc`(반복 활성 && 함께 아님 && 기간(`range_group_id`) 아님 && 다중방(`multi_group_id`) 아님 && `viewDate!==ev.date`)이면 **`askRecurringDelete()`를 범위선택 다이얼로그로 재사용**(신규 `opts` 파라미터로 제목/버튼 라벨 오버라이드 — `{title:'반복 일정 수정', oneLabel:'이 일정만 변경', allLabel:'이후 일정도 변경'}`, 기존 삭제 호출은 `opts` 생략이라 기본 라벨 그대로 무영향) → 선택값을 `_editOccurrenceScope`('one'|'all'|null, 신규 전역 상태, `_editGroup` 옆에 선언)에 저장, 취소하면 편집 폼 자체를 안 엶. 이후 `_selectedDate`는 스코프가 있으면 `viewDate`(발생일), 없으면 기존처럼 `ev.date`(origin)로 세팅 — 폼 헤더/미니달력이 사용자가 실제로 편집하려던 날짜를 정확히 보여줌. 스코프가 있으면 **기간/여러날 모드 전환 버튼(`evModeRow`) 숨김**(하루 모드 고정, 분리 편집이 range/multi까지 감당 못 함) + **'one'(이 일정만)은 반복 설정 행(`evRepeatRow`)도 숨김**(분리되는 단일 발생은 항상 비반복이라 무의미) — `evRepeatRow`는 매 편집마다 먼저 `display=''`로 리셋 후 조건부로 숨겨 이전 편집의 잔여 상태가 안 남게 함. **저장(`saveEditedEvent`)**: 함수 최상단에 `_editOccurrenceScope` 분기 신설(기존 Case1~4 로직보다 먼저 처리, 매칭 안 되면 기존 로직 그대로 통과) — **'one'**: origin의 `repeat_except`에 발생일(`_dayDate||_selectedDate`) 추가(반복 규칙 자체는 그대로 유지되어 다음 주기부터 계속 반복) + 같은 날짜에 **새 단독(비반복) row를 INSERT**(폼의 수정된 title/time/color 등 반영) — origin은 안 지우므로 향후 발생 계속 생성됨. **'all'**: origin의 `repeat_until`을 **발생일 전날**(`shiftDateKey(occDate,-1)`)로 잘라 그 이후로는 origin이 더 이상 발생을 만들지 않게 하고(기존 `repeat_until`이 이미 그보다 이르면 그 값 유지 — 시리즈를 도로 늘리지 않음), **새 origin row를 발생일에 INSERT**(폼의 수정된 필드 + 폼에서 현재 선택된 반복 패턴을 `getEvRepeatPayload(occDate)`로 재계산 — 편집 중 반복 패턴 자체를 바꿔도 반영됨) — 과거 발생은 옛 패턴 그대로, 이 날짜부터는 새 패턴/새 값으로 이어짐. 두 경로 모두 처리 후 `_editOccurrenceScope=null` 리셋+모달 닫기+재로드. `closeEventModal()`에도 `_editOccurrenceScope=null` 추가(저장 없이 취소해도 다음 편집에 스코프가 새지 않게). ⚠️ **범위 제한(의도적)**: 기간(range)·함께(shared)·다중방(multi_group_id) 일정은 이 기능 대상에서 제외(각각 별도의 복잡한 그룹 동기화 로직이 있어 지금 범위에 포함하면 리스크가 큼) — 이 셋은 기존처럼 origin을 직접 수정하면 전체(해당 로직 범위)에 적용됨, 변경 없음. Node 격리 테스트로 검증(트리거 조건 6종: 발생일≠origin 날짜에서만 트리거, 함께/기간/다중방/비반복 제외, 컷오프 날짜 계산, 기존 repeat_until 보존 로직). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영.

- 🐛 **기간(range) + 반복 일정 삭제 시 기간 전체가 한 번에 삭제되지 않던 문제 수정** (2026-08-13, 사용자 "반복일정으로 기간일정 생성하고 기간중 하루를 일정삭제하면 하루만 삭제 되고있어 기간반복일정 삭제시 해당기간일정 한번에 삭제시켜줘") — **원인**: 바로 위 항목(기간+반복 중복 버그 수정)에서 기간의 **첫 날짜(origin) row에만** 반복 필드를 남기도록 고쳤는데, `deleteEvent()`는 `ev.repeat_type!=='none'`이면 무조건 (기간인지 여부와 무관하게) **"이 날짜만/모두"** 라는 단일-행 전용 삭제 분기로 빠졌음 — 이 분기는 `range_group_id`를 전혀 몰라서 **origin(첫날)을 삭제해도 그 row 하나만** 지워지고 나머지 N-1일은 그대로 남았음(반대로 origin이 아닌 날을 클릭하면 `repeat_type==='none'`이라 이 분기를 안 타고 기존 기간-그룹 삭제 로직(`findEventGroup`)으로 갔어야 하지만, 실제로는 대부분 origin인 첫날부터 삭제를 시도하는 케이스가 많아 "하루만 삭제된다"는 증상으로 나타남). **수정**: `deleteEvent()` 최상단, 기존 반복 판정보다 **먼저** — `range_group_id`가 있으면 `findEventGroup(ev)`로 기간 전체 그룹을 모으고, 그 그룹 안에서 반복 필드를 가진 행(`rangeOrigin`)을 찾음(그룹을 날짜순 정렬해 찾으므로 신규 데이터든 옛 중복버그 데이터든 항상 가장 이른 날짜의 행이 선택돼 일관됨). **그룹 길이>1 && origin 존재**(=기간+반복 결합)면 새 전용 분기로: `askRecurringDelete()`에 커스텀 라벨(`'이번 기간만 삭제 (N일)'`/`'모든 반복 삭제'`)로 범위를 물음 — **"이번 기간만 삭제"**: origin을 제외한 나머지 N-1일 row를 DELETE + origin은 남기되 **자기 자신의 날짜를 `repeat_except`에 추가**(물리 row는 남지만 자기 날짜 표시는 숨겨짐 — `_getDayEventsRaw`가 물리 row에도 `repeat_except` 필터를 적용하는 것을 확인 후 활용) → 결과적으로 이번 기간의 N일이 전부 화면에서 사라지고, 반복 규칙 자체(및 origin row)는 살아있어 **다음 주기부터는 계속 반복됨**(사진 정리 없이 origin 사진은 유지, 나머지 날짜는 원래 같은 사진 URL을 공유하므로 스토리지 삭제는 하지 않음 — 안 그러면 origin의 사진까지 깨짐). **"모든 반복 삭제"**: 그룹 전체(origin 포함 N일)를 DELETE — origin이 사라지므로 향후 반복도 함께 종료됨(사진은 이 경로에서만 정리). 반복 없는 순수 기간 일정(플레인 range)은 `rangeOrigin`이 null이라 이 새 분기를 안 타고 **기존 로직 그대로**(무회귀) — 이미 정상 동작하던 "기간 일정 N일이 모두 삭제돼요" 확인 후 일괄 삭제. Node 격리 테스트로 검증(중간 날짜/첫 날짜 어느 쪽을 클릭해도 그룹 5일+origin을 정확히 인식, '이번 기간만'은 origin 제외 4개만 삭제 대상+origin이 자기 날짜를 exception 처리, '모든 반복'은 origin 포함 5개 전부, 플레인 기간은 새 분기 미적용 확인). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 09:03:48`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

- ✅ **기간(range)+반복 일정 — 매 주기 '기간 전체'가 다시 투영되도록 구현(핵심 기능 확장)** (2026-08-13, 사용자가 실기기 스크린샷 제시 후 "기간반복일정 이제 하루만 생성되고 있어 기간반복일정 생성시 모든 일정들이 기간으로 생성되어야해") — 위 두 항목(중복버그 수정·삭제 수정)에서 의도적으로 남겨뒀던 "알려진 제약"(기간의 첫날 하루만 반복됨)을 정식으로 해소. **아키텍처**: 기간 일정은 날짜마다 별도 물리 row가 있고 반복 필드는 origin(첫 날짜) row에만 있는데, 기존 반복 펼치기(`_getDayEventsRaw`)는 각 셀마다 "이 셀 하루가 패턴과 일치하는가"만 봤기 때문에 매 주기 **origin의 요일 하루만** 가상으로 나타났음(스크린샷 증상 — 9/7~12 원본 기간은 막대로 보이는데 9/14·21·28은 단독 칸). **구현**: (1) `loadEvents()`에 `_allRecurringOrigins` 배열 신설(반복 활성 origin 전체 수집) + 각 origin에 `_rangeSpanDays` 계산(같은 room에서 이미 로드된 전체 row 중 같은 `range_group_id`를 가진 물리 row 개수 — **추가 DB 조회나 새 컬럼 없이 기존 로드 데이터만으로 계산**, 기간 아닌/반복 없는 origin은 항상 1). (2) 신규 헬퍼 `_originMatchesDate(ev,d)` — 기존 5개 인덱스(`_yearlyEvents`/`_monthlyEvents`/`_weeklyEvents`/`_monthlyNthEvents`/`_monthlyLastEvents`)가 "이 셀 자신"에 대해서만 하던 패턴 매칭(연/월/주/N째주/말일)을 **임의 날짜**에 대해 재사용 가능하게 일반화. (3) `_findSpanContinuation(ev,date)` — spanDays>1인 origin에 대해 `date`가 어느 사이클의 몇 번째 날(offset 1~spanDays-1)인지, i=1부터 역방향으로 `_originMatchesDate`를 호출해 찾음(offset0=사이클 시작일은 기존 5개 인덱스가 이미 찾아주므로 제외 — **기존 로직 완전 보존**, 이 함수는 순수 추가). (4) `_getDayEventsRaw`가 기존 5개 인덱스 매핑을 `_mkRecurEntry(ev,date,offset,anchor)`로 감싸 — spanDays===1이면 `{...ev,_yearlyDisplay:true}`로 **완전히 기존과 동일**(무회귀 보장), spanDays>1이면 추가로 `range_group_id:'v:'+ev.id+':'+anchor`(사이클별 합성 그룹 id)·`_virtualSpanAnchor/_virtualSpanOffset/_virtualSpanLen`·`_isRangeEvent:true`·`_isFirstSegment`·`_rangePos`(`_spanRangePos` 헬퍼가 오프셋+spanDays로 물리 기간과 동일한 주 경계 wrap 규칙을 직접 계산 — 물리 기간처럼 인접 셀의 `_events[prevDate]` 조회로는 가상 사이클을 못 찾으므로 **오프셋 산술로 직접 계산**하는 방식 채택). 그리고 `continuationOnes`(=spanDays>1 origin마다 `_findSpanContinuation` 호출) 목록도 같은 `_mkRecurEntry`로 만들어 `evs`에 추가. **repeat_except/repeat_until 필터를 앵커 기준으로 전환**(`e._virtualSpanAnchor||date`) — spanDays>1이면 사이클 전체가 한 단위로 숨겨지고, spanDays===1은 `_virtualSpanAnchor`가 없어 기존처럼 `date` 기준(무회귀). (5) 물리 기간의 `_rangePos` 계산 루프(`result.forEach`, 인접 셀 물리조회 기반)에 `if(ev._virtualSpanAnchor)return;` 가드 추가 — 내가 이미 계산해둔 값을 덮어쓰지 않게. (6) `_eventBarHTML`의 "spill"(제목이 여러 칸에 걸쳐 보이는 CSS 트릭) 계산도 가상 사이클이면 인접 셀 조회 대신 `Math.min(spanDays, 7-dow)`로 직접 계산해 물리 기간과 동일한 시각 효과. (7) **레인 배치는 코드 변경 없이 자동으로 맞음** — `_eventLaneKey`가 `!ev._isRangeEvent`일 때만 날짜별 독립 레인을 쓰는데, 가상 사이클에 `_isRangeEvent:true`를 설정했으므로 자동으로 `_eventItemKey`(제목+시간+색+유저 기반) 하나의 레인 아이템으로 묶여 **사이클 전체가 같은 레인에서 이어지는 막대**로 패킹됨(다른 사이클끼리도 날짜가 안 겹치니 같은 레인 키를 공유해도 안전 — 오히려 주가 바뀌어도 막대가 같은 세로 위치를 유지하는 보너스 효과). **삭제 로직 확장(`deleteEvent`)**: 기존엔 클릭한 날짜가 origin의 실제 물리 그룹에 속하는지 확인 안 하고 무조건 `findEventGroup(ev)`(=원본 occurrence의 물리 row들)로 처리했는데, 신규 렌더로 **가상 사이클도 삭제 버튼이 뜨게 되므로** 이 경로로 잘못 흘러들면 엉뚱하게 몇 주 전 원본을 지우려 들 위험이 생김 → `occDateR`(현재 보고 있는 날짜)가 `rangeGroup`의 실제 물리 날짜에 포함되는지(`isOwnOccurrence`)로 분기: **포함되면**(원본 occurrence 자체를 삭제) 기존 로직 그대로. **포함 안 되면**(미래/과거로 투영된 가상 사이클을 삭제하려는 것) 신규 분기 — `_resolveVirtualCycleAnchor(rangeOrigin,occDateR)`로 이 사이클의 시작일을 역산해 **"이번 기간만 삭제"**(그 앵커를 origin의 `repeat_except`에 추가 — 물리 row는 원본 것 그대로 두고 다음 사이클부터 계속 반복)/**"모든 반복 삭제"**(원본 occurrence의 물리 row 전체를 삭제 — origin이 사라지므로 이 사이클을 포함한 향후 모든 발생이 함께 사라짐) 선택지 제공. **편집(`openEditEvent`)은 의도적으로 변경 없음** — 기간(`range_group_id`) 일정은 여전히 "이 일정만/이후 일정도" 범위선택 대상에서 제외(그대로 origin을 직접 열어 수정하면 제목/색/시간 등이 전체 시리즈에 소급 적용, 기존 동작과 동일) — 사이클별로 새 origin을 분리해내는 건 물리 row 재생성까지 필요한 별도 대공사라 이번 범위 밖. Playwright 대신 **실제 index.html에서 함수 소스를 추출**(정규식+중괄호 밸런스 파싱)해 Node에서 직접 실행하는 방식으로 검증(재구현이 아닌 진짜 배포 코드 테스트) — 스크린샷과 동일한 시나리오(9/7~12 6일 기간+매주 월요일 반복)로 2·3주기 전체 오프셋의 앵커/오프셋/`_rangePos`(start/mid/mid/mid/mid/end)/합성 `range_group_id` 공유 확인, 매월 N째주 반복+기간 조합 확인, **spanDays===1(전체 반복 이벤트의 절대다수)은 필드 자체가 안 생겨 기존 로직과 100% 동일함을 별도 확인**(무회귀 보장), `_eventLaneKey`가 사이클 전체를 하나의 레인 아이템으로 묶는 것도 실제 함수로 확인. ⚠️ **알려진 갭(후속 필요)**: 위젯(iOS/Android)의 `_wgExpandEvent`(반복 펼치기)는 이번에 손대지 않음 — 위젯은 여전히 기간+반복을 origin 요일 하루만 투영(앱과 위젯 간 표시 불일치, 위젯 payload+양쪽 네이티브 렌더 코드까지 건드려야 하는 별도 작업이라 후속으로 남김). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 09:22:45`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

- 🐛 **[핫픽스] 기간+반복 전체투영 배포 직후 "일정 불러오기 실패" 발생 — 스코프 버그 수정 + 방어 폴백 추가** (2026-08-13, 사용자가 실기기 스크린샷 제시 "갑자기 일정불러오기 실패라고 뜨는대") — 바로 위 항목(기간+반복 전체투영) 배포 직후 캘린더 진입 시 간헐적으로 "일정 불러오기 실패" 토스트가 뜨는 문제 제보. **실제로 확인된 버그**: `loadEvents()`에서 `_allRecurringOrigins.push(ev)`가 반복 종류별 `if/else if` 체인 **바깥**에 있어, **반복이 아예 없는(`repeat_type==='none'`) 모든 row**(순수 기간 일정의 형제 날짜, 반복 없는 일반 일정 전부 포함)까지 몽땅 이 배열에 들어가고 있었음 — 원래 "반복 활성 origin만" 담아야 하는 배열인데 사실상 room의 전체 이벤트가 다 들어간 것. 사용자의 실제 계정 데이터(방 `7b56978f-...`, "테스트1ㄱ"·"ㅂㅂ" 두 개의 기간+반복 일정 포함, "재주도"·"서울여행"·"부산 여행" 등 순수 기간 일정 다수)로 **index.html에서 실제 함수를 그대로 추출해 Node로 재현 테스트**를 만들어 7/20~9/10 전체 날짜 + `_computeMonthLanes`/`_dayAbsSlots`까지 돌려봤지만 이 스코프 버그 자체는 크래시를 일으키지 않음을 확인(각 헬퍼가 `repeat_type==='none'`을 안전하게 `false`로 처리) — 즉 **정확한 크래시 트리거는 재현하지 못함**(네트워크 일시 오류·배포 직후 재시작 타이밍 등 다른 원인일 가능성도 배제 못 함). 그럼에도 이 스코프 버그는 명백히 잘못된 동작(매 렌더마다 전체 이벤트를 불필요하게 순회)이라 **원인 규명과 무관하게 수정** — `_allRecurringOrigins.push(ev)`를 `yearly`/`monthly`(nth·last·dom 각 분기)/`weekly` 매칭 **각 분기 안으로** 이동해 반복 비활성 row는 애초에 안 들어가게 함. **추가로 방어적 폴백 3중 설치**(정확한 원인을 못 찾았으므로, 앞으로 어떤 예기치 못한 데이터 조합이 나와도 캘린더 전체 로딩이 죽지 않도록): (1) `loadEvents()`의 spanDays 계산 루프를 try/catch로 감싸 실패 시 해당 origin만 `_rangeSpanDays=1`(스팬 없음)로 안전 폴백, (2) `_mkRecurEntry()` 자체를 try/catch로 감싸 실패 시 기존(스팬 없음) 단일 표시 형태로 폴백, (3) `_findSpanContinuation` 호출부(연속일 탐색 루프)를 try/catch로 감싸 실패한 origin만 건너뛰고 나머지는 계속 진행, (4) **최종 안전망**으로 `getDayEvents()`(모든 렌더 경로의 실제 진입점)가 `_getDayEventsRaw()` 호출 자체를 try/catch로 감싸 — 그래도 뭔가 실패하면 그 날짜는 반복 펼치기 없이 **물리 row만**(`_events[date]||[]`) 표시해 최소한 그 날짜의 실제 일정은 보이게 하고, 달력 전체가 죽는(=로딩 실패 토스트) 일은 없게 함. 재현 테스트로 수정 후에도 동일 시나리오 무크래시 확인 + `_allRecurringOrigins`가 이제 정확히 2개(테스트1ㄱ·ㅂㅂ)만 담기는 것 확인. index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. ⚠️ **재발 시**: 이 방어 폴백 덕분에 이제 "일정 불러오기 실패" 대신 브라우저 콘솔에 구체적 경고 로그(`_mkRecurEntry 실패`/`_findSpanContinuation 실패`/`_getDayEventsRaw 실패` 등)가 남으므로, 다시 발생하면 콘솔 로그로 정확한 원인을 특정할 수 있음. **✅ `deploy_version`을 `2026-08-13 09:35:19`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

- ✅ **기간(range) 미니 달력 ↔ 반복 요일 칩 양방향 자동 연동** (2026-08-13, 사용자 "기간으로 하면 미니캘린더에서 4~6선택하고 생성하면 4,5,6은 되는대 그 이후가 기간일정으로 생성이 안돼" + "미니캘린더랑 반복할요일 연동시켜서 서로 자동적용 처리해줘") — **조사 결과**: 사용자가 만든 "법" 일정(방 `7b56978f-...`, 2026-08-04 화~08-06 목 3일 기간 + 매주 반복 `repeat_weekdays:[2]`)을 DB에서 직접 조회해 실제 index.html 함수로 재현 테스트해보니, **저장된 데이터와 로직 자체는 정확**했음(다음 사이클 8/11~13·8/18~20이 3일 기간으로 정확히 투영됨, 바로 위 두 항목에서 이미 검증된 로직 그대로) — 즉 "그 이후가 기간으로 생성 안 됨" 증상은 **바로 위 두 항목(전체투영 구현+핫픽스)을 배포하기 전의 옛 캐시 버전**을 보고 있었을 가능성이 높음(앱을 완전히 껐다 켜야 `deploy_version` 체크가 새로 돎). ⚠️ 이 부분은 100% 확정은 못 함 — 새로고침 후에도 재현되면 다시 알려달라고 안내함. **다만 사용자가 지적한 진짜 근본 원인(왜 처음에 어긋날 뻔했는지)은 확인**: "반복할 요일" 칩과 기간 미니 달력이 **완전히 서로 무관하게 동작**하고 있었음 — `_evRepeatWeekdays`(반복 요일 상태)는 반복을 "매주"로 처음 켤 때 그 순간의 `_dayDate`(주로 오늘 날짜) 요일로 딱 한 번만 기본값이 잡히고, 이후 미니 달력에서 기간 시작일을 바꿔도 전혀 갱신되지 않아 **시작일 요일과 반복 요일이 어긋날 수 있는 구조**였음(우연히 맞으면 정상 동작, 어긋나면 반복이 엉뚱한 요일부터 투영됨). **구현**: (1) **미니캘린더→칩**: `onRangeCellClick(key)`(기간 시작/종료일 탭 핸들러)이 시작일이 바뀔 때마다 신규 `_syncRepWeekdayFromRange()`를 호출 — 반복이 매주 또는 매월 N째주 모드면 `_evRepeatWeekdays`를 **시작일의 요일 하나로 자동 설정**+칩 UI 갱신(`syncEvRepeatUI()`). (2) **칩→미니캘린더**: `_toggleRepWeekday(dow)`(요일 칩 클릭 핸들러)이 기간 모드(`_selectedMode==='range'`)이고 시작일이 있으면, 방금 켠(또는 남은 첫) 요일과 현재 시작일 요일의 차이만큼 `_rangeStart`를 **그 요일로 이동**시키고 `_rangeEnd`도 **기존 기간 길이(일수)를 유지**하며 같이 이동(`shiftDateKey`로 단순 요일차만큼 시프트 — 같은 주 안에서 자연스럽게 이동). 반복 '안 함'일 때나 기간 모드가 아닐 때는 완전히 무동작(기존 동작에 영향 없음). **할 일(todos)도 동일 구조로 적용**: `todoMiniPick(key)`(기간 모드 날짜 탭)에 `_syncTdRepWeekdayFromRange()` 추가, `_toggleTdWeekday(dow)`에 동일한 역방향 시프트 로직 추가(사용법·UI가 일정과 대칭 구조라 완전히 같은 방식으로 확장). 저장/펼치기 로직은 전혀 안 건드림(순수 UI 상태 동기화만 추가). **실제 index.html에서 함수를 그대로 추출**(중괄호 밸런스 파싱)해 Node로 검증(미니캘린더 화요일 선택 시 칩이 [2]로 자동 동기화·종료일 선택 후에도 유지·칩에서 목요일 추가 선택 시 시작일이 8/4→8/6으로 이동하며 3일 기간 길이 유지·매월N째주 모드 동일 동작·반복 안 함일 때 무동작·할일도 동일 5종 확인). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 09:56:54`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.** **→ 탭식 미니캘린더 제거 + 반복 요일 칩이 기간을 직접 결정하는 방식으로 재설계(2026-08-13, 사용자 "화요일 선택 시 11일 활성화, 화·수 선택시 12~13활성화... 이런식으로 해줘. 복잡하면 반복일정 선택시 미니캘린더는 숨김처리 해줘")**: 사용자가 요구한 정확한 날짜 매핑은 예시 자체에 내적 모순이 있어(화·수 선택인데 왜 12~13=수·목이 활성화되는지 등) 그대로 재현이 불가능하다고 판단, **사용자가 제시한 대안(반복 선택 시 미니캘린더 숨김)을 채택**하되 완전히 숨기지 않고 **반복 요일 칩 자체가 기간을 결정하는 방식**으로 재설계 — 화 하나만 선택→1일, 화·수 선택→2일(화~수), 화·수·목 선택→3일(화~목)처럼 **선택된 요일들의 최소~최대 범위를 기간 길이로 직접 계산**(사용자가 예시로 든 "선택 요일 수 = 기간 일수" 규칙은 그대로 구현, 정확한 날짜 위치만 무모순 버전으로). **일정(evCard)**: `#evRangeRow`의 탭식 미니캘린더(`#evRangeMiniCal`, 종전 `onRangeCellClick` 탭 인터랙션)를 매주/매월N째주 반복이 활성화되면 숨기고, 대신 `#evRangeWeekNav`(‹ 이전 주 / 다음 주 › 버튼 + "반복 요일로 기간 자동 계산" 안내문)만 노출. 신규 함수 `_evRepeatUsesWeekday()`(weekly 또는 monthly+nth 여부)·`_evRangeAutoFromWeekdays()`(`_evRepeatWeekdays`의 min~max dow로 `_rangeEnd` 재계산, 시작일은 기존 시작일의 주를 유지한 채 최소 선택 요일로 스냅)·`_evRangeShiftWeek(dir)`(시작·종료일을 ±7일씩 통째로 이동)·`_updateRangeCalVisibility()`(그리드/주네비 표시 전환 + hide면 자동계산 트리거, `syncEvRepeatUI()`와 `setEventMode('range')` 양쪽에서 호출). 기존 `_toggleRepWeekday`의 "직전 span 길이 유지+한 요일만 이동" 로직(바로 위 항목에서 만든 것)은 **폐기**하고 이 min-max 방식으로 교체. `_syncRepWeekdayFromRange`(그리드 탭→칩 동기화)는 삭제하지 않고 잔존(그리드가 숨겨진 상태에선 호출될 일이 없어 사실상 비활성, 반복 없는/dom·yearly 케이스에서는 여전히 유효). **할일(todoCard)**: 동일 구조로 `_tdRepeatUsesWeekday()`·`_tdRangeAutoFromWeekdays()`·`_tdRangeShiftWeek(dir)`·`_updateTdRangeCalVisibility()`(신규 `#todoRangeWeekNav` 마크업, `$('todoMiniCal')` 자체를 숨김 — 할일은 단일/기간/여러날 전부 이 하나의 캘린더를 공유하므로 `renderTodoMiniCal()` 진입부에 가드 추가해 숨김 상태면 그리드를 아예 다시 안 그림). `_syncTdRepeatUI()`가 `renderTodoMiniCal()`을 호출하도록 연결(반복 종류 변경 시 자동 반영), `todoMiniPick`의 옛 `_syncTdRepWeekdayFromRange()` 호출은 그 함수 자체를 삭제하며 함께 제거(그리드가 숨겨진 상태에서만 의미 있던 호출이라 죽은 참조 방지 차원에서 완전히 정리). 실제 index.html에서 함수를 추출해 Node로 검증(화 단독→1일·화수→2일(간격 정확)·화수목→3일·요일 해제 시 남은 최소요일로 재계산·주 이동 ±7일, 일정+할일 양쪽 동일 시나리오 전부 통과, `_evRepeatUsesWeekday`가 weekly/monthly-nth에서 true·dom/yearly/none에서 false임을 별도 확인). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 10:16:47`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.** **→ 플레이스홀더 요일이 사용자 선택에 섞이거나 토글-OFF되던 버그 수정(2026-08-13, 사용자 "반복에 매주 선택하고 기간을 기간으로 선택 후 회수목 선택하고 저장누면 기간일정으로 생성처리 해줘")**: 위 재설계 직후 정확히 이 시나리오(매주→기간→화수목 클릭)로 재현 테스트해보니 **두 가지 버그**가 있었음 — (1) 반복을 처음 켤 때 자동으로 잡히는 요일 1개(오늘 또는 시작일 요일 플레이스홀더, `onEvRepeatSelect`/`setTodoRepeat`의 기존 로직)가 이후 사용자가 실제로 누르는 화·수·목 클릭에 그대로 **더해져** 기간이 엉뚱하게 4일(예: `[0,2,3,4]`)로 계산됨. (2) 하필 **오늘 요일이 사용자가 원하는 요일 중 하나와 겹치면**(예: 오늘=목요일, 목표=화수목) 그 요일 클릭이 "이미 선택돼 있음"으로 오인되어 **토글-OFF**되며 화·수 2일만 남음(3일이 아니라 2일로 저장). **1차 시도**(전환 시점 `_evRangeCalHidden` false→true 감지로 리셋)는 리셋값이 우연히 플레이스홀더와 같으면 무의미해 두 버그 모두 재현됨 → **최종 수정**: 신규 플래그 `_evRangeWeekdaysUserTouched`/`_tdRangeWeekdaysUserTouched` 도입 — 기간+요일반복 숨김 상태로 전환된 뒤 **사용자의 첫 클릭**은 배열에 추가하는 대신 플레이스홀더를 그 요일 하나로 **통째로 교체**(`_toggleRepWeekday`/`_toggleTdWeekday`), 이후 클릭부터는 정상 추가/제거 토글. 추가로 **세션 재사용 버그**도 발견 — `_evRangeCalHidden`/`_tdRangeCalHidden`가 폼을 닫아도 리셋 안 돼 **같은 세션에서 두 번째 기간+반복 일정을 만들 때부터** 리셋 로직 자체가 재발동을 안 하는 문제가 있어, `resetEvRepeat()`(일정, `openAddEventModal` 경로)·`openTodoAddSheet`의 인라인 초기화(할일)·`openEditEvent`/`openTodoEditSheet`(수정 진입 시 저장된 실제 값을 '확정값'으로 표시해 플레이스홀더 리셋 대상에서 제외)에도 두 플래그를 명시적으로 정리하도록 추가. **실제 index.html에서 함수를 추출해 Node로 검증**(사용자의 정확한 재현 시나리오: 매주→기간→화수목 클릭 → `[2,3,4]`+8/11~8/13 3일 기간 확정, 오늘=목요일 충돌 케이스, 세션 중 두 번째 기간+반복 이벤트가 이전 것과 안 섞임, 일정·할일 양쪽 총 8개 검증 항목 전부 통과). index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 14:32:04`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

- ✅ **반복 설정 UI 전면 재설계 — 인라인 컨트롤 → "반복" 요약 버튼 + 아이폰 캘린더 스타일 팝업** (2026-08-13, 사용자가 iOS 기본 캘린더 반복 설정 화면 스크린샷 4장 제시 후 "너무 복잡해졌는대 전반적으로 아예 바꿔보자 / 기존 반복 레이아웃을 없애고 반복 사용자화 버튼을 생성한다음 사진 사용자화 할 수 있도록 만들어줘") — 위 항목들에서 누적된 인라인 반복 컨트롤(반복종류 세그먼트+요일칩+매월방식+N째주+종료일 Y/M/D 드롭다운이 폼 안에 전부 펼쳐져 있던 것)이 너무 복잡하다는 피드백. **범위는 사용자에게 직접 확인**(AskUserQuestion) — "UI만 단순화(기존 반복 로직·DB 스키마 무변경)"를 선택(반복 주기 N일/N주 간격이나 횟수 기반 종료 같은 iOS의 추가 기능은 이번 범위 밖). **구현**: (1) 일정 폼(`#evCard`)의 `#evRepeatRow` 인라인 블록(세그먼트+상세+종료일, 옛 60줄)을 **요약 버튼 하나**(`#evRepeatSummaryBtn`, "안 함"/"매주 화,수,목요일"처럼 현재 설정을 한 줄로 표시 + `›` 화살표, `.fi` 인풋 스타일 재사용)로 교체, 탭하면 `openEvRepeatSheet()`가 **새 팝업**(`#evRepSheetOverlay`, 기존 확인모달과 동일한 `.vc-overlay`/`.vc-card` 패턴 — 크림 카드+어두운 배경 딤+중앙정렬, iOS 스크린샷과 동일 구조)을 띄움. **팝업 내부 컨트롤은 옛 인라인 블록을 그대로 옮긴 것**(반복종류 세그먼트 `#evRepeatSeg`·요일칩 `#evWeekdayChips`·매월방식 `#evMonthlyMode`·N째주 `#evNthWeekSel`/`#evNthChips`·종료일 Y/M/D `#evUntilY/M/D` — id·CSS클래스·onclick 핸들러 100% 동일, DOM 위치만 이동) → **반복 로직 자체(펼치기·저장·`_evRepeatType`/`_evRepeatWeekdays`/`_evMonthlyMode`/`_evNthWeek`/`_evRepeatUntil` 등 state)는 단 한 줄도 안 건드림**(직전까지 몇 시간에 걸쳐 검증한 화수목 버그수정·기간 자동계산 로직이 그대로 재사용됨). 하단에 iOS 스타일 **"취소"/"확인"** 버튼 추가. (2) **취소/배경탭 = 열기 전 상태로 완전 복귀**: `openEvRepeatSheet()`가 열 때 `_evRepSheetSnapshot`에 관련 state 전부(`type/weekdays/monthlyMode/nthWeek/until/rangeCalHidden/rangeTouched/rangeStart/rangeEnd`) 스냅샷 → `cancelEvRepeatSheet()`(취소 버튼 + `ov.onclick`으로 배경 탭도 동일 처리, 기존 `vConfirm`의 `if(e.target===ov)` 패턴 재사용)가 스냅샷으로 복원 후 `syncEvRepeatUI()` 재호출(칩·세그먼트·기간자동계산까지 전부 정확히 되돌아감 — `_rangeStart`/`_rangeEnd`도 포함해 스냅샷해야 사용자가 팝업 안에서 주 이동까지 했다가 취소해도 완전히 원복됨을 확인 후 포함). `confirmEvRepeatSheet()`는 스냅샷을 버리고 요약 텍스트만 갱신(state는 이미 실시간 반영돼 있으므로 추가 작업 불필요). 팝업은 `document.body.appendChild(ov)`로 열 때마다 body 최하단 이동(evCard가 `transform` 있는 `.mo-card`라 안 하면 `position:fixed` 팝업이 화면 밖으로 잘못 배치됨 — 기존 `vConfirm`이 쓰던 동일 방어 패턴). (3) **할일 폼(`#todoAddCard`)도 완전 대칭 구조로 재설계**: `#todoRepeatRow`+`#todoRepeatSummaryBtn`, 팝업 `#tdRepSheetOverlay`(옛 `.todo-repeat-row` 4버튼+`#tdRepeatDetail`+`#tdUntilWrap`을 그대로 이동), `openTdRepeatSheet()`/`confirmTdRepeatSheet()`/`cancelTdRepeatSheet()`(일정과 동일 스냅샷/복원 구조, `_todoRepeat`/`_tdRepeatWeekdays`/`_tdMonthlyMode`/`_tdNthWeek`/`_tdRepeatUntil`/`_tdRangeCalHidden`/`_tdRangeWeekdaysUserTouched`/`_todoRangeStart`/`_todoRangeEnd` 스냅샷). **요약 텍스트 헬퍼**(`_evRepeatSummaryText`/`_tdRepeatSummaryText`, 완전 대칭) — "안 함"/"매주 화,수,목요일"/"매월 19일"/"매월 둘째 화,목요일"/"매월 마지막 날"/"매년"(+종료일 있으면 "· 8월 25일까지" 접미), `resetEvRepeat()`·`openEditEvent()`(일정)와 `openTodoAddSheet()`·`openTodoEditSheet()`(할일)에서 폼이 열리거나 초기화될 때 각각 `updateEvRepeatSummaryText()`/`updateTdRepeatSummaryText()` 호출해 요약 버튼이 항상 최신 상태를 반영. **⚠️ IIFE 스코프 함정(코드 23 재확인)**: 할일 시스템은 `(function(){'use strict';...})()` IIFE(20906~22634행) 안에 있어, 새로 만든 `openTdRepeatSheet`/`confirmTdRepeatSheet`/`cancelTdRepeatSheet`가 HTML `onclick="..."` 속성(전역 스코프에서 이름 해석)으로 호출되려면 **`window.openTdRepeatSheet=...` 등으로 명시 노출 필수** — 처음엔 빠뜨려 Playwright 실브라우저 테스트에서 "openTdRepeatSheet is not defined" 즉시 발견, IIFE 끝 `window.setTodoRepeat=setTodoRepeat;` 옆에 3개 추가해 해결(`_toggleTdWeekday` 등 내부 헬퍼는 `_renderTdChips`가 `.onclick=` **프로퍼티 할당**으로 붙이므로 클로저로 자동 해석돼 노출 불필요 — 이 차이를 명확히 구분해서 처리). **검증**: (a) Node로 전체 `<script>` 블록 문법 검사(3블록·오류0), (b) 신규 id 전수 유일성 확인, (c) 새 팝업 두 블록 div 태그 균형(29open/29close 각각) + 전체 파일 div 균형이 원본과 동일한 +8/+8 증가만 있음 확인(원본 자체에 이미 있던 1개 불일치는 커밋 HEAD에도 존재하는 무관 기존 이슈로 확인), (d) **Playwright로 실제 크로미움 브라우저에 index.html을 그대로 로드**(Supabase/lunar CDN 3개를 `page.route()`로 무해한 스텁 JS로 가로채 초기화 차단 없이 통과시킴 — 순수 함수추출 방식과 달리 실제 DOM/CSS/클릭 인터랙션까지 검증) → 실제 버튼 클릭으로 사용자의 정확한 원본 버그 시나리오(기간모드+매주+화·수·목 클릭)를 처음부터 끝까지 재현해 `_evRepeatWeekdays=[2,3,4]`+`_rangeStart='2026-08-11'`(화)+`_rangeEnd='2026-08-13'`(목)+요약 "매주 화,수,목요일" 전부 정확 확인, 취소/배경탭 시 직전 확정 상태로 완전 복귀 확인(둘 다), 할일 쪽도 동일 시나리오+매월 N째주 모드까지 확인, 스크린샷으로 실제 렌더 결과(크림 카드+세그먼트+칩+종료일 드롭다운+취소/확인) 육안 확인. index.html 변경 → deploy_version 갱신 시 PWA 즉시 반영. **✅ `deploy_version`을 `2026-08-13 15:54:18`(KST)로 재갱신해 이미 일반 사용자에게 반영됨.**

## 새 세션 체크리스트
1. 이 `CLAUDE.md` 읽기.
2. 작업 대상이 프리미엄/광고면: `my_premium_status()` 정의 + `subscriptions` 조회로 DB 진실 먼저 확인(추측 금지).
3. 클라이언트 코드는 `main`의 `index.html` 기준으로 작업(현재 최신 동기화됨 — 단 개발자 로컬이 더 앞설 수 있으니 확인).
4. 광고 관련이면 네이티브 여부와 `_admobDebug` 값부터 확인.
